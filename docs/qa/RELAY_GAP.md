# Relay — Gap Report against the Product Transformation Brief

**2026-08-29 · verified against `0338244` (branch `main`), version `0.1.0-4`.**

This document answers one question: **the transformation brief proposes ~80 things — which of
them already exist, which are half-built, which are missing, and which would break a decision
this product has already made and recorded?**

It began as the *output* of `docs/GPT.md` — since deleted (see below) — a paste-into-any-model prompt that front-loaded
Relay's constraints so a model argued inside the product's shape instead of recommending
accounts, RBAC and cloud sync at it.

> **`docs/GPT.md` was deleted on 2026-08-30, deliberately.** It was 770 lines that restated
> `CLAUDE.md`, `AGENTS.md` and this file for a model with no repository access — and it carried
> its own copy of every count, all of which had drifted (§18). `AGENTS.md` is now the
> tool-agnostic contract for an agent working IN the repo, and this document is what the
> repository answered. A third copy of the same constraints was a third thing to keep true, and
> it was already the least true of the three.

> **This report changed no engine code.** Its §21 buckets and §25 checklist are proposals to a
> human, not work in progress. That is the brief's own §79 rule — *establish what exists, then
> what is wrong, then what is missing, then implement* — and Relay's own scoping rule.

> ## Fix log — 2026-08-29
>
> **RG-01 … RG-05 are closed**, in the order the register ranked them, with the
> reasoning recorded as [DECISIONS](../DECISIONS.md) §39–§42 and the tests named below.
> Everything else in §23 stands. **The release decision is unchanged and is still
> NO-GO**, for the reason §24 gives: none of this was a defect count, and none of it
> is a Sunday.
>
> | ID | What changed | Pinned by |
> |---|---|---|
> | **RG-01** | Live shows real per-channel health; a screen that is not answering can never read amber. Live and the Outputs tab now decide from one backend fact through one shared helper | `outputhealth.test.js` (26), `describeScreen` |
> | **RG-02** | Every output page reports that it is still painting — the native window over the bridge, kiosk/OBS over the socket it already has. Anonymous; §35's *who* and *where* untouched | `channels::tests` (6 new, incl. the WS round trip and the malformed-beat case) |
> | **RG-03** | Service Lock: 17 irreversible or engine-stopping actions held back while a service records. Nothing on the fire path. The operator lifts it in one action | `servicelock.rs` (9), `e2e::a_recorded_service_holds_back_a_deletion_but_never_the_wall`, `servicelock.test.js` (12) |
> | **RG-04** | `service_events` + `perf_samples`: an ordered record that survives the app, merged with detections and cues on the way out, carrying nothing a preacher said | `timeline_tests` (6), `e2e::a_service_records_what_happened_and_it_survives_the_service`, `timeline.test.js` (7) |
> | **RG-05** | Safe Screen: the one door content leaves by now refuses a payload that would paint an empty screen or carry an unreadable template; the fit loop reports when it has shrunk below legibility | `pipeline::tests` (6 new), two `e2e` tests, `safescreen.test.js` (11) |
>
> Three of them changed what an existing decision had drawn, so each is written up
> rather than absorbed: §39 narrows one word of §35 (**when**, anonymously — never
> *who*), §40 introduces a lock and then subordinates it to the operator, and §42 adds
> a gate in front of the wall that deliberately cannot touch a panic control.
>
> ### P1 — RG-06 … RG-12, closed the same day
>
> | ID | What changed | Pinned by |
> |---|---|---|
> | **RG-06** | Preflight, snapshot (`VACUUM INTO`), verify-on-next-launch, and a restore that happens before the database is opened. **Not** binary rollback — the installers are public; the church's data is what cannot be got back | `updates.rs` (12), `updatesafety.test.js` (13) |
> | **RG-07** | A timeline row opens to show what was being said around it, what Relay decided, and how fast it was going | `report.test.js` replay group |
> | **RG-08** | The Sunday report, derived and never stored — and it names what it does **not** measure | `report.test.js` (23 total) |
> | **RG-09** | The half-dozen graceful fallbacks are now one line in the shell, on every tab, opened for the detail | `degraded.test.js` (21) |
> | **RG-10** | A room, remembered — microphone, language, length, voice profile, displays. **Not** the audio levels | `db::environments` (6), `rooms.test.js` (19) |
> | **RG-11** | Settings → Languages, every number derived from the shipped data; accuracy and native review render as absences | `detection::language_report_tests` (4), `languages.test.js` (11) |
> | **RG-12** | One file a church can send, composed as an allow-list, with the home directory scrubbed | `diagnostics.rs` (5), `diagnostic_bundle_tests` (2), `diagnostics.test.js` (11) |
>
> Six more decisions were earned and written up: §43 (the binary is replaceable, the
> data is not) · §44 (only what was measured appears) · §45 (an invisible fallback is
> indistinguishable from a fault) · §46 (a room may be remembered; its audio levels
> may not) · §47 (the moat is measured from the shipped data) · §48 (the one artefact
> meant to leave the building gets the strict rule).
>
> ### P2 — RG-13 … RG-17, closed 2026-08-30
>
> | ID | What changed | Pinned by |
> |---|---|---|
> | **RG-13** | Both accessibility lists at zero — **and eleven of the thirteen findings were the instrument's own bugs**, which is the larger half of the fix | `inventory.test.js` (10), `surface.test.js` R3-12 inverted |
> | **RG-14** | p99 live, stored and shown; week-on-week trend across services; and the live Diagnostics screen stopped printing an unreached stage as `0ms` | `timeline_tests` (+3), `report.test.js` (31 total) |
> | **RG-15** | Say one verse, watch six stages. Runs in rehearsal or not at all | `pathcheck.test.js` (17) |
> | **RG-16** | Six drills with the real controls, panic first. **Not** a simulated service | `training.test.js` (21) |
> | **RG-17** | Settings → Privacy, read from the live settings, stating the LAN exposure in the same size type as the reassuring half | `privacy.test.js` (8) |
>
> Four more decisions: §49 (an instrument that cries wolf) · §50 (p95 and the worst
> sample bracket the tail; neither answers it) · §51 (twenty-one checks that pass on
> a machine where nothing works) · §52 (practice is drills, not a simulation).
>
> ### P3 — RG-18 and RG-19, closed 2026-08-30. **The register is now empty.**
>
> | ID | What changed | Pinned by |
> |---|---|---|
> | **RG-18** | Contrast (exact over a solid background, **"cannot be checked" over a picture**), size at distance from two numbers only a person can know, a distance preview, and High Visibility as a THEME rather than a parallel renderer | `legibility.test.js` (21) |
> | **RG-19** | Install a model from a file already on the machine, a three-folder scan, and `scripts/offline-bundle.mjs`. **Signed language packs deliberately NOT built** | `models::tests` (+3), `offline.test.js` (13) |
>
> One more decision: §53 (offline installation is one missing file, and language packs
> are not that).
>
> **RG-18's thresholds are unverified and say so in the UI.** WCAG is a specification
> for screens at arm's length and the character-height rule is broadcast practice;
> neither has been checked against a projector in a church. That is Stage B, and the
> caveat rides with the verdict rather than living here.
>
> **RG-19 shipped its offline half and refused its signed half.** The word doing the
> work in "signed language packs" is *signed*, and signing needs a key, a ceremony and
> a distribution channel that do not exist. An unsigned pack that can rewrite the book
> aliases is a wrong-verse-on-a-wall vector, and the operator cannot check 66 names in
> a language they may not read. It waits on the same thing §47 names: a native speaker
> who has actually reviewed the tables.
>
> ### Where that leaves the register
>
> **Every RG item is closed.** Three of the nineteen shipped deliberately narrower than
> written — RG-10's audio seed, RG-16's audio replay, RG-19's language packs — and all
> three are waiting on the same thing, which is not a commit:
>
> | Waiting on | Blocks |
> |---|---|
> | **Stage C** — a person, a microphone, a real room | RG-10 (seeding the audio gate), RG-16 (replaying a service), the WER measurement that is the moat |
> | **Stage B** — a projector | RG-18's thresholds |
> | **Stage F11** — one full service | the long-service latency question |
> | **A native speaker** | RG-19's language packs, and §47's empty column |
> | **Two code-signing certificates** | shipping at all. **Neither platform has one** (RG-73) — Windows is the platform most churches are on, and macOS is the one that has actually run a service |
>
> That is the same list `KNOWN_ISSUES.md` §1 has carried since before any of this work, and
> none of it moved. **The register being empty is not the same as being ready**, and
> §24 below still says NO-GO for exactly that reason.
>
> **RG-16 is deliberately narrower than the register described.** "Replay recorded
> audio through the real pipeline" is not buildable here: Relay cannot produce a
> sermon, and a simulation would teach a volunteer the shape of a fake. Replaying a
> church's *own* recorded service audio would work — and needs the audio corpus that
> Stage C has never produced. What shipped is the half that does not need it.
>
> **RG-10 deliberately stops short of its most attractive option.** Seeding the audio
> learner from a stored floor may well be right, and it is not being done, because the
> instrument that could show it safe (`cargo test audio::gate -- --ignored`, against
> real room audio) has never been pointed at a real room. That is Stage C, and it is
> still unrun.
>
> ---
>
> ## Fix log — 2026-08-30 · the two open defects
>
> With the register empty, the only in-repo work left that was **not** blocked on the
> world was the pair of defects `e2e.rs` had been carrying as `#[ignore]`d tests since
> the R2 audit — each with its own repair direction written into the test. Both are
> closed, reasoning in [DECISIONS](../DECISIONS.md) §54, and `e2e.rs` now has **no ignored
> tests at all**.
>
> | ID | The defect | Fix |
> |---|---|---|
> | **R2-C** | A spoken in-passage jump that could not move said nothing — no toast, no banner, no log line. The fourth door into the bug `NavResult` was built to prevent | It reports `NoPassage`/`NotInLibrary`, and **announces its own outcome** rather than returning it to its one caller |
> | **R2-D** | A song, notice, picture or countdown left the previous reading armed, so `nav("next")` walked a passage the congregation had stopped looking at and answered `Fired` | `ContextMemory::forget`, called at `broadcast_with_clock` — the choke point, so every content path is covered at once |
>
> Both tests were re-run with the defect deliberately reintroduced, and both fail again.
> **The release decision is still NO-GO**, unchanged and for the unchanged reason: two
> closed defects are not a Sunday.
>
> ---
>
> ## Fix log — 2026-08-30 · RG-21 · RG-22 · the packaged build
>
> **The application was rebuilt and re-verified from source** before this round:
> `npm run tauri build` produced `Relay.app` and `Relay_0.1.0-4_aarch64.dmg`, and
> `./scripts/sign-local.sh` reproduced §17's conditions on the bundle — hardened
> runtime ON, microphone entitlement present, usage string present. That is the
> check that cannot be made from source, because the trap it exists to catch only
> appears on a signed build.
>
> Two new register entries, closed together. **RG-20 (doc drift) is also ticked** —
> its own validation command has returned nothing since the first round and had
> simply never been marked.
>
> | ID | What changed | Pinned by |
> |---|---|---|
> | **RG-21** | A song's running order can be built at all — the editor the whole arrangement chain was missing. `song_arrangements` moves from WRAPPER ONLY to a create path, and the repository has no dead command left | `arrangements.test.js` (13), `qa.rs::a_component_can_create_a_song_arrangement`, `surface.test.js` R3-12 |
> | **RG-22** | An arrangement whose sections moved is marked, refused into a plan, and repaired by a person — never remapped by guessing. The same rule on the plan-cue door | `db/mod.rs` (3 new), both re-run with the check disabled and failing |
>
> **They had to ship together.** RG-22 was latent only because nothing could create
> an arrangement; the editor is what would have made it reachable. Shipping RG-21
> alone would have introduced a path to the wrong words on a wall.
>
> **What was deliberately not built: automatic remapping.** Matching an old section
> index to a new one means guessing whether a section was moved or replaced, and a
> wrong guess is indistinguishable from a right one until it is on a screen in front
> of a congregation. DECISIONS §55.
>
> Counts **on 2026-08-30**, and left at that date on purpose: 595 Rust (26 ignored)
> + 857 frontend, `fmt` and `clippy -D warnings` clean, inventory at zero unnamed
> controls and zero handlerless buttons. A fix log is a dated record, so its numbers
> are not maintained — §0 holds the current ones and the commands that produce them.
> **Still NO-GO** — the list below did not move.

---

## WHERE THIS IS UP TO — read this first

*Regenerate the counts with `npx vitest run src/lib/relaygap.test.js` (it checks the table)
and by reading §23. Last updated 2026-09-21.*

**181 entries. 165 closed, 1 withdrawn as wrong, 15 not closed** — **RG-179 to RG-181 were filed and closed on 2026-09-21 from the Phase 1 audit, and RG-179 is the second wrong verse from the same morning as RG-178**: a bare *"verse 1"* answered from the psalm on the wall while the preacher quoted Hosea, labelled `Direct` and fired; it is RG-32's deferral meeting its second Sunday, and the label is now `UncertainBook` (DECISIONS §106). RG-180 and RG-181 are two more guarantees kept on one door: the transport bar over an unattended fire, and the context keys under an open dialog. — **RG-178 is the first row in this register filed from a wrong verse a congregation actually saw, with the audio and both decodes on disk.** One sentence was decoded twice, 3.2 seconds apart, into two different books, and both auto-fired at 0.88: `Numbers 10:29`, which the preacher said, and `Genesis 10:29`, which nobody said. The repeat cooldown is keyed per reference and could not see it; corroboration could not either, because the second pass did not disagree, it asserted a different book just as confidently. **The part worth carrying out of it is that no threshold would have helped**: both readings scored 0.88 while six correct auto-fires that morning scored 0.55, so raising the bar discards the six and keeps the wrong one. — **RG-177 was filed and closed on 2026-09-20, and RG-156 lost half of itself the same day.** RG-177 is the fourth instance of the twin-door shape this register keeps recording: a plan cue's `Screens` row renders for all five cue kinds and only three of them carried the set to the backend, so an operator who ticked one screen of three and read the hint saying the others keep what they are showing watched all three take the cue. **RG-156 is NOT closed and the half that closed is named in the row rather than in this sentence**: `output.html` no longer repaints a Stage Message at the next fire (DECISIONS §104), and `channels::stage_alert` still emits no Tauri event, so a stage screen wired as a native window is reached by neither the message nor the programme rail. **RG-175 and RG-176 were filed by the timer work as RG-171 and RG-172 and renumbered when the branches met.** It is the THIRD time this has happened and the cause is the one §23a already names: two branches off the same commit, both filing upward from the next free number, for entirely different findings. `feat/channel-looks` landed first and keeps 171 to 174; the timer rows moved up in order. **The union by id would have destroyed both of them silently** — RG-171 means per-kind looks on one side and a Stage Timer that cannot be held on the other, RG-172 means the hello reply on one side and a clock flooring at zero on the other, and `crossrefs.test.js` checks that a citation resolves, not that it resolves to what its author meant. — **RG-171 to RG-174 were filed and closed on 2026-09-18, from two operator reports and one thing found underneath them.** The reports were *"each screen should be able to have their own template set to what they want"* (RG-171, per-kind looks, DECISIONS §97) and *"timers still show on all screens, even the live screen"* (RG-173, per-screen reach, §98). **RG-172 is the one to look at first, because it is the only one that was already broken rather than merely absent**: the hello reply sent a screen its own template only when the client named a `template_id`, and the URL `Copy URL` produces does not — so the URL every operator is told to use was the one shape that received no template at all, four screens carrying four different templates painted the same one, and it was wrong on every reconnect and every cold start. Measured against the running backend before it was named: `?channel=3` painted `James 2:1` at 32px Fraunces centred and `?channel=3&template_id=68` painted the same verse at 15.97px Inter left. It is rule 43's hole one fact over, in the same handshake, and it was closed FIRST because building per-kind looks on a rung nobody had ever seen work would have been building on sand. **RG-174 is the converter half of RG-140** and is the one with a consequence that survives a restart: `TemplateGallery.upgradeLegacyToLayers` runs on mount and SAVES, so a scrolling notice template was being permanently re-shaped from a footer ticker into a full-frame runner by the act of opening the Templates tab. **None of the four is a detection fault, none moves the release decision, and `qa-inventory` reported 154 of 154 commands addressed, 0 handlerless buttons and 0 unnamed controls throughout.** — **RG-161 to RG-165 were filed by wave 4 as RG-145 to RG-149 and renumbered when the branches met.** This is the second time it has happened and the reason is the same both times: two waves branched from the same commit and both filed upward from the next free number, for entirely different findings. Wave 3 keeps the ids it filed under and wave 4's five moved up in order, exactly as wave 5's did. **Renumbering is not cosmetic here.** RG-145 to RG-149 exist on both sides and mean ten different things, so a union by id would have destroyed five wave 4 findings, three of them closed — and no instrument would have said a word, because the old ids still resolve. `crossrefs.test.js` checks that a citation resolves, not that it resolves to what the author meant. §23a maps the old ids to the new ones — **four of those closures arrived in the merge rather than in an audit.** RG-149, RG-150, RG-152 and RG-153 were closed on `feat/wave3-timers` after `feat/waves-3-5-merge` had already taken its copy of this file, so the branch that looked like the newest one carried this table with all four still open. That is recorded here rather than quietly corrected, because "the newest branch has everything" is the belief the consolidation was most likely to die of. — **RG-156 to RG-160 were filed by wave 5 as RG-143 to RG-147 and renumbered when the two waves met.** Both waves branched from `140a2cb` and both filed from RG-143 upward, for entirely different findings; wave 3 keeps the ids it filed under and wave 5's five moved up in order, so nothing in either audit has to be re-read to find its row. **RG-156 changed meaning as well as number**: it filed the two stage surfaces disagreeing about what a panic control does to a Stage Message, and the operator's ruling (DECISIONS §91) closed that half by making the stage page clear it. What is left in the row is the limit nobody had noticed — `output.html` keeps the message in renderer state, so it paints again at the next fire. **RG-146, RG-147, RG-148, RG-154 and RG-155 were closed on 2026-09-17**, the three of wave 3's ten that live on the preacher's own screen. The programme row's width budget now comes from the string it is painting and a figure that still does not fit ellipsises instead of being sliced into a shorter time that reads as correct; the row wears the one warning rule with the per-timer `warn_ms` the frame already carried; and the height cap is a share of the row's own ceiling rather than a `cqh` that had no container and was silently measuring the viewport. **The tests that hold them pin CSS declarations and rendered attributes, not painted pixels** — jsdom computes no layout, and the browser measurements in §8 and §9 of the audit have not been re-taken. **RG-146 was closed by making the transport row a floor rather than a fixed height**, which is what its own narrow media steps had always done, and the browser pass that closed it reproduced the audit’s §10 figures to a tenth of a pixel before and measured zero overflow after. It costs up to 52 pixels of slide grid on a window shorter than about 970 pixels, which the row records rather than leaves to be found. **RG-155 closed with a third error the row had not found**: reproducing both of its stale counts showed CLAUDE.md wrong twice in one paragraph rather than once, and `docs/ARCHITECTURE.md` §6 listing twenty of the twenty-one events, all three caused by one event that landed in wave 2 and reached no number. **RG-146 to RG-155 were filed on 2026-09-16 from the wave 3 browser-driven pass** (`audits/DESIGN-2026-09-16-WAVE3.md`), which drove the six scenarios that wave owns against the real backend and read the kiosk wire directly rather than inferring it from the code. The wave's own reason for existing PASSES, measured frame by frame: a countdown re-aimed after a verse succeeds, changes the registry, publishes nothing, and comes back through `show_timer` carrying the adjusted figure with `countdown_from` not re-stamped and the template triple verbatim. **Three of the ten are the ones to look at first, and they are on three different screens.** RG-147 is the preacher's: a programme timer past one hour paints seven characters into a six-character budget, so `1:30:13` reads `1:30:1` and runs into its neighbour on any stage screen narrower than about 1554 px — which is what a stage tablet is. RG-146 is the operator's: **Start timer**'s centre hit-tests to the transport's transition-duration picker at 1320 × 860, the window size `tauri.conf.json` opens at, because `.rack` overflows a shorter `.con-top` that does not clip. RG-151 is the room's: `B` leaves the preacher's *up next* standing where `Esc` takes it down, because `output://black` does not clear `live` and its twin does — one line apart, under a comment documenting the same fix for the clear. **Two of the ten are the wave contradicting its own plan rather than getting code wrong**: Track D landed `countdownWarning`'s third argument and nothing passes it, and landed the Settings default into a module the output and stage bundles cannot reach (RG-148, RG-149); Track E landed a control for the half of the registry that never touches a congregation screen and none for the half that does (RG-152). Two are wholly pre-existing and the wave put something in front of them (RG-146, RG-151); seven are wholly in code this wave wrote; RG-155 is one of each. **None is a detection fault, none moves the release decision, and `qa-inventory` reported 0 handlerless buttons, 0 unnamed controls and 0 unaddressed commands throughout — and was right every time.** **RG-145 was filed on 2026-09-16 from wave 3's Track C, and it is a gap that DECISIONS §89 opened rather than found**: the stage alert now comes down with the screens on a panic control, and the console store that reports it is still written only by the send. The badge in Quick tools therefore says a word is on the preacher's monitor after the operator has taken it off, which is rule 35 on the one surface that could have said otherwise. The fix is one line in a wrapper, in a file that track did not own. **RG-142 was filed on 2026-09-16 from the final whole-branch fix pass, and it is the only row in this register ever filed from running a migration against a copy of a REAL database rather than an in-memory fixture.** `ensure_retired_presets_are_gone` matches a retired row on its exact bytes, and on any install whose Templates workspace has been opened those bytes have already been rewritten twice over — once by the layer conversion, which saves, and once by `serde_json` reordering the style keys. Measured against a genuine pre-wave install: 31 templates in, 57 out, **0 of the 21 frozen rows retired**. The failure direction is safe for data and useless for the cleanup the migration exists to do, and exactly one of the twenty-one — the contested name — escapes both rewrites and still works. The row also records the trade the same pass made on the diagnostic beside it: a false positive on every healthy install was swapped for a false negative on the rare one, and the follow-up written into the row is the version that would be right in both. Every existing test in that module inserts the frozen fixture verbatim, so none of them has ever asked what happens after the conversion has been over the same rows. **RG-139 to RG-141 were filed on 2026-09-16 from the wave 2 browser-driven pass** (`audits/DESIGN-2026-09-16-WAVE2.md`), which drove the six checks that wave owns against the real backend rather than reading them. All three are pre-existing rather than wave-2 regressions, and all three are about which rendering a church actually reaches rather than about code the wave got wrong. **RG-141 is the one to look at first**: 30 of the 37 seeded templates are region-model, so until somebody opens the Templates workspace a countdown takes the region branch — and there it collapses to about a sixth of its designed size, 30 px digits and a clipped 7.88 px label on a 1920 wall, which is the pre-service screen a church sees on its first Sunday. RG-140 is the same conversion seen on announcements: one template paints a footer ticker band before that click and a full-frame block with a mid-screen crawl after it, with nothing saying so. RG-139 is the layer half of the countdown — a running countdown makes every other text layer paint its declared size instead of its fitted one, so a long label on `Stage · Large type` is sliced through the middle while `onFit` reports a successful fit. **RG-139 and RG-141 were closed on 2026-09-18, and RG-140 partly; the sentence above is left as the audit filed it and is now wrong in one place.** *"30 of the 37 seeded templates are region-model"* stopped being true when wave 5 Track A rebuilt the shelf as forty layer-model looks, so a FRESH install no longer reaches the region countdown at all — what was closed is the branch an EXISTING install still reaches through a channel, a cue, a content look or the configured default. **All three turned out to be the same failure told three ways, and none of the three was where its row looked.** RG-141 is a fit loop measuring a box that is sized by the type it is adjusting, so its overflow is a fixed fraction of the type and an absolute one-pixel tolerance can only be satisfied by going tiny; RG-139 is Svelte compiling an inline `font-size` into a `set_style` guarded on the dirty bit with no value comparison, which overwrites the fit on every countdown tick while `data-sized` — a one-way latch — tells both recovery paths there is nothing to do; RG-140's reduced-motion half is a crawl that cannot crawl inside a box that still clips. In every one of them the report said the fit had succeeded, which is rule 37 and rule 35 in the same place three times. **What was left of RG-140 is `regionsToLayers` giving a `scroll: true` region row a full-frame layer instead of a band**, in `src/lib/layers.js`, which a second worktree was editing in the same session and which was therefore reported rather than touched. It was filed as RG-174 and closed there, on that worktree, in the same session — so RG-140 is closed in substance across two rows, and the row itself stays open until somebody has measured the History-restore question it also carries. **None of the three was measurable in jsdom and all three were measured in a browser against the real component**, which is the fourth time this register has recorded that the instrument could not see the defect it exists for. **The audit was itself corrected after review**: its first version asserted that a fresh install seeds 37 layer-model templates, three checks were rescoped when that turned out to be false, and one finding was withdrawn and rewritten — the register's own recurring lesson, one more time. **RG-138 was filed and closed on 2026-09-15**: the recognition language did not persist, on the control RG-116 names as its own mitigation. It now writes to the active voice profile, which is where the language was already stored durably. **RG-135 to RG-137 were filed on 2026-09-13 from the first field service with NO wrong verse**: eight auto-fires, eight correct, on `ggml-large-v3-turbo` pinned by hand before the microphone opened. None of the three is about detection. One is a translation the preacher named that Relay does not have and never mentioned (RG-135), one is a service timeline that stamped a recovery with no matching loss and then ended on a loss that never recovered, leaving the operator’s screen dead for the final 56.5 minutes (RG-136), and one is a 28.6 second latency tail that this audit could NOT explain and deliberately did not guess at (RG-137). **The service also puts RG-117 in doubt without settling it** — the device disappeared mid-service and the debug recording was written anyway, which that row says cannot happen; §6 of `audits/FIELD-2026-09-13.md` records the one question that separates the two readings and leaves the row open. What did not move: the release decision, RG-116, RG-32, and word error rate, still unmeasured in every language against what is now 196 minutes of church audio — **RG-115 to RG-122 were filed on 2026-09-06 from a live service**, eight rows off one morning and only one of them closed, which is the honest shape of a field audit: most of what a service finds cannot be settled by a commit. Three of the eight are **defects in the instruments** (RG-118 cadence counting silence, RG-120 an end-to-end stage that recorded 0 samples against three fires, RG-122 no record of which microphone a service used), which continues the pattern this register keeps recording about itself. **RG-119 is the one to look at first**: the main output was lost and recovered three times in 85.5 minutes, 22.6% of the service, and nobody yet knows whether the screen stopped painting or the heartbeat stopped arriving. The original three, and only the first of the three is closed: a chapter the preacher stated out loud was answered from memory and put **Psalms 92:10** on a real output at 0.88, which is the second wrong verse in two field services and is fixed and held by a test that reproduces it when reverted. The two left open are the ones a commit cannot settle — the STT model turns out to be an **accuracy** trade the product offers as a speed setting (**5 of 9 auto-fires correct on `ggml-base` against 3 of 3 on `turbo`**, in the same service), and a runtime audio-stream error is one line on stderr that stops nothing and tells nobody. RG-32 open, RG-41 and RG-50
flagged, **RG-83 carried over** from the 2026-09-02 audit, RG-98 and RG-101 **closed on 2026-09-05** — the dependency tree is now audited in
CI on both sides, and `cargo audit` reports zero vulnerabilities — and **RG-113 filed the same
day and deliberately not fixed**: seven smaller database findings that would have meant seven
unproven edits riding in the same commit as a P0 migration repair. **RG-85 and RG-108 were filed as declined and then taken on the same day**,
at the operator's instruction: both are now closed, held by tests, and probed against the
packaged binary — what neither can prove without a room is stated in its own row. **RG-95, RG-96 and RG-97 were closed on 2026-09-04**, and
**RG-102 … RG-112 were filed on 2026-09-05 and all but RG-108 closed the same day** — among them **two P0s in the corpus
repair committed the day before**: it was placed on the `user_version == 0` branch, which no
shipped install is on, and it opened a transaction inside one, so it returned an error on every
path it could have run down. Of the eight not closed, **two are not code**: RG-83 is a
publishing action and RG-73's certificates are a purchase.

*These four numbers are asserted against the table itself by `relaygap.test.js`; they drifted
apart twice (this block once said 54 and 51 in consecutive sentences) and a count in prose beside
the table it counts is the easiest possible thing to check automatically.*

| | |
|---|---|
| ✅ **165 closed** | Every gap this report could reach from the code. **The two added on 2026-09-04 were in the corpus, not the code** — the bundled KJV was six verses short, and a verse number in it is a position, so "Matthew 22:37" put the words of 22:38 on a wall. Both instruments that could have caught it were derived from the broken file and agreed with it, which is the same seam as everything below. **The last thirty-four were not in the brief at all — they were found by auditing the things that audit Relay**, and that is now the most productive seam in the project: a contract test that scanned one Rust file, another that read four frontend files out of nine, a CI job that ran one Node version so the fix for every other one was never exercised, a launch screen whose checks could not fail, an edit-time gate whose watch list had drifted behind four safety files, an agent brief describing a deleted component and a closed defect, four citations pointing at a section that does not exist, sixteen more pointing at a directory that is not in the repository, and a register table this report corrupted itself. **The instrument is wrong more often than the code it audits.** **RG-178 is the exception that is worth naming**: it came from neither the code nor an instrument, but from a recording of a real service, and it is the first row here filed from a wrong verse a congregation actually saw with the audio still on disk to prove it. Nothing that audits Relay was going to find it, because every instrument agreed with the decoder. |
| ~~1 withdrawn~~ | **RG-27** was filed from a mid-service snapshot and was wrong. Struck through, not deleted |
| ⚠️ **RG-41** | Not work. A correction kept on the record: two of six views were wrong to "fix" — they are routers, and a heading there would give one screen two |
| ⏳ **RG-32** | **Open on purpose.** A context-resolved bare verse is labelled `Direct` at a hardcoded 0.88 — by rule 10 that label is a lie, because Relay inferred the book rather than hearing it. Changing it makes every in-passage *"verse eighteen"* cost a click, and **one service is not enough evidence to spend that**. Wants a second and third Sunday |
| ⚠️ **RG-50** | **Needs a human, not a commit.** `translations` is the last table an operator cannot fill. The reason first recorded here was wrong — see §19b for the decision it actually needs |
| ✅ **RG-83 · RG-114** | **Closed 2026-09-05 at the second attempt, and not by publishing anything.** The first fix pinned the endpoint at a version tag — which resolves, and leaves every build able to learn only about its own version. RG-114 is that mistake, filed rather than quietly corrected, because *"the instrument went green"* is exactly how it survived review.** The endpoint is pinned at the tag of the release that already exists and already carries a signed `latest.json`, so the channel is live with nothing new shipped — and `version.mjs` owns the pin, so a version bump cannot leave it behind. The row below is what it used to say. ~~The updater endpoint still resolves to 404 — that is a *publishing* action (promote a full release), not a commit, and the fix pass instead made it **visible**: `npm run updater:check` reads the endpoint out of both configs and fetches it, and Settings → Updates now reports the channel rather than the absence of news (RG-92) |
| ✅ **RG-85 · RG-108** | **The two the audit declined, taken on 2026-09-05 at the operator's instruction.** The console's policy names Relay's own ports instead of the whole internet, and the kiosk hub refuses a handshake from a page Relay did not serve. Both are held by tests and both were probed against the **packaged binary** — 403s and 101s, by origin. The part that still needs a room is unchanged and is stated in both rows: whether a background video paints on a wall, and what `Origin` a real OBS browser source sends |
| ✅ **RG-84 · RG-86 · RG-87** | **Closed 2026-09-03.** Both audio queues are bounded and count what they shed; rule 33 now states its one deliberate exception instead of overclaiming; and `crossrefs.test.js` grew a fifth dimension that resolves every relative Markdown link, with a guard asserting the scanner still sees links |
| ✅ **RG-95 · RG-96 · RG-97** | **Closed 2026-09-04**, having been filed and deferred the day before. Seven list surfaces now say why a read failed instead of telling the operator to redo the work, and `chapterVerses` stopped swallowing into a bare `catch {}`. The media server answers `Range` — `206`, `416` and `Accept-Ranges` on every reply — verified with `curl` against the packaged binary, not only in a test. The LAN server caps concurrent requests at 64 and refuses over that with an immediate `503` rather than a queue, and its read deadline now covers the whole request head rather than the first packet |
| ✅ **RG-102 … RG-105** | **The corpus repair, repaired — 2026-09-05.** RG-99's fix was committed on 2026-09-04 and **could not reach a single existing install**: the rung sat on the v0-only branch (RG-102) and, on the branch it did sit on, opened a transaction inside one and errored out of `migrate` altogether (RG-103). Both are P0s of the same shape as the defect they were fixing — a correct-looking repair that never runs. Fixing them exposed two more: the repair erased the verse reference from every past detection (RG-104), and it deletes 31,102 parent rows against an unindexed child column, which measures **8.3 s at boot** (RG-105) |
| ✅ **RG-95, completely** | **Closed across three passes and finished 2026-09-05.** Eleven operator-facing lists say why a read failed instead of telling somebody to redo work they have already done, and the two reads that had no key — `chapterVerses` and `planItems` — are guarded. `planItems` stays a swallowing read on purpose: the run surface's `itemsLoaded` flag has no `finally` behind it |
| ✅ **RG-106 · RG-107 · RG-109 · RG-110 · RG-111 · RG-112** | **Also closed 2026-09-05.** The kiosk port gained the handshake deadline, the connection cap and the frame-size limit the HTTP port already had; an imported SVG is no longer served as an active document; fifteen documented claims that were false against the code were corrected — including a safety table scoring *Preview ≠ Programme* against 17 tests deleted three weeks earlier, and a README still advertising the 31,100-verse corpus a P0 had just repaired; five template controls stopped rendering `[object Object]` at an operator; `Channels`'s error state was made reachable at all; and a `.proplaylist` can no longer expand without bound on import |
| ✅ **RG-98 · RG-101** | **Both closed 2026-09-05, and the second one's own diagnosis was wrong.** `cargo audit` now reports **0 vulnerabilities**: `h2` went by lockfile update, and the two 7.5-HIGH `quick-xml` advisories — recorded here as needing an upstream Tauri bump — went the same way, because `plist` 1.10.0 already depends on the fixed `quick-xml` and a dependency arriving *through* Tauri is not necessarily pinned *by* it. The npm advisories were answered by moving the lever that existed: the dev server is loopback unless `RELAY_DEV_LAN=1` (DECISIONS §65). Both tools now run in CI |
| ❓ **§19b** | **Two open questions, neither a commit.** The translation one above, and — added 2026-08-31 — whether a detection should carry the audio quality it was heard in. The measurement exists (`dsp::AudioQuality`); this report previously said it did not, and that error is corrected in place |

### What the 2026-09-17 consolidation added, and what it deliberately did not

Four branches ran in parallel off one green commit and were merged one at a time,
each verified before the next. **None of them moves the release decision**, and none
touches detection, the router, or any threshold: rules 10, 28, 30 and 34 are not in
this diff at all.

| | |
|---|---|
| **A background is a payload** (DECISIONS §94) | The entire layer stack sat inside `{#if content}` and `media_url` was written at one site in the whole binary, so a verse and a picture were mutually exclusive: scripture over the church's own background was impossible. It has its own retained slot, its own hello replay and its own rehearsal verdict, and **`clear` and `black` still take everything** — the invariant was written first and proved by reverting it, which replayed the picture onto a screen that joined after the operator had cleared the wall |
| **The stage page has an identity** | `stage.html` sent `hello` with no channel and accepted `stage_alert` unconditionally, so a word meant for the preacher was shown to anyone on the LAN who opened the URL. Not claimed as a security boundary — §35 and T4 say the LAN is trusted and anyone can type `?channel=2`. It closes the accident |
| **Per-screen clear and blackout** | The whole-wall controls are byte-for-byte untouched and `preflight` gained nothing: the split is in the call, never inside a panic function |
| **A projector that stops guessing** (DECISIONS §95) | `open_channel_output` let a stale monitor index fall through the placement block, so unplugging the projector and pressing Open put **the congregation's output over the operator's console**, silently. Stable display identity is deliberately not built and §95 records the reading: no native id exists on either platform that survives a replug |
| **A shared control layer** (DECISIONS §93) | `.r-iconbtn.sm` did not exist, which is the literal cause of the 26-in-a-row-of-22 misalignment: earlier waves correctly stripped the local overrides, which made the step permanent. Plus a row painted in two different ambers, and amber spent on *selected*, *pressed* and a *focus ring* on the preacher's own screen |
| **The rail ruling** (DECISIONS §92) | Two waves built opposite behaviour into twelve lines and the merge settled it on evidence rather than recency — prose in a slot `--tch` sizes from digits re-creates the failure `MIN_TIMER_PX` exists to prevent |

**Carried forward, in the agent's own words rather than smoothed away:** `paintState`
still reports `clear` for a backdrop-only screen. It is a closed enum parsed on the
Rust side, and widening it touches the health contract on both sides plus
`describeScreen`. Arguably rule 35's shape, left rather than half-widened.

**What none of this has:** a browser-driven pass against a real backend. jsdom
computes no layout, so nothing here verifies a painted pixel, and every defect that
has reached a congregation was invisible to every static instrument in this repository
while `qa-inventory` reported zero problems and was right.

### The 2026-09-17 sweep, and what it says about this register

**Eight rows were open against code that had already been fixed.** RG-117, RG-118,
RG-120, RG-121, RG-122, RG-127, RG-142 and half of RG-162 were all closed by work that
landed on one branch or another and never reached this file, because the branch that
fixed them was not the branch this file was edited on. One row, RG-144, was genuinely
open and was fixed in the same sweep.

That is a **35% false-open rate** on the rows this register was carrying, and it is
worth stating plainly because an open row is how this project decides what to work on
next. Somebody reading this file on 2026-09-16 would have found eight pieces of
finished work waiting to be done, and one of them — RG-117 — **directly contradicted
CLAUDE.md rule 5**, which had been updated and was telling the truth.

The cause is structural rather than careless. Five waves ran in parallel off one
commit, each with its own copy of this file, and a register is a document that cannot
merge: two branches closing different rows produce a conflict in every row between
them, so the safe resolution is to take one side, and the other side's closures are
lost in silence. The renumbering in §23a is the same failure in its other form.

**What would actually fix it is not more discipline.** It is closing a row in the same
commit as the code, and a check that a row marked open still reproduces. Neither exists
yet, and this paragraph is not a plan — it is the evidence for one.

### What is left is not a list of bugs

Nothing in the six rows above is what stands between Relay and a church. **This is:**

| | Why no commit closes it |
|---|---|
| **A projector** | RG-18's contrast and distance thresholds have never been checked against a wall |
| **The audio half of a real room** | A preacher has now been transcribed for 49.5 minutes — but **word error rate has never been measured, in any language.** That is the moat, and it is still an assertion |
| **A Yorùbá speaker** | The numerals parse as of RG-126 and are **capped at *suggest* until somebody checks them**; the alias table is unreviewed. A wrong numeral does not fail safely; it silently shows a different verse — the cap is what makes that survivable, and a signature is what lifts it |
| **Two code-signing certificates** | **Neither platform has one** — Apple's six `APPLE_*` secrets are absent as well as Windows's, so every release so far is unsigned on both. The macOS chain is wired and `sign-local.sh` reproduces its conditions ad-hoc, which is not the same as having shipped one (RG-73) |
| **A second and third service** | RG-32 waits on them, and so does any claim that 2026-08-30 was typical |
| **One service run by somebody who did not write Relay** | The largest unknown in the project |

### The one number that moved

**Field validation, 0 → 2 / 10.** Relay ran a live sermon on 2026-08-30
([`audits/FIELD-2026-08-30.md`](audits/FIELD-2026-08-30.md)): Stage F11 answered — **no
drift** across 49.5 minutes and 2,423 decodes — and five of six auto-fires correct.

**That morning is the argument for NO-GO, not against it.** Fifty minutes in a room produced
**seven findings** that months of reading source had not, including a wrong verse on a
congregation's wall. One of the seven was itself wrong and was withdrawn. That ratio is what
"half of this product has never been reached by an instrument" means when somebody finally
reaches it.

---

## 0. Method, and what this report cannot see

**How every claim here was produced:**

| Kind of claim | How it was established |
|---|---|
| Counts | A command, named beside the number. Re-run it; if it disagrees, this file is the bug — and see the standing note below about what that disagreement means |
| "EXISTS / PARTIAL / MISSING / DECLINED" | Read the code, and cite a **file and a symbol** — never a line number (§2's preamble says why) |
| Latency figures | Quoted from `audits/PERF-2026-08-24.md` and `audits/PERF-MODELS-2026-08-30.md`, not re-measured. Those documents state what their own numbers do not establish, and none of that changed |
| Detection behaviour | Quoted from the corpus gate (`eval.rs`), the closed findings in `audits/QA-2026-08-14.md`, and the one live service in `audits/FIELD-2026-08-30.md` |

**Counts: this document does not keep its own copy any more.**

[`QA_HARNESS.md`](QA_HARNESS.md) §0 is **the** register of counts for this repository —
every row there carries the command that reproduces it. This section used to restate a
dozen of those values and, on 2026-08-31, restated them wrongly **within the same day**:
`main.rs` was measured at 5,573 lines in the morning and was 5,640 by the afternoon,
because the afternoon's own commits changed it. That is the fifth time these numbers have
been corrected in a week (§18) and the fourth time this document did the correcting.

So the two that drift fastest are named by their command and nothing else:

| | Command |
|---|---|
| `main.rs` size · registered `#[tauri::command]` | `wc -l src-tauri/src/main.rs` · `grep -c '#\[tauri::command\]' src-tauri/src/main.rs` |
| `stores/capture.js` size | `wc -l src/lib/stores/capture.js` |
| Rust and frontend test totals | `cd src-tauri && cargo test` · `npx vitest run` — **the runner's own summary line**, never a grep |
| Components · controls · the command map | `node scripts/qa-inventory.mjs` |
| Tables in the schema | `grep -c 'CREATE TABLE' docs/data/schema.sql` |
| Numbered decisions | `grep -cE '^## [0-9]' docs/DECISIONS.md` |
| Labelled detection cases | `python3 -c "import json;print(len(json.load(open('src-tauri/data/eval_corpus.json'))['cases']))"` |

> ### The standing note about counts — read this before "correcting" one
>
> **Every number this document ever wrote down was wrong within a week, and twice within
> a day.** That is not carelessness; it is what a number in prose *is*. RG-20 was filed
> because six documents disagreed about the size of `main.rs`; this report corrected them
> to 4,369 / 118 / 1,941 on 2026-08-29; §18 corrected *those* to 5,723 / 137 / 2,195 on
> 2026-08-30; a fourth pass said 5,573 / 132 / 2,156 on 2026-08-31 — and by that
> afternoon it was 5,640 / 132 / 2,159.
>
> **The durable half of every row is the command, not the value.** Three counts here were
> also wrong because a plausible one-liner was believed over the tool that actually knows:
> `npx vitest list | wc -l` counts compiler warnings on the same stream, and
> `grep -n '#\[ignore'` counts the phrase inside doc comments (`e2e.rs` has **three** such
> lines and **zero** ignored tests). **A grep is not a test runner.**

**What no instrument in this repository reached, and this report therefore does not claim:**

pixels out · hardware other than one M4 Pro · a second operator · a congregation that did not
know it was a test. **Audio in and a packaged build are no longer on that list** — a live
sermon was transcribed for 49.5 minutes on 2026-08-30 (`audits/FIELD-2026-08-30.md`) and the
signed-bundle conditions were reproduced on a real `.dmg` with `scripts/sign-local.sh`.
Anything below marked **BLOCKED** is blocked on the world, not on a commit.

---

## 1. The verdict, on one page

**The brief's single highest-priority recommendation — "Phase 1: make Relay extremely fast",
"⚡ Real-Time Speech Engine 2.0", "faster partial transcription (P0, build now)" — was already
built, already measured, and already hit most of the brief's own targets five days before the
brief was written.**

| Brief's target (§10) | Relay, measured | Source |
|---|---|---|
| First visible partial ≤ 300 ms | **139 ms** median on `ggml-base` | PERF §4 |
| Perceived transcript lag ≤ 1 s | **P95 339 ms** on `base` | PERF §4 |
| Dropped partials < 1 % | **0 of 1075** passes | PERF §4 |
| Real-time factor < 0.7 | decode 144 ms per 200 ms cadence step → **~0.72 duty**, and the cadence *is* the decoder's own speed by construction | DECISIONS §38 |
| Mic → screen ≤ 2 s p95 | still **NOT MEASURED** — it needs a stopwatch in a room, not an instrument (§61) | PERF §5 |

Two of the brief's specific mechanisms are worse than what shipped: a **two-speed / short-window
fast path** (§6, §8) buys nothing, because whisper pads its mel window internally and an 8 s and
a 4 s window cost the same (DECISIONS §36); and **adaptive window sizing** (§11) is superseded by
adapting the *cadence* to measured decode cost, which is the lever that actually moved 349 ms →
139 ms.

**The five weaknesses this section named on 2026-08-29 have all been closed.** They are listed
here rather than deleted, because a verdict page that quietly loses what it used to claim cannot
be checked:

| What was weak on 2026-08-29 | Now |
|---|---|
| **Output truth** — Live derived every badge from global state, so a kiosk that went away still read *On Air* | Closed, RG-01/RG-02. Live and the Outputs tab read one backend fact through one helper, and every output page reports that it is still painting |
| **Nothing survived a quit** — every latency measurement died with the app; no timeline, no replay, no report | Closed, RG-04/RG-07/RG-08/RG-14. `service_events` + `perf_samples`, a replay, a Sunday report, p99 and a week-on-week trend |
| **No rollback** — a bad install had no way back | Closed, RG-06 — for the **data**, which is the half that cannot be got back. Binary rollback is deliberately not built (DECISIONS §43) |
| **No pre-air validation** — fit was measured, never *validated before* air | Closed, RG-05. One validator at the one door content leaves by (DECISIONS §42) |
| **The moat is unmeasured** | **Unchanged, and it is now the whole list.** WER: zero, in every language. Native-speaker review: zero. Yorùbá numerals: present as of RG-126, and **capped at *suggest* because that review is still zero** — parsing them changed the coverage, not the measurement |

**And two of the brief's proposals would reverse recorded decisions** — LAN device pairing
(DECISIONS §35) and an optional cloud layer (ROADMAP §3). Both are written up as reversal
*proposals* in §20. Neither is adopted, and §2 now records them as **DECLINED** with the reason
in the row rather than as gaps.

**So the honest one-line summary has inverted since the first pass.** On 2026-08-29 this report
said *the fast half is done, the trustworthy half is thin*. Both halves are now built, and
**every remaining item is blocked on something that is not a commit**: a projector, a Yorùbá
speaker, two code-signing certificates (neither platform has one — RG-73), and nine more Sundays.

**Release decision: NO-GO for general release · GO for a supervised pilot** — made on
2026-08-31, recorded in §24 and DECISIONS §60, and unchanged by this pass. Not because of
anything in this report: because one service, watched by the person who wrote the software, is
one service.

---

## 2. Status matrix — the brief's §4–§77, one row each

*Re-verified 2026-08-31 against the working tree, after RG-01 … RG-22 and the two `e2e`
defects closed. **31 rows moved.** The 2026-08-29 version of this matrix is not preserved:
every row it got wrong it got wrong by being a week old, and a stale matrix beside a current
one is two answers to the same question.*

> **The evidence column names a FILE and a SYMBOL, never a line number.** §18 is the reason:
> this document has already corrected three counts that were wrong again within the week, and
> `file:line` rots the same way — faster, and more convincingly, because a wrong line number
> still looks like a citation. A symbol survives an edit above it; where a test pins the claim,
> the test is named instead, because that one cannot rot silently at all.

Legend: **EXISTS** (implemented and reachable from a rendered control) · **PARTIAL** ·
**BROKEN** · **MISSING** · **DECLINED** (would build the wrong thing, with the reason) ·
**N/A** (conflicts with the product's scope) · **FUTURE** (valid, deferred) ·
**BLOCKED** (waits on the world, not on a commit).

### Reliability, readiness and the live-service envelope

| § | Requirement | Status | Evidence | What is actually missing |
|---|---|---|---|---|
| 4 | Relay Reliability Engine — one unified READY / DEGRADED / ACTION REQUIRED state | **EXISTS** | Three layers, one answer: 23 launch probes on a 4-level severity ladder (`boot/probes.js`, `boot/boot.js`), a rolled-up verdict on the readiness screen (`views/Dashboard.svelte`), and — since RG-09 — a live one-line degraded state in the **shell**, on every tab, opened for the detail (`degraded.js`, mounted in `App.svelte`; DECISIONS §45) | Nothing that should be built. **An engine-side health state is deliberately NOT built** — see DECISIONS §61. The only thing Rust could do with one is refuse to put content on a screen, and a gate that can refuse the wall is the failure mode §20 and §42 both exist to prevent |
| 5 | Sunday Readiness screen | **EXISTS** | The Dashboard hero says "Ready for a service." / "N things not working." over the same `makeProbes()` the launch ladder runs — and RG-15 added the part that was actually missing: **say one verse and watch six stages** between a microphone and a screen (`pathcheck.js`, driven from `Dashboard.svelte`, pinned by `pathcheck.test.js`). It runs in rehearsal or not at all | Nothing. The 21 probes can all pass on a machine where nothing works end to end; that is precisely what the walk exists to catch, and it says which stage was not reached rather than blaming the next one |
| 6 | Service Lock — block destructive config while live | **EXISTS** | `servicelock.rs`. **17 actions** held back while a service records — **ten** `delete_*` (the tenth is `delete_service`, added 2026-09-03 with RG-89), plus the **seven** that take the engine away mid-sermon (model download / swap / reload / install-from-file, the Bible translation, media import, saving an import). Enumerated, never predicate-matched. The operator lifts it in one action. `e2e::a_recorded_service_holds_back_a_deletion_but_never_the_wall`, `servicelock.test.js` | Nothing. **Nothing on the fire path is protected and that is the design** (DECISIONS §40) — panic, nav, fire, rehearsal, sensitivity, template swap and template editing are all deliberately unaffected, because each is a repair tool at 10:31 |
| 30/63 | Graceful degradation + a formal failure matrix | **EXISTS** | RG-09. Every fallback that was previously silent now produces a row saying what it costs the service and what to do: denoise off below 48 kHz, audio-only with no model, a CPU-only build, detection disarmed, safe mode, rehearsal. **Nothing is inferred** — a row appears only when something Relay measured says so (`degraded.js`, `degraded.test.js`; DECISIONS §45) | Nothing. The failure matrix the brief asked for is this table plus SECURITY.md's T1–T10, both in-repo |
| 31/32 | Automatic recovery, operator-confirmed | **EXISTS** | `RecoverSession.svelte` restores **position only** and says so; `liveOnAir` is a separate fact precisely so it cannot be restored (`qa-r5-onair.test.js`). Since RG-04 the service's own record survives the crash as well (`service_events`) | A crashed service still leaves an open `services` row with no in-app way to close it. It is cosmetic in the history list and has never cost anything live |
| 33/50 | Panic system: Clear · Black · Restore, and never claim an unachieved success | **EXISTS** | DECISIONS §20. `panicRun` returns a boolean **and** sets `panicError`; shell-level panic bar in `App.svelte`; `shortcuts.js` suppresses `Esc` while a dialog is mounted; the pre-air validator deliberately cannot touch a panic control (DECISIONS §42) | **Restore-what-was-there is still not a control, deliberately.** Putting back content the operator cleared, without them choosing it again, is the one panic action that can itself put a wrong verse on a wall. Clearing is safe by construction; un-clearing is not |
| 34/46 | Emergency manual mode when the AI is unavailable | **EXISTS** | Manual fire is first-class and always present (`Live.svelte`); detection can be disarmed (`toggleDetection`); safe mode disarms it wholesale — and RG-09 is what closed this: a lost model, a disarmed detector or a CPU-only build now each say so **in the shell, on every tab**, in the operator's words | Nothing. There is still no *mode* called "manual", and there should not be: manual is not a mode in Relay, it is the floor the product stands on |

### Speech, transcription and detection

| § | Requirement | Status | Evidence | What is actually missing |
|---|---|---|---|---|
| 7 | PARTIAL / STABLE / FINAL transcript states | **DECLINED** | PARTIAL and FINAL exist end to end (`TranscriptUpdate.is_final`, `applyTranscript`, and a visually distinct render — partial in `<mark>` with a caret, finals as timestamped rows) | **STABLE is the wrong instrument for the problem it was proposed to solve.** A stable-prefix state describes the *text*; the harm is a wrong *verse*, and the router already holds a reference from a partial window at `Suggest` until a second pass agrees (`router::decide_live`, CLAUDE.md rule 28). A text-level state would add a third vocabulary to the same fact and could not gate anything the corroboration rule does not already gate |
| 6/8 | Two-speed transcription (fast path + accuracy path) | **N/A as framed** | DECISIONS §36 and PERF §4: whisper pads the mel window internally, so a shorter window costs the same; above `base` the model is the entire remaining latency | A genuinely cheaper second decoder is a different *model*, not a different window — a large piece of work, and not what §8 describes |
| 9 | Streaming / rolling / incremental decoding | **MISSING** | Every emission is a whole-window `state.full()` re-decode (`stt.rs`) | Token-level streaming is a whisper.cpp capability question, not a Relay wiring question |
| 10 | Hard latency acceptance targets | **EXISTS** | `audits/PERF-2026-08-24.md` §5 scores against a target table per model and marks three rows **MISS** and four **NOT MEASURED** rather than rounding them off; `audits/PERF-MODELS-2026-08-30.md` adds what each model costs an operator in cadence | Targets on **church hardware**. Every number is one M4 Pro (§72) |
| 11 | Adaptive inference | **PARTIAL — superseded** | The cadence adapts to measured decode cost (`step_samples_for`, EMA, clamped to one chunker hop … 1000 ms). That is the lever that moved 349 ms → 139 ms (DECISIONS §38) | `sysprobe.rs` is **advisory only** — nothing in the pipeline branches on probed hardware. `gpu_backends` is a compile-time fact, deliberately (DECISIONS §36) |
| 12 | Scripture candidate prefetch during the utterance | **DECLINED** | Detection already runs off the decoder's thread behind a bounded queue (CLAUDE.md rule 33) and costs ~2.6 ms/query on 31k verses | Prefetch optimises the cheapest stage in the pipeline. §3 and CLAUDE.md rule 31 |
| 13 | Voice confidence shown separately from claim type | **PARTIAL** | Claim type is first-class and correct: amber chip + meter for Direct, cyan chip and **no number at all** for a guess (`detect.js`, `Live.svelte`; DECISIONS §21). RG-63 added the second question beside it — *is there a verse behind this reference at all?* — because a confidently-heard "Psalms 23:99" is a claim the operator could not previously distinguish from a working one | There is still no **voice** confidence *per detection*. Audio quality is surfaced continuously (clipping / too quiet / noisy) but is never attached to the claim it was measured beside. **An earlier draft of this row said that would need an SNR "the decoder does not currently expose" — that was wrong and is corrected here rather than quietly amended:** `dsp::AudioQuality` already carries `snr_db`, `clip_ratio`, `speech_prob` and a `warning` naming the single most important thing wrong right now. What is missing is a *decision*, not a measurement — see §19b |
| 14 | Explainable detection — "why this verse?" | **EXISTS** | `DetectionInspector.svelte`, opened from Live. Renders real evidence only — the parsed span, or the shared rare-word chips — and explicitly refuses to fabricate reasoning. `inspector.test.js` | Nothing |
| 15 | One Scripture Safety Firewall | **EXISTS** | Closed by RG-05. `Fire` is constructed only via `resolve_fire`; `broadcast_with_clock` is the **one** caller of `channels::broadcast_content`, and `pipeline::preflight` sits in it — so the AI path, the manual box, spoken nav, plan cues, media, the emergency announcement and the countdown are validated by one gate (CLAUDE.md rule 36, DECISIONS §42) | Nothing. It refuses only what is unambiguously broken and silently so; it never checks that a screen is attached, and the panic controls do not pass through it at all — both deliberate |
| 27 | Church-local vocabulary learning | **PARTIAL** | `voice_profiles.bias_terms` is editable in Settings and feeds whisper's decoder prompt | No learning. Nothing observes what this church actually says and adapts ranking. Blocked on the same thing as §43: nobody has measured whether it would help |
| 28 | Speaker profiles | **PARTIAL** | `voice_profiles`: name, language, bias vocabulary, sensitivity, learned `auto_fire`/`suggest` pair. RG-10 attaches one to a room | No speaking-rate, typical-volume, microphone or common-books calibration |

### Outputs, screens and templates

| § | Requirement | Status | Evidence | What is actually missing |
|---|---|---|---|---|
| 16 | Safe Screen — validate before air | **EXISTS** | RG-05. `pipeline::preflight` refuses a payload that would paint an empty screen, or one carrying a template the output page cannot parse, at the single choke point — and the screens are left exactly as they were (DECISIONS §42). `safescreen.test.js`, two `e2e` tests | Nothing that should be added. It deliberately does **not** refuse for an unreachable screen (a service runs on the console preview all the time) and deliberately cannot refuse a panic control |
| 17 | Output health monitoring | **EXISTS** | RG-01. Live and the Outputs tab now decide from one backend fact through one shared helper (`outputHealth.js::describeScreen`, imported by `views/Live.svelte`), so a screen that is not answering can never read amber on the one surface an operator watches. `outputhealth.test.js` | Nothing. `network_client` is still reported as served-not-seen, which is what it is |
| 18 | Output heartbeats | **EXISTS** | RG-02. Every output page reports that it is still painting — the native window over the bridge (`output_beat`), kiosk/OBS over the socket it already has (`{"kind":"beat"}`). The grace window is derived from the interval, not written beside it, and a lost beat degrades to "silent", the safe direction (`channels.rs::OutputHealth`) | Nothing. It is **anonymous by construction** — a beat says "the screen for channel N painted", never who or from where (DECISIONS §39 narrows §35 by one word: *when*, never *who*) |
| 35 | Automatic text-fit safety | **EXISTS** | Fit is measured, not assumed (`TemplateRender.svelte::fitOne`, binary search for layered templates) — and since RG-05 a fit that has shrunk below **45 % of the size the template's designer asked for** reports it, and Live says how small it went. The floor is a RATIO, because cqw is a share of the output's width (CLAUDE.md rule 37) | Nothing. It still shrinks and still shows the verse below the floor — blanking a screen is strictly worse for a congregation |
| 36 | Distance preview (5/10/15/20 m) | **EXISTS** | RG-18. `legibility.js::PREVIEW_DISTANCES_M` + `previewScale`, rendered in `views/templates/TemplateEditor.svelte`. The arithmetic uses two numbers only a person can know — image width and back-row distance — remembered with the room, and with no numbers there is **no verdict** rather than a guess | **The thresholds are unverified and the UI says so.** WCAG is a specification for screens at arm's length and the character-height rule is broadcast practice; neither has been checked against a projector. Stage B |
| 37 | Accessibility mode (high visibility) | **EXISTS** | RG-18. High Visibility is a **look a template carries**, not a parallel renderer — so it inherits the one template engine and cannot drift from it. It shipped as a THEME; themes were folded into templates (DECISIONS §87), and Track D then seeded it as one of five coordinated template FAMILIES across all five content kinds (DECISIONS §88) rather than leaving it as a style only an existing pin could reach. Its bytes are still the frozen `src-tauri/data/legacy_themes.json` values, at the same 21:1 (`legibility.test.js`, which measures those frozen bytes and says so). Contrast is computed exactly over a solid background and reported as **"cannot be checked"** over a picture, never guessed. The console side was already served: `sr-only` live regions, `role="meter"`, `trapFocus`, reduced motion | Nothing on the shelf-pick question: a church that never chose the old theme now has `High Visibility · Scripture/Lyrics/Media/Announcement/Timer` to pick directly, the same day the row briefly named its own gap (2026-09-16) — the status is a same-day flip, not a later sweep catching a stale one. Plus the same Stage B caveat as §36: the thresholds are unverified |
| 54 | Signed template library / marketplace | **FUTURE** | Deferred with reasoning (`KNOWN_ISSUES.md`) — plugins must never modify the presentation engine | Agreed; not v1. Blocked on the same absent signing ceremony as §44 |
| 44/45 | Distance/fit/contrast gate before air | **EXISTS** | Consolidates §16, §35, §36, §37 — one validator (`pipeline::preflight`), one refusal path, one honest message, plus the editor-side review that catches it before Sunday | Nothing |

### Security, privacy and trust

| § | Requirement | Status | Evidence | What is actually missing |
|---|---|---|---|---|
| 19/20/21 | Local device pairing · trusted devices · authenticated LAN | **DECLINED — would reverse DECISIONS §35** | The `:8032` control plane is unauthenticated **deliberately**; the `:8031` hub is broadcast-only and counts clients without recording who they are. What *was* closed (2026-08-20) is the bystander-browser vector: mutating routes require `POST` and are denied the CORS wildcard (SECURITY.md T2, T3) | Nothing to build. §20 (a) is the written-up reversal proposal and it is **not adopted** — the preacher's phone has no way to hold a credential |
| 22 | Local security event log | **DECLINED — follows §19** | No table, no events | It would log an identity that, by §35 and §39, is deliberately never established. A log of anonymous events is a log of nothing |
| 23 | Service history as an event timeline | **EXISTS** | RG-04. `service_events` + `perf_samples` — an ordered record that survives the app, merged with detections and cues on the way out (`db/services.rs`, `views/library/History.svelte`). Output-lost and output-recovered are real events now that §18 detects them | Nothing. **It carries nothing a preacher said**, pinned from both sides (`timeline_tests::nothing_a_preacher_said_reaches_the_timeline`, `timeline.test.js`) |
| 24 | Tamper-evident service record | **DECLINED** | The prerequisite (§23) now exists, so this became cheap — and cheap is not the test | A hash chain defends against somebody with the machine, and the malicious operator is explicitly out of scope (SECURITY.md T10). Building it would claim a guarantee against the one actor Relay has already said it does not defend against |
| 25 | Service replay | **EXISTS** | RG-07. A timeline row opens to show what was being said around it, what Relay decided, and how fast it was going (`report.js::replayAt`, `History.svelte`) | Nothing. It replays the **record**, not the audio — Relay never stores audio (SECURITY.md T1) |
| 26 | Sunday report | **EXISTS** | RG-08. `report.js::sundayReport`, derived and never stored, and it names what it does **not** measure. Every field can come back `null`, and `null` renders as "—", never as 0 (DECISIONS §44) | Nothing. There is deliberately no "crash-free" line: crashes are recorded per launch, not per service, and there is no honest way to attribute one |
| 56 | Diagnostic bundle | **EXISTS** | RG-12. `diagnostics.rs` — one file a church can send, composed as an **allow-list**, with the home directory scrubbed (DECISIONS §48). `diagnostics.test.js` | Nothing. `telemetry.rs` learned the blocklist lesson expensively and this is the correction |
| 57 | Privacy centre | **EXISTS** | RG-17. Settings → Privacy, read from the live settings, stating the LAN exposure in the same size type as the reassuring half (`privacy.test.js`) | Nothing |
| 58 | AI transparency centre | **EXISTS** | `docs/AI_DISCLOSURE.md`, the per-detection inspector (§14) — and, as of this pass, the honest half **in the app and offline**: Help's *"What the AI is bad at"* carries the never-generates-text rule, the African-language weakness and the unmeasured word error rate. `aidisclosure.test.js` (RG-59) fails if either document loses a claim the other keeps | Nothing. A link to a docs file would have been useless: the operator who needs this is offline in a hall |
| 64 | Security threat model T1–T10 | **EXISTS** | `SECURITY.md` now carries the T1–T10 table: content leaving the device, LAN screen control (accepted), the closed drive-by, the broadcast-only hub, path traversal, template injection, the update channel, model integrity, language packs (no surface yet, on purpose) and the malicious operator (out of scope) | Nothing. Two rows are honest absences rather than mitigations, and say so |

### Environment, language and operators

| § | Requirement | Status | Evidence | What is actually missing |
|---|---|---|---|---|
| 29 | Room / environment profiles | **EXISTS** | RG-10. `environment_profiles` + `rooms.js` — microphone, language, service length, voice profile, displays, and the two room numbers §36 needs. Applying is a **list, not a call**, so a room applied to a machine where the projector moved reports which four of six pieces took (DECISIONS §46) | Nothing that should be added yet — see §30 |
| 30 | Automatic room calibration | **PARTIAL, deliberately** | Levels are **learned, never assumed** (DECISIONS §19, CLAUDE.md rule 12) and that is load-bearing: three individually-reasonable absolute thresholds once made Relay deaf to a quiet preacher, silently. `FirstRun` step 3 runs a live meter | **The audio floor is deliberately not seeded from a stored room.** It may well be right, and the instrument that could show it safe (`cargo test audio::gate -- --ignored`, against real room audio) has never been pointed at a real room. Stage C |
| 41 | Language Mode (primary + secondary + code-switching) | **N/A as framed** | Recognition language is a single choice — Auto / en / yo / sw / ha — because that is what the decoder accepts: whisper takes **one** language token per decode. `Auto` *is* the code-switching path, and instability is surfaced (`stt://language_unstable`) | A declared primary/secondary pair would be a setting the model cannot consume. What genuinely helps a bilingual congregation already exists and is per-preacher: `bias_terms` (§27) |
| 42 | Language Quality Centre | **EXISTS** | RG-11. Settings → Languages, every number derived from the shipped data — 66/66 books in all three, alias counts, which numerals are parsed — and **accuracy and native review render as absences, not as zeroes** (`detection::language_report_tests`, `languages.test.js`; DECISIONS §47) | Nothing. The instrument now exists and its most important column is empty on purpose |
| 43 | African-language validation programme | **BLOCKED** | ROADMAP §1: 30 minutes of real sermon audio and a native speaker. `bench/.gitignore` refuses audio into the repo by design | Not a coding task. **This is the moat and it is entirely unmeasured** |
| 44 | Signed language packs | **FUTURE — refused on purpose** | RG-19 shipped the offline half and refused this one | The word doing the work is *signed*: signing needs a key, a ceremony and a distribution channel that do not exist. An unsigned pack that can rewrite the book aliases is a wrong-verse-on-a-wall vector (SECURITY.md T9), and an operator cannot proof-read 66 names in a language they may not read |
| 39/40/48 | Operator training mode · live simulation · rehearsal replay | **EXISTS — narrower than written, deliberately** | RG-15/RG-16. Six drills with the **real** controls on the **real** surface, in rehearsal, panic first — and each knows it was done because the same event fired that would fire on a Sunday (`training.js`, `practice.js`, driven from `Help.svelte`; `training.test.js`; DECISIONS §52) | **Not a simulated service.** Relay cannot produce a sermon, and a simulation would teach a volunteer the shape of a fake. Replaying a church's *own* recorded service audio would work and needs the corpus Stage C has never produced |
| 59/60 | Church onboarding · saved environment | **EXISTS** | The 6-step first-run wizard (welcome · screen · audio · model · language · finish), re-runnable from Settings → Backup; RG-10's rooms for the saved half; the wizard's last step already **fires a real verse to the real screen**, which is the brief's "test scripture"; and RG-61 added the hand-off it was missing — the drills, the six-stage path check and rehearsal, each named with the tab it lives on (`firstrunmic.test.js`) | Nothing. **The three remaining items were deliberately NOT made into wizard steps.** This wizard's rule is that it asks as little as it can — Welcome and Finish ask nothing — and a drill, a path check and a rehearsal are things to *do* on another day, not answers to give now. The gap was never the features: all three shipped. It was that nothing told a new volunteer they existed |
| 61 | Time-to-first-verse metric | **MISSING — needs a stopwatch, not a commit** | — | It is a **human** span: from the preacher saying it to the congregation reading it. `latency.rs` measures machine stages, and the one span that claimed to cover this end to end was measuring how long the preacher had been talking (FIELD F-6, RG-31) |

### Distribution, updates and the cloud question

| § | Requirement | Status | Evidence | What is actually missing |
|---|---|---|---|---|
| 32/48 | Update preflight (signature · platform · integrity · DB compat) | **EXISTS** | RG-06. `updates::preflight` refuses to start an update onto a database that is not already healthy, and checks free disk against the snapshot it is about to take; the payload's minisign signature is verified by the updater plugin; model downloads are separately SHA-256 verified before rename. `updatesafety.test.js` | Nothing |
| 33 | Never update during a service | **EXISTS** | `updater.js::idle()` is now `!capturing && !serviceLock.engaged`, checked on both check and install — so a recorded service with the microphone momentarily stopped is protected, which is the gap this row used to name | Nothing |
| 49 | Update rollback | **EXISTS — and it rolls back the right thing** | RG-06. Snapshot (`VACUUM INTO`) before, verify on the next launch, and a restore that happens **before the database is opened**. DECISIONS §43 | **Binary rollback is deliberately not built.** The installers are public and signed; reinstalling a previous version is a five-minute job. What cannot be got back is the church's database, and that is what this protects |
| 47 | Release channels (stable/beta/nightly) | **FUTURE** | One endpoint: `releases/latest/download/latest.json` | Low value before there is a first release |
| 45 | Offline / air-gapped installation | **EXISTS** | RG-19. Install a model from a file already on the machine, a three-folder scan, and `scripts/offline-bundle.mjs` — installers + model + README on a USB stick (`offline.test.js`) | Nothing. The signed-language-pack half was refused, separately: §44 |
| 46 | Signed distribution, both platforms | **BLOCKED — on BOTH, which this row used to hide** | The chain is wired and pinned on macOS (`relay.entitlements`, `Info.plist`, `models::config_boots`, and `scripts/sign-local.sh` reproduces the hardened runtime **ad-hoc**, which is what makes rule 17 testable without a certificate), and the release gate is **per-platform** and refuses a real tag not covered on both (CLAUDE.md rule 23) | **Two certificates, not one.** `gh secret list` holds only the two `TAURI_SIGNING_PRIVATE_KEY*` **updater** keys; the gate wants six `APPLE_*` secrets and a Windows one, and finds none — so all four releases to date went out on the unsigned pre-release path. **Rule 17's trap is still ahead of this project.** ROADMAP §1 |
| 55 | Deliverability as its own discipline | **EXISTS** | `docs/RELEASING.md` covers signing, the gate, the updater and the version-in-three-files rule; RG-06 and RG-19 added the data-rollback and offline halves this row used to be missing | One **observed** end-to-end install on a machine that is not this one. That is Stage A, not a commit |
| 34–38, 50–53, 77 | Relay Cloud · church account · device registry · fleet management · multi-campus · optional backup | **N/A — declined, with reasoning** | `KNOWN_ISSUES.md` declines cloud sync, accounts, RBAC/SSO, multi-tenancy, analytics dashboards, marketplaces, billing and compliance, each with a stated reason; `PRODUCT_AUDIT.md` marks the same set NOT APPLICABLE | §20 (b) is the written-up reversal proposal if this is ever reopened. It is not adopted |
| 51 | Cloud must never become a live dependency | **EXISTS — as the product's shape** | Offline-first is enforced by construction; `probes.js` reports offline as **ok**, not as a problem | Nothing |
| 76 | Do-not-build list | **EXISTS** | `KNOWN_ISSUES.md` §3 and §5 | Nothing |

### Process, IA and measurement

| § | Requirement | Status | Evidence | What is actually missing |
|---|---|---|---|---|
| 38/49 | Sunday-mode / simplified live screen | **PARTIAL** | Live has a full-screen mode. **The `Normal / Compact` density toggle this row used to cite was deleted on 2026-09-14** on the operator's instruction — this cell claimed it for a while after it was gone | The brief's stripped layout was always a *different* proposal from density, and density is now not even a half-answer. Worth prototyping against a real operator — which is a pilot question (§24), not a design one |
| 65 | Database audit | **See §13** | 21 tables + 1 FTS | Two concepts have no home, and both are declined rather than missing |
| 66 | Event architecture audit | **See §14** | Producers now exist for four of the six candidates | Two remain producerless, and both follow §19 |
| 67 | UI information architecture | **EXISTS** | 8 tabs, **18** Settings sections — Languages (§42) and Privacy (§57) are two of them — retired keys remapped rather than 404'd (`session.js`) | The brief's suggested Settings → Devices would need §19, which is declined |
| 68/69 | Live screen states · design system colour law | **EXISTS** | The four promise-carrying colours are defined once (`src/app.css`) and the six distinctions are each pinned by tests (QA_HARNESS §4.2). Cued is grey and never amber; a guess is cyan and never amethyst | Nothing. This remains one of the strongest parts of the product |
| 70/71 | Performance observability + per-stage budgets | **EXISTS** | Nine named stamps on one monotonic clock, one trace id from microphone to projector (`latency.rs`), 1 ms-bucket histograms with counted tails, and four anti-flattery properties each pinned by a test. RG-14 added **p99**, persisted it to `perf_samples`, and stopped the live Diagnostics screen printing an unreached stage as `0ms` | Nothing. A stage never reached is an ABSENCE, not a zero — in the histogram, in the table, and in the report |
| 72 | Hardware matrix | **BLOCKED** | Every figure is one M4 Pro | Windows low/mid/high, and a real church laptop |
| 73/74 | Real church field test · first ten churches | **PARTIAL — 2 / 10** | Relay ran a live sermon on 2026-08-30 (`audits/FIELD-2026-08-30.md`): Stage F11 answered, no drift across 49.5 minutes and 2,423 decodes, five of six auto-fires correct — **and one wrong verse reached a congregation** | Nine more services, and at least one run by somebody who did not write Relay. That is the largest unknown in the project |
| 75 | Feature priority model (P0/P1/P2/P3/REFUSE) | **EXISTS in substance** | ROADMAP sorts into blocked-on-world / parked / deferred / declined, and this matrix now carries **DECLINED** as a first-class verdict with the reason in the row | Adopting the brief's live-critical / operator-critical labels in `CONTRIBUTING.md` would be an improvement, not a gap |
| 62 | Success metrics | **PARTIAL** | Latency is instrumented **and persisted**, with a week-on-week trend (`report.js::weekOnWeek`). Detection is a CI build gate over a labelled corpus scored **through the real router**, failing above SPEC's 5 % wrong-verse rate, across the whole sensitivity slider. **Acceptance and override are now real** (RG-62, DECISIONS §63): the report separates a verse the operator typed from a suggestion they took, and counts the rejections — which were previously recorded nowhere at all, while the report printed `0` for them | Time-to-first-verse (§61) still needs a stopwatch. **The acceptance denominator is answers, not offers** — a suggestion that scrolls away unanswered is deliberately not recorded, because suggestions are not debounced and one paraphrase would write hundreds of rows a minute. The report says so itself. Language metrics need §43 |

---

## 3. Performance audit

Nothing in this report re-measured performance. `audits/PERF-2026-08-24.md` is current, was
produced five days ago, and its §6 already states what it does not establish.

The one thing worth adding, because the brief asks for optimisations that would be wasted:

**Do not prefetch scripture candidates (brief §12) and do not shorten the window (§6, §8, §11).**
Detection is not the bottleneck and has not been since 2026-08-24. It costs ~2.6 ms per query on
a 31k-verse linear scan, runs on its own thread behind a bounded queue (`main::DETECT_QUEUE`), and shed
**zero** partials across 1075 passes. Above `ggml-base`, PERF §4 is explicit: *the model is the
entire remaining latency.* Optimising the 2.6 ms stage to make the 144 ms stage feel faster is
the classic version of this mistake, and CLAUDE.md rule 31 exists because this project already
made it once ("twice now the reflex answer was 'STT is slow' and twice more than half of it was
not").

**The one performance unknown that matters is Stage F11** — a full service, watching for a rising
per-minute line in Diagnostics. The cadence now runs the decoder continuously for a model slower
than one 200 ms hop, and thermal throttling cannot be produced in six minutes.

## 4. Voice / transcription audit

| Question | Answer |
|---|---|
| Current latency | `base` P50 **139 ms**, P95 339 ms, worst 543 ms (PERF §4). `small` 573/989. `turbo` 2360/2556 |
| Partial behaviour | Whole-window re-decode on a cadence of `clamp(decode_ema, 200 ms, 1000 ms)`; a partial *replaces*, a final *appends* and is capped at 12 lines (`capture.js:311`) |
| Final transcript | Closed by `SILENCE_FINALIZE = 7` (~1.4 s of silence), then the window is cleared |
| Queue behaviour | `sync_channel(8)`. A full queue **sheds a PARTIAL** and **blocks on a FINAL** — finals carry persistence and spoken commands and are never dropped (`main.rs`, the `relay-detect` handoff) |
| Dropped partials | Counted (`latency.rs`) and shown in Diagnostics. 0 in 1075 passes on a dev machine, 0 across a real 49.5-minute service |
| RTF | Not reported as a named metric; derivable from `stt_decode` median vs the cadence |
| Bottleneck | The model, above `base`. Below that, the batch decoder's floor of 1.0–1.6 × decode cost |

**Gaps, corrected 2026-08-31.** Two of the four this row used to name are closed: **p99 ships**
and **samples persist** to `perf_samples`, with a week-on-week trend (RG-14). What remains:

- **No STABLE state — and it is now DECLINED rather than missing** (§2 row 7). The corroboration
  rule already holds a reference from a partial window at `Suggest` until a second pass agrees,
  which is the harm STABLE was proposed to prevent.
- **No measurement with a webview in the path.** `audio_to_visible_transcript` in the rig means
  *"a consumer was handed the text"*, not *"a person could read it"*. Closing that needs a camera
  pointed at a screen, which is the same blocker as **pixels out** — §15, and it is why §61
  (time-to-first-verse) needs a stopwatch rather than a commit.
- **RTF is still not a named metric**, and deliberately: it is derivable from `stt_decode` median
  against the cadence, and the cadence *is* the decoder's own speed by construction (DECISIONS
  §38), so a reported RTF would be a restatement rather than a measurement.

## 5. Detection audit

| Method | May auto-fire | Where capped |
|---|---|---|
| `Direct` | **Yes**, and only when corroborated by a second pass unless the window is FINAL | `router::decide_live` |
| `Semantic` | No — `Suggest` at any score | `detection::may_auto_fire`, `router::decide` |
| `Ambiguous` | No | same |
| `UncertainBook` | No — added after the "hymn number three sixteen" → **Numbers 3:16** P0 | same; CLAUDE.md rule 10 |

One window may inform about several verses and put **at most one** on a wall (`rank_for_wall`,
`main::rank_for_wall`, `main::emit_detections`). The debounce is per-reference and derived from the window
(`DEFAULT_DEBOUNCE_MS = (WINDOW_SECS + 2) × 1000`). Manual fires are recorded as `'manual'`, and
rehearsal feedback is explicitly excluded from calibration — *"a rehearsal is not evidence"*
(`router::DEFAULT_DEBOUNCE_MS`).

**This is the strongest subsystem in the product and the brief adds nothing to it.** Its §14
(explainability) is already built; its §7 (safety) is already stricter than proposed. The gap is
not in the gate, it is that **detection accuracy over audio has never been measured** — `eval.rs`
scores detection over *text*.

## 6. Output audit

Surfaces: native Tauri window · kiosk WebSocket `:8031` · embedded HTTP `:8032` (output/stage
pages, `/media/<id>`, and the seven-route preacher API).

| Property | State |
|---|---|
| Content delivery | Fire-and-forget `broadcast::Sender`; `Lagged` silently skipped |
| Client → server | Three inbound kinds, none of which can carry content: `hello` (registers, answered with the template and themes), `beat` (RG-02 — "the screen for channel N painted", anonymous) and `rendered` (a latency mark, documented as inert). Everything else ignored |
| Liveness | **Real, since RG-02.** A per-channel beat with a grace window derived from the interval rather than written beside it; a lost beat degrades to "silent", which is the safe direction |
| Identity | Still none, by design. The hub records *when* a screen last painted and never *who* connected or from where (DECISIONS §35, narrowed by §39) |
| `output_channels.status` | Still a dead column — seeded `'offline'`, never written at runtime. Health is derived live, deliberately |
| Where health is shown | Outputs tab (polled), **and Live** — both through `outputHealth.js::describeScreen`, so they cannot disagree |
| Pre-air validation | `pipeline::preflight` at `broadcast_with_clock`, the one caller of `broadcast_content` (RG-05, DECISIONS §42) |
| Honesty | Unchanged and still right: *"A screen reading LIVE means something is attached, not that the picture is good."* |

**RG-01 and RG-02 were the report's most actionable findings and both are closed.** The failure
this section used to describe — an operator watching Live during a service, shown a badge that
could not detect the failure it appeared to be reporting — cannot happen now, and the test that
proves it (`outputhealth.test.js`) was re-run with the old global-state derivation restored and
fails.

## 7. Security audit

The LAN posture is deliberate, recorded (DECISIONS §35), and correctly described in `SECURITY.md`
and `PRIVACY.md`. `:8031` is broadcast-only; `:8032` is an unauthenticated control plane; mutating
routes require `POST` and are denied the CORS wildcard, which closed the bystander-browser vector
without pretending to be authentication.

Template injection is treated as untrusted input reaching the wall and is pinned by five tests,
plus an allow-list test that fails if `{@html}` appears in any renderer that reaches a screen.
The kiosk CSP and `X-Content-Type-Options: nosniff` are set; path traversal is rejected; `/media/`
takes only leading digits as an id.

**`SECURITY.md` now carries the T1–T10 threat model** the brief's §64 asked for — including the
two rows that are honest absences rather than mitigations. **No device identity (§19) and no
security event log (§22) are consequences of §35, and §2 now records both as DECLINED rather than
missing**: a log of events attributed to nobody is a log of nothing, and building the identity
first is the reversal §20 (a) proposes and this report does not adopt.

## 8. Privacy audit

Still the strongest-documented part of the product, and no longer only documented. Crash
reporting is off by default, has no DSN in OSS builds, is scrubbed of transcript, verse, lyric,
announcement, service and plan text, and is compiled out of release unless configured.
`session.js` persists position and never content. `crash.js` renders technical detail with
`textContent`, never `innerHTML`. Offline is reported as a **normal** state, not a fault.

**Both gaps are closed.** RG-17 added Settings → Privacy — one screen answering "what is on this
machine, and what can leave it", read from the live settings, stating the LAN exposure in the
same size type as the reassuring half. RG-12 added the diagnostic **export**: one file a church
can attach to an email, composed as an allow-list with the home directory scrubbed
(DECISIONS §48). Neither changed what leaves the machine; both changed whether an operator can
see that nothing does.

## 9. Reliability and recovery audit

| Mechanism | State |
|---|---|
| Crash boundary | Plain-DOM, deliberately not Svelte, because Svelte may be what broke. Says *"Your output screens are still live."* |
| Crash record | `localStorage['relay.boot.v1']`: `cleanExit`, `lastCrash`, `crashStreak`, `safeMode` |
| Safe mode | Auto-offered after 3 crashes; disarms outputs and detection; banner on the shell |
| Session resume | Operator-confirmed modal; **position only**, never on-air. Pinned by `qa-r5-onair.test.js` |
| Leave guard | `markCleanExit()` always; `preventDefault` only while the mic is live |
| Migrations | Individually retryable, with `DROP TABLE IF EXISTS` on the scratch table and rollback on failure (CLAUDE.md rule 25) — and since #41/#42 a test proves an old database really receives the columns a new one has |
| Service lock | **New (RG-03).** 16 irreversible or engine-stopping actions held back while a service records; nothing on the fire path |
| Degraded state | **New (RG-09).** Every silent fallback now says what it costs and what to do, in the shell, on every tab |
| Update safety | **New (RG-06).** Preflight, snapshot, verify-on-next-launch, restore before the database is opened |
| Latency history | **Survives a quit (RG-14)** — `perf_samples`, with p99 and a week-on-week trend |
| Rust-side session | Still not persisted. A crashed service leaves an open `services` row — cosmetic in the history list, never observed to cost anything live |

## 10. Update / distribution audit

Wired: minisign-verified payload, per-platform release gate, three-file version agreement asserted
in CI and against the tag, resumable checksummed model downloads, an in-app update banner that
never appears while capturing **or while a service is locked**, a database-compatibility preflight,
a snapshot-and-restore path for the data, and an offline USB bundle (`scripts/offline-bundle.mjs`).

Still missing: release **channels** (deferred, low value before a first release), and **one
observed end-to-end install on a machine that is not this one** — which ROADMAP §1 lists as
blocked on thirty minutes with a real machine, not on code.

Deliberately not built: **binary rollback.** The installers are public and signed; what cannot be
got back is the church's database, and that is what RG-06 protects (DECISIONS §43).

## 11. Language audit

`docs/LANGUAGES.md` is honest and should not be softened. Verified against the data files, and
the same numbers are now derived live in **Settings → Languages** (RG-11) rather than only
written down here:

| | Kiswahili | Yorùbá | Hausa |
|---|---|---|---|
| Book aliases | 68 entries | 69 | 69 |
| In-language numerals | ✅ (`numerals.json` `sw`) | ⚠️ **`yo`, suggest-only** (unreviewed — RG-126) | ✅ (`ha`) |
| Native-speaker review | ❌ | ❌ | ❌ |
| WER | never measured | never measured | never measured |
| UI locale file | 1 key (ships near-empty on purpose) | 1 key | 1 key |

*(Unchanged 2026-08-31. A supervised pilot with `RELAY_RECORD_WAV` set is what turns the WER row
into numbers — §24, §25.)*

The brief's §42 "Language Quality Centre" **now exists**, and its most important column is empty
on purpose: accuracy and native review render as absences, not as zeroes (DECISIONS §47). The
brief's §43 validation programme is correct and is **blocked on the world**. The moat is the
one thing on this page that no commit has moved.

## 12. UI / UX audit

48 components, 47 reachable (the one orphan is a test probe). 463 controls, none in an unrendered
component. From `node scripts/qa-inventory.mjs`:

- **0 buttons with no handler.**
- **0 controls with no accessible name.**
- **0 commands registered in Rust that no frontend caller addresses**, and 0 in the other
  direction.

**Both lists reached zero at RG-13, and eleven of the thirteen findings that stood here were the
instrument's own bugs** — `type=submit` inside a form counted as handlerless, a label supplied by
`aria-labelledby` counted as unnamed. Fixing the instrument was the larger half of that work, and
it is why the numbers above can be trusted now: a scanner that cries wolf is a scanner whose
zero means nothing (DECISIONS §49).

Colour law, empty/loading/error separation, error humanisation and the six live distinctions are
all pinned by tests and are in good shape.

## 13. Database audit

**21 tables + 1 FTS virtual table.** Present and adequate for: services, transcripts, detections,
cues, plans, songs, sections, arrangements, saved scripture, announcements, media, templates,
channels, settings, voice profiles, translations, verses — **and the three this section used to
list as absent**:

| Concept | Added by | Note |
|---|---|---|
| `service_events` | RG-04 | The append-only ordered record §23/§25/§26/§61 all needed. It carries nothing a preacher said, pinned from both sides |
| `perf_samples` | RG-04 / RG-14 | Latency that survives a quit; percentiles, never traces |
| `environment_profiles` | RG-10 | A room that survives a restart — **without** its audio levels (DECISIONS §46) |

**Still absent, and both declined rather than missing:**

| Concept | Why not |
|---|---|
| `devices` | Requires reversing DECISIONS §35. §20 (a) is the proposal; it is not adopted |
| `security_events` | Follows `devices`. Events attributed to nobody are not a security log |

`cues` gained two kinds on 2026-08-31 — `suggestion_accepted` and
`suggestion_dismissed` — because the operator's answer to a suggestion was recorded
**nowhere**: not in `detections` (whose only insert runs for a fire that reaches a
screen), not in `cues`, not in `service_events`. DECISIONS §63.

`perf_samples` is written by the backend and reached by no command, which
`scripts/qa-inventory.mjs` correctly reports as **BACKEND ONLY** — that is right for a table the
engine samples into and the report reads through `service_events`, and it is recorded here so the
next reader does not "fix" it.

## 14. Event architecture audit

Candidates from the brief, and whether they have a producer **now**:

| Proposed event | Producer exists? |
|---|---|
| `stt://partial` / `stt://final` | **Already covered** — `stt://transcript` carries `is_final`. Do not split it |
| `stt://stable` | No producer, and **declined** — the corroboration rule already gates what STABLE was proposed to gate (§2 row 7) |
| `output://heartbeat` | **Producer exists (RG-02)** — every output page beats, and the state reaches Live through one helper |
| `service://locked` / `unlocked` | **Producer exists (RG-03)** — the lock is engaged and lifted through `set_service_lock`, and its state is recorded in `service_events` |
| `readiness://changed` | No producer — readiness is frontend-only, deliberately (DECISIONS §61) |
| `device://connected` / `disconnected` | No producer, and **declined** — the hub counts, it does not identify (DECISIONS §35 / §39) |

**Recommendation unchanged in shape, inverted in effect:** add no event until its producer
exists. Four of the six now have one, and the two that do not are the two the product has
decided against.

**The full catalogue is larger than this table and was not being checked.** `ipc.test.js`
asserts that every emitted event has a frontend listener, and until RG-64 it built its list
from `main.rs` alone — so `models.rs`'s four events and everything `channels.rs` emits were
outside it. Two events are listened for and were missing from every document until this pass:
`channel://retemplate` (a screen's template was reassigned; the output filters by its own
channel id, which is what makes a template swap live — DECISIONS §29) and `rehearsal://changed`
(pushed, so no surface can be a poll interval behind the others). And **`model://done` is
emitted with no listener on purpose**, because `download_model` resolves only once the file is
installed and verified — allow-listed with that reason, and a test requires it to still be
both emitted and unheard, so the exception cannot outlive it.

## 15. Field-readiness audit

**Moved for the first time: 2 / 10.** Relay ran a live sermon on 2026-08-30
(`audits/FIELD-2026-08-30.md`) — Stage F11 answered, no drift across 49.5 minutes and 2,423
decodes, five of six auto-fires correct, and **one wrong verse reached a congregation**. The
signed-bundle conditions were separately reproduced on a real `.dmg` with
`scripts/sign-local.sh`.

Still at zero: **pixels out** (nothing has ever measured what a projector actually showed),
**hardware other than one M4 Pro**, **a second operator**, and **nine more services**.
`audits/QA-2026-08-14.md` §16 Stages A–F remains the script; F6/F7/F8 are regression tests and
green in CI.

**Fifty minutes in a room produced seven findings that months of reading source had not.** That
ratio is the argument for the pilot in §24, and it is also the argument against calling anything
here finished.

---

## 16. Missing functionality — consolidated

> **Rewritten 2026-08-30, re-checked 2026-08-31.** Everything the original list named is
> now built except three, and leaving the old list in place would have made this document
> say "missing" about features that shipped the same week — the exact failure §26 is about.

**Still missing, and each for a stated reason:**

| Item | Why it is still missing |
|---|---|
| **Signed language packs** | The signing infrastructure does not exist, and an *unsigned* pack is a wrong-verse-on-a-wall vector (SECURITY.md T9). RG-19 shipped the offline bundle without them, deliberately |
| **Explicit room calibration** | DECISIONS §19 / rule 12: nothing may compare a signal to a stored level. The instrument that could show a seeded floor safe (`cargo test audio::gate -- --ignored`, against real room audio) has never been pointed at a real room — Stage C |
| **Time-to-first-verse** | A HUMAN metric: from the preacher saying it to the congregation reading it. `latency.rs` measures machine stages, and the one span that claimed to cover this end to end was measuring how long the preacher had been talking (FIELD F-6, RG-31). It needs a stopwatch in a room, not a commit |

**Built since this list was written:** service replay · Sunday report · service event
timeline · output heartbeat and per-channel liveness on Live · Service Lock · update
data-rollback · DB-compat preflight · offline installer · pre-air Safe Screen validation ·
contrast validation · distance preview · output accessibility mode · room / environment
profiles · language quality centre · training drills · diagnostic bundle export · privacy
screen · persisted latency · p99 · the T1–T10 threat model · the AI's own weaknesses stated
in the app, offline.

**Three things in §2 moved to DECLINED rather than being built**, and that is a result, not
an omission: STABLE transcript state (§7), candidate prefetch (§12), and the device
identity / security event log pair (§19, §22). Each row carries the reason. A register
where nothing is ever declined is a register that has not been read.

## 17. Redundant functionality — do not build

| Brief item | Why it is redundant |
|---|---|
| §6/§8 two-speed transcription | The window is not the lever (DECISIONS §36) |
| §11 adaptive window sizing | Superseded by adaptive **cadence** (DECISIONS §38) |
| §12 candidate prefetch | Optimises a 2.6 ms stage inside a 144 ms budget (CLAUDE.md rule 31) |
| §7 splitting `stt://transcript` into two events | `is_final` already carries it |
| §7 a STABLE text state | The corroboration rule already gates the harm STABLE was proposed to gate (CLAUDE.md rule 28) |
| §5 a second readiness implementation | `boot/probes.js` is the implementation; extend it, do not fork it |
| §4 an engine-side health state | Its only possible action is refusing the wall, which §20 and §42 both forbid (DECISIONS §61) |
| §14 explainable detection | Built: `DetectionInspector.svelte` |
| §33 never update during a service | Built: `updater.js::idle()`, and it now reads the service lock as well as the microphone |
| §13 claim-type display | Built, and stricter than proposed (DECISIONS §21) |
| §69 design system | Built, and pinned by tests |
| §24 tamper-evident record | Would claim a guarantee against the one actor Relay says it does not defend against (SECURITY.md T10) |

## 18. Technical debt

No new debt is recorded here. `KNOWN_ISSUES.md` §4 owns the register.

**The one piece of debt this document is itself responsible for is counts in prose, and the
history below is kept as evidence rather than maintained.** RG-20 was filed because six
documents disagreed about how big `main.rs` was. Every correction since has been wrong within
the week:

| | RG-20 filed | 1st correction | 2nd correction | 2026-08-31 |
|---|---|---|---|---|
| `main.rs` lines | 4,024 | 4,369 | 5,723 | **5,573** |
| registered commands | 114 | 118 | 137 | **132** |
| `capture.js` lines | 1,908 | 1,941 | 2,195 | **2,156** |

**This table is deliberately not maintained past this row.** Its point is the shape, not the
values: three of the four columns went up and the last went *down*, because five dead commands
were deleted — so even the direction of drift is not guessable. A number in prose is wrong the
moment somebody commits, and re-correcting it is not a fix, it is the same bill paid again.

**The durable version is the command beside the number.** Every count in §0, in `QA_HARNESS.md`
Part 0 and in `CLAUDE.md` now carries one:

```bash
wc -l src-tauri/src/main.rs
grep -c '#\[tauri::command\]' src-tauri/src/main.rs
cd src-tauri && cargo test          # the runner's own summary line, not a grep
npx vitest run                      # likewise
node scripts/qa-inventory.mjs
```

---

## 19. Decisions that stay untouched

Every one of these was reaffirmed by this audit, and each is cited by code:

| Constraint | Where |
|---|---|
| No native SDI, ever, unless reopened | DECISIONS (brainstorm table), ROADMAP §5 |
| Offline-first; cloud never in the live path | CLAUDE.md, `probes.js:98-104` |
| Operator override is first-class | DECISIONS §20, §21 |
| One template engine; output channels are render targets | CLAUDE.md, `TemplateRender.svelte` |
| Local-first data | `PRIVACY.md`, `telemetry.rs` |
| Only `Direct` may auto-fire; a cosine is not a probability | DECISIONS §21, `detection::may_auto_fire` |
| Rehearsal gates at the broadcast, not the caller | DECISIONS §18, `channels::rehearsing` |
| Never report a success that did not happen | DECISIONS §20, `capture.js:1712-1732` |
| Audio levels are learned, never assumed | DECISIONS §19, CLAUDE.md rule 12 |
| One window, one wall | DECISIONS §37, `main::rank_for_wall` |
| Never make the live path faster by making it less safe | CLAUDE.md rule 34 |
| Planner builds; Live runs; Planner cannot reach an output | `ServicePlanner.svelte:1-18` |
| Relay sits above OBS/ATEM/ProPresenter, never replaces them | SPEC §9, ROADMAP §5 |
| No accounts, no RBAC, no SSO, no billing in the live app | ROADMAP §3, PRODUCT_AUDIT §13 |
| Free, MIT | README, DECISIONS |

---

## 19b. Decisions this report needs and cannot make

**Two open. Neither is a commit.**

**One, added 2026-08-30.**

> **Should Relay ship a second Bible translation, an import path for one, or neither?**
>
> `translations` is the only table an operator still cannot fill (RG-50). The plumbing
> has always existed — the table, `active_translation`, `listTranslations`,
> `setActiveTranslation` — and DECISIONS **§32.4** records that **WEB, ASV, YLT and BBE
> are public domain and addable as data today, with no licence**. The legal blocker it
> describes is real but applies to TPT, MSG, NIV and ESV.
>
> So three different things are possible and they are not the same decision:
>
> 1. **Seed a second public-domain corpus** (≈31,000 more verses per translation). No
>    licence needed. Costs bundle size and a validation pass nobody has done.
> 2. **Ship an import path** — `add_translation` plus a screen — so a church brings its
>    own. Touches the one table detection reads, so a malformed import is a
>    wrong-verse-on-a-wall vector and would need the same care as `pipeline::preflight`.
> 3. **Neither**, and say so where an operator can read it, which is roughly where
>    Settings already lands.
>
> **This report does not choose.** CLAUDE.md is explicit: if a decision is not in
> DECISIONS it has not been made, and an agent's job is to ask rather than assume — and
> the last time this report reasoned about it unprompted, it got the reason backwards
> (see RG-50).

**Two, added 2026-08-31.**

> **Should a detection carry the audio quality it was heard in?**
>
> The brief's §13 asks for voice confidence shown separately from claim type. Claim type
> is built and is stricter than proposed. The other half is not built — and the reason
> this report previously gave for that was **wrong**: it said the measurement did not
> exist. It does. `dsp::AudioQuality` carries `snr_db`, `clip_ratio`, `speech_prob` and
> a `warning` that already names *the single most important thing wrong right now*, and
> it reaches the console continuously on `audio://quality`.
>
> What is missing is the join: the quality is shown for **now**, and never attached to
> the claim it was measured beside. So an operator reading *"heard the reference · 91%"*
> cannot tell whether Relay heard it cleanly or through a clipping microphone — and the
> field service's wrong verse is exactly the case where somebody would want to know.
>
> **Why this is a decision and not a task.** Three of this product's hardest-won rules
> pull against each other here:
>
> 1. **A number that lies is worse than no number** (DECISIONS §21). `snr_db` is
>    documented as "rough". Printing *"SNR 12 dB"* beside a verse invites a volunteer to
>    read it as a probability, which is the paraphrase-percentage mistake in a new
>    costume. A *word* — "heard in a noisy signal" — carries the fact without the false
>    precision, and is what §45 does for degradation already.
> 2. **An instrument that cries wolf stops being read** (DECISIONS §49). A church PA
>    feed may sit near a warning threshold for a whole service. A caveat on every
>    suggestion is a caveat on none, and this repo already has one finding about
>    warnings an operator learns to scroll past.
> 3. **Only what was measured appears** (DECISIONS §44). The quality is sampled
>    continuously; a detection spans a window. "The quality when the window closed" and
>    "the worst quality during the window" are different claims and only one of them can
>    be called *the* audio the verse was heard in.
>
> So the shape is genuinely open: a chip only when the window carried a real warning; or
> a field on the detection that only the inspector shows; or nothing on the live surface
> and the quality recorded to the service record instead, where a post-mortem would find
> it and a Sunday operator would not be interrupted.
>
> **This report does not choose**, for the same reason as the one above — and
> specifically because option 2's failure mode is invisible until a real service in a
> real room, which is Stage C. It is written down so the next reader inherits the
> question rather than the wrong reason.

---

## 20. Reversal proposals — for a human, not for an agent

### (a) DECISIONS §35 — device pairing on the LAN control plane

- **Existing decision.** The HTTP API on `:8032` is an unauthenticated control plane on the local
  network, deliberately. The WebSocket hub on `:8031` is broadcast-only and records nothing about
  who connected.
- **Original reasoning.** *"A password on a device shared between a preacher, a tech volunteer and
  a stand-in every Sunday is a password written on a sticky note behind the desk."* The threat
  model accepted is a LAN appliance in a building whose network the congregation already trusts.
- **Proposed reversal.** A 4-digit pairing code with a short-lived token, a trusted-device list
  with revoke, and identity on the WS hub — the brief's §19–§21.
- **Benefit.** Closes the on-network attacker, and is the prerequisite for output heartbeats with
  identity (§18), a security log (§22) and per-device health.
- **Cost.** A new `devices` table and token lifecycle; a pairing step in the preacher's-phone flow
  that must survive a dead battery ten minutes before a service; and the sticky-note failure the
  original decision names. It also contradicts *"Relay does not record who connected"*
  (`channels::kiosk_content_json`), which is currently a privacy guarantee.
- **New risks.** A church that cannot pair a phone at 10:29 loses the remote entirely. Token
  expiry during a 90-minute service. A revoke UI a volunteer can misuse.
- **Evidence required before deciding.** One church actually asking for it; or Relay being run on
  a guest/shared network, which §35 already names as the realistic trigger.
- **Why the current approach may be insufficient.** A laptop that also joins café WiFi serves the
  media files *and* the remote to that network. That is §35's own stated tripwire, and it has not
  been tested.
- **Note.** §18 output heartbeats do **not** require this. A heartbeat can be anonymous — a
  connection id, not an identity — and that version is compatible with §35 as it stands.
  **Build the anonymous heartbeat first; it is not a reversal.**

### (b) ROADMAP §3 — an optional cloud administration layer

- **Existing decision.** Cloud sync, accounts, multi-tenancy, RBAC/SSO, analytics dashboards,
  marketplaces and billing are declined, each with a stated reason.
- **Original reasoning.** Offline-first is the moat, not a constraint; there is one operator,
  standing in the room; nothing leaves the device without a visible reason.
- **Proposed reversal.** A strictly optional Relay Cloud outside the live path: device registry,
  configuration/template/plan backup, update management, language-pack distribution, support
  diagnostics.
- **Benefit.** Real, for the two items that are *already* pain: distributing language packs (§44)
  and receiving a diagnostic bundle from a church (§56).
- **Cost.** An entire second product — a server, an account model, a privacy surface, a security
  boundary, and a support burden — before the first product has run one Sunday.
- **New risks.** The strongest one: once a cloud exists, features drift into it. Every "optional"
  cloud in this category eventually became required.
- **Evidence required.** Ten churches running three consecutive services each. Not before.
- **Recommendation.** **Do not reopen now.** The two genuine needs it serves are better met
  offline: a signed language pack on a USB stick, and a diagnostic bundle the operator emails.

---

## 21. Prioritised plan, in the three buckets the brief asks for

> **Rewritten 2026-08-31. The first bucket used to hold RG-01 … RG-19 as work to start; all
> nineteen shipped.** Leaving them listed as "can start today" is the exact failure §16 and §26
> are about, so what shipped moved to the closed register (§23) and this bucket now holds only
> what is genuinely un-started.

### BLOCKED ON CODE — can start today, no external dependency

**The bucket is nearly empty, and that is the finding.** Nothing below is on the critical path to
a pilot; §24's conditions are all about rooms, not commits. The one item that *was* on it —
finishing the onboarding wizard — shipped as RG-61, and shipped **narrower than written**: a
hand-off naming three instruments and the tab each lives on, rather than three more wizard steps.

| P | Item | Why, and why it is not urgent |
|---|---|---|
| **P2** | **Prototype the stripped Sunday layout** (§38/49) | Live has density and full-screen; the brief's layout is a different proposal. It should be judged by an operator who is not the author, which makes it a pilot question rather than a design one |
| **P3** | **Release channels** (§47) | Low value before a first release exists |
| — | ~~**Per-detection voice confidence** (§13)~~ | **Moved to §19b — it is a decision, not a task.** The measurement exists (`dsp::AudioQuality` carries `snr_db`, `clip_ratio`, `speech_prob` and a named warning); what is unresolved is what an operator should be shown, and three recorded rules pull against each other over it (§21 a number that lies, §49 an instrument that cries wolf, §44 only what was measured). **This row previously said the measurement did not exist. It was wrong** |
| **P3** | **Token-level streaming decode** (§9) | A whisper.cpp capability question, not a Relay wiring question. The cadence work already took the latency it would have bought |

> **"Nearly empty" is about PRODUCT features, and it should not be read as "nothing left to do".**
> Nine of the entries closed on 2026-08-31 came from auditing the instruments rather than the
> product — contract tests that scanned one file, a CI job on one Node version, an edit-time gate
> whose watch list had drifted behind four safety files, an agent brief describing a deleted
> component, citations pointing at a section that does not exist. **That seam is still open**, and
> it is not listed as a task here because it is not one: it is a habit. AGENTS.md carries it.

**Deliberately not in this bucket, and each recorded as DECLINED in §2 with its reason:** a STABLE
transcript state (§7), candidate prefetch (§12), device identity and a security event log
(§19–§22), a tamper-evident record (§24), and an engine-side health state (§4 · DECISIONS §61).

**Open on purpose, not un-started:** RG-32 — a context-resolved bare verse is labelled `Direct` at
a hardcoded 0.88, and by rule 10 that label is a lie. Changing it makes every in-passage
*"verse eighteen"* cost a click, and one service is not enough evidence to spend that. It wants a
second and third Sunday, which puts it in the next bucket rather than this one.

**Flagged for a human:** RG-50 — whether Relay ships a second public-domain corpus, an import
path, or neither, is a product decision `DECISIONS.md` does not contain. §19b holds it.

### BLOCKED ON REAL-WORLD VALIDATION — no commit can close these

- **Stage F11 — answered.** One full service, 49.5 minutes, 2,423 decodes, no drift
  (`audits/FIELD-2026-08-30.md`). Kept here because one service is one service.
- Stages F1–F5, F9–F10, F12–F14 — church hardware, a quiet room, a noisy room, a quiet speaker.
- Stages A–E — the projector, the ATEM, the eyes. **Pixels out remains at zero.**
- **Word error rate, any language.** Thirty minutes of real preaching on tape. This is the moat
  and it is the highest-value unrun item in the project.
- One observed end-to-end update install, on a machine that is not this one.
- **One full service run by a non-author operator.** The largest unknown in the project.
- RG-18's contrast and distance thresholds, against a real wall.
- RG-32's second and third services.

### BLOCKED ON EXTERNAL DEPENDENCY

- **Windows code-signing certificate** (~$10/month, Azure Trusted Signing) — ROADMAP §1. It
  blocks the platform most churches are on.
- **A native speaker** of Yorùbá, Kiswahili or Hausa, for the alias table and for the Yorùbá
  numerals — which parse as of RG-126 and stay capped at *suggest* until one reads them — and,
  until one exists, §44's signed language packs stay refused.
- **A decision on the product's name**, due before the first church installs.

---

## 22. Production readiness — scored per dimension, no hiding average

Scored against **the first ten churches**, not against enterprise scale.
**Re-scored 2026-08-31 (second pass)**, after one live service and **77 closed** register entries of 81 (as re-scored that day; the register is 87 / 78 since 2026-09-02).
Each row keeps the previous score beside it so movement is visible rather than asserted.

> ### Not one score moved this pass, and that is the result
>
> **24 commits** landed between the first re-score and this one (`git log --oneline 436050c..HEAD`).
> **Nothing in the table changed**, because a score here is meant to track *evidence*, not effort — and the evidence
> that would move any row is a room, a certificate, a native speaker or a second Sunday. A
> readiness table that rises whenever somebody is busy is a morale chart.
>
> Two rows would have been *entitled* to rise on the work done, and are deliberately held:
> Documentation (its guarantees became machine-checked) and Security (its posture became
> reviewable). Both are argued in place below. Raising them would have widened the gap between
> the ten rows that read 8–9 and the two that decide anything, which is the failure this table's
> last paragraph already warns about.

> ### The caveat that changed, even though no number did
>
> **Nine of this pass's closures were defects in the instruments, not in the product** — a
> contract test that scanned one Rust file, its other half reading four frontend files out of
> nine, a CI job on a single Node version, an edit-time gate whose watch list had drifted behind
> four safety files, an agent brief describing a deleted component, four dead `DECISIONS §N`
> citations and sixteen dead `docs/…` paths.
>
> That matters *here* more than anywhere else in this document, because **several of these scores
> were partly self-reported by those instruments.** The confidence behind the high rows was
> resting, in part, on checks that were reading less than they claimed. They now read what they
> claim, and each is pinned so it cannot narrow again. **Read the 8s and 9s below as
> better-founded than they were, not as higher.**
>
> **An earlier version of this paragraph said the missed gaps "turned out to be closed or benign".
> Two hours later that was false, and it is corrected here rather than quietly dropped** — which
> is the rule the rest of this document runs on:
>
> * **RG-73** — the readiness checklist ticked *"Signed + notarized macOS build"*. **No Apple
>   certificate has ever existed**; all four releases went out unsigned. That is release-blocking,
>   it was hiding on the page a release decision is read from, and rule 17's
>   first-signed-build trap is therefore still ahead of this project.
> * **RG-74** — *"every text token at AA"* had been ticked since July and measured by nothing.
>   When finally measured it was **false**: `--v-faint` sat at 4.38:1 on `--v-surf2`, below AA,
>   with five shipped rules using that pairing.
>
> **So the honest statement is the stronger one: two unchecked claims were load-bearing and wrong,
> and both were found only by building the instrument that could disagree with them.**
>
> **The rest of §16 was then checked the same way, and the rest of it holds.** In-app model
> download really is resumable, SHA-256-verified and cancellable (`sha2::{Digest, Sha256}`,
> per-model constants, verified before the rename); crash reporting really is opt-in
> (`telemetry::tests::disabled_by_default`, and the gate requires the setting to be exactly
> `"1"`, so an absent setting is off); the LICENSE really does name a copyright holder. **Two
> wrong ticks out of twenty-three is the finding — not "the checklist was fiction", and not
> "the checklist was fine."** Neither
> moved a score — the first is counted under Distribution, which was already held down for exactly
> this, and the second was one marginal token, now fixed and measured. But "we checked and it was
> fine" would have been the comfortable sentence, and it was not true.

| Dimension | Was | Now | Why it did or did not move |
|---|---|---|---|
| Code | 9 | **9 / 10** | Held. 260 lines of dead component CSS deleted — proved inert by rebuilding and diffing the emitted bundle, not assumed — and the repository carries **zero** `TODO`/`FIXME` markers and zero unused CSS selectors. Against that: RG-62 found that two of `detections.status`'s four documented values were **structurally unwritable**, which is a correctness defect that lived a long time in a heavily-tested area. Fixed, but the ceiling is unchanged: one god-file (`wc -l src-tauri/src/main.rs`) |
| Performance | 8 | **9 / 10** | Held, and nothing was measured this pass. Stage F11 still answers it: **no drift across 49.5 minutes and 2,423 decodes** in a real room, every model on this machine measured, the bench predicting the room within 15%. Not 10 for the unchanged reason — one machine |
| Live safety | 9 | **9 / 10** | Held. RG-63 closed a real defect on the path the operator actually touches: a suggestion for a verse that does not exist rendered an identical amber Approve and failed *after* the click. That is the console's version of a badge that cannot detect its own failure. It does not move the score, because **a wrong verse reached a real congregation on 2026-08-30** and only a second service can show that class is closed |
| UX | 8 | **9 / 10** | Held, and now partly measured rather than asserted. RG-61 gave the setup walk-through the hand-off it never had; RG-63 made a dead control say why it is dead rather than merely go grey; **RG-74 found a real WCAG AA failure in shipped text** (`--v-faint` at 4.38:1 on `--v-surf2`, five rules using it) that a ticked box had covered since July — fixed, and every text token is now checked against every surface it is placed on. Still loses a point for surfaces no instrument here has ever seen, which is the same reason it does not gain one |
| Security | 7 | **8 / 10** | **Held on purpose, and this is the row most tempting to raise.** `SECURITY.md` now carries the T1–T10 threat model (RG-60), and two of its rows are honest absences rather than mitigations. But a threat model documents a posture; it does not change one. The LAN control plane is still unauthenticated by decision (§35), and that is the ceiling. The one genuine hardening was small and is counted under Privacy |
| Privacy | 9 | **9 / 10** | Held — with a near-miss recorded rather than smoothed over. RG-62's first cut passed a **raw string from the webview** into `cues.payload_json`, which `service_timeline` reads back and which is the part of the history most likely to be emailed. Every other cue writer guarantees its shape by construction; that one would have trusted it. Caught in review before it shipped, now parsed to a canonical reference or not stored at all, and pinned by a third e2e test |
| Reliability / recovery | 7 | **9 / 10** | Held, untouched this pass. Rollback, DB-compat preflight, Service Lock, degraded state, and a migration path proved from both sides against a database that predates it |
| Distribution | 5 | **6 / 10** | Held — and the reason is worse than it read. **Neither platform has a code-signing certificate**: the readiness checklist ticked "signed + notarized macOS build" and that was wrong (RG-73). All four releases went out unsigned on the pre-release path, and no update has ever been watched installing on a machine that is not this one. CI builds the Windows MSI on every push, so the *build* is not the risk — the *signature* and the *install* are, and nobody has seen either |
| **Language (the moat)** | 3 | **3 / 10** | **Unmoved, three times running, and this is the honest number.** Yorùbá numerals now parse (RG-126) and the score does not move for it: they are capped at *suggest* precisely because no native speaker has reviewed them, no native speaker has reviewed the aliases, and **word error rate has never been measured in any language.** Nothing in twenty-six commits could touch it, and nothing in the next twenty-six will either |
| Observability | 7 | **9 / 10** | Held, and the 9 is better-founded than it was. RG-62 found the Sunday report printing **`0 suggested · 0 dismissed` for every service ever recorded** — a number for something nothing was recording, which is an observability defect of the worst kind: it reads as a measurement. Now it records what the operator actually did, and **names its own denominator** ("of the suggestions you answered") instead of implying the larger one |
| Documentation | 8 | **9 / 10** | **Held, and this is the other row that could have risen.** The change is real and structural: documentation stopped being *corrected by sweeps* and started being *checked by tests* — cross-references in three dimensions (`DECISIONS §N`, `RG-` ids, `docs/…` paths), the register's shape and its own summary counts, the AI disclosure against the app, and the edit-time gate's watch list. This pass alone that machinery caught a fourth dead citation a careful hand-sweep had missed. It stays at 9 because prose accuracy about *behaviour* is still unmechanised, and because a 10 next to a 3 is how a table like this starts lying |
| **Field validation** | 0 → 2 | **2 / 10** | **Unmoved.** One real service, 49.5 minutes, packaged build, instrumented — [audits/FIELD-2026-08-30.md](audits/FIELD-2026-08-30.md). Stage F11 answered; 5 of 6 auto-fires correct; **one wrong verse reached a congregation**. Still one church, one preacher, one language, one machine, and an operator who wrote the software |

**No overall score is given.** An average would hide the **3** and the **2**, which remain the only
two that decide anything. Twelve dimensions, two passes, and those two have not moved once —
because neither is a code problem, and **45 merged pull requests** (`gh pr list --state merged --limit 500 --json number --jq length` — **the `--limit` is not optional**: `gh pr list` defaults to 30, so the obvious form of this command answers `30` and quietly contradicts the sentence it is evidence for) could not touch them.

> **The trap in this table.** Ten of twelve rows read 8 or 9, which is exactly the shape that makes
> somebody ship. **Do not read it that way.** The two low rows are not weak spots in an otherwise
> finished product — they are the two that say whether it works at all in a room, and the strongest
> evidence in this document is still that fifty minutes in one produced seven findings that months
> of reading source had not.
>
> **And the second pass adds a sharper version of the same warning.** Nine of its closures were
> defects in Relay's own instruments. If the checks that produce this table were reading less than
> they claimed, the honest inference is not that the product is worse than the scores say — it is
> that **a high score is a statement about what has been looked at, and the looking is younger than
> it appears.**

---

## 23. Gap register

| ID | Gap | Impact | Evidence | Solution | Depends on | P | Complexity | Validation |
|---|---|---|---|---|---|---|---|---|
| ✅ RG-01 | Live's per-channel badge derives from global state | Operator believes a dead screen is On Air | `Live.svelte:973-979` vs `Channels.svelte:88-108` | Call `channelStatus()` on Live; badge per channel | RG-02 for truth | P0 | S | A component test that mounts Live and asserts a stale channel does not read On Air |
| ✅ RG-02 | No output heartbeat; liveness is a client count | "LIVE" cannot detect a frozen browser source | `channels::ClientRegistry`, `channels::OutputHealth` | Periodic anonymous ping/pong; last-seen per connection | — | P0 | M | Kill a kiosk client; assert the status flips within one interval |
| ✅ RG-03 | No Service Lock | A template edit or model change mid-service | grep: zero hits | Lock keyed on `Session`, not on the mic; every blocked action explains itself | — | P0 | M | e2e: start a service, assert the blocked commands refuse with a typed error |
| ✅ RG-04 | No event timeline; latency dies on quit | No replay, no report, no human metric, no evidence from a church | `docs/data/schema.sql`, `latency.rs` | Append-only `service_events` + `perf_samples`; retryable migration | — | P0 | M | Migration retryability test (CLAUDE.md rule 25); a service produces an ordered event list |
| ✅ RG-05 | No pre-air validation | Unfittable or unreachable content goes to air silently | `TemplateRender.svelte:131-160` | One validator in front of `Fire::output`; refuse and report | — | P0 | M | e2e: an over-long verse on a tiny template refuses rather than shrinking to unreadable |
| ✅ RG-06 | No update rollback, no DB-compat preflight | A bad update bricks a church until someone drives there | grep: zero hits; `db::SCHEMA_VERSION` | Keep the previous bundle; health-check after relaunch; compare `SCHEMA_VERSION` before install | — | P1 | L | Install a deliberately broken build; assert recovery |
| ✅ RG-07 | No service replay | Nothing can be reconstructed after Sunday | — | Timeline viewer over RG-04 | RG-04 | P1 | M | Replay a recorded service; every fire has a trace |
| ✅ RG-08 | No Sunday report | Churches cannot report, and you cannot learn | — | Derived view over RG-04 | RG-04 | P1 | S | Only metrics actually measured appear |
| ✅ RG-09 | No degraded state | Fallbacks are invisible to the operator | `dsp::FrontEnd`, `main::fire_media`, `pipeline::Fire` | One `Degraded` enum surfaced to the shell | — | P1 | S | Kill the model mid-service; assert the banner |
| ✅ RG-10 | Room calibration is lost on every start | The hall is re-learned each Sunday | `audio::Vad`, `dsp::FrontEnd` | `environment_profiles`; store the learned floor, never a fixed threshold | RG-04 | P1 | M | Reopen; assert the floor is restored and still adapts (DECISIONS §19 must survive) |
| ✅ RG-11 | Language coverage is prose, not an instrument | The moat cannot be tracked or improved | `docs/LANGUAGES.md`, `numerals.json` | A per-language status view: aliases, numerals, review state, WER (or "not measured") | — | P1 | S | The view must render "NOT MEASURED" rather than a plausible number |
| ✅ RG-12 | Diagnostics is a screen, not an export | A church cannot send you what you need | `Settings.svelte:998-1054` | Export a scrubbed bundle; never include audio or transcript unless ticked | — | P1 | S | Assert the bundle contains no verse, lyric, announcement or transcript text |
| ✅ RG-13 | 9 controls with no accessible name | Screen-reader users cannot operate Live | `qa-inventory.mjs` | Add labels | — | P2 | S | Re-run the inventory; zero |
| ✅ RG-14 | No p99; no latency history | Tail behaviour and long-service drift are invisible after a restart | `latency::report` | Report p99; persist samples | RG-04 | P2 | S | Diagnostics shows p99 and a prior-service comparison |
| ✅ RG-15 | Readiness never tests the spoken path | Green checks, dead microphone chain | `boot/probes.js` | Synthetic "say John 3:16" walk | RG-01 | P2 | M | Fail it by unplugging the mic; assert it goes red |
| ✅ RG-16 | No training / simulation mode | A volunteer's first Sunday is their first attempt | — | Replay recorded audio through the real pipeline into a sandboxed UI | RG-07 | P2 | M | Nothing reaches a real output — watch the **hub**, not the wall |
| ✅ RG-17 | No privacy screen | The best privacy story in the product is invisible | `PRIVACY.md` | One screen stating current state | — | P2 | S | Reflects the real setting, never a hardcoded "off" |
| ✅ RG-18 | No contrast validation, distance preview or output accessibility mode | Unreadable from row 20, and nothing says so | grep: zero | Ratio check + a distance simulator | — | P3 | M | Needs a real projector |
| ✅ RG-19 | No offline installer or language packs | A church with poor internet cannot install | `models.rs` | Bundle app + model + corpus; sign language packs | RG-06 | P3 | L | Install with the network cable out |
| ✅ RG-20 | **Doc drift** — 114 / 4,024 / "35 decisions" / e2e layer count wrong across six files | `CLAUDE.md` is read first by every agent and is on the wrong side | §26 | Fix, and add the reproducing command beside every count | — | P0 | S | `grep -rn '114 cmds\|4,024\|4.0k lines\|35 decisions' CLAUDE.md docs/` returns nothing — **verified clean 2026-08-30** |
| ✅ RG-21 | **No arrangement editor** — the one dead command. A song's running order (verse · chorus · verse · chorus · bridge) could not be created at all | A worship team cannot express how they actually sing a song; the Planner's picker is unreachable code and always falls to "Standard" | `qa-inventory.mjs` (`song_arrangements`: WRAPPER ONLY); `qa.rs::a_component_can_create_a_song_arrangement` | Build the editor in Library → Lyrics; it is the only missing link in a chain that already had a table, three commands, a wrapper, an expander and a picker | — | P1 | M | The inventory reports a create path, and a test asserts something RENDERS the editor — not merely that it exists |
| ✅ RG-22 | **Arrangement index drift** — the sequence is section positions, so reordering, inserting, deleting or renaming a section silently repoints it | The wrong words on a wall, on a Sunday, with nothing saying so. Latent until RG-21 shipped, which is why they shipped together | `db/songs.rs` (`sync_song_in_plans` re-expanded through stored indices unconditionally) | Record `built_shape` per arrangement and per plan cue; mark drift, refuse it into a plan, fall a drifted cue back to the song's own order. Never remap by guessing | RG-21 | P1 | M | Both Rust tests fail with the check disabled; the picker offers a stale arrangement and will not add it |
| ✅ RG-23 | **Ordinary preaching auto-fires a wrong verse.** A run the book cannot support, split, scored 0.83 — *higher* than the 0.45 given to the reading it replaced. The garble guard saw digits only, while whisper writes words on accented speech. And at dial 100 the auto-fire bar is 0.30, which IS the confidence floor, so every deliberate demotion was inert | A verse nobody said, on a wall, unattended | `detection::r4_audit` R4-01 · R4-02 · R4-03, all three reproducing | `DetectionMethod::UncertainNumber` — the numbers were inferred, repaired or did not line up. Capped at Suggest by `may_auto_fire`, at any score and any dial. **A demotion expressed as a number is a demotion a dial can erase** | — | P0 | M | The three tests pass; the detection suite does not regress; the CI scorecard holds |
| ✅ RG-24 | **FIELD F-1** — a live sermon citing **Luke 10:32-37** auto-fired **Proverbs 3:32** at 0.88. A bare "verse 32" was hung on a passage fired by hand five minutes earlier, with Luke 10 in the same sentence | The failure this product exists to prevent, observed in a real service | `docs/qa/audits/FIELD-2026-08-30.md` §3; `detections.heard_text`, verbatim | `anchor_for_bare_verses` — a reference named in the window outranks memory; memory is the fallback, not the default. **The first diagnosis blamed the parser and was wrong**: the test written against it passed with the fix reverted | — | P0 | S | `field_a_bare_verse_belongs_to_the_book_this_sentence_names`, and the fallback case still works |
| ✅ RG-25 | **FIELD F-2** — a detection born in a partial window is stored against the last FINAL transcript, so the service record shows a verse beside a sentence that did not produce it | History and replay misreport what Relay heard; any accuracy scored from `transcripts` scores the wrong text | 72 finals in the service contain "verse"/"chapter"/"bible" **zero** times; four `heard_text` values contain all three | `persist_fire` reuses the last final only when it really is the words the detector read, and otherwise persists the window in its own right — six extra rows in a fifty-minute service, and the join stops lying | — | P1 | M | `e2e::a_detection_points_at_the_words_that_produced_it` |
| ✅ RG-26 | **FIELD F-3** — `perf_samples` persists CUMULATIVE percentiles (`latency::report(0)`), which are structurally insensitive to drift; `worst` can only rise | Stage F11 asks whether the per-minute line rises, and the persisted series cannot answer it. The answer had to be inferred from a flat p50 under a growing denominator | §2 of the field audit | **Narrower than filed: the per-minute means already existed** in the live report (`Drift`) and were never written down. `perf_samples.last_minute_ms` persists the last COMPLETE bucket — the one still filling reads as a dip, and a dip is the shape somebody mistakes for good news | RG-04 | P1 | S | `a_perf_sample_carries_the_last_minute_not_only_the_whole_service`, and a pre-column row reports an absence not a zero |
| ~~RG-27~~ | **WITHDRAWN — FIELD F-4 was wrong.** Filed mid-service when the metric sat at 28 samples against `stt_decode`'s 1,877. It finished the service with **411**: sparse, not stopped. **The finding was drawn from a snapshot and the snapshot was not the run.** Struck through rather than deleted — a register that quietly loses its wrong entries teaches nothing | — | — | — | — | — | — | — |
| ✅ RG-28 | **The display sleeps during a service.** Nothing held the screen awake, so a projector could go black mid-sermon with Relay working perfectly and nothing anywhere saying why | A dark wall in front of a congregation, with no fault to find | Requested from the field, 2026-08-30 | `wake.rs` — an OS assertion held while the mic is live, a service is recording, or an output window is open, and released when none is true. No new dependency: IOKit on macOS, `SetThreadExecutionState` on Windows | — | P1 | S | Held and released on the real rule; merely being open never holds it |
| ✅ RG-29 | **A screen could be reported down with no way to bring it back.** Live's Output Status pane was read-only, so restoring a screen meant leaving the run surface mid-service | The pane's whole purpose is to report a screen that is down; it was a dead end | Requested from the field, 2026-08-30 | Per-screen on/off on Live, from one pure rule (`screenSwitch`) shared with the Outputs tab so badge and button cannot disagree. A browser source says where to go instead of showing a button that would do nothing | RG-01 | P1 | S | Both commands are already Service-Lock exempt; the control is not, and must never look like, a panic control |
| ✅ RG-30 | **FIELD F-5** — **filed too strongly, and corrected rather than quietly narrowed.** Per-model figures did exist (DECISIONS §36, §2 of this report). The real gap: the HEADLINE numbers everything quotes are `base`-only, and nothing converted a model choice into the update rate an operator actually gets | A church picked `turbo` with no way to know it was choosing a quarter of the cadence | §2 of the field audit | `docs/qa/audits/PERF-MODELS-2026-08-30.md` — all three models measured on one machine. **`small` is free** (152 ms, same single 200 ms hop as `base`); `turbo` is 597 ms → four hops → ~1.25 updates/s. The bench predicts the field within 15% | — | P2 | S | `RELAY_BENCH_MODEL=… cargo test --release decode_cost -- --ignored` |
| ✅ RG-31 | **FIELD F-6** — `end_to_end_speech_to_scripture` reports 39–90 s where its own constituent stages sum to ~1.1 s. Five samples; two of the service's eight fires were manual, and a manual fire has no audio origin to measure from | The one number a congregation would recognise is the one that cannot be trusted | §2 of the field audit | **Not manual fires — the clock.** It ran from `VoiceDetected`, and `voice_opened_us` is cleared only when an utterance CLOSES, so unbroken preaching pinned it: a verse quoted sixty seconds in was reported as sixty seconds of Relay latency. It now runs from `AudioReceived`, bounded by the window rather than by a sermon | — | P1 | S | `the_end_to_end_span_starts_at_this_windows_audio_not_at_the_utterance` |
| ⏳ RG-32 | A context-resolved bare verse is pushed as `Direct` at a hardcoded **0.88**. Relay inferred the book and chapter; it did not hear them | By rule 10's own principle the label is a lie, and 0.88 is a constant rather than a measurement | `main.rs`, the `detect_bare_verses` loop | Unresolved on purpose: "verse eighteen" while walking a passage is what the path is FOR, and one service is not evidence enough to make all of those ask for a click | RG-24 | P2 | ? | Needs a second and third service before it can be decided |
| ✅ RG-33 | **The confirm arm of the self-calibrating gate never fired.** `confirm_detection` re-parsed the reference STRING and taught `record_feedback` that parse's score — a constant for every canonical `Book C:V`, and always above the auto-fire bar, so the correction never ran | Relay's headline claim is that it learns your preacher. On the confirm side it never had | R4-09; `router.rs`'s own unit test passed throughout by calling `record_feedback` directly — the bug was one call site up | The console sends the suggestion's own confidence and method. Clamped; a paraphrase carries no number (a cosine is not a probability); an unknown method reads as `Semantic` | — | P1 | S | `e2e::confirming_a_suggestion_teaches_the_gate_what_was_accepted`, failing when the defect is restored |
| ✅ RG-34 | **Which of two verses in one window reached the wall was unspecified.** A `HashMap` threw away the order the preacher spoke in — and beneath it the sort comparator asked `pipeline::better` both ways, which is `>=`, so on a tie it claimed `a < b` AND `b < a` | A window may fire at most one verse (DECISIONS §37), so the tie IS what the congregation sees, and two runs of one sentence could differ | R4-07 | `Vec` dedup keeps first-seen order; the comparator orders explicitly, ties `Equal`. **Fixing the HashMap alone would have left it unspecified** | — | P1 | S | `main::rank_for_wall_tests::a_tie_keeps_the_order_the_preacher_spoke_in`, asserted in both directions |
| ✅ RG-35 | **The two Settings sliders left the voice profile in a state the router was never in.** `set_thresholds` moved the gate and neither the baseline nor the profile row; its twin `set_sensitivity` did all three | Calibration dragged the bar back to the dial position the operator had just overruled — inside the same service, and again at the next launch | R4-10 | One `apply_thresholds`, used by both. **The rule lives in the doorway both use** rather than copied into one of them | — | P1 | S | `r4_10_…`, asserting consistency to one dial step and that the BASELINE moved |
| ✅ RG-36 | **The rehearsal colour had a second meaning.** `VerseDeck` painted an EDITED slide amethyst — six lines from where the same badge means REHEARSAL. In a rehearsal the deck showed two identical amethyst pills meaning two unrelated things, one of them the signal that says nothing can reach the congregation | A colour that means two things means neither, and this one is a safety signal (DECISIONS §22) | `surface.test.js` R3-08, written as a FINDING | "Edited" is a fact about a slide, not a state of the wall — grey, with Sending and Queued | — | P2 | S | The test is inverted: it now fails if the colour is spent again, and asserts never-amber too |
| ✅ RG-37 | **Five `aria-modal` dialogs with no focus trap** — the four boot gates and the template preview. Tab walked out of them into the app behind, and focus was never restored | A boot gate is the FIRST thing a keyboard operator meets, and it opens onto an app that is not ready yet | `surface.test.js` R3-09 / R3-12 | `use:trapFocus` — `focus.js` was already the one implementation and one attribute to opt in. It deliberately does not bind Escape; that stays in `shortcuts.js`, because two opinions about a panic key is how a wall gets wiped | — | P2 | S | Both tests inverted; the mounted one asserts focus is INSIDE the dialog |
| ✅ RG-38 | **Two of VerseDeck's four fire controls were disabled in safe mode without saying why** — and the list row's label still promised "Put John 3:16 on the screens", which it could not do | Disabled and silent reads as a bug; disabled and still promising reads as a broken promise | `surface.test.js` R3-11 | One sentence across all four doors, so an operator learns it once — and it appears ONLY when true, because a control that always mentions safe mode teaches them to stop reading it | — | P2 | S | Inverted, plus a new test for the safe-mode-OFF case so it cannot pass by always explaining |
| ✅ RG-39 | **Six views a screen reader lands on had no heading at all** — the whole Library tab, Themes, Templates | A screen-reader operator navigates by heading. With none there is nothing to jump to and no way to tell where you have landed | `surface.test.js` R3-12, written as a FINDING | A heading in each; Scripture's existing bold title became a real `<h2>` so the visible text and the accessible name are the SAME string and cannot drift | — | P2 | S | Inverted — they now fail if a heading is removed |
| ✅ RG-40 | **The RUN surface never said the engine was missing.** 18 controls on Live disable on `!$capture.available`; Channels, ServicePlanner and History each explain it, and Live rendered nothing | The one tab an operator is looking at when something has gone wrong simply appeared broken | `surface.test.js` R3-12 | A rose `role="alert"` banner naming the one thing still true: the manual controls need the engine too, so there is no reassuring half-truth to offer | — | P2 | S | Inverted, and asserts the alert role — every control on the tab is dead, so it is not a quiet note |
| ⚠️ RG-41 | **Two of the original six "no heading" views are ROUTERS** — `Themes.svelte` and `Templates.svelte` are three lines that pick a child. Giving them a heading would produce two headings for one screen, so a reader jumping by heading lands twice on the same view | — | The finding as filed was wrong for those two | The requirement belongs to the children, and both children now carry one. **Recorded rather than silently dropped from the list** | RG-39 | — | — | Asserted explicitly: the routers must have NO heading, and must really be just the switch |
| ✅ RG-42 | **The Templates tab told a new operator "No templates yet" before it had asked.** A fresh install ships five built-ins, so an empty list before the first answer is not an empty list — it is a list nobody has read | It is the one sentence that makes somebody go and build five more, and it was the first thing they saw | `surface.test.js` R3-04 | An `asked` flag; `Loading` until the read returns. `EmptyState`'s own doc already said it — *if you do not KNOW the list is empty, you are Loading* | — | P2 | S | Inverted: it now fails if Empty appears before the answer |
| ✅ RG-43 | **The empty state told a volunteer what to do and made them find the control.** `EmptyState` styles a button in its slot — it was built expecting an action — and **not one of ~15 call sites passed one** | A minute lost in a dark booth, at the one moment a new operator has no idea where anything is | `surface.test.js` R3-05 | The Templates one now offers **New template**. The FILTER case deliberately gets no button: the templates exist, the filter is the problem. **The other call sites still pass no action, and the test says so** — this closes one door, not the class | — | P2 | S | Inverted, and asserts the button's text |
| ✅ RG-44 | **`surface.test.js` described itself as a file where every test asserts a DEFECT** — so a green run meant "every defect is still here". After this session most are closed | A future agent reads that header first and mistrusts every assertion in the file, or trusts the wrong ones | The header itself, plus one `FINDING` label left behind by a fix in August | Header rewritten to say what it is now: closed findings are INVERTED, never deleted, and a `CLOSED` header means the assertion guards the repair | — | P3 | S | 9 `CLOSED` headers; no stale `FINDING` labels remain |
| ✅ RG-45 | **A corrupt session payload turned the install into a fresh one — permanently.** `load()`'s corrupt branch is commented *"A CORRUPT payload is NOT a fresh install"* and returned `{...EMPTY}`, whose `setupDone: false` IS the fresh-install signal and the only thing `App.svelte` reads | The six-step modal wizard opens over a console that may be mid-service — and `session.subscribe` persists immediately, so the fallback was written back over the corrupt payload before anything could look at it | `coldstart.test.js`, and `session.test.js`'s own test was NAMED for the guarantee it asserted the opposite of | A key that EXISTS is proof the app has run here; a genuinely fresh install has no key and is handled a branch earlier. `setupDone` survives, and the unreadable bytes are kept under a sidecar key | — | P2 | S | Both tests inverted, and the mis-named one in `session.test.js` corrected rather than the finding being filed twice |
| ✅ RG-46 | **Three test files described themselves as recording defects after those defects were fixed** — `surface.test.js`'s header, and two `THE FINDING` headers in `qa-r5-groups.test.js` over tests fixed in August | A reader takes the header first: a green run then appears to mean *"every defect is still here"*, and the assertions get mistrusted or trusted wrongly | The files themselves | Headers say what they are now. **Closed findings are INVERTED, never deleted** — the header is the finding title and the assertion is the guard | RG-44 | P3 | S | No `FINDING` label remains over a passing repair |
| ✅ RG-47 | **RG-28 was fixed at the call sites and two of three doors were missed** — including `auto_open_outputs`, which `App.svelte` calls on mount, so **every launch** reopened the projector windows without telling the OS to keep the display up | The exact failure `wake.rs` exists to prevent, on the most common path there is — and introduced by the commit that introduced the module | Found by applying CLAUDE.md's own rule to my own change: *enumerate every caller of the thing you fixed* | Both doors call `refresh_wake`, and a test now **enumerates them mechanically** instead of a person doing it from memory — same shape as `servicelock::every_protected_command_actually_guards_itself` | RG-28 | P1 | S | Re-run with the miss restored: the guard fails and names `auto_open_outputs` |
| ✅ RG-48 | **This report had itself drifted.** §16 listed as "missing" twenty features that shipped during this session, and §18's corrected counts — the very numbers RG-20 was filed to fix — were wrong again: `main.rs` 4,369 → **5,723**, commands 118 → **137**, `capture.js` 1,941 → **2,195** | A gap register that says "missing" about built features is worse than no register: it is the same failure as a test file whose header says every defect is still present | The document itself, measured | §16 rewritten to the three that really are missing, each with the reason, and the old list preserved below the line. §18 states the drift as the finding rather than re-correcting the numbers a third time — **the durable form is the command beside the number, not the number** | RG-20 · RG-44 · RG-46 | P2 | S | The reproducing commands are in §18; run them |
| ✅ RG-49 | **Two of the three entries in Library → New Item did nothing.** `newPasteSong` set `lyricAction` and `newSaveScripture` set `scriptureAction`, and neither was ever passed to the pane it was meant to drive — `<LyricsPane>` and `<Scripture>` declared no such prop. The third IS wired, which is what made the other two look correct at a glance | **A cold-start blocker**: "Paste / draft song" is the only create path for `songs` that does not need a FILE, so a church whose lyrics live on a website had no way to add a song at all | `qa.rs::all_three_new_item_menu_entries_do_something`, a GAP asserted as open since the cold-start audit | Wired to the work, not to a flag: pasting opens a sheet and hands the text to the **same `parse_import` review a file goes through**; "Save scripture" puts the cursor in the search box on the Saved tab, because saving happens by starring a result and a second editor would be a second create path for a table that already has a good one. The dead flags are **deleted**, not left unread | — | P1 | M | Test inverted; it asserts the assignment is gone, the review path is shared, and the sheet traps focus |
| ⚠️ RG-50 | **`translations` is the last table an operator cannot fill** — there is no `add_translation` command, so a church cannot add a Bible version even when it is free to | **The reason I first recorded was WRONG and is corrected here rather than quietly amended.** I wrote that no corpus exists to import and called it "a rights question before it is a code question". DECISIONS **§32.4** says the opposite in as many words: **WEB, ASV, YLT and BBE are public domain and addable as data today, no licence.** The legal blocker applies to TPT, MSG, NIV and ESV — I took the licensed case's reasoning and applied it to the whole table | `qa.rs::a_fresh_install_still_cannot_be_given_a_second_translation`; DECISIONS §32.4 | **Not built, and deliberately not decided by me.** Whether Relay ships a second public-domain corpus, or an import path, or neither, is a product decision and DECISIONS does not contain it. CLAUDE.md: *if the decision isn't there, it hasn't been made — ask, don't assume.* **Flagged for a human** | — | P3 | M | The test fails the day an importer ships. A wrong reason in a register parks work forever, which is why this row now carries the correction and not the tidier sentence |
| ✅ RG-51 | **Five registered commands no rendered control could reach** — `create_template`, `import_song`, `import_pro`, `list_output_windows`, `open_output_window`. `ipc.test.js` checks wrappers; `qa-inventory.mjs` traces one hop further, and that is the level they failed at | **A security reduction, not only a tidy-up**: every registered command is invokable from the webview, and `open_output_window` opened an arbitrary fullscreen window on any monitor. A command nothing calls is attack surface nobody is watching | `qa.rs::no_registered_command_is_unreachable_from_a_rendered_component`, a GAP asserted as open since the cold-start audit | **Deleted**, with their wrappers, their Service-Lock entries, the `Outputs` label counter and two db functions left dead behind them — rather than given a UI, because each was superseded by a path every control already used. Same precedent as the five deleted before them | — | P2 | M | Both tests inverted; `qa-inventory.mjs` reports **0** unreachable commands, 132 registered |
| ✅ RG-52 | **The register table itself was corrupted by my own conflict resolutions.** Nine rows had been concatenated onto the end of a neighbour's line, so the table rendered them as one cell and the entry was invisible; three were outright duplicated | A gap register whose rows silently vanish is not a register. RG-47 was unreadable for two merges and I only noticed because a rebase conflicted on it | The file, checked by ID: RG-01…RG-51 with no gaps and no repeats | Split and de-duplicated. **The trap is that the "depends on" column reads `\| RG-02 \|`** — identical to a row start — so the first repair split rows at their own dependency cells. A row start is now identified by its STATUS MARKER, never by the id alone | — | P2 | S | **`src/lib/relaygap.test.js`** — five assertions: one contiguous table, nothing glued, ids complete AND in order, full cell count, and an open entry must carry its reason. It found three more faults the moment it ran |
| ✅ RG-53 | **Two launch-screen rows could only ever say `ok`.** The kiosk row counted configured channels and printed `ws://…:8031`; the HTTP row printed `http://<ip>:8032`. **Neither asked whether anything was listening** | On the one screen whose job is answering "is this machine going to work?". These are ordinary TCP ports on a volunteer's laptop and binding can fail — and when it does, every OBS browser source and the preacher's stage page are dead while the check says fine | `probes.js`; `sysprobe::probe_integrations` probed OBS and ATEM but not Relay's own ports | `probe_own_ports` rides on the command the launch screen already calls. The kiosk row **fails** when a browser source needs the hub and only **warns** when none does — painting red at a church with one HDMI projector teaches an operator to ignore red | RG-01 | P1 | S | Six tests including "neither row can report ok without the port answering", plus a Rust test that a really-bound port reads as listening — without it the prober could return false for everything and every other assertion would still pass |
| ✅ RG-54 | **`qa_r5.rs` said two of its tests were expected to be RED.** All three findings are closed and all six tests are green | A reader takes the header first: it says a green run is wrong, so either the tests get mistrusted or the closures go unnoticed | The module header | Corrected, with the same rule as RG-46: closed findings are inverted, never deleted | RG-46 | P3 | S | No file in the suite now claims a defect that is fixed |
| ✅ RG-55 | **One fact, two verdicts.** `degraded.js` calls a CPU-only macOS build *reduced* — "roughly three times slower, the transcript will lag the preacher" — and the launch screen's GPU row reported the identical fact as **`ok`, in green**, on the screen an operator reads before a service | CLAUDE.md rule 27 measured it: **~1710 ms per window against a ~1000 ms budget**, which is slower than real time. A green row over that is the launch screen's own version of a badge that cannot be wrong | `probes.js::gpu` vs `degraded.js` | The rule is exported once (`gpuIsReduced`) and **asked** by both, so they cannot drift. macOS only, deliberately: no equivalent measurement exists off it, and a warning nobody measured is one an operator learns to scroll past | RG-09 · RG-53 | P2 | S | Four tests, including one that asserts both surfaces agree by calling the same function |
| ✅ RG-56 | **Nothing compared a FRESH install's schema to an UPGRADED one.** `schema.sql` is `include_str!`d so it cannot drift from the code; the upgrade path — a sniffed `ALTER … ADD COLUMN` per late column — is separate and was unchecked | Invisible exactly where it is written: every test and developer database is fresh, so a forgotten migration works perfectly here and is missing on every machine that has run an older build. The first symptom is a church's laptop failing a query that works everywhere else | `db/mod.rs`; per-column migration tests existed, no comparison did | **Two tests, and the second closes what the first could not.** One builds an old database honestly — drop every column an `ALTER` claims to add, wind `user_version` to 0, run the real `migrate`, require them all back. The other diffs today's schema against `schema-baseline.sql` — the schema at the project's first commit, checked in — and fails if a column was added to an existing table with no migration behind it | RG-04 | P2 | M | Breaking a pragma sniff fails the first; adding a column with no `ALTER` fails the second. **I recorded this as unclosable for want of an old schema — wrongly, git had one all along** |
| ✅ RG-57 | **The CI detection gate contained only invented sentences.** 57 cases, every one written by somebody imagining how a preacher talks, guarding SPEC's <5% wrong-verse rate | The gate that decides whether detection quality regressed had never seen a real ASR transcript. The only real-world data the project has was sitting in `detections.heard_text` and in one Rust test | `eval_corpus.json`; `audits/FIELD-2026-08-30.md` | Six verbatim lines added, `source: FIELD-2026-08-30` — five references the preacher really made, and the Luke 10 line that produced a wrong verse, as a NEGATIVE. The corpus note also records what the scorer **cannot** see: it drives `detect_direct` and the router, not `ContextMemory`, so a green scorecard is not evidence about the path that actually failed | RG-24 | P2 | S | 63 cases, 100% recall, 0 wrong verses, 0 paraphrase auto-fires — `cargo test eval::tests::print_scorecard -- --nocapture` |
| ✅ RG-58 | **The wrong-verse gate ran at ONE dial position.** `eval::run` builds `Router::default()`, so SPEC's <5% was enforced at sensitivity 50 and nowhere else — while R4-03 had just proved that at dial 100 the auto-fire bar equals the confidence floor and every deliberate demotion goes inert | A church that moves the slider was running a configuration nothing had ever measured, and the one check that would have noticed was not looking there | `r4_04`, `#[ignore]`d as "a measurement" since the R4 audit | Un-ignored — it costs 20 ms and asserts six positions from 0 to 100. **And it needed teeth:** run as it was, it passed with the `UncertainNumber` repair torn out, because no corpus case fires without it. Eleven ordinary-preaching negatives added (`source: R4-audit`) — the sentences that really did put verses on walls | RG-23 · RG-57 | P1 | S | With the repair removed the gate reports **11.8% at dial 63** and fails — the band where the demotion sits. 74 cases, 0.0% at every position |
| ✅ RG-59 | **The AI disclosure never reached the operator.** `docs/AI_DISCLOSURE.md` is the honest account of what the AI does and where it is weak, and it was readable only from the repository. In the app, Help carried the reassuring half — the never-guess rule — and none of the honest half | A church that never opens GitHub was shown only the part that builds confidence. Publishing the reassuring half alone is worse than publishing neither: it is the same asymmetry as a status badge that cannot detect its own failure | `Help.svelte` TOPICS vs `docs/AI_DISCLOSURE.md`; no link, and a link would have been useless — the operator who needs this is offline in a hall | A Help topic, **What the AI is bad at**: Relay never writes scripture and reads the KJV verbatim; African-language listening is the weakest part *and* the headline claim; word error rate has never been measured in any language; keep `Esc` under your hand | RG-11 · §58 | P2 | S | `aidisclosure.test.js` — four claims, asserted in **both** documents by substance rather than wording, so the prose can improve and only deleting a claim breaks it |
| ✅ RG-60 | **No threat model in the shape the brief asked for.** `SECURITY.md` argued three priorities well and left the rest of the surface as an absence rather than as rows | An absence cannot be reviewed. Two of the ten rows are honest gaps that follow DECISIONS §35, and they were indistinguishable from oversights until they were written down as decisions | `SECURITY.md` before this pass: no T1–T10 table; RELAY_GAP §2 §64 read PARTIAL | T1–T10 added: content leaving the device · LAN screen control (ACCEPTED, §35) · the closed drive-by · the broadcast-only hub · path traversal · template injection · the update channel · model integrity · language packs (no surface yet, on purpose) · the malicious operator (out of scope) | RG-19 for T9 | P2 | S | Each row names the mechanism or the test that holds it; T9 and T10 are recorded as absences with the condition that would change them |
| ✅ RG-61 | **A volunteer finishing the first-run wizard had no way to learn that the practice drills, the six-stage path check and rehearsal exist.** All three shipped; nothing pointed at any of them | The wizard is the last moment somebody is guaranteed to be looking. An instrument nobody can find is an instrument nobody runs — and the path check in particular is the one thing that catches a chain that fails end to end while every part passes | `FirstRun.svelte` STEPS; brief §59/60, carried as PARTIAL in §2 | A hand-off block on the finish step, under the verse it just fired: each instrument named **with the tab it lives on**, plus a line saying this is not the operator's last chance to find them. **Not three more steps** — the wizard's own rule is that it asks as little as it can, and a drill is a thing to do on another day | RG-15 · RG-16 · RG-10 | P2 | S | `firstrunmic.test.js` — four tests: all three named with their location, the path-check sentence that stops it reading as a duplicate of the wizard, the not-your-last-chance line, and **that the step count is still six** |
| ✅ RG-62 | **Two of `detections.status`'s four values had never been written, and the Sunday report counted them anyway.** The only production insert is inside `persist_fire`, which runs for a fire that reaches a screen — so a real service can write `'auto'` or `'manual'` and nothing else. `'suggested'` and `'dismissed'` were structurally unreachable | The report printed **`0 suggested · 0 dismissed`** for every service ever recorded. Zero is a claim, and it reads as *"Relay never offered you anything"* — the exact inversion DECISIONS §44 forbids, broken two fields away from where it was written. It hid because the report's own tests fed it synthetic rows the product cannot produce: **a test whose fixture is impossible is not a test** | `persist_fire` call sites (`main.rs`), `report.js::sundayReport`, `report.test.js`'s `det(…, 'suggested', …)` | Record **what the operator did**, not what the AI offered: `confirm_detection` writes a `suggestion_accepted` cue, `dismiss_detection` a `suggestion_dismissed` cue, neither during a rehearsal. Uptake is of the suggestions **answered**, and the report names that limit itself. **Persisting suggestions was rejected** — they are not debounced (rule 28), so one paraphrase writes hundreds of rows a minute | RG-04 · RG-08 | P1 | M | Three `e2e` tests, each re-run with its defect reintroduced and each failing; plus a `report.test.js` case that feeds the OLD impossible rows and requires `null`, so the previous shape cannot come back by accident. **The third is a privacy test on this fix's own new door:** `dismiss_detection` takes a reference as a STRING across the bridge and `cues.payload_json` is read back by `service_timeline`, so it would have been the first cue payload whose shape was trusted rather than guaranteed — it is parsed and stored canonical, or not stored at all |
| ✅ RG-63 | **A suggestion whose verse does not exist looked exactly like one that does.** `emit_detections` deliberately keeps a reference that parsed cleanly but resolves to nothing ("Psalms 23:99" out of garbled speech) and marks it `in_library: false` — **and no frontend file read the flag** | The card rendered with the same amber Approve beside it, and the click failed *afterwards* with a backend error. A control that looks identical to its working neighbours and cannot work is the RG-01 defect class on the console instead of the wall. `main.rs` carried a comment naming this exact case, unfixed | `pipeline.rs` sets it; `grep in_library src/` returned only the unrelated `not_in_library` nav result | `detect.js::inLibrary`, read by Live's primary card and its "also pending" rows. (It was read by the Library's rail too, which was a SECOND implementation of this guard on a duplicated suggestion panel; REBRAND §10 removed that panel on 2026-09-14 — the Library renders no Approve at all now, and `liveoutputrail.test.js` holds its absence so the duplicate cannot come back unnoticed.) The suggestion **stays visible** — it is the operator's evidence that a number was misheard, and dropping it would be silence — but the Approve is disabled and **says why**, and `acceptTop`/`accept` refuse it so the `A` key cannot walk past the warning the markup renders | RG-13 | P2 | S | `detect.test.js` (3) — including that an ABSENT flag means present, so the warning can only ever be added on evidence. **Corrected 2026-09-14**: this cell also named `liveoutputrail.test.js` (4), which tested the Library's duplicate panel; that panel and those four tests are gone, and the claim is now carried by Live's markup plus the absence test named above |
| ✅ RG-64 | **The event contract test scanned one file.** `ipc.test.js` asserts that every event the backend emits is listened for on the frontend — and built its list from `main.rs` alone, so the four `model://` events and everything `channels.rs` emits were outside a contract whose entire claim is exhaustiveness | The second version of the same mistake: this test had already been widened once, because its regex did not allow `_` and had silently excluded `stt://language_unstable` and `output://panic_failed` — **a panic path**. A scanner that reads one file cannot report on a repository, and eleven of thirteen findings in the accessibility pass were the instrument's own bugs | `const emitted = … mainRs.matchAll(…)`; `models.rs` emits four events | Scan **every** `src-tauri/src/*.rs`. `model://done` turned out to be genuinely unheard and is allow-listed **with its reason** — `download_model` resolves only on success, so a listener would double-handle it — and documented at `models.rs::download`, in `ARCHITECTURE.md` and in `DATA_MODEL.md` | RG-53 · RG-13 | P2 | S | A test that the scanner sees `model://*` at all (so a re-narrowing to `main.rs` fails), a test that each allow-listed event is **still emitted and still unheard** (so the list cannot outlive its reason), and a bogus event added to `channels.rs` — which the widened scanner caught and the old one could not have |
| ✅ RG-65 | **The other half of the same contract read four files out of nine.** `ipc.test.js` asserts every command the frontend calls is registered in Rust, from a hand-written list — `capture.js`, `probes.js`, `outputHealth.js`, `updater.js` — while **nine** files import `@tauri-apps/api/core`. `App.svelte`, `Output.svelte`, `FirstRun.svelte`, `latency.js` and `Settings.svelte` were outside it | The list grew by one entry each time somebody happened to notice another direct caller, and the file's own comment says why that fails: *"a command called from a third file that this test did not read is precisely the door nobody checks."* It was written beside a list that was already missing five. **No live defect** — all six commands outside the contract do exist — and "nothing was broken" is exactly what an unchecked door looks like until it is not | `grep -rl '@tauri-apps/api/core' src/` returns nine files; the test named four | The inputs are **derived from the tree**: every `.js`/`.svelte` under `src/` that imports the Tauri core, so a tenth file joins the contract by existing. The same for the `greet`-has-one-caller test, which was scanning five named files while the whole value of `greet` is its count | RG-64 | P2 | S | A test naming four commands each called only from a previously-unread file, so a re-narrowing fails rather than passing quietly; and a bogus command planted in `Settings.svelte` — caught now, invisible before |
| ✅ RG-66 | **CI ran the frontend suite on Node 20 and nothing else, so the fix that exists for Node ≥ 22 was never exercised.** `src/test-setup.js` hands the global a real jsdom Storage because Node ≥ 22 defines `localStorage` as an own accessor returning `undefined`, and vitest's jsdom environment leaves keys the global already owns | The 60-odd tests it rescues are exactly the ones covering **what an operator sees when the app relaunches** — the session, the crash record, the first-run flag. CLAUDE.md says "don't delete it because 'jsdom provides that'", and deleting it would have left CI green while every contributor on a current Node saw a broken checkout. **A guard nothing exercises is a guard that guards nothing** | `ci.yml`'s `frontend` job: `node-version: 20`, no matrix | A Node matrix — **20** (the floor the Rust job also uses), **22** (where the break was) and **24** (a current runtime, so the fix is proved forward and not only backward), with a comment saying why it must not be trimmed back to one | RG-64 · RG-65 | P2 | S | Measured, not assumed: disabling the shim on this machine's Node 26 fails **66 tests in 6 files** and passes again when restored. On Node 20 it is a no-op, which is exactly why one runtime could not see it |
| ✅ RG-67 | **Four citations pointed at a DECISIONS section that does not exist.** `ARCHITECTURE.md`, `SPEC.md` (×2) and `PROMPT.md` cited "DECISIONS.md §16" for the 0.50/0.35 threshold seed — **the numbered log starts at §18**, and that decision is one of the unnumbered rows that predate the numbering | **A citation that resolves to nothing is worse than no citation**, which at least announces that it needs checking. A reader following it lands on nothing and cannot tell whether the decision is missing or the document is wrong. This repository leans on cross-references more than most — CLAUDE.md cites DECISIONS, DECISIONS cites the register, code comments cite all three — so they are load-bearing, and three sweeps corrected counts without once checking whether a section number still pointed anywhere | Resolving every `DECISIONS §N` against the actual headings | Each now names the row and says why it has no number. **And the check is a test, not a sweep** — which is what caught the fourth: a hand pass over the docs missed `PROMPT.md`, and the test found it on its first run | RG-20 | P3 | S | `crossrefs.test.js` — every `DECISIONS §N` resolves to a heading, every `RG-` id resolves to a register row, plus a guard that the scan reads a real slice of the repository (both assertions would also pass if it found nothing) |
| ✅ RG-68 | **A QA agent's own instructions described a deleted component and a fixed defect, both in the present tense.** `relay-qa-live-path.md` told the agent that `PreviewProgram.svelte` "reads exactly like the safety model … and nothing imports it" — it was deleted — and that `liveoutputrail.test.js` "carries one skipped test for a known defect … do not re-file it". That defect is closed and the file has **zero** skipped tests | This is RG-54 again, one directory across: a file in the audit apparatus asserting a defect that is fixed. It is worse in an agent brief than in a test header, because it does not merely mislead a reader — **it instructs an agent not to file something, and points it at a file that is not there** | `grep PreviewProgram .claude/`; `grep -c 'it.skip' src/lib/liveoutputrail.test.js` → 0 | Both corrected, and the paragraph **kept rather than deleted** — the near-miss it describes (fourteen green tests against a component nothing rendered) is the durable lesson, so it now ends on the rule instead of on the stale fact | RG-54 · RG-46 | P2 | S | The other five agent briefs were scanned for the same pattern and carry none. `crossrefs.test.js` covers `.claude/` for `RG-` and `DECISIONS §` citations |
| ✅ RG-69 | **The edit-time fast gate's watch list had drifted behind the code it watches.** `outputHealth.js` (the ONE rule for what Live and Outputs may say about a screen), `degraded.js` (the shell line on every tab), `updater.js` (refuses to install while a service is locked) and `servicelock.rs` all shipped with RG-01 … RG-09 with tests, and **none of them was added to the gate** | The gate's whole value is that it is quiet when things are fine — which is also how it fails. A rule whose path no longer matches, or that names a renamed test file, **does not error: it goes quiet**, and quiet is what "everything is fine" looks like. Four service-critical files gained tests and no edit-time guard | The `WATCHED` table vs the files added since it was written | Four rules added, each justified against the gate's own criterion (*a silent break measured in Sundays*) — and the drift itself is now pinned | RG-64 · RG-65 · RG-66 | P2 | S | `fastgate.test.js` — every watched path matches a real file, every named test file exists, the named service-critical files are watched, plus a guard that the rules parsed at all (an empty list would pass every other assertion). Verified by renaming one rule's path: two assertions fail |
| ✅ RG-70 | **Sixteen source comments cited `docs/relaydesign/`, a directory that is not in this repository**, plus three that cited the `Working-Agent-*` documents `QA_HARNESS.md` superseded and one that named an HTML mock never checked in | A comment pointing at a file nobody can open is **worse than no comment**: it reads as *"the reasoning is written down over there"* and sends the next person looking for something that was never there. The same class as RG-67, in the dimension that had the most instances — file paths rather than section numbers | Resolving every cited `docs/…` path against the filesystem | Six were the right file under the wrong directory name (`docs/design/`) and one had also been renamed (`relay-library-screen` → `relay-main-library-screen`) — all repointed. Four named documents that were never here (a design log ×2, a screens reference, an HTML mock): each now **says so in place**, and where it carried a rule the rule is written out instead of cited. Three `Working-Agent-*` citations now point at `QA_HARNESS.md`, which replaced them | RG-67 | P2 | S | `crossrefs.test.js` gained a third dimension — every cited `docs/…` path must exist, unless the citation **says** the file is gone (the register records its own deletions on purpose, and losing that would be worse). Verified by breaking one citation: it fails |
| ✅ RG-71 | **The contributor's front door quoted July's test counts.** `CONTRIBUTING.md`'s setup block said `cargo test # 250 tests` and `npm test # 138 tests` — the real numbers are **627** and **908** — and told a new contributor that CLAUDE.md's hard-way section is "25 numbered rules" when it is **40**. `PRODUCT_AUDIT.md` carried a "(35 today)" that was itself stale | This is the drift that costs the most per instance: it is read by somebody who has never seen the repository, at the moment they are deciding whether their checkout is broken. A count wrong by 3× reads as *"something is missing on my machine"* | `CONTRIBUTING.md` §setup, §the-rules; `PRODUCT_AUDIT.md` developer-experience row | **The numbers were removed rather than corrected** — the block now says the runner prints the count and points at `QA_HARNESS.md` §0, which is the one register. The rule count carries its `grep`. Same treatment the `main.rs` line got, for the same reason: this is the fifth time these have been wrong | RG-20 | P2 | S | No count in `CONTRIBUTING.md` now, and a repo-wide sweep for the old values returns nothing |
| ✅ RG-72 | **`PRODUCT_AUDIT.md` §10 listed four finished pieces of work as outstanding**, in the present tense, in the one section a reader opens to decide what to build next: make the live commands able to fail (`nav` has returned `error::Result<NavResult>` for weeks), add an e2e test (`e2e.rs` has 38), introduce a typed error (`error.rs`; **zero** `Result<_, String>` remain), and normalise the throw-vs-swallow contract (stated at the top of `capture.js` **and pinned** by `micstop.test.js`) | RG-54 and RG-68 again, in the most expensive place. A reader acting on §10 would have spent a day rebuilding four things that exist — and the section already had the convention for this: item 2 was struck through and marked done, and the other four simply never were | Each claim checked against the code before striking it | All four inverted with a ✅ and **their original wording kept beneath**, per RG-46: closed findings are inverted, never deleted. The section header now says all five are closed and why leaving them was costly | RG-54 · RG-68 | P2 | S | Every claim verified: `nav`'s signature, `cargo test e2e::` at 38, the `Result<_, String>` grep returning only `error::Result<String>` (a String *value*, not a String *error*), and both group tests present |
| ✅ RG-73 | **The production-readiness checklist ticked "Signed + notarized macOS build". No Apple certificate has ever existed.** `gh secret list` holds two secrets, both `TAURI_SIGNING_PRIVATE_KEY*` — the **updater** minisign keys. The release gate requires six `APPLE_*` secrets for macOS and finds none, so all four releases (`v0.1.0-1` … `-4`) went out on the **unsigned pre-release path** | **The only wrong tick in that checklist, and it was release-blocking.** §16 is the page a release decision is read from, and this made an absent certificate look closed. Worse: **CLAUDE.md rule 17 says the microphone dies on the FIRST correctly-signed build** — so the trap is still ahead of this project, and the tick implied it was behind | `gh secret list`; `release.yml`'s `MAC_SIGNED` loop; `gh release view v0.1.0-4` | Unticked, with what IS true stated in its place: the chain is wired and `scripts/sign-local.sh` reproduces the hardened runtime **ad-hoc**, which is what makes rule 17 testable without a certificate — and is not the same as having shipped a signed build. Corrected in four places that had inherited the claim: §2 row 46, §22's Distribution row, this report's header table, and `KNOWN_ISSUES.md` §1 | RG-72 | **P1** | S | `gh secret list` returns two updater keys and no `APPLE_*` or Windows certificate; the gate's own `MAC_SIGNED` check is the specification |
| ✅ RG-74 | **"Every text token at AA" was a ticked box with no instrument, and it was false.** `--v-faint` was `#8a8a8a`: **4.38:1 on `--v-surf2`**, below WCAG AA's 4.5, with five real rules putting muted text on chips and badges there — plus one rule in Settings pairing it with `--v-surf3` at **3.76:1** | `PRODUCT_AUDIT.md` §16 has claimed it as done since July. `contrastRatio` existed in `legibility.js` and was pointed at TEMPLATES — what a congregation reads — and never at the console's own palette. **The hand-written comment beside the token knew about the wrong surface**: it warned about `--v-surf3`, where nothing pairs them, and never checked `--v-surf2`, where five rules do. An analysis naming two of four surfaces reads exactly like one naming all four | `contrastRatio(parseColor('#8a8a8a'), parseColor('#262626')) = 4.38` | Token to **`#8c8c8c`** — two steps, invisible, and the difference between failing and passing on surf2 (now 4.50). The Settings "Soon" pill moved from surf3 to surf2. `--v-surf3` stays **excluded and asserted**, not assumed: clearing it needs `#9b9b9b`, which collapses muted into `--v-dim` | RG-18 | P2 | S | `tokencontrast.test.js` — every text token × every surface it is placed on, **plus** that nothing pairs muted with surf3, plus a guard that the ratio is a finite number. **That guard earned itself immediately: the first version passed hex strings, `contrastRatio` returned `NaN`, `NaN < 4.5` is false, and all four token assertions passed by checking nothing** |
| ✅ RG-75 | **Three register rows pointed their own validation at tests that no longer exist.** This repository **inverts a test's name when the defect closes** — `two_of_the_three_new_item_menu_entries_are_dead` became `all_three_new_item_menu_entries_do_something` — which is exactly right (RG-46), but the rows citing the old names were never updated | **A register row whose evidence cannot be found reads as an unproven claim**, and the reader has no way to tell *"the test was renamed"* from *"there was no test"*. Verifying RG-51 by searching for the name it cites returns nothing, on the row whose entire point is that a security reduction was proved | Resolving all **57** `module::item` citations in the repository against the Rust tree | Three repointed to their current names. **And a fourth dimension added to `crossrefs.test.js`** — module-prefixed citations must resolve, with the module set **derived from the tree** rather than an allow-list of external crates, because an allow-list goes quiet the moment it falls behind | RG-67 · RG-70 | P3 | S | Verified by restoring one old name: three assertions fail. `docs/qa/audits/` is excluded **because those documents may not be edited** — the one dangling citation in there (`detection::r4_07`, which has never existed) is recorded in that audit's own fix log instead, which is the mechanism it already uses |
| ✅ RG-76 | **The forty "learned the HARD WAY" rules were guarded by prose alone.** Their own header says *"These caused real crashes, freezes, or silent failures in front of people"* — and nothing enforced any of them. Five are mechanically checkable and were protected by nothing: a regression would land, pass CI, and be found by whoever next ran a service | These are not style preferences. Rule 1's violation is a **hard freeze of the webview with no error**; rule 3's is a **silent SIGSEGV** that stops speech recognition mid-sermon; rule 9's left packaged **Windows** unable to find the STT model; rule 24's makes the fire path undrivable by `e2e.rs`; rule 36's would let the pre-air validator be missing from the sixth call site, **which is the shape of four separate bugs already in this repository** | No test referenced any of them; `grep` says none is currently regressed | `hardrules.test.js` pins rules **1, 3, 9, 24 and 36** — and *only* those five. **A test that guessed at the other thirty-five would be worse than none**: it would fail on legitimate code, get weakened, and take the five real ones down with it. Rule 26 was already pinned by `ipc.test.js` | RG-64 · RG-69 | P2 | S | **Each rule was broken and the test confirmed to fail** — including rule 36's second half, that the single call stays *inside* `broadcast_with_clock`, where the preflight lives. Plus a guard that the file walk found a real tree, since all five would pass over an empty list. **A sixth assertion covers a trap this session nearly walked into**: `docs/` is not purely documentation — `db/mod.rs` `include_str!`s `docs/data/schema.sql` and `schema-baseline.sql`, so deleting them breaks `cargo build` while **the whole frontend suite stays green**. This session was asked to delete unneeded folders under `docs/`; it checked first, and the next person might not |
| ✅ RG-77 | **The pilot's single highest-value instruction had no test and two undocumented failure modes.** `RELAY_RECORD_WAV` is what converts *"word error rate has never been measured in any language"* into a number — `ROADMAP` §1 calls it the highest-value line on that page and §24 makes it a pilot condition. Its WAV writer is a **hand-rolled 44-byte RIFF header with zero coverage**, it **buffers the entire service in RAM** (~570 MB for 50 minutes), and it writes **once, at Stop** | A wrong header field yields a file that opens as noise — discovered *after* the service, with no second take. A force-quit or crash loses the whole recording, which is exactly the morning somebody most wanted it. **Neither was written anywhere a church would read**, and the instruction as given was "set one environment variable" | `grep RELAY_RECORD_WAV src-tauri/src` → two hits, no test; `write_wav_f32` untested; the buffer is a `Vec<f32>` flushed after the capture loop | Round-trip test through this module's own `load_f32` — the reader every bench here uses — asserting format 3 (IEEE float), mono, the capture rate, 32 bits, **the `36 + data` RIFF size field** (the classic hand-rolled bug) and sample-for-sample fidelity. The RAM cost and the **press Stop, never force-quit** requirement are now in `CLAUDE.md`, `ROADMAP` §1 and §25's pilot checklist | RG-73 | **P1** | S | Verified by breaking the header two ways — the format tag and the RIFF size — and confirming each fails |
| ✅ RG-78 | **Two lists of invariants, and each was missing what the other had.** `ARCHITECTURE.md` §8 was titled *"Invariants — the rules that keep it from breaking"* and listed **eleven** while `CLAUDE.md` carries **forty-one**. The eleven omit *only `Direct` may auto-fire*, *levels are learned never assumed*, *a panic control may never report a success it did not achieve*, *a migration must be retryable*, and *the choke point is where the check goes* — **the five that decide whether a wrong verse reaches a congregation.** Meanwhile ARCHITECTURE held one rule CLAUDE.md did not have at all | `docs/README.md` sends a new engineer to CLAUDE.md **then ARCHITECTURE**. A subset presented as the set, in the second document they read, is how somebody learns eleven rules and believes they have learned the rules | Diffed the two lists item by item | ARCHITECTURE §8 renamed to *"the ARCHITECTURAL ones"* and now **names the five it omits** and says to read CLAUDE.md first — pointing, not duplicating, which is the rule this repository already applies to counts. The orphan (`confirm()`/`alert()`/`prompt()` do not work in Tauri's webview — `confirm()` returns `false` **without showing a dialog**, so a two-step delete guarded by one deletes nothing and reports success) is promoted to **CLAUDE.md rule 41** | RG-70 · RG-76 | P2 | S | Rule 41 pinned in `hardrules.test.js`, verified by planting a real `confirm()` — and verified NOT to trip on the six existing comments that explain why it is not used, since a test that cries wolf gets weakened |
| ✅ RG-79 | **Four documents hard-code the default sensitivity, and nothing tied them to the constant.** `Thresholds::default()` calls `from_sensitivity(DEFAULT_SENSITIVITY)`, so the *"exactly ONE baseline"* invariant is unrepresentable otherwise — that part is sound. But `CLAUDE.md`, `DATA_MODEL`, `DECISIONS` and `ARCHITECTURE` write the **number** out, and quote the seed thresholds (0.50 auto-fire / 0.35 suggest) beside it | **Two baselines once existed and disagreed** — 0.50/0.35 against 0.90/0.60 — and a profile save silently snapped the live gate from one scale to the other, wiping the operator's calibration. Changing the constant without the docs puts that second number back into circulation on paper, next to the sentence that says there is only one | `DEFAULT_SENSITIVITY: u8 = 50` in `router.rs`; the claim restated in four files | A test that **reads the constant and requires the documents to agree** — drift in either direction fails, and nothing needs updating when the constant legitimately moves | RG-76 | P3 | S | Verified by changing the constant to 60: it fails. **The first version was over-broad and would have shipped a false failure** — it flagged `from_sensitivity(100)` in DECISIONS, which is a legitimate statement about the TOP of the dial. Narrowed to match the *claim* (`default() == from_sensitivity(N)`), not the call, plus an assertion that the claim has not vanished |
| ✅ RG-80 | **A reader could not tell which of the forty-one hard-way rules are enforced and which rest on discipline.** Only twelve named any instrument, and several that ARE pinned did not say so — rule 32's cadence floor and headroom, rule 10's auto-fire cap, rule 16's `Esc`-inside-a-dialog suppression, rule 25's retryable migration, rule 38's disarmed passage | The section header says these caused real crashes *"in front of people"*. Without knowing which are held by a test, every one reads as equally fragile — so a careful engineer spends attention re-verifying the enforced ones, and a hurried one assumes all of them are covered. **Both readings are wrong in the expensive direction** | Parsed the rules and looked for a named test in each | **Ten now name their instrument** (search *"Pinned by"*), each verified to exist before it was cited — no claim was made from memory. The header states plainly that the rest rest on discipline, and that **a rule with no named instrument is not a weaker rule; it is one where you are the instrument** | RG-76 · RG-78 | P3 | S | Rule 32's pin was verified by breaking BOTH halves — restoring `STEP_SAFETY = 1.5` and setting a floor finer than one chunker hop — and confirming `a_fast_decoder_steps_at_the_floor_not_at_a_fixed_second` fails on each. `crossrefs.test.js` resolves every `module::item` cited |
| ✅ RG-81 | **`heard_text` — the column `db/services.rs` calls THE EVIDENCE — was written on every fire and returned by no query.** It appears in the INSERT, in the schema, and in four documents; it was in **no SELECT** and referenced by **no frontend file**. The only way to read it was to open the SQLite file by hand | **§24 makes *"write every wrong verse into the register, verbatim from `heard_text`"* a condition of the supervised pilot** — and that condition was unimplementable by the operator it is addressed to. It is the loop that turned the field service's Luke 10 wrong verse into a regression test *and* a CI corpus case; a pilot that cannot close it loses its single most valuable output. It is also **not** the transcript: detection runs on partials and only finals persist, so nine auto-fires were once logged against a final from three minutes earlier that yields no matches at all | `grep heard_text` → INSERT and struct only; `ServiceDetection` had five fields, none of them it | Added to `service_detections` and rendered on the History screen under the reference it explains, quoted in serif like every other quotation of speech | RG-77 | **P1** | S | **One test asserts both halves, so neither can be satisfied alone**: the operator can read it, and it still never reaches the timeline — the part of the record most likely to be emailed. Verified by removing it from the SELECT (fails) and by wiring a real leak into the timeline query (fails, quoting the sentence) |
| ✅ RG-82 | **`cargo clippy --all-targets -- -D warnings` failed on this branch**, and CI runs exactly that on macOS and Windows. A duplicated `#[test]` attribute in `db/services.rs`, introduced when a new test was inserted between an existing doc comment and the function it documented | **The branch could not pass its own gate**, and the doc comment explaining *why* `nothing_a_preacher_said_reaches_the_timeline` exists was stranded above the wrong test. **It was also inflating the count register**: `cargo test` reported 630 of 647 declared; it is 629 of 646 | `error: duplicated attribute --> src/db/services.rs:952:5`, reproduced locally | The doc block moved back onto its own test rather than the stray attribute being deleted — the comment is the half that was actually lost. `QA_HARNESS.md` §0 corrected, with the reason rather than just the number | — | **P1** | S | `cargo clippy --all-targets -- -D warnings` exits 0; both affected tests run individually; `cargo test` reports 629 |
| ✅ RG-83 | **The auto-updater endpoint returned 404.** Both configs pointed at `…/releases/latest/download/latest.json`; GitHub's `/latest/` excludes pre-releases, and every published release is one | **Every copy that ever shipped was un-updatable.** The updater is built, signed with a real minisign key and covered by tests — and resolved to nothing. This class of failure is invisible from the source and green in every test, because the URL is data | `curl -sI …/releases/latest/download/latest.json` → **HTTP/2 404**, run 2026-09-02, 09-03, 09-04 and 09-05 | **Closed 2026-09-05, at the second attempt — see RG-114.** The first fix pinned the endpoint at a version tag (`…/releases/download/v0.1.0-4/latest.json`), which is the second half of what this row always proposed and which **resolves without working**. The endpoint is now one permanent address, `…/releases/download/updates/latest.json`, re-uploaded on every publish. The `v0.1.0-4` release is already published and already carries `latest.json` and the signed artefacts, so the channel went live with **nothing new published**. The other route, promoting a release to non-prerelease, is refused by `release.yml` while either platform is unsigned (RG-73) — and should be: a full release of an unsigned build is a build a church cannot open, presented as the stable one. **The pin has its own failure mode**: a version bump that leaves it behind ships an updater looking at the previous release for ever, silently, because the manifest still resolves. So `scripts/version.mjs` owns it — the pin is part of the version now, not a thing to remember | RG-73 for why the other route is shut | **P1** | S | `npm run updater:check` → **2 update endpoints live**. `npm run version:check` — a CI gate on every PR — fails when the pin disagrees with the three version files **or** when an endpoint drifts back to the `/latest/` shape; both failure modes verified to exit 1. `--set` moves the pin and says the endpoint 404s until that release is published, which is expected |
| ✅ RG-84 | **The audio→worker channels are unbounded and undocumented as such.** `audio::AudioEngine`→DSP and capture→`relay-stt` are plain `mpsc::channel` with no capacity and no shed counter; only `main::DETECT_QUEUE` behind them is bounded | `CLAUDE.md` rule 33 describes the bounded queue and its shed counter and is silent about the two in front, which reads as though the whole path is bounded. The worker's drain-the-batch-then-decode-once loop bounds **latency**, not **memory** | Read of `audio.rs` and `stt.rs`; no observed failure | Either bound them, or say plainly in rule 33 which half is bounded and why the other does not need to be. **Do not bound them without measuring** — a shed on the audio side loses speech, not a redundant re-decode | — | P2 | M | A long soak on a machine slower than one hop per decode, watching RSS |
| ✅ RG-85 | **The CSP granted `http:` and `ws:` to any host** in `img-src`, `media-src` and `connect-src`, where what it means is the LAN | The reason is real — the kiosk hub and the media server cannot be TLS on a LAN appliance — but the grant was written far wider than the need, and on 2026-09-05 it was shown to be **reachable**: `TemplateRender`'s `bgPaint` emits `url("${L.image}")` straight from template JSON, so a template that arrived by email beacons an attacker-controlled `http://` URL the moment an operator previews it — the church's IP and the time it was opened, out of an offline-first app. `KIOSK_CSP` already blocked the same template on the LAN pages, so the operator webview was the only affected surface | `tauri.conf.json` `app.security.csp`, read 2026-09-02; the beacon path found 2026-09-05 | Narrowed to Relay's own ports — `http://*:8032` for images and media, `ws://*:8031` for the hub. **By port, because the host cannot be named**: a LAN address is whatever the church laptop was given that morning, and CSP's host-source grammar has no way to express a private range | T4 · RG-108 | P2 | M | `qa::kiosk_headers::the_console_policy_allows_relays_own_media_url_and_no_other_host` reads the policy out of `tauri.conf.json` and asserts it still permits `http://192.168.1.9:8032/media/12` — the URL `main::fire_media` actually builds — while refusing `http://evil.example.com` and the same LAN host on another port. Verified by restoring the old `http:` grant and watching it fail; the packaged build boots and prints its one heartbeat, which proves the bundle still loads. **It does NOT prove a background video paints on a projector** — `tauri dev` does not exercise the CSP, and that check needs a wall |
| ✅ RG-86 | **"The decoder decodes; nothing else runs on its thread" is absolute in the rule and not in the code.** The STT callback still performs one Tauri `emit("stt://transcript")` before the hand-off | Nothing is broken — it is deliberate and documented at the call site. But a rule stated absolutely and held approximately is how the next person justifies the second exception | `main.rs`, the `relay-detect` hand-off | Amend rule 33 to say *one emit, and why*, or move the emit. The rule is the artefact to change | RG-84 | P3 | S | The rule and the code say the same thing |
| ✅ RG-87 | **No instrument checks relative Markdown links.** `crossrefs.test.js` resolves `DECISIONS §N`, `RG-` ids, `docs/…` paths and `module::item` — and not a Markdown link target | **Twenty-eight broke** in the 2026-09-02 reorganisation and were found only by a throwaway script. The next move will break more, silently, and the documents are load-bearing enough that four tests already exist to protect their citations | The reorganisation itself | A fifth dimension resolving every Markdown link target in every tracked file. Deliberately not built by the audit that found it, because that pass was report-only — filed so the decision is somebody's | RG-67 | P2 | S | Break one link and watch it fail |
| ✅ RG-88 | **The Library imported a file of any size, and left a row behind when the write failed.** `fileToBase64` read the whole file, built a binary string and a base64 string of it, Tauri serialised that across the IPC bridge, and Rust decoded a further complete copy before writing — four copies at the peak, with no limit anywhere. The row was also inserted BEFORE the write (its id is half the on-disk name), so a failed write left an asset that lists in the Library and plays nothing | A volunteer dragging a 1.5 GB service video into the Library on a Saturday does not get a slow import — they get the operating system killing Relay, with no error, no message and nothing in any log. The orphan half is the only place in the schema that could produce one, and it surfaces on Sunday as a blank output | Read of `main::import_media`, `main::parse_import`, `capture.js::fileToBase64`; the existing QA test `a_half_finished_media_import_leaves_a_row_that_serves_a_404` had pinned the orphan as a KNOWN defect since 2026-08-16 | One limit (`MAX_IMPORT_BYTES`, 256 MiB) held on BOTH sides of the bridge — the webview copy prevents the allocation, the Rust copy is the door a command can always be called through. `write_media_file` undoes the row when the write fails, split out so the failure branch can actually be executed | — | P1 | S | `import_guard_tests` (5) + `mediaimport.test.js` (5); the old QA test inverted to `a_half_finished_media_import_leaves_no_row_behind`. Verified by reintroducing both defects and watching three tests fail |
| ✅ RG-89 | **There was no way, from inside Relay, to erase a recorded service.** `PRIVACY.md`'s only answer to *"remove that"* was **delete the folder** — quit Relay and destroy every service ever recorded, or keep all of them | `transcripts.text` is verbatim text of what a preacher said to a congregation, and `detections.heard_text` is the exact sentence behind every verse that went on a wall. Every document promises it never leaves the device; none of them could say how to get rid of it. A church that wanted one sermon gone — a pastoral conversation read into the room, a visiting speaker who asked — had no path | `grep` for retention/deletion across `db/`, `main.rs`, `capture.js`, `History.svelte`: **zero hits** | `db::delete_service` in ONE transaction, children first, returning what it removed; `delete_service` command behind the service lock; a two-step arm/confirm **Erase service** in History (never a native `confirm()` — rule 41) | RG-04 for the tables | P1 | M | `db::services::erase_tests` (4), including *"erasing one service leaves the others untouched"*, verified by removing a `WHERE` and watching it fail |
| ✅ RG-90 | **Three defects in the LAN HTTP server**, all on the port a church LAN can reach: an idle connection was held **forever** (the first read had no deadline), JSON replies carried no `X-Content-Type-Options` while static replies did, and `serve_media_file` read the whole file into memory per request | A socket that connects and says nothing keeps a task, an 8 KiB buffer and a descriptor for the life of the process — no hostility required, just a browser opening a speculative connection or a scanner sweeping the network, accumulating silently across a service. A 400 MB background loop cost 400 MB resident **per request**, and a wall, a stage screen and an OBS machine asking during a service cost three copies on the laptop running the sermon | Read of `channels::run_output_http_server`, `serve_json`, `serve_media_file` | 5 s `REQUEST_READ_TIMEOUT`; `nosniff` on every reply; the media file opened, measured and streamed in 64 KiB frames instead of read whole | — | P2 | S | `a_connection_that_never_speaks_is_dropped` (tokio paused clock, so it costs no wall time), `a_json_reply_is_never_content_sniffed`, `a_mutating_reply_still_withholds_the_cors_wildcard`; all three re-verified against the **packaged** binary with `curl` |
| ✅ RG-91 | **`transcripts.service_id`, `detections.transcript_id` and `cues.service_id` had no index.** SQLite indexes a PRIMARY KEY and a UNIQUE constraint on its own; it does not index a `REFERENCES` column | Opening a service in History, building its timeline, replaying a moment in it and erasing it all scanned the whole table — and those are the two tables that grow one row per utterance, without limit, for as long as a church keeps using Relay | `grep 'CREATE INDEX' docs/data/schema.sql` — six indexes, none on these three | Added to `docs/data/schema.sql` (the shipped baseline) and to a retryable `ensure_history_indexes` rung, so a fresh install gets them at creation and an existing one on the next boot | RG-89 | P2 | S | `db::services::index_tests` (3), including `EXPLAIN QUERY PLAN` — the only witness that tells a created index from a used one |
| ✅ RG-92 | **Settings → Updates said *"up to date"* in four different situations**: there is no newer version, the laptop is offline, no check has ever run, and the update manifest has been returning 404 since installation. `checkForUpdate` swallowed every outcome into `null` | Rule 35 — a status badge that cannot detect its own failure — on the one path by which a fix reaches a church that already has Relay. It is the surface that would have reported RG-83, and it reported the reassuring half | Read of `updater.js::checkForUpdate` and `Settings.svelte` | `updateChannel` records the outcome; `describeChannel` is the one place that turns it into words; `unavailable` (no updater in this build) is distinguished from `failed` so the row is not noisy on a dev machine. A failed check still never interrupts an operator | RG-83 | P2 | S | `updatechannel.test.js` (7) |
| ✅ RG-93 | **History conflated Loading, Empty and Error**, saying *"No services yet — press Start listening on the Live tab to record one."* in all three | The last screen still doing it, and the one where it costs most: a confident claim that a church has never recorded a service, shown to a church whose database did not open. `listServices` is a GROUP 2 read returning `[]` on failure, so the array carried no information about which had happened | `surface.test.js` had pinned it as a known defect since 2026-08-15 | The `asked` flag plus `readErrors.listServices`, rendering `Loading` / `ErrorState` / the empty sentence — the same three-way `TemplateGallery` already used | RG-89 (same file) | P2 | S | The two pinning tests INVERTED and passing; the fix was confirmed by watching them fail first |
| ✅ RG-94 | **The Rust suite bound fixed ports and could silently talk to somebody else's server**, and one frontend assertion was timing-flaky | A PHP dev server for an unrelated project held `127.0.0.1:8199`; Relay's test server bound `0.0.0.0:8199` and **succeeded** (tokio sets `SO_REUSEADDR`), the more specific binding won the connection, and `kiosk_ws_forwards_published_content` failed with `x-powered-by: PHP/8.5.9`. Nothing errored, the server printed that it was listening, and the failure read as a regression in code that had not changed. Separately, a `surface.test.js` assertion one scheduler pass after mount failed about one full-suite run in five | Observed on 2026-09-03, twice | `free_port()` asks the OS, on loopback, so a foreign wildcard listener cannot shadow it; `until()` waits for the DOM instead of assuming one tick, and throws with a message rather than hanging | — | P2 | S | The whole Rust suite passing with the PHP server still on 8199; `until()` verified to fail loudly by pointing it at a string that never appears |
| ✅ RG-95 | **The ERROR third of Loading/Empty/Error existed on 3 of about 15 list surfaces.** `Channels`, `TemplateGallery` and (since RG-93) `History` were the three — and `Channels`'s did not actually work, which RG-110 records. Everything else showed an empty sentence over a failed read | Lower stakes than RG-93 in one sense — those tables are ones the operator filled, so *"you have none"* is at least plausible — and higher in another: **every one of those sentences tells the operator to do the work again.** *"No songs yet — import or paste one"*, *"No media yet"*, *"No announcements yet"*, *"No plans yet"* on a Tuesday evening, *"Empty plan — use ＋ Add Cue"* about a plan somebody spent an evening building, and *"That chapter is empty"*, which is a claim about **scripture** rather than about a database. The model chooser was worse than a wrong sentence: it renders an `{#each}`, so a failed read drew a **blank panel** on the screen whose whole job is getting speech recognition working | Survey of `EmptyState` / `Loading` / `readErrors` usage per view, 2026-09-03; re-surveyed 2026-09-04 by comparing every `guardedRead` key against the keys any view reads — **2 of 29 keys had a surface** | **Closed in three passes and it is now complete for every operator-facing list.** Eleven surfaces read `$readErrors.<key>` and render `ErrorState` before their empty sentence: Announcements, MediaLibrary, Scripture, LyricsPane, Browse (books, chapter and search), ServicePlanner (plan rail and cue table), ModelSetup, Channels, and the run surface's three lists — cue rail, plan picker and screen strip. **Two reads had to be guarded first**: `chapterVerses` and `planItems` swallowed into bare `catch {}` blocks, so there was no key to read even if a view had wanted one. `planItems` stays a GROUP 2 read **deliberately** — `Live::loadPlan` sets `itemsLoaded` false, awaits it and sets it true with no `finally`, so promoting it to a wrapper that throws would leave the run surface saying *Loading cues…* for the rest of the service. What is NOT surfaced is the diagnostic tail — `listMonitors`, `listTranslations`, `perfHistory`, `latency*`, `rooms`, `listVoiceProfiles`, `listTemplateVersions`, `loadContentTemplates`, `findModelFiles`, `serviceLock`, `verseRepeatCount`, `servicePerf`, `serviceTimeline` — none of which tells an operator to redo work | RG-93 for the pattern · RG-110 for the surface that had one and could not fire it | P2 | M | Twelve `surface.test.js` cases: nine mount the view and reject exactly that view's read, asserting `[role="alert"]` and the absence of the sentence; the run surface is read as source (it is mounted nowhere in the suite) and its assertion checks the error branch sits **before** the empty sentence, because a branch after it is decoration. **Every one verified by deleting the branch again and watching it fail** |
| ✅ RG-96 | **`serve_media_file` answered no HTTP `Range` request.** It streamed the whole file from byte zero, every time | A browser `<video>` on a kiosk or an OBS source could not seek, and could not resume a dropped connection — it started the clip again. An operator reads that as *"the video restarted itself"* | Read of `channels::serve_media_file`, 2026-09-03 (the RG-90 streaming fix removed the memory cost, not this) | `parse_range` reads one byte range against the real length; the reply is `206` with `Content-Range`, a `416` with `bytes */len` for a range past the end, and `Accept-Ranges: bytes` on **every** reply including the 200 — without that header a browser disables its scrub bar however well the 206 works. Anything unsupported (multiple ranges, a unit other than bytes) falls back to the whole file: a 200 is always correct, where a 206 carrying the wrong bytes hands a player a corrupt file. The request head is now read to its terminator under one deadline, so a `Range:` header arriving in a second TCP segment is still seen | RG-90 | P2 | S | `a_range_header_is_read_against_the_real_length` (11 forms), `a_ranged_media_request_is_answered_with_the_bytes_it_asked_for` (byte-exact slice, 416, and traversal still 404), `a_header_that_arrives_in_a_second_packet_is_still_read`. **Verified against the packaged binary with `curl`**: 200 + `Accept-Ranges`, `206` + `Content-Range: bytes 500-599/1024` whose 100 bytes matched the file exactly, and `416` + `Content-Range: bytes */1024` |
| ✅ RG-97 | **The LAN control plane had no rate limit and no connection cap.** Every accepted socket spawned a task; `/api/search` runs a semantic scan and an FTS query per request | Not a new exposure — DECISIONS §35 already accepts that anyone on the church wifi can drive the wall — but an accidental loop in a kiosk page can saturate the laptop running the sermon, and nothing would say why. The read timeout (RG-90) bounds an idle connection, not a busy one | Read of `run_output_http_server`, 2026-09-03 | A `Semaphore` of `MAX_CONCURRENT_REQUESTS` (64) held for the life of each reply. Over the cap a connection is answered **`503` with `Retry-After` and closed at once** rather than queued: a client that is told no retries, a client left waiting holds a socket. A church uses four clients. The header read is also bounded at `MAX_HEAD_BYTES` (8 KiB) and its deadline now covers the WHOLE head, so a client dribbling headers forever is dropped like any other idle one — it was outside the deadline before, because only the first read was timed | — | P3 | S | `a_flood_of_connections_is_refused_rather_than_queued` (fills every slot, asserts the 503) and `a_client_that_dribbles_headers_forever_is_still_dropped`. Both verified by removing the fix and watching them fail |
| ✅ RG-98 | **The Vite dev server bound every interface, and `npm audit` reports advisories against exactly that surface.** 10 vulnerabilities — 1 critical, 1 high, 8 moderate — **all ten in dev tooling**; production dependencies are clean | Vite's path traversal in optimized-deps `.map` handling and esbuild's *"any website can send any request to the dev server and read the response"* are reachable precisely because `npm run tauri dev` was putting a dev server on the church LAN. The critical is vitest's **UI server** reading and executing an arbitrary file — this repository never starts it (`npm test` is `vitest run`; no `--ui` in `package.json` or any workflow) | `npm audit` and `npm audit --omit=dev`, 2026-09-04; `vite.config.js` `server.host` | **Decided 2026-09-05 (DECISIONS §65): the default is loopback, and the LAN is `RELAY_DEV_LAN=1`.** The version numbers were never the lever — every fix is a semver major and Svelte 4 is a recorded stack choice — so the lever used was who can connect. A packaged Relay has no server on 5032 at all, so this is a development convenience, and a convenience should not be on by default on somebody else's network. An env var rather than a config edit, because a config edit is committed by accident and an env var is not | — | P2 | M | `hardrules.test.js` asserts neither `server` nor `preview` binds every interface by default and that both read the opt-in; **`npm audit --omit=dev` is now a CI gate** so the production tree cannot drift from zero silently |
| ✅ RG-99 | **The bundled KJV was six verses short and carried four split ones — 31,100 against the KJV's 31,102 — and a verse number in that file is a POSITION, so every verse after a gap was renumbered.** "Matthew 22:37" returned the words of 22:38; "Matthew 2:23" did not exist | The failure this product exists to prevent, shipped in the data rather than in the detector: a correct reference, heard correctly, gated correctly, putting the wrong verse on a congregation's wall. Matthew 2:16, 22:1, 26:38, Mark 4:40, 7:11 and 8:8 were missing; 1 Samuel 20:42, 1 Kings 22:43, 3 John 14 and Revelation 13:1 were split | Full structural diff of all 66 books against an independent public-domain KJV whose verses carry explicit numbers, 2026-09-04. **Both existing instruments agreed with the defect**: `db::tests::seeds_full_kjv` asserted `> 31_000` beside a comment recording the shortfall as expected, and `detection::VERSES_PER_CHAPTER` had been regenerated from the broken corpus and was pinned to it | Repair the corpus verse by verse (6 inserted, 4 merged); regenerate the detection table; pin all 1,189 chapters to the KJV's own versification from **outside** this repository; forward-fill any database whose count is not exactly 31,102 | — | P0 | M | `the_bundled_kjv_matches_the_kjvs_own_versification` and `the_repaired_references_hold_their_own_words`, both watched to fail against the old file |
| ✅ RG-100 | **A marginal note could reach the wall as scripture, and supplied words could be deleted from it.** `is_gloss` decided by wording — a brace group containing `": "` was a note | Luke 17:36 rendered "…and the other left. this verse is not found in most of the Greek copies"; Genesis 30:27 lost "tarry: for" and read "if I have found favour in thine eyes, I have learned by experience". Eight verses in one direction, seven in the other | Whole-corpus text diff against the independent KJV, 2026-09-04 | Decide by POSITION: every marginal note in `kjv.json` sits in the trailing run of brace groups, checked group by group over all 31,102 verses. Wording markers kept only for the one note that appears mid-verse (Hebrews 10:34) | RG-99 for the corpus | P1 | S | `supplied_words_containing_a_colon_survive` and `a_trailing_note_is_never_scripture_however_it_is_worded`; the re-diff shows 15 verses changed and no others |
| ✅ RG-101 | **Nothing had ever checked Relay's RUST dependencies.** `cargo audit` was not installed, the launch checklist carried the row as ⬜ never checked, and no CI job ran it — so the half of the dependency tree that actually ships (the npm half does not: a packaged Relay contains no Node) was the unaudited half | Run 2026-09-04 after installing `cargo-audit`: **three advisories.** `h2 0.4.15` (unbounded empty DATA frames, reached through `reqwest` by the model download, the updater and Sentry) and **two 7.5-HIGH advisories against `quick-xml 0.39.4`** — unbounded namespace-declaration allocation, and quadratic time checking a start tag for duplicate attributes | `cargo audit`; `cargo tree -i quick-xml` and `cargo tree -i h2` for the paths | **All three fixed by lockfile updates, and the first diagnosis of the second pair was wrong.** `h2` → 0.4.19. The `quick-xml` pair was recorded here as needing *"an upstream Tauri bump"*; it did not — `cargo update -p plist` moves `plist` 1.9.0 → 1.10.0, which depends on `quick-xml` 0.41.0, the fixed version. **A dependency that arrives through Tauri is not necessarily pinned by Tauri**, and assuming it was would have left two HIGH advisories in the shipped tree for a release cycle. The remaining 18 rows are warnings, not vulnerabilities: unmaintained **GTK3 Linux bindings**, which neither shipped platform builds, plus a yanked `chacha20` | — | P2 | S | `cargo audit` reports **0 vulnerabilities**, and a new CI job runs it on every push beside `npm audit --omit=dev`. It fails on a vulnerability and not on a warning, deliberately: `--deny warnings` would fail the build over Linux code this project never compiles |
| ✅ RG-102 | **The corpus repair was placed on the branch no shipped install is on.** The `verse_count != 31_102` re-import lives in `db::baseline_forward_fill`, which `migrate` calls only when `user_version == 0`; `SCHEMA_VERSION` was still 2 and RG-99 added no rung to `run_migrations`, so a v2 database ran `ensure_tables` and nothing else, for ever | **Every copy of Relay that a church already has** — v0.1.0-2, -3 and -4 all stamp `user_version = 2` on a fresh install — would have kept the six-verse-short, mis-numbered Bible and the RG-100 gloss defects permanently. The P0 was fixed for people who did not have it and left in place for everybody who did. The file's own comment at that rung warns about this exact trap, from the last time it happened (the v2 `heard_text` column) | `git show v0.1.0-4:src-tauri/src/db/mod.rs` line 51 → `SCHEMA_VERSION: i64 = 2`; `migrate`'s `if from < SCHEMA_VERSION` is `2 < 2` → false. Found by an independent database audit track, 2026-09-05, and re-verified by hand before it was acted on | `SCHEMA_VERSION` bumped to 3 and a v3 rung, `ensure_corpus_repair`, added to `run_migrations`. It probes **two** things, because the count cannot see the gloss defect: the row count, and whether Genesis 30:27 still has the supplied word *"tarry"*. A database with no verses at all is left alone — that is an install waiting for a seed, not a broken corpus | RG-99 · RG-100 | **P0** | M | `a_versioned_database_with_a_short_corpus_is_repaired_on_boot` and `a_corpus_with_the_right_count_and_the_wrong_words_is_still_repaired` — both drive the real `migrate` on a v2 database and assert the DATA, which every other migration test in the file does not. Verified by setting `SCHEMA_VERSION` back to 2 and watching both fail |
| ✅ RG-103 | **The corpus repair could never have run at all: it opened a transaction inside one.** `reimport_full_kjv` begins `unchecked_transaction`, then calls `import_full_kjv`, which begins another | `cannot start a transaction within a transaction` — returned from `migrate`, which `db::open` propagates, so the repair fails **and takes the database open with it**. A v0 install would have hit this on the boot after upgrading. Both existing corpus tests build a FRESH database, where `seed` calls the outer function with no transaction open and it works, so nothing in 659 Rust tests touched the failing path | Probed directly, 2026-09-05: `migrate` on a v0 database with one verse deleted returned `Err(SqliteFailure(… "cannot start a transaction within a transaction"))` and left the corpus at 31,101 | The insert split out as `insert_full_kjv`, which owns no transaction; `import_full_kjv` wraps it for a fresh seed and `reimport_full_kjv` wraps it for a repair that must also remove the old rows atomically. A half-imported Bible is not a state a church may boot into | RG-102 | **P0** | S | The same three migration tests: they exercise the repair end to end, which is what nothing did before |
| ✅ RG-104 | **Repairing the corpus erased the reference from every detection ever recorded.** `reimport_full_kjv` nulls `detections.verse_id` to get past the foreign key, and `service_detections` builds its reference by `LEFT JOIN verses` | Every service report, replay and History row would show a time, a confidence and the heard text with **no verse** — for every service in the database, silently, on the boot after an upgrade. The rows still look present, which is what makes it worse than a deletion | Read of `db::verses::reimport_full_kjv` against `db::services::service_detections`, confirmed by test | The links are taken down and put back, keyed by (book, chapter, verse) rather than by id. The reference is the right key even though the old corpus numbered its verses wrongly: *"Matthew 22:37"* is what the wall said that morning, and that is what the record should keep saying — what changes is that it now resolves to the words the KJV actually has there | RG-102 | **P1** | S | `repairing_the_corpus_keeps_the_reference_of_every_past_detection`, verified by stripping the re-link and watching it fail |
| ✅ RG-105 | **`detections.verse_id` had no index, and the repair deletes 31,102 parent rows.** SQLite must scan the child table once per deleted parent to enforce the reference | **8.343 s measured** against 20,000 detections (0.007 s with the index) — spent at boot, before the window exists, with nothing on screen to say why, on the laptop a volunteer is trying to start ten minutes before a service. Cost is quadratic in a table that grows one row per detection with no prune path. It could only bite a v0 database before; fixing RG-102 makes the repair run on every install that needs it | Synthetic reproduction on the same schema shape with foreign keys on, 2026-09-05 | Added to `ensure_history_indexes` (retryable, `IF NOT EXISTS`) and to `docs/data/schema.sql`, so a fresh install gets it at creation and an existing one on the next boot | RG-91 · RG-102 | P2 | S | `the_rung_adds_them_to_an_old_database_and_survives_being_run_again` now expects four indexes and says why the fourth is not for a query |
| ✅ RG-106 | **The kiosk WebSocket port had neither of the two bounds the HTTP port was given.** `accept_async` had no deadline, the accept loop had no cap, and tungstenite's defaults allow a 64 MiB message and a 16 MiB frame, each buffered whole before `serde_json` sees it | RG-90 and RG-97 verbatim, on the door those passes did not check — *"a guarantee is only kept on the doors you checked"*. A socket that completes the TCP handshake and never upgrades held a task and a descriptor for the life of the process; a port scanner on the church LAN produces them with no hostility at all | Read of `channels::run_kiosk_server`, 2026-09-05, filed by an independent security track and re-verified | `REQUEST_READ_TIMEOUT` around the handshake, a `MAX_KIOSK_CLIENTS` (32) semaphore held for the life of the connection, and a 64 KiB message/frame cap — a `hello`, a `beat` and a `rendered` are a few hundred bytes | RG-90 · RG-97 | P2 | S | `a_kiosk_socket_that_never_upgrades_is_dropped`, verified by lengthening the deadline to an hour and watching it fail |
| ✅ RG-107 | **An imported SVG is served from `/media/<id>` as an active document, on the same origin as the output page and the kiosk socket.** `mime_for` answers `image/svg+xml`, correctly — and that is a type a browser executes | A church downloads a graphics pack, imports a logo, and anything scripted inside it runs against `http://<relay>:8032` if that URL is ever opened top-level. `nosniff` does not help: the type is right, it is the type that is executable | Read of `channels::mime_for` and the media reply headers, 2026-09-05 | A `Content-Security-Policy` of `default-src 'none'` on **SVG replies only**. Videos and images deliberately get none: a CSP on a subresource is ignored by the browser painting it, and one that were honoured would risk a blank wall on a Sunday to protect a case that does not exist | — | P3 | S | `only_an_svg_is_served_under_a_content_policy`, which asserts both halves — the policy on the SVG and its absence on the video |
| ✅ RG-108 | **The kiosk hub streamed the content feed to any client that connected, before and without a `hello`, with no `Origin` check.** `tx.subscribe()` happens at accept and the loop forwards every broadcast; registration gated nothing | WebSockets are exempt from CORS, so a plain-`http:` page opened on a phone on the church wifi could `new WebSocket('ws://<relay>:8031')` and receive the service — **including `stage_note`, `next_reference` and `next_text`, the preacher's own monitor**, which exists precisely because the congregation cannot see it. DECISIONS §35 accepted that somebody in the room can SEE the wall; this is content leaving the building, which `SECURITY.md` §1 ranks above every other harm here. Those are different claims and only the first was ever decided | Read of `channels::run_kiosk_server`, 2026-09-05 | `accept_hdr_async_with_config` with `origin_gate`, refusing a handshake whose `Origin` is not one of Relay's own — **by port, since the host is whatever LAN address the laptop has that morning**: `:8032` packaged, `:5032` in dev, plus `tauri://localhost` for Relay's own webview. An absent `Origin` is allowed (a browser always sends one; native clients and this repo's tests do not). `RELAY_KIOSK_ANY_ORIGIN=1` restores the old behaviour for a church hosting its own kiosk page elsewhere, and the refusal names that variable in the line it prints — the way out is in the same place as the problem. DECISIONS §64 | RG-85 | P2 | M | `only_a_page_relay_served_may_join_the_kiosk_feed` (11 origins) and `a_page_from_another_origin_is_refused_the_socket`, which asserts a real **403** — the first version returned `ErrorResponse::new`, which builds a 200, and tungstenite will not write a successful refusal, so the socket closed with no status at all. **Verified against the packaged binary**: `101 Switching Protocols` for no origin, `:8032` on two hosts, `:5032` and `tauri://localhost`; `403 Forbidden` for `evil.example.com`, `null`, a LAN host on `:3000` and an `https://` origin. **What is still unverified is a real OBS browser source's `Origin` header** — the reasoning says it is the page's own `:8032` origin, and no OBS install here can confirm it |
| ✅ RG-109 | **Four documented claims were false against the code, and one of them scored a safety distinction against tests that had been deleted.** `QA_HARNESS.md` §4.2 rated *Preview ≠ Programme* **"Yes, now"** citing *"`liveoutputrail.test.js` — 17 tests"* about staging and a TAKE button; that component and those tests went on 2026-08-15 and the file holds 11 tests, none about staging. §1.4 still described `PreviewProgram.svelte` in the present tense and narrated a `.lo-behind` chip that `grep` finds in no component. `README.md` and `PRIVACY.md` said the audio is **"never even saved"** while `RELAY_RECORD_WAV` writes a whole service to a path the operator names. `CLAUDE.md` said ten of the forty-one hard-way rules name an instrument when fifteen do, and listed five pinned by `hardrules.test.js` when seven are | A safety table that scores a distinction against deleted evidence is worse than one that leaves it blank: it is the exact failure the table was built to catch, committed by the table. The privacy claim is the summary a church reads, and *"never"* is absolute — off-by-default does not make it true. The rule arithmetic under-reported the project's own safety coverage, so a careful reader re-verifies pinned rules and a hurried one assumes unpinned ones are covered | Counted, 2026-09-05: `grep -cE '^\s*(it\|test)\(' src/lib/liveoutputrail.test.js` → 11; `ls src/lib/views/library/PreviewProgram.svelte` → absent; the `RELAY_RECORD_WAV` path in `audio.rs`, documented 130 lines below the table that denies it; and a script that splits `CLAUDE.md`'s rules and looks for a named instrument in each | Each corrected in place, with the correction and its date left visible rather than silently rewritten. §1.4 now opens by saying it is history and pointing at §4.2 for the current state | RG-68 for the pattern | **P1** | S | `crossrefs.test.js` and `hardrules.test.js` still pass; the counts in the new text are the ones its own commands produce. **No test catches a dead `src/…` citation** — `crossrefs.test.js` resolves `docs/…` paths only, which is why this set survived four documentation passes |
| ✅ RG-110 | **Five template controls rendered `[object Object]`, one view's error state could not fire at all, and every "it is on the screens" line was silent to a screen reader.** `error.rs` serialises a typed `{ kind, message }`, and six catch blocks did `String(e)` on it. `Channels.svelte` HAD an `<ErrorState>` whose `error` was only ever set by a mutation — every read in its `onMount` is a GROUP 2 wrapper that swallows. `Announcements` put a failed save through the same emerald `flash()` as a successful one, with no role, and its delete — a wrapper that THROWS — was unguarded | The most likely `[object Object]` an operator will ever see is **the Service Lock refusing a delete mid-service**, which is the one message in the set already written for a volunteer. Channels' dead error state meant a database that would not open read *"No screens yet — add one below"*, and the answer to that sentence is to add a screen they already have. The unguarded delete left the row on screen and said nothing, which reads as a delete that did not take | Filed by an independent surface-audit track, 2026-09-05, and re-verified line by line before anything was changed | Six `String(e)` → `humanError(e)`; `$readErrors.listOutputChannels` in the Channels empty arm; a separate `an-err` line with `role="alert"` and a guarded delete; `role="status" aria-live="polite"` on the success line of all six library surfaces | RG-95 | **P1** | S | Three `surface.test.js` cases, one per defect, each verified by putting the defect back |
| ✅ RG-111 | **A `.proplaylist` could expand without bound on import.** The 256 MiB `MAX_IMPORT_BYTES` cap holds on both sides of the bridge and measures the **compressed** file; `read_to_end` on a zip entry allocates whatever that entry expands to | A playlist is a file somebody **emails to a church**. A megabyte of zeros is a gigabyte unpacked, and the volunteer importing it gets the operating system killing Relay during setup — no error, no message, nothing in any log. Same shape as RG-88, one layer further in | Read of `proimport::parse_proplaylist`, 2026-09-05 | A shared 64 MiB unpack budget across all entries, enforced with `take`, so neither one large entry nor a hundred medium ones can pass it. A `.pro` is XML with a song's words in it; a large one is a few hundred kilobytes | RG-88 | P3 | S | `a_playlist_that_unpacks_to_gigabytes_is_refused_not_allocated` builds a real 96 MiB-of-zeros bomb that is under 2 MiB on disk, and asserts the refusal. Verified by restoring `read_to_end` and watching it fail |
| ✅ RG-112 | **Eleven more documented numbers were stale or had no reproducer**, on top of RG-109's four: `docs/README.md` carried its own copy of the register size (**87 entries** against 109) and said *"only one is a commit"* where the checklist says two are not; `README.md` and an agent brief still said the corpus holds **31,100** verses, which is the RG-99 defect; `CLAUDE.md`'s event list was missing five of the nineteen, one of them an error path; `QA_HARNESS.md` §1 restated a button and file count §0 already owns, and said 22 views where there are 23; `RELAY_GAP.md` said 462 controls against 463; `e2e.rs` still said R2-C and R2-D *"remain open, remain RED, and remain ignored"* after both were closed; and `RELAY_SWEEP_TRANSCRIPT`/`RELAY_SWEEP_TRUTH` appeared in no document at all | Individually trivial, and that is the point: **the pattern is a second copy of a number that has one home.** Every one of these is a value restated away from the register that owns it, and every one of them drifted. The verse count is the exception that is not trivial — 31,100 is the defect a P0 was filed to fix, still being advertised as the product's own figure | Counted 2026-09-05, each with the command that settles it | Each replaced by the count or by a pointer to the one place that owns it; the two undocumented variables added to `bench/README.md`, beside the experiment they belong to | RG-109 | P3 | S | `grep -rhoE 'RELAY_[A-Z_]+'` over source and over documents now returns the same set; `crossrefs.test.js` and `relaygap.test.js` pass. **No test catches a stale number that is not a citation** — that is what makes this the most repeated finding in the register |
| ⏳ RG-113 | **Seven smaller database findings from the 2026-09-05 audit track, filed and NOT fixed.** (1) `ensure_manual_detection_status` drops `detections` and renames the scratch table over it, taking `idx_detections_transcript` with it — `ensure_history_indexes` has already run by then, so the index is missing for the rest of that boot and returns on the next one. (2) No `busy_timeout` and no `journal_mode`: `open()` sets only `PRAGMA foreign_keys`, so any contention returns `SQLITE_BUSY` immediately — and `plans.rs` explicitly anticipates two Relay processes on one file. (3) `set_active_environment` is two unguarded `UPDATE`s outside a transaction with no `ensure_one_active` equivalent, so a failure between them leaves NO active room and nothing repairs it; `profiles.rs` has the same shape but does repair, by promoting `MIN(id)` — silently switching the preacher to a different profile, and with it a different auto-fire threshold. (4) `docs/data/schema.sql` lacks `plan_items.section_title` and `duration_sec`, which a rung adds — so the file described as *"the baseline every fresh install is created from"* is not the shape a fresh install ends in, and every fixture built from it has a different table. (5) `main.rs:1444` writes `transcripts.language` as a hardcoded `"en"` on evidence rows, on the product whose Tier-1 languages are Yorùbá, Swahili and Hausa. (6) `delete_template` clears `output_channels.template_id` and leaves `app_settings`'s `tpl_*` and `default_template_id` pointing at a template that is gone (stale selection, not a wrong wall). (7) `MIGRATION_TABLES` omits `translations`, `cues`, `song_sections` and `verses_fts` — the last matters most, because it is the whole of scripture search and the Database Migration screen would read green over a missing index | None of the seven is a wrong verse on a wall, which is why they are one row and not seven. Two could bite in a room: the missing `busy_timeout` turns a second Relay window into silent write failures during a service (every insert on that path is called as `let _ = …`), and the environment swap can leave a church with no active room after a crash | Read of `db/`, `main.rs` and `schema.sql` by an independent audit track, 2026-09-05; the specific claims re-checked by hand before filing. **None was observed at runtime** | Each is small and independent. Taken together they are a session's work, and taking them in the same pass as RG-102 … RG-105 would have put seven unproven edits into the same commit as a P0 migration fix, which is how a repair becomes the next defect | RG-102 for the migration path | P3 | M | A test per item; (2) and (3) want a two-process rig, which nothing here has |
| ✅ RG-114 | **The fix for RG-83 resolved and did not work, which is worse than the 404 it replaced.** The endpoint was pinned at each release's own tag. A build therefore asks its OWN release's manifest, is told its own version, and concludes it is up to date — **for ever**. The next release lives at a URL that build has never heard of | RG-83's 404 was at least loud to anyone who looked: `curl` said 404 and `updater:check` said dead. This says **live**, on every instrument, while no installed copy can ever learn about a newer version. It would have shipped in 0.2.0-1 and been discovered when 0.2.0-2 failed to reach anybody — one release cycle later, with the evidence pointing at the wrong thing | Reasoned out from the built config before publishing 0.2.0-1: the endpoint baked into that build is `…/download/v0.2.0-1/latest.json`, and the manifest at that URL advertises `0.2.0-1`. **The symptom was printed by `updater:check` all along** — *"note: same version as this checkout — nothing would update"* — and read as benign, which is the actual lesson here | One permanent address: a release tagged `updates` whose only asset is `latest.json`, re-uploaded by `update-channel-promote.yml` on every publish. It runs on `release: published` rather than on the tag, because a draft's assets are not downloadable and a manifest advertising a version whose installers 404 is an update that fails halfway. `0.2.0-1` was built with the flaw, never published, and its draft and tag were deleted | RG-83 | **P1** | S | `version:check` — a CI gate on every PR — refuses **both** drifts: back to `/latest/` (404 in the field) and forward to a version tag (resolves, never advertises anything newer). Both verified to exit 1. The promote workflow asserts the release is not a draft before promoting, and re-fetches the channel URL afterwards to confirm it serves the version it just uploaded |
| ✅ RG-115 | **A chapter the window stated out loud was answered by memory, and a second wrong verse reached a real output.** Field service 2026-09-06: the preacher said *"is taken from **4th Peter chapter 5 verse 10**"* — 1 Peter 5:10, misheard into a book that does not exist. `detect_direct` therefore produced nothing, `anchor_for_bare_verses` was `None`, and the bare `10` fell through to `ContextMemory`, which still held **Psalms 92** from ten minutes earlier. **Psalms 92:10 auto-fired at 0.88, unattended.** This is RG-24's own repair meeting the case it did not cover: F-1 established that a book named in this breath beats memory, and here the window still named a chapter, memory named a different one, and memory still won | **"The words do not say" is not the same as "the words did not parse"**, and the whole bare-verse mechanism rests on that distinction. The phrasing is also out of `detect_passage_nav`'s reach — it caps at 8 tokens on purpose so sermon prose full of numbers cannot trigger a jump, and this sentence is 9 — so nothing else was going to catch it. Second wrong verse on a real congregation-facing output in two field services, both from the same 0.88 hardcoded `Direct` label on a book Relay inferred rather than heard | `detections` row 161 of service 15, with `heard_text` intact; the mechanism isolated by a probe printing `detect_direct` / `anchor_for_bare_verses` / `detect_bare_verses` / `book_named` for the sentence — all four confirmed before anything was changed. `book_named` is **false** here, which killed the first hypothesis: bare "peter" is not an alias, only "1 peter" / "first peter" | `detection::resolve_bare_verse_with_source` now owns the whole decision as one pure function — a reference parsed from this window wins, a **stated chapter with no parsed reference resolves to nothing**, and memory answers only when the words do not say. It **declines** rather than pairing the heard chapter with a remembered book: that would have shown Psalms 5:10 instead, a different wrong verse and rule 10's lesson exactly, a real confidence about a word nobody said. `chapter_named` uses `is_chapter_word` + `skip_linkers`, so it speaks Swahili and Hausa like every other detection helper rather than being an English-only check applied to a multilingual product | RG-24 for F-1 (`audits/FIELD-2026-09-06.md` F-8) | **P0** | S | Four tests in `detection::r4_audit`. **Verified the way CLAUDE.md rule 40 records the first F-1 diagnosis was not** — with the check removed, `field_f8_a_chapter_this_window_states_is_not_one_memory_may_answer_for` fails and reproduces `Some(VerseRef { book: "Psalms", chapter: 92, verse: 10 })`, the exact verse that reached the output. The other three hold the mid-passage case the mechanism exists for, F-1's guarantee through the new function, and `chapter_named` in Swahili |
| ⏳ RG-116 | **The STT model is an unpriced accuracy trade, and the operator is offered it as a speed setting.** The same field service ran `ggml-large-v3-turbo` and then `ggml-base` — the model was switched mid-service — and the two halves are not comparable products. Turbo: `stt_decode` p50 682 ms, cadence 938 ms, **3 of 3 auto-fires correct**. Base: p50 **125 ms**, cadence **206 ms**, **5 of 9 auto-fires correct — 4 wrong verses**. The app's own in-product advice points one way only: it prints *"Switch to a smaller model in Settings → Speech"* with per-model millisecond figures and says nothing about accuracy | **44% wrong-verse rate against SPEC's 5% bar**, and it is reachable by following Relay's own printed instruction. This is not a threshold and cannot be fixed by one: base heard *"1 to 2"* as *"12"* and *"71 verse 21"* as *"21 verse 27, 21"*, and a deterministic parser is confidently right about a number nobody said. Two of the four also show that **a faster cadence weakens corroboration rather than leaving it neutral** — rows 153 and 154 fired *different* verses 1.1 s apart from one utterance, because DECISIONS §36 assumes the two passes fail independently and consecutive passes at 206 ms see near-identical audio. CLAUDE.md rule 34 covers not trading safety for speed deliberately; it does not cover a safety margin shrinking as a side effect | `audits/FIELD-2026-09-06.md` §2 and §3 (F-9), all 11 detections with `heard_text`; the two model loads in the same launch log; wrong verses confirmed against the bundled KJV (*"Thou shalt increase my greatness, and comfort me on every side"* is **Psalms 71:21**, not the 89:27 and 89:7 that fired) | **Not fixable by a commit and deliberately not guessed at.** One service is one sample per model, and the two samples are of different services as well as different models. The instrument now exists, and so does the rig: `service-20260906-112459-seg01-5127.9s.wav` is 85.5 minutes of this church's own desk feed, RIFF-verified, and **`stt::bench::engine_shootout` already scores every installed model through the real detector** across five degradation conditions, replaying with timestamps so the router's debounce behaves as it does live. Its own doc comment says the first honest assertion here is a **wrong-verse ceiling**, writable *"the day this has been run once"*. **Two things to get right before quoting anything it prints.** It asserts nothing by design, so a bad input produces a confident-looking table rather than a failure. And `load_f32` skips a 44-byte RIFF header and reads raw f32 with **no resampling**, against `TARGET_RATE = 16_000` — so the 48 kHz artefact must be converted first (`bench/README.md` line 43 gives the command) or the sermon decodes at 3x speed and every number is nonsense. `RELAY_BENCH_REFS` is the other half and it is **incomplete by construction**: the reference list can only hold what Relay noticed or the operator fired, so the rate it yields is a ceiling on correctness and says nothing about recall. Replaying it through `base`, `small` and `turbo` and scoring **through the router** (rule 13) is the measurement this row is open for. **The advice half is done, 2026-09-07, and the row stays open because the measurement is the row.** The lag warning in `stt.rs` named three millisecond figures and no accuracy cost; it now names the field result and points at `small` rather than at the smallest model, because rule 32 makes `small` free (153 ms and 59 ms round up to the same single 200 ms hop, so it is the larger model at the same cadence). The `base` catalogue row in `models.rs` said only that it runs on any laptop; it now carries the 5 of 9 against 3 of 3, on the screen where the choice is actually made. `recommended` is deliberately unchanged: moving every church onto a 488 MB download on one sample would be choosing the number we would like. `PERF-MODELS-2026-08-30.md` carries the accuracy column in a dated standing note above §1, not as an edit to its frozen findings. **And the rig is now runnable rather than merely existing.** The 85.5 minute recording is converted to 16 kHz mono f32 (`afconvert`, `--src-quality 127`); the reference list is built from `detections` for service 15 with the four wrong verses corrected to what was SAID; `load_f32` walks the RIFF chunk list instead of assuming a 44-byte header and refuses anything that is not 16 kHz mono f32, because `afconvert` writes a 4044-byte `FLLR` chunk that puts the payload at 4096 and a misread decodes as noise into a bench that asserts nothing; and `RELAY_BENCH_CONDS` / `RELAY_BENCH_MODELS` make the run affordable, since the five-condition grid over 85.5 minutes is seven hours per model and twenty-one for three. Smoke-run end to end on a 70 s slice of the real recording (`clean`, `ggml-base`): 2 of 2 correct, 0 wrong, lag 0.3 s, `voiced 313/363`, so loader, refs, filters and router scoring all work on this audio. **One limit the smoke run exposed, and it must be read before quoting any figure this bench produces**: it scores `detect_direct` alone, while `main::emit_detections` also runs `detect_bare_verses` against `ContextMemory` and the window anchor. That is the path that produced F-8, so the bench cannot reproduce the memory class at all, and a reference the preacher gave as a bare "verse 22" scores here as a miss. Its wrong-verse rate is about the decoder plus the direct parser, and it is not the same quantity as the field audit's 44%. **FIRST NUMBERS, 2026-09-07, and the row stays open because one model is still decoding.** The 85.5 minute recording replayed through `stt::bench::engine_shootout`, `clean` condition, scored through the real `Router`. **`ggml-base`: 4 of 8 correct, 5 wrong by the bench, 3 that could actually have reached a wall, worst lag 0.3 s.** **`ggml-large-v3-turbo`: 6 of 8 correct, 3 wrong by the bench, 2 that could have reached a wall, worst lag 0.9 s.** **`ggml-small`: 1 of 8 correct on the full recording, and that number is WITHDRAWN — it measured the language detector, not the model.** The run itself said something was wrong: 2583 distinct transcripts against `base`'s 13029 and `turbo`'s 4954 on identical input (`voiced 19972/25653` for all three), while decoding faster than `turbo`, and not one transcript containing *"Psalm 92"* where the other two produced 23 and 13. **A 70 second slice settled it in four minutes.** Over the passage where the live service heard *"Psalm 92, verses 1 to 2"*: `base` produced **231** distinct transcripts of coherent English and found `Psalms 92:1`; `small` produced **17** — *"sous interpersonal work"*, *"birthday ab"*, *"pee Samus putzein thrilled"*, *"LEGO"* — and found nothing, at **3.4 s** lag against 0.3 s. That is whisper's language election wandering, which `stt.rs`'s own header records (one real service went en·yo·pt·sw·sv·ms), and the bench never pinned a language. **Re-run on the same slice with `RELAY_BENCH_LANG=en`: 161 transcripts, coherent English, `Psalms 92:1` found, lag 0.4 s.** It also reproduced the field's own `Psalms 92:12` — the *"verse 12"* mishearing of *"verses 1 to 2"* that put a wrong verse on the console that morning. So `small` is not a bad model on this audio; it is a model that left English and nothing stopped it. `RELAY_BENCH_LANG` now pins the language for `engine_shootout` and the header prints which mode ran, because an auto number and a pinned number are not comparable and nothing else in the output distinguished them. **This is a product finding as well as a bench one, and it widens what a pilot must pin.** The advice half of this row says pin the model; auto language detection can lose a whole service on a model that is otherwise fine, and Relay ships auto as the default . **The control this row named as the fix was not one when the sentence was written, and is one now.** `Settings → Scripture & Languages → Recognition language` set a field on the live whisper engine and stored nothing, so the pin a pilot was told to set was gone by the next Sunday — pointing at it as the mitigation is what RG-138 was filed against. `set_stt_language` now persists through `db::set_active_profile_language` BEFORE it touches the engine (RG-138, closed 2026-09-15; `grep -n 'fn set_stt_language' -A 20 src-tauri/src/main.rs`). **What the fix does not change is the default**: the seeded `Default` profile still carries `language = NULL`, so auto-detect is what a church that never opens the control gets, and until 2026-09-17 nothing on the readiness screen named it either, which is the half now answered below. The measurement this row is open for is untouched by any of it. **What is still owed: the full 85.5 minutes through `small` with the language pinned.** Until that has run, this row has two measured models and one that has only been cleared of the charge against it. The row stays open on that alone, and `small` is the model the advice now points at, so getting it wrong in either direction is expensive. **A structural ceiling applies to every row above and it is not 8.** `RELAY_BENCH_REFS` lists what the live run fired, and three of those eight cannot be reached by this bench at all: `Psalms 89:22` was spoken as a bare *"This is verse 22"* (resolved live against `ContextMemory`, which the bench does not run), and `Proverbs 16:7` and `Psalms 92:2` were operator fires rather than parsed references. `turbo`'s 6 is therefore close to the achievable maximum, and no model can score 8. **The direction the field audit reported holds on identical audio**, which is the whole reason for the measurement: 3 of 3 against 5 of 9 was two different services, and this is one recording through both models. `turbo` also caught BOTH references the live run got wrong — `1 Peter 5:10` (F-8, the one memory answered as Psalms 92:10) and `Psalms 71:21` (rows 153 and 154) — while `base` found neither. **THE BENCH OVERSTATES WRONG VERSES, and the corrected figure is the honest one.** It scores through `router::decide` and stops there; the live path then runs `emit_detections`, which demotes a parsed-but-absent verse to a suggestion (`fire.status.goes_to_screen() && fire.verse_id.is_none()`) rather than broadcasting a reference with no words. `base` produced **`Psalms 721:27`** and both models produced **`Psalms 71:27`**; both are **0 rows** in the bundled KJV, verified by query, so neither could have reached a congregation. That is 2 of base's 5 and 1 of turbo's 3, and the ordering is unchanged either way, which is why both are stated. **A second limitation cuts the other way**: the bench scores `detect_direct` alone, while the live path also resolves bare verses against `ContextMemory` and the window anchor — the path that produced F-8. So it is not a strict upper bound on everything a model can put on a wall, only on what the direct parser produces from it. Neither limitation discounts the comparison: both models were scored by the same rig on the same audio. What remains is `ggml-small`, and it is the one that decides the advice: rule 32 makes it free on cadence (153 ms and 59 ms round up to the same single 200 ms hop), so a `small` that lands anywhere near `turbo` on accuracy is the recommendation this row is looking for with `Settings → Recognition Language` as the fix. **What is still owed: the full 85.5 minutes through `small` with the language pinned.** Until that has run, this row has two measured models and one that has only been cleared of the charge against it. The row stays open on that alone, and `small` is the model the advice now points at, so getting it wrong in either direction is expensive. **A structural ceiling applies to every row above and it is not 8.** `RELAY_BENCH_REFS` lists what the live run fired, and three of those eight cannot be reached by this bench at all: `Psalms 89:22` was spoken as a bare *"This is verse 22"* (resolved live against `ContextMemory`, which the bench does not run), and `Proverbs 16:7` and `Psalms 92:2` were operator fires rather than parsed references. `turbo`'s 6 is therefore close to the achievable maximum, and no model can score 8. **The direction the field audit reported holds on identical audio**, which is the whole reason for the measurement: 3 of 3 against 5 of 9 was two different services, and this is one recording through both models. `turbo` also caught BOTH references the live run got wrong — `1 Peter 5:10` (F-8, the one memory answered as Psalms 92:10) and `Psalms 71:21` (rows 153 and 154) — while `base` found neither. **THE BENCH OVERSTATES WRONG VERSES, and the corrected figure is the honest one.** It scores through `router::decide` and stops there; the live path then runs `emit_detections`, which demotes a parsed-but-absent verse to a suggestion (`fire.status.goes_to_screen() && fire.verse_id.is_none()`) rather than broadcasting a reference with no words. `base` produced **`Psalms 721:27`** and both models produced **`Psalms 71:27`**; both are **0 rows** in the bundled KJV, verified by query, so neither could have reached a congregation. That is 2 of base's 5 and 1 of turbo's 3, and the ordering is unchanged either way, which is why both are stated. **A second limitation cuts the other way**: the bench scores `detect_direct` alone, while the live path also resolves bare verses against `ContextMemory` and the window anchor — the path that produced F-8. So it is not a strict upper bound on everything a model can put on a wall, only on what the direct parser produces from it. Neither limitation discounts the comparison: both models were scored by the same rig on the same audio. What remains is `ggml-small`, and it is the one that decides the advice: rule 32 makes it free on cadence (153 ms and 59 ms round up to the same single 200 ms hop), so a `small` that lands anywhere near `turbo` on accuracy is the recommendation this row is looking for | RG-30 for the latency figures being `base`-only | **P0** | M | Score all three models over the 85.5-minute recording through `router::decide`, and state the wrong-verse rate per model. Word error rate is still unmeasured in every language and this does not change that — it measures which verse would reach a wall, which is the only question SPEC sets a bar for **THE READINESS HALF IS ANSWERED, 2026-09-17. THE ROW STAYS OPEN.** `views/Dashboard.svelte` now names the speech model and the recognition language on the pre-service screen, shortened through `statusbar.js`'s own `modelLabel` so the two surfaces cannot disagree about what to call one path. **`auto-detect` is rendered as a real answer rather than left blank**, which is the case this row is about: a fresh install's seeded profile carries no language, so automatic election is what a church gets on its first Sunday. A machine with no model says so in words. The row is shown whatever the verdict is, because these are facts about how the machine will listen and are equally true when something else is broken. The status bar already had a `Model` cell and it is the wrong place for this: it is read DURING a service, and this is the screen somebody opens BEFORE one, which is the only moment either setting can be changed without the service lock in the way. Held by `readiness.test.js`, verified by deleting the row and watching all three fail. **What is NOT answered, and is why this stays open: word error rate is still unmeasured in every language, and nobody has priced the accuracy trade this row names.** Naming a setting is not measuring it.|
| ✅ RG-117 | **A runtime audio-stream error is a single line on stderr: nothing stops, nothing is told, and the debug recording is lost.** `audio.rs:592` is `let err_fn = \|e\| eprintln!("audio stream error: {e}");` — the whole handler. It does not set the stop flag, does not emit `audio://error`, and does not reach the operator. Observed 2026-09-06 when the desk feed was unplugged: *"The requested device is no longer available. For example, it has been unplugged."* The capture loop is `while !stop.load(…)` with a 100 ms `recv_timeout`, so a dead stream leaves it spinning on `Timeout => continue` for ever | **Three failures in one, and the operator sees none of them.** (1) The transcript simply stops mid-sermon with no banner, no degraded line and nothing in the UI — indistinguishable from a preacher who has gone quiet. (2) `RELAY_RECORD_WAV` writes after the loop exits, so a device drop means the recording is never written — the instrument for the one measurement this project most needs is destroyed by the failure it would most want to have captured. (3) CLAUDE.md rule 5 states *"device errors come back via the `audio://error` event"*; that is true of **start** errors and false of runtime ones, so the handbook overstates the coverage. This nearly bit today: the WAV sat unwritten in RAM for 26 minutes while the app looked alive, and only a deliberate Stop released it | `audits/FIELD-2026-09-06.md` F-10 and F-11; read of `audio::build_stream` and its one-line `err_fn`, 2026-09-06, after the unplug line appeared in a live log. The buffered-and-unwritten state was observed directly: `voiced` climbing 18377 → 18870 with **one** `RECORDING` line in the log and no `audio: wrote`, and RSS falling 1.06 GB → 484 MB as the OS compressed the still-live buffer rather than because anything was freed | Set the stop flag from `err_fn` so the loop exits, `drop(stream)` runs and the recording is written; emit `audio://error` on the same path so the shell's degraded line and the operator both learn about it. **Not done in this pass on purpose** — it is a change to the audio thread's shutdown path on the day a service used it, and it wants the two-process rig RG-113 also wants. A related but separate hazard found the same day: `RELAY_RECORD_WAV` names ONE path and the capture thread **truncates** it on every Stop, so a second Start/Stop cycle silently destroys the first segment — 1837.8 s of a real service was overwritten by 170 s, leaving a valid-looking file. Worked around outside the app for now by archiving each segment on its write line | RG-09 for the degraded surface it should use | **P1** | M | Unplug the input mid-capture and assert three things: the recording is on disk, the shell shows a degraded state, and the operator is told. None of the three holds today **CLOSED, and this row was open against code that no longer exists.** `audio::note_stream_error` stores the reason, sets the stop flag, and the loop's exit path writes the debug recording FIRST and then returns the error into `on_error`, so all three failures this row describes are answered: the transcript stops with a banner, the recording survives, and rule 5 no longer overstates its coverage. Pinned from both sides by `audio::tests::a_stream_error_stops_capture_and_keeps_its_reason` and `audioerror.test.js`. **The 2026-09-13 field note does not contradict this**: that audit established that `err_fn` was still one `eprintln!` on the day, so the service ran the code this row describes and not the fix. Why the loop exited that morning is still unexplained and is the audit's question, not this row's. |
| ✅ RG-118 | **`transcript_cadence` counts silence, so the one metric an operator reads as "is it keeping up" has an inflated tail.** `latency::transcript_emitted` admits a gap only when the previous pass was not final, so rule 31's guarantee (*the gap after a closed utterance is silence, not cadence*) holds exactly as written. A silence **inside an unclosed utterance** is admitted, because the guard has no way to see it. Service 15 recorded a **43480 ms** cadence sample that is a pause in speech | Bounded and specific rather than alarming: at **12663 samples** a handful of leaked gaps cannot move a median, so the p50 of 206 ms and every median quoted in `audits/FIELD-2026-09-06.md` §3 stand. It sets `worst` and can reach `p99`, which are the numbers Diagnostics shows a church and the numbers a drift comparison uses week on week. The reason it matters more than its size is rule 31's own history: this project has twice concluded *"STT is slow"* from an instrument that was measuring something else | **Verified by replaying the guard's own decision against the run**, `audits/FIELD-2026-09-06.md` §9. Of the five gaps at or above 20 s, the four largest (389122, 121714, 76276, 30692 ms) all followed a `final=true` and were correctly excluded; only the 43480 ms one followed a `final=false` and was counted. **And it is silence, not a stall**: that sample carries `decode=3ms` on a `window=600ms` with `voiced` unchanged across the gap (19115 to 19115) while `silent` increments. Had `voiced` climbed it would have been a 43-second pipeline stall, which is why the second check was made rather than assumed | Widen the rule rather than repair the guard: *"the gap after a closed utterance is silence"* becomes *"a gap with no voiced audio in it is silence"*. The recorder already knows whether the pass it is stamping saw voiced audio, so this is a condition and not a new instrument | RG-14 for the persisted percentiles this feeds | P2 | S | Replay a wav with a long mid-utterance pause and assert the cadence histogram's `worst` is on the order of a window, not of the pause. `the_pause_after_a_closed_utterance_is_not_counted_as_cadence` already covers the half that works, and must keep passing **CLOSED, and this row was stale.** `latency.rs` separates a gap with no voiced audio in it from a real stall by carrying the worker's own voiced count into `transcript_emitted`, and the rule is pinned by `a_pause_inside_an_unclosed_utterance_is_not_counted_as_cadence`, with the older, narrower half still passing beside it. CLAUDE.md rule 31 records the same correction and records that the first version of the rule was right and incomplete. |
| ⏳ RG-119 | **The main output screen was lost and recovered three times in an 85.5 minute service, with gaps of 641 s and 519 s.** `service_events` for service 15: `output_lost` at 4.9 s recovered at 5.0 s, lost at 3257.7 s recovered at 3898.7 s, lost at 4597.7 s recovered at 5116.7 s. Service 14 had one 2.0 s pair. This is `audits/FIELD-2026-08-30.md` F-7 recurring and an order of magnitude worse | **19.3 minutes, 22.6% of the service**, during which no output was reporting that it was painting. What that means depends on a cause nobody has established: either the screen really was not painting for 19 minutes, or `OutputHealth` is losing a heartbeat it should be keeping. **Both readings are bad and they need opposite fixes**, which is exactly why this cannot be closed by guessing. RG-01/RG-02 built this detection so a dead screen could never read On Air, and the detection is working. What it is detecting is unknown | `service_events`, services 14 and 15, `audits/FIELD-2026-09-06.md` F-12. On 2026-08-30 the same events appeared twice in 49.5 minutes and a sleeping display was named as the leading candidate, which is what `wake.rs` (RG-28) exists to prevent | **Instrumented 2026-09-07 so the next service answers it; the cause is still not established and this row stays open.** Relay cannot tell the two apart from its own side, because both look identical here: no beat arrived. The page knows, so it now says, on the beat that ends its silence. `channels::BeatGap` carries two numbers the page measures on its own monotonic clock: `since_ms`, the gap since its previous tick, and `hidden_ms`, how much of that it spent `document.hidden`. About one interval means the page kept ticking and the beats were lost on the way, which is Relay's fault; minutes mean the OS stopped running the page, which is a screen that genuinely was not painting; and `hidden_ms` separates a window covered by another window from a frozen renderer, because on macOS an occluded window is hidden. Both are ABSENT rather than zero when the page cannot know, both are clamped and dropped at the door on the kiosk side because they arrive over an unauthenticated LAN (DECISIONS §35), and nothing routes, gates or fires on either: they become one phrase on the `output_recovered` timeline entry, which is where an audit will look. **One half of the row is fixed rather than instrumented.** The 0.1 s pair at 4.9 s was not a fault at all: a window is attached the instant it is created and its page has to load before it can beat, so the first status poll wrote `output_lost` into the permanent record and `output_recovered` a fraction of a second later. Both services on 2026-09-06 open with that pair, service 14's 2.0 s pair is the same thing, History renders it as "Screen stopped responding", and the Sunday report counts it. `OutputHealth::transition` now gives a never-answered channel until `BEAT_STALE_MS` from when it was first seen attached, and **still reports it after that**, so a page that never loads at all is not swallowed. Verified by removing the grace and watching the test reproduce `Some(false)` on the first poll. **What is still guesswork, and is left as guesswork:** the two long outages. The circumstantial evidence is that both ended when the operator came back to the machine (the last recovery is 11 s before Stop), that the app was demonstrably alive throughout (STT ran, no dropped audio, `wake.rs` held `PreventUserIdleDisplaySleep` because capture was live, so the display did not sleep), and that both the beat and the poll that detects the edge are `setInterval` inside webviews the OS may throttle. That is a hypothesis, not a finding. Note also that it may be an artefact of a single-monitor setup: an output window on a projector is never occluded, and this service had one screen with a terminal and an IDE in front of it. The remaining suspects, unchanged: a webview the OS throttled or backgrounded (the run surface sat behind a terminal and an IDE for much of it), `wake.rs` not covering a native output window on the primary display, or a beat interval tight enough that one missed tick reads as a loss. The 0.1 s pair at 4.9 s is a fourth possibility on its own, a beat that had not started yet being read as a loss at startup | RG-02 for the heartbeat · RG-28 for `wake.rs` | **P1** | M | A service-length run with the console backgrounded, asserting `service_events` records no loss while the page is in fact painting; and the reverse, that a genuinely frozen page still reports one |
| ✅ RG-120 | **`end_to_end_speech_to_scripture` stamped 0 samples in a service with three auto-fires.** Service 14: `reference_detection_to_fire` recorded 3 samples and the end-to-end stage recorded **none**. Service 15 recorded 7 against nine auto-fires. The stage is unreliable, not merely sparse | Rule 31 states that a stage never reached is an ABSENCE and not a zero, and this is the stage that answers the only question a church actually asks: **how long after the preacher says it does it appear.** An absence is at least honest, and 0 of 3 means the number cannot be quoted from a single service at all. The figures it did produce in service 15, **p50 2282 ms / p99 3855 ms**, are the first credible end-to-end numbers this project has and they do **not** reproduce F-6's 39 to 90 s. On a different model in a different service, so they do not close F-6 either | `perf_samples`, services 14 and 15, `audits/FIELD-2026-09-06.md` F-13 | Find why the trace is dropped between `ReferenceToFire` and `SpeechToScripture` for a fire that demonstrably reached an output. The `latency` open-trace list is a candidate: a trace evicted before the fire completes would produce exactly this, and eviction is more likely on the slower model, which service 14 was | RG-14 · F-6 in `audits/FIELD-2026-08-30.md` | P2 | M | A test that drives one auto-fire and asserts the end-to-end stage records exactly one sample. Nothing asserts that today, which is why 0 of 3 shipped unnoticed **CLOSED, and this row was stale.** `latency.rs` counts `FIRES_NEVER_PAINTED` and `MARKS_AFTER_CLOSE` separately, which is what makes an absence readable instead of silent: *the AI never fired* and *nothing was attached to paint it* used to look identical, and one of them is a fact about a church's setup while the other is a fact about this instrument. The first service's three fires ran before any output window existed, so zero was the correct answer and nothing could say so. |
| ✅ RG-121 | **The audio input device is not persisted, so every launch silently reverts to the system default.** `capture.js:62` holds `inputDevice` in memory only (`inputDevice: ''`), and both `Live.svelte` and `Settings.svelte` pass `$capture.inputDevice \|\| null` into `startCapture`. Nothing writes it to `app_settings` or to `environment_profiles`, which is empty on this install | **This is the difference between the desk feed and the room**, and it is silent in both directions. On 2026-09-06 both launches started on `MacBook Pro Microphone` with a Blackmagic Web Presenter 4K plugged in and selected in the previous session. A volunteer who does not know to re-select it runs the service on a laptop microphone at the back of a booth, and rule 12's learned gate will do its best with it. Relay stores the learned room floor across sessions (RG-10) and does not store which device it learned it on | `capture.js:62`; both launch logs of 2026-09-06; `environment_profiles` empty; `audits/FIELD-2026-09-06.md` F-15 | Persist the selected device and restore it on launch. **It must degrade honestly**: a stored device that is absent this Sunday has to say so and fall back visibly, not silently pick the default, or the fix reintroduces the same class of failure with an extra step. RG-10's room profile is the natural home, since a device and the floor learned on it belong together | RG-10 for the profile it belongs in | **P1** | S | Reopen with a device selected and assert it is still selected; unplug it and assert the operator is told rather than silently moved to the default **CLOSED, and this row was stale rather than open.** `initAudio` reads the stored device through `get_setting` at launch and `chooseInputDevice` resolves it against the devices actually present, with `inputDeviceMissing` carrying the case where the remembered input is gone. A remembered device that has been unplugged is reported rather than silently replaced, which is the half a bare persist would have got wrong. |
| ✅ RG-122 | **No log line names the audio input device, so no audit can establish which input a service used.** All that is printed is `audio: capture @ 48000 Hz · denoise on (RNNoise)`. Both candidate devices on 2026-09-06 run at 48 kHz, so nothing in the log, the database or the diagnostic bundle distinguishes them | Small, and it invalidates the most important caveat an audit can carry. `audits/FIELD-2026-09-06.md` states its input as the Blackmagic desk feed **on the operator's word alone**, because no instrument in the run recorded it. Any future comparison of detection accuracy between services is comparing two unknown microphones, and with RG-121 unfixed the device genuinely does change between launches without anybody choosing it | Read of `audio::build_and_run`'s startup prints, 2026-09-06; `audits/FIELD-2026-09-06.md` F-16 | One `println!` of the resolved device name where the sample rate is already printed. `list_input_devices` already returns the name and `build_and_run` already resolves the device, so nothing has to be plumbed. It belongs in the diagnostic bundle too, and it is content-free: a device name is hardware, not anything a preacher said | RG-121 · RG-12 for the bundle | P3 | S | Start capture on a named device and assert the log and the bundle both name it **CLOSED, and this row was stale rather than open.** `audio.rs` prints the input device on the capture line, and the doc comment above it records why: both candidate inputs on 2026-09-06 ran at 48 kHz, so nothing in the log, the database or the diagnostic bundle could tell a laptop microphone from a desk feed, which invalidates the most important caveat a field audit carries. |
| ✅ RG-123 | **Four verses still carried marginal-note text after the RG-100 repair, and one of them was a whole note rendered as scripture.** `clean_verse` took the FIRST `}` as a brace group's close, and `kjv.json` contains nested and misplaced braces. **Micah 7:12** ended *"…from mountain to mountain. {and from the fortified cities: or, even to the fortified cities}"*; **Hebrews 10:34** — the one note RG-100 carved out as mid-verse — ended *"…enduring substance. yourselves}"*; **Romans 16:27** and **1 Corinthians 16:24** ended *"…Cenchrea.}"* | RG-100's own failure, one fix later: a translator's note on a congregation's wall as though it were the words of the verse. Micah 7:12 is Luke 17:36 verbatim. It survived RG-100 because RG-100 reasoned about POSITION and this is a parsing error underneath that reasoning — `is_gloss` never saw the note, it saw a fragment of one. Both existing instruments were green: the count is 31,102 and Genesis 30:27 has its *"tarry"*, so **neither of the v3 rung's two probes can see it**, and a database already repaired by v3 still carries it | Scanned the CLEANED corpus for a brace rather than re-reading the rule, 2026-09-09: `SELECT … FROM verses WHERE text LIKE '%{%' OR text LIKE '%}%'` on the real dev database returned 4 rows | Match brace groups by DEPTH, not by the first `}`; and treat a `}` with no `{` of its own as belonging to the note that was just dropped, or as a stray character when nothing was dropped before it. `SCHEMA_VERSION` 4 with a rung whose probe is the brace itself, because a brace is never scripture in any verse and naming the four would have to be rewritten for the fifth | RG-99 · RG-100 | **P1** | S | `no_cleaned_verse_anywhere_carries_a_brace` sweeps all 31,102 verses and `a_note_wrapped_around_a_supplied_word_is_still_a_note` names the four; both watched to FAIL against the restored `after.find('}')`. `a_v3_database_still_carrying_note_text_is_repaired_on_boot` drives the real `migrate` on a v3 database and asserts the DATA, and fails with the rung disabled. Confirmed on the real dev database: `user_version` 3 → 4, brace residue 4 → 0, and all 161 detections across 15 recorded services kept their `verse_id` |
| ✅ RG-124 | **Fourteen verses ended with a translator's subscription on the wall, as though it were the last words of the epistle.** `«Written to the Hebrews from Italy, by Timothy.»` under the benediction of Hebrews 13:25, and the same in Romans, both Corinthians, Galatians, Ephesians, Philippians, Colossians, both Thessalonians, both Timothys, Titus and Philemon. `clean_verse` had only ever looked at braces; no version of it had looked at the guillemets | RG-100's rule applied to the delimiter nobody had checked. A note about where a letter was posted is not scripture, and it rendered in the same type as the verse | Whole-corpus scan for the delimiter, 2026-09-09. The guillemets are the ONLY non-ASCII characters in `kjv.json` and mark nothing else; all 14 groups are balanced and terminal, and the only verse with anything after its closing `»` is Ephesians 6:24, where that anything is itself a marginal note the brace pass drops | `strip_subscriptions` runs BEFORE the brace pass, so the braces inside a subscription go with it. Removing them changes exactly fourteen verses, no others, and leaves no verse empty | RG-100 · RG-123 | **P1** | S | `no_cleaned_verse_anywhere_carries_a_subscription` sweeps all 31,102 verses and asserts none is emptied; `a_subscription_is_never_the_verse` names Hebrews 13:25 and Ephesians 6:24. Both watched to fail — the sweep reproduces all fourteen |
| ✅ RG-125 | **The subscription repair was first written as a WIDER v4 probe, and v4 was already stamped — so it would never have run.** `run_migrations` takes `if from < 4`, this machine's database had taken v4 an hour earlier, and the fourteen verses sat there through a boot of the packaged build | **RG-102, for the third time on the same ladder.** A repair placed where the install that needs it has already gone past. The v3-start test passed the whole time, because a v3 database runs every rung below it — only a v4-start test can tell the difference | Found by BOOTING the packaged binary against the real dev database and reading `verses` back, not by a test: `user_version` 4, fourteen guillemet rows still present, 2026-09-09 | Split into its own v5 rung with its own delimiter (`NoteDelimiters`), and the principle written at the call site: **a rung that has already run cannot be edited, only followed** | RG-102 · RG-124 | **P1** | S | `a_v4_database_still_carrying_a_subscription_is_repaired_on_boot`. Watched to fail by folding v5 back into a widened v4 — the v3 test stays green and the v4 test goes red, which is the whole point of the version a test starts from |
| ✅ RG-126 | **Yorùbá numerals now parse, and are capped at `Suggest` because nobody has reviewed them.** `data/numerals.json` had `sw` and `ha` and no `yo` key, so a reference spoken entirely in Yorùbá parsed to nothing — the book alias matched and the chapter number was not a number the FSM knew. `r4_05` recorded this as an `#[ignore]`d red test for the life of the project | Yorùbá is the largest addressable market of the three, and the gap was invisible in the scorecard: all twelve `yo` cases in `eval_corpus.json` use digits or English number words, so the report printed a 100% Yorùbá row beside Swahili and Hausa rows that really had parsed their own numerals | `r4_05` run explicitly, 2026-09-09: *"Jòhánù orí kẹta ẹsẹ̀ kẹrìndínlógún" produced nothing at all* | A `standalone` table — Yorùbá is vigesimal and subtractive, so one word IS the number and joins to nothing either side, and a wrong entry can never alter a neighbouring one. **The block is marked `unreviewed`, and every reference resolved through it is demoted to `UncertainNumber`**, which the router refuses at any score and any dial. The bare units 1–9 are left out because `normalize()` folds tone marks and they collide with ordinary sermon words (*èje* 7 / *ẹ̀jẹ̀* blood, *àrún* 5 / *àrùn* disease); the ordinal *k-* forms are the register chapter and verse are actually spoken in. Deleting the flag lifts the cap, and that is a signature rather than a commit | RG-32 for the same class of demotion | P2 | M | `r4_05` is no longer ignored and runs in CI. `r4_05b_an_unreviewed_yoruba_numeral_is_offered_never_fired` asserts across the WHOLE dial (0–100) that nothing fires and something is still offered; setting `unreviewed` to false makes it auto-fire John 3:16 at 0.95, which is the check that proves the cap is a method and not a score |
| ✅ RG-127 | **Two `channels` tests need `dist/` and never say so, so a fresh clone fails `cargo test` for a reason the failure does not name.** `output_http_serves_embedded_pages` and `a_header_that_arrives_in_a_second_packet_is_still_read` serve the real built frontend; `dist/` is gitignored (`.gitignore:8`). They report `got HTTP/1.1 404 Not Found` and *"a request split across packets must still be served"* — neither mentions the frontend build | **CI is green only because of an ordering nothing states or enforces**: `ci.yml` runs `npm run build` at line 54 and `cargo test` at line 70. CLAUDE.md's own Commands section tells a contributor to run `cd src-tauri && cargo test`, which on a clean checkout fails twice and blames the HTTP server. Same shape as the fast-gate hook and the Node-20-only CI: an instrument that is fine on the one machine it was tried on | Reproduced 2026-09-09 in a detached `git worktree` of `cbdc545` with no `dist/`: 678 passed, **2 failed**; the same commit in a tree that had been built passes 680 | Have the two tests state the precondition instead of asserting through it — skip loudly with *"run `npm run build` first: this serves the real `dist/`"*, or assert the directory exists before binding. **Do not delete the coverage**: serving the real built page is the point of both tests. A note in CLAUDE.md's Rust command block is the cheaper half and does not fix the message | — | P3 | S | Delete `dist/` and run `cd src-tauri && cargo test channels::tests` — it must fail with a sentence naming the build, or skip, and never with a bare 404 **CLOSED, and this row was stale rather than open.** `channels.rs`'s test module carries a `NO_DIST` constant used by both assertions: a fresh clone or a new worktree now fails with the reason, the fix and this row's own number, instead of a bare 404. The tests still need `dist/`, which is correct — they serve the REAL built frontend, and mocking it would retire the only check that the packaged LAN server can reach a page at all. |
| ✅ RG-128 | **The FIRST thing put on a freshly-opened output page was fitted against a fallback typeface and painted clipped.** A webfont is fetched only when something first uses it, so an output page — which opens with nothing on it — reports `document.fonts.status === 'loaded'` at mount, `TemplateRender`'s deferred re-fit hook is skipped, and the binary search then measures the first verse in `serif`, which wraps into fewer lines and lets a larger size through. The real face lands a moment later and the text grows past the box it was fitted to | **A verse on the congregation's screen with its first and last lines sliced through the middle**, permanently: nothing re-fits, because the fit signature has not changed. It is the FIRST verse of the service, on any screen opened before the sermon — which is every screen | Reproduced against the running backend, 2026-09-10: fresh `output.html` at 1920×1080 on the default `Classic Serif`, `POST /api/fire?ref=Romans 8:28` → the fitted block measured **732px inside a 583px `overflow:hidden` box** and was still 732px at 3.25 s. A 1px window resize corrected it to 546px, which is what proved the metrics stale rather than the text unfittable | The fit checks whether it measured the face that will be PAINTED (`document.fonts.check`) and re-fits if it did not, bounded to two attempts so rule 37's genuinely-unfittable case is still reported through `onFit` rather than retried for ever. Asking `overflowing()` straight after the fit cannot work: the reflow the new face causes has not happened yet | RG-05 for the validator this sits under | **P0** | S | `templatefit.test.js` pins `needsRefit`, watched to FAIL against the pre-fix rule; re-verified in the browser at 546px in 583px |
| ✅ RG-129 | **A kiosk client that connected mid-service was sent its template and its themes and then left blank.** `run_kiosk_server`'s `hello` branch replied with the cached template and the custom themes and nothing else; the hub kept no record of what was last published, so a client that joined after a fire received the next fire and nothing before it | **A congregation screen that goes away and comes back shows black for as long as the reading lasts.** An OBS source restarting, a kiosk page reloading, a lobby television dropping off the wifi and this hub's own 1.5 s reconnect loop all produce it. RG-119 records the main output going away three times in one 85.5 minute service | Reproduced against the running backend, 2026-09-10: with `John 3:16` live, opening `output.html` connected, was sent `{"kind":"template"}` and `{"kind":"themes"}`, and `document.body.innerText` was empty. A second fire painted it | `KioskHub` retains the last frame of the three kinds that decide what a screen is SHOWING (`content`, `clear`, `black`) and sends it on `hello`, after the template so the render has what it needs. **It can never undo a panic control**, because `clear` and `black` are published through the same door and become the retained frame in their turn; a rehearsal publishes nothing here at all, so nothing a rehearsal did can be replayed | RG-119 · RG-02 | **P0** | S | `a_client_that_connects_mid_service_is_sent_what_is_on_the_screens` and `a_cleared_wall_stays_cleared_for_a_screen_that_joins_after_it`, both watched to fail against the pre-fix branch, plus `the_screen_frame_matcher_agrees_with_what_is_published` — the first matcher was a `starts_with` and `serde_json` sorts map keys, so it matched nothing and looked exactly like the bug it fixes |
| ✅ RG-130 | **Live's Output Status could not fit a screen's NAME, and the screen with the worst news was the worst affected.** Name, note, badge and switch shared one flex line on a ~275px rail, and the failure badge is the longest of them | On the pane whose whole job is telling an operator which screen has died: **"Streaming" rendered 7px wide**, "Overflow (NDI)" 25px, "Lobby screen" 37px, while the note wrapped one word per line and pushed rows to 122px. The shell's warning strip, reading the same data, said **"3 is not responding"** — `App.svelte` mapped `st.id` because `ChannelLiveness` carried no name, though `degraded.js` has documented that argument as names since it was written. A screen with nothing to report showed `native_window`, a raw column value | Measured in the browser, 2026-09-09: `.out-nm` client widths 7 / 25 / 37 / 61 / 75px against scroll widths of 47–275px, at both 1920 and 1366 | Two-line row (name + badge, then the screen's own word + the switch); `name` added to `ChannelLiveness` and used by the strip; `screenKind()` in `outputHealth.js` shared with the Outputs table's TYPE column; the inert half of the switch pair dropped from the run surface; a scroll shadow so a pane with more rows below says so | RG-01 · RG-02 | **P1** | S | Two scans in `outputhealth.test.js` — the Rust row carries `name`, and the shell maps it — the second watched to fail against `.map((st) => st.id)`. Names re-measured at 108–144px |
| ✅ RG-131 | **Accept & fire and Dismiss sat below the bottom of the AI Detection panel.** The claim card repeated the reference and the method — both already the headline and the chip above it — and rendered the whole verse a second time under a preview that was already showing it in its real template | The two controls the product exists to offer, invisible without scrolling, on the panel an operator watches during a sermon. macOS draws no scrollbar at rest, so the panel did not look scrollable either | Measured in the browser with one ordinary suggestion, 2026-09-09: the card needed **436px in a 281px panel** and `.acts` sat 35px below the visible bottom | The duplicated Method/Reference grid removed, the second copy of the verse clamped to three lines, and the decision row made `position: sticky` at the bottom of the panel | RG-63 | **P1** | S | Re-measured in the browser: the row is inside the panel at every content length tried |
| ✅ RG-132 | **Seven Settings controls wrote a preference nothing in the application ever read**, and one of them promised a safety guarantee that has never existed. `Confirm Before Going Live` — *"Show a confirmation dialog before sending content live"*, default **on** — plus Auto Save, Default Content Type, Time Format, Date Format, Restore Previous Session and Default Startup Screen | An operator who reads that switch believes there is a step between them and the congregation's screen. The other six are the same lie in a quieter register: the clock is not formatted by Time Format, Relay always opens on Live, and no "new item" anywhere consults a default content type | `grep` for each key across `src/` and `docs/`, 2026-09-10: every one occurs only in `Settings.svelte`, where it is written and never read | **Removed**, not marked "Soon" — "soon" is a promise too, and none of them is on a roadmap. Same precedent as the `StageDisplays` subtree (DECISIONS §25). The two rows that ARE honestly marked (`autoStart`, `minimizeTray` — disabled, tagged and explained) stay. Wiring any of them is a product decision, not a repair | DECISIONS §25 | **P1** | S | The controls are gone from the rendered page; `qa-inventory` still reports 0 handlerless buttons and 0 unnamed controls |
| ✅ RG-133 | **The RG-92 sentence was still hard-coded on the OTHER Settings surface.** The Updates section was fixed to report the CHANNEL's state; the Settings **Overview** page carries a second copy in a Quick Links card, and it still read `$updateAvailable ? … : "You're on the latest version"` | The same lie, one scroll apart, on the copy an operator sees FIRST — Overview is where Settings opens. This repository has had four separate bugs of exactly this shape, which is why the guard is a scan and not a component test | Read in the browser, 2026-09-10, against a channel whose last check had failed: the Updates row said *"could not reach the update server"* and the card beside it said *"You're on the latest version"* | The card calls `describeChannel`, the one describer, as the row does | **RG-92** | **P1** | S | `no view hard-codes "the latest version"` scans every `.svelte` file's MARKUP by design — a script may legitimately branch on channel state elsewhere. `doCheckUpdates` no longer carries a ternary of its own: it calls `describeChannel` directly (wave1 Task 3), and `settingssections.test.js` now pins that against `SCRIPT_ONLY` too, closing the exemption this row used to describe — watched to fail against the restored literal |
| ✅ RG-134 | **Twelve smaller defects on rendered surfaces, none of them visible to any existing instrument.** `qa-inventory` reports 0 handlerless buttons and 0 unnamed controls and is right on both counts; not one of these is either | Each costs an operator something specific. **Save Template** was off the right edge of the window at 1440 (the header wanted 1184px in 1158px); the Template editor's layer list showed 1.5 layers of 3 (75px for 138px of content); its "Select a layer" hint shared the class `te-guide` with the canvas's 1px alignment hairline and so rendered as grey-on-amethyst at about 1.9:1, absolutely positioned across the panel heading; **Service History** gave the service title 58px of a 626px row so every service read "Sunda…"; the **Dashboard's** health rows were ellipsised mid-word on all six; 19 range inputs across three views drew the browser's white track on a dark panel and the detection dial's was **4px tall**; the Theme editor named a typeface `var(--f-display)`; the verse deck's bulk checkbox had an empty `<label>` and so no accessible name; the stage remote's search box had none either; at 375px the **Emergency Stop** button was off the edge (the topbar wanted 490px in 375px) and the degraded strip covered six of the eight tab-bar labels; the sidebar's engine line was decided once at startup and could never recover; and an unrecognised `cue_type` was drawn as **SCRIPTURE · AUTO-DETECT**, the one kind that fires by itself — `docs/data/schema.sql` documented the notice type as `'announcement'` while every writer uses `'announce'` | All measured in the browser at 1920 / 1440 / 1366 / 1280 / 1024 / 768 / 375, 2026-09-09→10 | Fixed at the shared level where there was one (`app.css` for sliders and checkboxes, `outputHealth.js` for the screen-kind word, `themes.js` for the typeface names, `plan.js` for the unknown cue kind) and per-view where there was not. The engine line is re-asked on the existing 15 s timer with **`ping`**, never `greet` — rule 26 | RG-13 for the instrument that cannot see these | P2 | S | Re-measured: 0 clipped strings and no horizontal overflow on any of the eight tabs at 1440, or on Live at 1920 / 1366 / 1280 / 1024. 293 focusables across the eight tabs, all named, all with a visible focus indicator |
| ✅ RG-135 | **A translation the preacher NAMES and Relay does not have is invisible: the fire looks exactly like a matched one.** Field service 2026-09-13, 3016.4 s: *"Let's read it in the **TPT**. He kind of explained it better there. **TPT says verse 19**."* Relay fired Hebrews 11:19 at 0.88 from the King James Version — the only row in `translations` — and the preacher read the Passion Translation aloud against a KJV screen | **The rule 35 shape, on the AI Detection panel.** The fire carried the same `direct` method, the same 0.88 and the same badge as the seven correct fires around it, so nothing distinguished "the translation you asked for" from "a translation nobody asked for". The reference was RIGHT, so this is not a wrong verse and no existing instrument treats it as a fault of any kind. Distinct from RG-50, which is an operator unable to ADD a translation; this is Relay unable to SAY that the one it used was not the one named in the audio | `audits/FIELD-2026-09-13.md` §2. Identified from the wording rather than assumed: the next window's tail is *"The faith operated powerfully in Abraham for when it was"*, TPT, against the KJV's *"By faith Abraham, when he was tried, offered up Isaac"*. `select * from translations` returns one row | Detect a translation name in the window and, when it is not one that is installed, say so on the claim — the operator learns that the words on the wall are not the words being read. **This is a labelling change, not a licensing one**: shipping TPT is a commercial decision and is explicitly not proposed here | RG-50 for the table an operator cannot fill · RG-18 | P2 | S | A window naming an uninstalled translation must render differently from one that names none, and the reference must still fire — refusing the verse would be strictly worse than showing it in KJV **CLOSED 2026-09-17, as the labelling change the row asks for and nothing more.** `detection::named_translation` finds a translation named in the window and is pure and DB-free like the rest of that module; `named_translation_gap` in `main.rs` asks the database half. The caveat is set ONLY when Relay can tell the operator something they do not already know: nothing is said when the wall already shows what was named, and nothing when the church actually has the named translation and is simply not using it for this fire. A failed read of `translations` answers nothing, which is the safe direction, because this is a footnote on a fire whose reference is RIGHT. `Live.svelte` renders it in faint type below the verse, wearing no law colour — none of amber, cyan or amethyst is a promise about this, and a colour that carries a promise cannot be borrowed for a footnote. Nine tests, including the field sentence verbatim and four windows that must stay silent. **It fetches, licenses and renders nothing**: RG-50 is an operator unable to ADD a translation and is still open. |
| ✅ RG-136 | **The service timeline stamped an `output_recovered` with no `output_lost` before it, and ended on a loss that never recovered.** `service_events` for service 16 is three rows entire: `service_started` at 0.0 s, `output_recovered` for **Streaming** at 3207296.6 ms, `output_lost` for Streaming at 3245372.7 ms. A screen came back from an outage the timeline never recorded | **The timeline understates the outage count, and it is the instrument the Sunday report and replay are both built on** (RG-04, RG-07, RG-08). Worse in the other direction: the operator’s connected screen died at 3245.4 s of a 6631.7 s service and **was dead for the final 56.5 minutes**. Nothing put scripture on a wall in that window only because nothing was called after 3097.1 s — luck, not a guarantee | `audits/FIELD-2026-09-13.md` §3; `select * from service_events where service_id=16` returns exactly those three rows. The recovery’s own detail string is *"Streaming · screen’s own clock: silent 2s, never hidden"*, so the page went quiet while VISIBLE — the case `OutputHealth` exists to catch, and it caught that half | **Two individually reasonable things, wrong together.** `OutputHealth::transition` CONSUMES the edge it reports (`reported` advances whether or not anything records it) and `log_event` is a silent no-op with no service running — its whole body sits inside `if let Some(st) = sess.as_ref()`. A screen already dead before the operator pressed record had its loss computed, thrown away and marked as reported; the recovery then landed in the service with no partner. The status poll’s body is now `record_output_edges`, which **forgets** an edge outside a service rather than consuming it, so recording starts from a real first sighting. It deliberately does NOT back-date the lost event into a service that had not started: an event cannot belong to a service that did not exist, and inventing a time for it would be worse than the gap. **The unrecovered final loss is not a defect** — the screen died and stayed dead, and the record says so correctly | RG-119 · RG-02 · RG-04 | **P1** | S | `e2e::r136_a_recovery_in_the_record_always_has_a_loss_to_recover_from` walks the timeline and refuses a recovery it has not seen a loss for. **Watched to fail against the pre-fix body**, where it reproduces the field artefact exactly: `["service_started", "output_recovered"]` |
| ⏳ RG-137 | **A 28.6 second worst case on `audio_to_visible_transcript` in a service where nothing was dropped and nothing panicked — and the model, the audio and the build profile are all now ruled OUT by measurement.** Service 16 final `perf_samples`: p50 2602 ms, p95 4694 ms, p99 5647 ms, **worst 28650 ms** over 4040 samples; `transcript_cadence` worst 20978 ms. The process log agrees independently — 4416 `LAG=` lines, median 1287 ms, **max 20977 ms** | By rule 31’s definition the clock starts at the OLDEST audio still waiting, so for 28 seconds the oldest unprocessed audio had been waiting nearly half a minute. **A reference spoken in that window reaches the wall half a minute late or never.** Nothing on the fire path is responsible: detection is 40 ms at p50, routing 9 ms, end-to-end 1521 ms p50 over eight fires | **Three instruments put the event in ONE 120-second window at ~100 minutes, not during the music** — `stt_decode` 3870→9326 ms at t=6002 s, `audio_to_visible` 13818→28650 ms and `transcript_cadence` 11961→20978 ms at t=6062 s. One decode of **9326 ms** against a 1161 ms median. That window cut out and replayed through `stt::realtime::live_transcript_latency` at wall-clock pace reproduces **nothing**, in either profile: release p50 1188 ms / worst **1264 ms**, debug p50 1169 ms / worst **1328 ms**, `audio_to_visible` worst 2461 ms and 2706 ms, 0 dropped partials in both | **The first reading was the music and it is disproved twice over** — the event is an hour after the last worship window, and the audio is flat. **The second reading was the debug profile and that is disproved too**: `whisper.cpp` is built by cmake with its own optimisation and never carried the Rust profile’s penalty, so debug and release decode within 19 ms of each other. Medians match the live service to within 27 ms, so it is not compute, not the model, not the content and not the profile. **One variable is left**: everything the rig does not have — capture thread, webview, kiosk hub, SQLite writes, detect thread, and 110 minutes of uptime against 200 seconds. A single 9326 ms decode inside an otherwise flat distribution is the shape of a STALL, not of slow compute, and the next step is to find what holds that thread rather than to tune anything | RG-118 for the cadence metric’s own defect · RG-116 | **P1** | M | Both replays are recorded and reproducible: `RELAY_BENCH_WAV=<slice> cargo test [--release] realtime::live_transcript_latency -- --ignored --nocapture`. What would close this is a stall reproduced with the running application attached — the rig has now shown it cannot be reached from the audio side |
| ✅ RG-138 | **The recognition language did not persist, and it is the control RG-116 names as its own mitigation.** `Settings → Scripture & Languages → Recognition language` called `set_stt_language`, which took `stt: State<Stt>` and **no `Db` at all**: it set a field on the live whisper engine and stored nothing. An operator who chose English had chosen it until they closed Relay | **A pin that a pilot is told to set, and that quietly is not there the next Sunday.** RG-116's own finding is that whisper's language election can lose a whole service — `ggml-small` on this church's own recording produced 17 incoherent transcripts (*"pee Samus putzein thrilled"*, *"LEGO"*) where the same 70 seconds with the language pinned produced 161 coherent English ones and found `Psalms 92:1`. So the register named a fix that did not survive a relaunch. It was also invisible from the surface: `stt_status` read the language back off the same live engine, so the choice looked sticky for the rest of the run and only failed at the one moment nobody is watching — the next launch | `main.rs::set_stt_language` before this change (no `Db` in the signature); `main.rs::stt_status` reading `e.language()`; `docs/USER_GUIDE.md` already documented the defect in its troubleshooting table (*"It does not survive closing Relay"*) and told operators to set the voice profile instead, on a different tab | **Write to the active voice profile — the store that already persists this fact.** `voice_profiles.language` is applied at startup (`main.rs`'s setup hook), on a profile switch (`select_voice_profile`) and after a model reload (`load_stt_model`), so a second `app_settings` key would have been a second source of truth for one fact and the two would have raced at startup. `db::set_active_profile_language` calls `ensure_voice_profiles` first, so the write cannot land nowhere on a fresh install; the command applies the WHOLE profile to the engine (`apply_profile_to_stt`) rather than the language alone, so the decoder-bias prompt is re-derived in the language now chosen. **Three things came with it.** `stt_status` reports the STORED language when no engine is loaded, because "no engine" is not "no language" and the select read Auto-detect over a profile that said English (rule 35); the select is no longer disabled without a model, since a church pins the language before the first download; and `setSttLanguage` moved from GROUP 2 to **GROUP 1** in `capture.js` — it swallowed every failure, which also meant `rooms.js` reported *"recognition language"* as applied on every room, unconditionally, from a wrapper that could not fail. **Deliberately NOT on `servicelock::PROTECTED`**: it unloads nothing and is undone by choosing again, and pinning the language is the operator's REMEDY when auto-detect wanders mid-sermon, not the hazard — the hazard is `select_stt_model`, which is protected | RG-116 names it | P1 | S | `qa::cold_start::the_recognition_language_is_written_to_the_active_voice_profile` (asserts the DATABASE, because that is what the next launch reads) and `the_stored_language_is_reported_when_no_speech_model_is_loaded`; `src/lib/sttlanguage.test.js` for the wrapper's throw. All three were watched to FAIL with the defect reintroduced |
| ✅ RG-139 | **A running countdown makes every OTHER text layer paint its DECLARED size instead of its fitted one, and where the fit had shrunk it the words are clipped with nothing reported.** On `Stage · Large type` (id 37, the one seeded template with a layer bound to `countdown`) the countdown's LABEL arrives through the `reference` binding — a 12%-high strip declared at `3.4cqw`. **Run B, 118-character label, 1920×1080**: the fit measured `data-fitted="2.005"` and the element painted at `3.4cqw`, giving `scrollHeight 221 / clientHeight 130` — **221 px of text in a 130 px `overflow:hidden` box, 91 px of it clipped**. **Run A, 155-character label, same viewport**: fitted `1.9234375`, painted `3.4`; static labels fitted `2.4671875` and `2.266796875`, painted `1.6` and `1.5`; only the `countdown`-bound layer (fitted `6.2535156`) painted its fitted value. The two runs are reported separately because the label lengths differ and the fit lands elsewhere; the pixel figures above are Run B's | **Congregation- and preacher-facing, and silent.** The first line of the label is gone entirely and two more are sliced through the middle — rule 42's failure on a surface rule 37 is supposed to be watching. `onFit` reports a `scale` taken from the FITTED value, so the report says the fit succeeded while the screen shows it did not. The blast radius is narrow but real: a layered template with a `countdown`-bound layer, which on the shelf as it stood was `Stage · Large type` alone — the preacher's stage monitor — plus any template a church builds with one. **WIDENED ON 2026-09-16 BY WAVE 5 TRACK A, AND THE ROW IS NOT CLOSED.** The shelf was rebuilt as forty layer-model looks and **six of them carry a `countdown`-bound layer**: `Timer · Monolith`, `Timer · Rail`, `Timer · Titled`, `Timer · Panel`, `Timer · Contrast` and `Stage · Rail`. The one template that reproduced this — `Stage · Large type` — no longer ships, so the reproducer named in the evidence column is gone; the six that replaced it are on the same path. The sentence above that the five Timer FAMILIES were unaffected because they used `.cd-default` no longer describes anything shipped: `.cd-default` is the fallback a template with NO bound layer takes, and every Timer look now has one, deliberately, because that is what closes RG-141's region branch. **Four of the six are congregation-facing** (a pre-service countdown on the wall) where the old reproducer was a stage monitor, so the exposure is wider than it was as well as more numerous. `Timer · Monolith` and `Timer · Contrast` carry the countdown layer ALONE and so have no other text layer to mispaint; the other four do. **The mitigation this row already named is no longer available**: it was "retire `Stage · Large type`, the only shipped reproducer", and retiring six looks would leave a fresh install with no timer at all. Either RG-139 is fixed, or the Timer role ships knowing this | `audits/DESIGN-2026-09-16-WAVE2.md` §1a, measured against the real backend at 1920×1080 and 1280×720. Same template, two contents, decisive: with a VERSE on screen every element paints its `data-fitted` value; with a COUNTDOWN on screen only the bound layer does. The element is not rebuilt — it was tagged with an attribute and the tag survived four seconds of ticking with the wrong size still on it, so this is not the `{#key text}` path `reapplyFitted` covers. **RE-MEASURED ON THE SHIPPED FORTY, 2026-09-16, `audits/DESIGN-2026-09-16-WAVE5.md` §3** — and the reach Track A widened to six is a reach of two. Six seeded looks carry a `countdown`-bound layer (`timer.monolith`, `timer.rail`, `timer.titled`, `timer.panel`, `timer.contrast`, `stage.rail`); only two of them also carry a text layer the fit had to SHRINK, and those two clip. `Timer · Titled` at 1920×1080 with a 118-character label: 38.28 px (the fitted `1.99375cqw`) at 300 ms, then **65.28 px — the declared `3.4cqw` — from the first tick onward, 339 px of words in a 97 px `overflow:hidden` box**. `Stage · Rail`, same viewport and label: 38.13 px, then **61.44 px, 319 px in a 108 px box**. **Isolated rather than inferred**: the identical template, viewport and words pushed as an ANNOUNCE frame instead of a countdown hold the fitted size for the whole run and clip nothing, so the running countdown is the variable. The revert is not confined to one layer either — `Stage · Rail`'s three static labels drop from their fitted `2.2773cqw` to their declared `1.6cqw` at the same tick, where it happens to be harmless. **The threshold a church meets**: the seeded example plan pins this template for its countdown cue with the label `Service begins in`, 17 characters, which fits at the declared size and never shows the defect; a 42-character label loses 73 px of a 97 px box | **Not proposed here, because the audit did not measure the cause.** The observation is that the painted size reverts to the declared base while `data-sized` stays `'1'` (so `reapplyFitted` declines) and `fitSig()` folds a ticking layer in by text LENGTH (which does not move as digits count down, so no re-fit is scheduled). Any repair must not bring back the 4 Hz reflow storm that gating exists to prevent. **An available mitigation, and it is a human's to choose**: the one seeded template that reproduces this is `Stage · Large type`, which the wave 2 plan originally proposed retiring — the controller withdrew the eight-row prune in favour of retiring only the row a name collision forced, because pruning cost eleven assertions across `legibility.test.js` and `shelf.test.js`. Retiring that row would remove the only shipped reproducer without touching the fit loop | RG-141 for the region half · RG-128 for rule 42 | **P1** | M | The bug, not the fix: a layered template with a `countdown`-bound layer and a label too long for its box must paint the fitted size, watched to fail against the current behaviour. **Very probably pre-existing** — `reapplyFitted`, `isTicking`, `fitSig`, `fitLayers` and the `.lfit` style attribute are byte-identical to the merge base `7851a79`. That is a diff, not an experiment: checking out the base and firing the same countdown was available and was not done, and wave 2 DID change this file's reactive graph, which is the kind of change that can start or stop a re-render overwriting an imperative style write **CLOSED 2026-09-18, and the cause is not where either the row or the audit looked.** The observation in this cell is right and the guess after it is wrong: `data-sized` does stay `'1'`, but the element is not rebuilt and `fitSig` is not the mechanism — it is only the reason nothing repairs it. `.lfit` declares `font-size:{baseSize(L)}cqw` inline on purpose (rule 42, `cardfit.test.js`), and **Svelte 4 compiles that attribute into one `set_style` call per interpolation whose update is guarded on the DIRTY BIT ALONE, with no comparison against the last value** — unlike the plain `data-base`/`data-fit` attributes two lines above it, which are cached. Read out of the served bundle rather than reasoned: `if (dirty[0] & 262144) { set_style(div, "font-size", baseSize(ctx[142]) + "cqw"); }`. So ANY update that dirties `stackLayers` writes the declared base over the imperative fit, in place, on every visible text layer; a countdown tick is that update, and it is the case nothing recovers from because a ticking layer folds into the signature by text LENGTH. **Both recovery paths declined for one reason: `data-sized` is a one-way latch** — it records that a fit once happened, never that its answer is still on the element, which is rule 37's own shape one level above the loop. `fitInForce` now asks the DOM whether the size on the element is the size answered for its box, attribute reads only, and both paths ask it. **Two things came out of measuring rather than reasoning and both cost a round.** The repair cannot wait for a frame: `afterUpdate` runs before paint and the next animation frame is one paint later, and with the repair left in the frame a per-frame sample of `getComputedStyle().fontSize` caught the declared base on **4 of 482 frames** — a 65px flash on a 38px label, four times a second. And the comparison may not be a string one: the CSSOM rounds `21.166796875cqw` to `21.1668cqw` on read, so a string compare said "different" forever and drove the full search through a forced reflow every frame — **118 fit passes and 2682 style writes in 29 seconds**, against 1 pass and 2 reports with `sizeIs`'s numeric compare against `FIT_EPS_CQW`. A layer the fitter DECLINES to measure (`lscroll`, `fit:'none'`) now records its declared base as its answer, which closes the same storm by a second road. **After, same instrument, 1920×1080**: `Timer · Titled` 98/97 with 964 of 964 frames at the fitted size; `Stage · Rail` nine boxes, every one wearing its fitted value. `countdownfit.test.js` enumerates the six from the shelf file; watched to fail with the fix reverted on `Timer · Rail`, `Stage · Rail` and the unrelated-redraw case. **`Timer · Titled` — the browser's own reproducer — passes either way in jsdom**, because its label is a LABEL_BIND and therefore 'shrink', so its ceiling is its base and there is nothing for a layout-free engine to see move. That limit is stated at the top of the test file rather than hidden by it |
| ⏳ RG-140 | **One template renders an announcement TWO ways, and opening a workspace switches every screen in the building from one to the other.** `Classic · Announcement` (9) ships region-model with `"scroll":true`, so before anyone opens the Templates workspace an announcement paints the `.ticker` footer band — label left under a CSS `max-width: 45%`, body crawling through the rest. `TemplateGallery.upgradeLegacyToLayers` converts the row on mount, and the same row then paints a full-frame block: label as a centred two-line heading **84% of the stage wide with no cap of any kind**, body crawling across the MIDDLE of the wall. Same template, same fire, two designs | **Congregation-facing, and what the operator is told is about a data model rather than about the wall.** An operator who opens Templates once — to look, not to edit — changes the look of every announcement in the building. **They are not left with nothing, and an earlier version of this row wrongly said "permanently and with no way back"**: `TemplateGallery` calls `snapshotTemplateVersion` before each conversion and renders *"Converted N classic templates to layers. Earlier versions are in each template's History."* into a `role="status"` pane. So it is neither silent nor irreversible. What it does not say is the thing that matters — that every notice now paints across the middle of the wall instead of along the bottom. A line about converting classic templates to layers cannot be read as that. **Neither the pane nor a History restore was measured by this pass**; both are read off the source, and whether a restore returns the region rendering is unknown. The 45% label budget this wave rebuilt governs the first rendering and does not exist in the second, so the rule is real for a church that has not opened that tab and absent for one that has. `Aurora · Announcement` (14), `Ember · Announcement` (19) and `Lower Third · Announcement` (24) ship the same way; `High Visibility · Announcement` (29) carries no `scroll` at all, so the five families are not even uniform before the conversion | `audits/DESIGN-2026-09-16-WAVE2.md` §2 and §2a, both halves measured on template 9 with the same announcement at 1920×1080, with the shelf model verified in SQLite either side. **Before**: band `y940.1 h139.9` `overflow:hidden`, label `clientWidth 778` = 40.5% of the stage, `scrollWidth == clientWidth`, shrunk to 12.4972 px, `.ticker-run` 5098.6 px at `relay-ticker 56.7s`. **After**: no `.ticker`, no `.ticker-label`, label layer 1612.8 px wide fitted `2.3937cqw`, `.lrun` crawl at `y388.8` | **A decision about which rendering is the product, then one implementation.** Either the region ticker is retired with the rest of the region model and the layer crawl inherits whatever of the 45% rule is worth keeping, or the announcement families stop shipping region-model so there is only ever one rendering. What must not stand is both. **Do NOT read this row as a licence to delete `fitTicker`**: on an install that still holds a region-model row it is the code that paints announcements. **THE DECISION WAS TAKEN ON 2026-09-16 BY WAVE 5 TRACK A, AND IT IS THE SECOND OPTION.** The announcement families stopped shipping region-model along with everything else: the shelf is forty layer-model looks, `nothing_region_model_is_seeded_any_more` holds both halves of that, and a fresh install therefore has exactly one rendering for a notice — the layer crawl. The five `Scroll · …` lower thirds are the shipped crawls and the five `Announce · …` looks are static full-screen notices, which is the distinction that keeps `templateKind`'s announcement rule (a text layer that SCROLLS) from claiming the static ones. **The row stays open for the same reason RG-141 does**: an existing install keeps a region-model row that something points at, so both renderings still exist in the field, and the assertion this cell asks for — one that distinguishes `.ticker` from `.lrun` — is still on neither path. **One thing Track A did NOT do and must not be read as having done**: the reduced-motion fallback for a crawl. `.lscroll .lrun` still sets `animation: none` under `prefers-reduced-motion`, inside a `white-space: nowrap; overflow: hidden` box, so a notice longer than its band is silently CUT OFF rather than paged. The wave 5 design asks for paging; that is renderer work in `TemplateRender`, it was out of Track A's scope, and the five shipped crawls are now the surface it applies to | RG-141 for the other half of the same conversion · RG-13 | **P1** | M | The assertion belongs on BOTH paths and today exists on neither: nothing in either suite distinguishes `.ticker` from `.lrun`, so a green suite says nothing about which of the two a church is looking at **PARTLY CLOSED 2026-09-18, and the half that is left is named.** The renderer half this cell assigns to `TemplateRender` — the reduced-motion crawl — is fixed. Measured at 1920×1080 on the shipped `Scroll · Banner` with a 185-character notice and `prefers-reduced-motion: reduce`: `clientWidth 1651` against `scrollWidth 4299`, so **2648px, 62% of the notice, was never painted at all**, while `onFit` reported `{ scale: 1, legible: true, clipped: false }`. The region ticker was worse — 1617 against 5080, 68% gone, same report. A crawl shows a notice longer than its band by moving it past a window, so the words that do not fit are shown LATER; with the animation off later never comes and the box is still clipped. `crawls(L)` is now the ONE fact deciding the paint, the clip and the measurement together, because a notice painted still while the fitter believes it is moving is the same defect wearing the fix. A stopped layer crawl becomes an ordinary text layer: it wraps inside `.ltext`, which is a real percentage box, and is fitted and REPORTED — 1651/1651 and 181/180 at 46.76px, `{ scale: 0.812 }`. **Wrapping the REGION band was tried first, measured and rejected**: it grew that band from 115px to 236px and lifted its top edge from y965 to y844, a legacy footer silently becoming a fifth of the wall over a camera. So the region band keeps its geometry (y972 h108 against y965 h115) and `fitStillBody` shrinks the notice into it — `{ scale: 0.440, legible: false }`, whole and said. It measures the TRACK, never the run: an `inline-block` under `nowrap` sizes its own box, the same trap `.ticker-label` and `.content` both fell into. **The assertion this cell asks for now exists**, in `noticecrawl.test.js`: a region row paints `.ticker`/`.ticker-run` and no `.lrun`, the same row converted paints `.lrun` and no `.ticker`, and the converted crawl's box reaches nowhere near the bottom of the frame. It is a CHARACTERISATION test and its header says so — the day the conversion is made design-preserving it should turn red and be replaced, not deleted. **What is NOT fixed, and why**: a `scroll: true` region row is a BAND crawl by construction, which is what the shipped `Scroll · …` family reproduces, so the repair is that `regionsToLayers` should give it a band rather than a full-frame layer at y20 h54. That function is in `src/lib/layers.js`, which another worktree was editing in the same session; two agents rewriting one converter is how this repository gets two of something in the first place, so it is reported rather than done |
| ✅ RG-141 | **The countdown a FRESH INSTALL actually renders collapses to about a sixth of its designed size.** 30 of the 37 seeded templates are region-model (§0a: read from SQLite with no frontend attached), and `showDefaultCountdown` requires `layered` — so until somebody opens the Templates workspace, a countdown takes the REGION branch. On seeded `Classic · Timer` (10) at 1920×1080 the label paints at **0.410226cqw = 7.88 px** and the digits at **1.57779cqw = 30.3 px** in a `312 × 43 px` blob in the centre of an otherwise empty screen. The template declares `verseSize 5` and the renderer draws a countdown at twice that — **192 px designed, 30 px painted** — and the 7.88 px label is still clipped at both ends | **This is the pre-service screen a church sees on its first Sunday, and it is unreadable from the second row.** It is rule 37's shape — a fit that settles on a size it MEASURED as fitting and is unreadable anyway. **An earlier version of this row explained the collapse and the explanation was wrong**, so the explanation is gone rather than patched: `fitOne`'s predicate is `scrollHeight > clientHeight + 1`, so the measured `44 / 43` is the loop's STOP condition and not unfitted residue, and `FIT_MIN_SCALE` is `0.01` while 30.3 px of a designed 192 px is scale `0.158` — thirty steps clear of the floor. What is recorded is the outcome. Scale `0.158` is below `MIN_LEGIBLE_SCALE` (0.45), so `onFit` should be reporting `legible: false`; this pass did not capture `onFit` on the output page, so whether anything was reported, or shown, is **unmeasured**. **It is not the label** — 7, 17 and 60-character labels all produced byte-identical sizes. **It is not the region fitter in general** — on the same template a verse fits correctly (`John 3:16` at the full declared `5.225cqw` / 100.3 px; `Esther 8:9` shrinking to `2.82338cqw` and settling with `scrollHeight == clientHeight`). Only the countdown collapses | `audits/DESIGN-2026-09-16-WAVE2.md` §1b, measured against the real backend on a virgin shelf with Templates never opened and `L\|7 R\|30` verified in SQLite before and after every fire. Also on ids 6 (`83.2 × 34.1`, digits `1.2758cqw`) and 30 (`364.9 × 49.6`). **And on a band template a region countdown paints nothing at all**: `Lower Third` (3) and `Lower Third · Timer` (25) render `.content` at `1920 × 0` with no text — `countdownAllowed = !!countdownTo && !bandMode`, deliberate and commented. That same row paints a full-screen countdown once converted, which is RG-140's split on a second content kind | **Not proposed here.** The measurement points at `fitOne`'s countdown path — the `!verse.classList.contains('countdown')` guard skips the `fitScale` seed, so the loop starts at 1 and shrinks by 5% a round against a box that reports overflow at every size — but the audit measured the outcome, not the cause. **The cheapest mitigation is not a fit fix**: the conversion that makes this go away already exists and already runs, so making it run before the first service (or seeding the families layer-model) removes the branch entirely. That is the same decision RG-140 needs, which is why the two belong together | RG-140 is the same conversion seen on announcements · RG-139 is the layer half of the countdown, and wave 5 Track A widened it from one shipped reproducer to six | **P1** | M | **The DEFECT is pre-existing, by diff rather than inference** — the whole region `fitOne` body is identical to the merge base `7851a79`, and the only change in that neighbourhood is `fitTicker`, appended after it. **The SHIPPING SURFACE is new in this wave, and the row must not be read as saying the wave added no exposure**: `git show 7851a79:src-tauri/src/db/templates.rs` contains the string `Timer` ZERO times. Before this wave each family had four kinds and a fresh install shipped no template whose purpose was a countdown; it now ships five, all region-model, and this is what all five render. `templates.rs` says it itself — *"Media and Timer are new"*. The wave did not write the defect and it did put a congregation-facing surface in front of it where there was none. A test must fire a countdown on a REGION-model Timer template and assert a floor on the painted size — no existing test fires a countdown on the region branch at all, which is why a green suite has never had an opinion about this. **PARTLY ADDRESSED on 2026-09-16, on the final whole-branch pass, and the row stays open.** The four FULL-SCREEN Timer rows — `Classic`, `Aurora`, `Ember`, `High Visibility` — are now seeded in the LAYER model, as exactly the bytes `TemplateGallery.upgradeLegacyToLayers` would have written on the operator's first visit to the Templates workspace, with stable layer ids in place of its generated ones. They therefore take `.cd-default`, which declares its designed size, clips, sits inside the fit loop and reports through `onFit`, so the shipping surface this wave ADDED is closed: a fresh install no longer ships a template whose purpose is a countdown and which renders the blob. Pinned at the time by `the_full_screen_timers_are_seeded_in_the_layer_model`, a test that no longer exists — see the 2026-09-16 wave 5 note at the end of this cell. **The fifth, `Lower Third · Timer`, was deliberately NOT converted** and was pinned as such by `the_keyed_timer_is_deliberately_not_converted`, also now gone: on the region path `countdownAllowed` refuses a countdown on a band and `.bandless` makes the band itself transparent, so what it paints is nothing — the clean camera the transparency law would have given anyway. The layer model has no equivalent refusal, so converting it would replace nothing with either the digits or an empty bar over a live camera. **What is NOT closed, and is why this row is still ⏳**: the region countdown path itself is untouched, and every other region-model seeded template still SHOWS `countdown` — the audit's own ids 6 and 30 measured `83.2 × 34.1` and `364.9 × 49.6`. A church that fires a countdown at a scripture or lyrics template, which the `shows` list permits on all five kinds, still gets the blob. And the conversion hole the keyed row exposes is real on the LAYER side too: `showDefaultCountdown` never asks `!bandMode`, so any band template that HAS been converted — every seeded band, after one visit to Templates — paints a full-screen countdown over its camera. That was found on this pass, is pre-existing on both counts, and was left alone rather than fixed inside a fix dispatch: the honest repair is more than one condition (the verse and reference layers are already suppressed under a countdown, so gating the default block alone would paint a label on the band instead of nothing). **MOSTLY CLOSED ON 2026-09-16 BY WAVE 5 TRACK A, AND THE ROW STAYS OPEN FOR ONE CASE.** The shelf was rebuilt as forty layer-model looks and **nothing region-model is seeded any more** — `builtin_templates()` and `theme_templates()` both stopped being seeded, and `seed_templates` now lands the shelf. A fresh install therefore cannot reach the region countdown branch at all, on any template, which is the repair this row and RG-140 jointly asked for: not a fix to the region fitter, but no longer seeding anything that reaches it. Pinned by `db::templates::preset_template_tests::nothing_region_model_is_seeded_any_more` (both halves: the two region lists are empty AND every shipped row carries a real layer stack), by `db::tests::seeds_the_builtin_templates`, and on the frontend by `shelf.test.js`'s *is entirely layer-model*. The two tests this cell used to cite were deleted with the families they asserted about. **What keeps it open**: an EXISTING install. Retirement removes a legacy row only when nothing points at it through the four doors, so a church whose channel, plan cue, content look or configured default names a region-model template keeps that row — correctly — and keeps this branch with it. The region `fitOne` countdown path is still untouched, and no test fires a countdown on it. **And Track A widened RG-139 in the course of closing this**: six of the forty carry a `countdown`-bound layer where one did **CLOSED 2026-09-18, by fixing the branch rather than converting the rows, and the cause is the BOX and not the fitter.** `.content` in region mode declares no width and no height: it is a flex item with `max-width: 90%` and `max-height: 92%`, so it is shrink-to-fit and its box IS its text. That is correct for a verse and this row already measured it as correct — prose wraps, grows to the caps, and only then genuinely overflows. A countdown is one `nowrap` line under `.countdown { line-height: 1.05 }`, a leading deliberately tighter than the face's own line box, so **the glyph box is a fixed FRACTION taller than the box measured around it, at every size**, while `fitOne`'s stop condition is an ABSOLUTE one pixel. Measured, scale by scale: residue 12px at 1, 10 at 0.8, 8 at 0.6, 5 at 0.4, 3 at 0.3, 2 at 0.15, 1 at 0.135 — where it stops. A loop whose overflow scales with the thing it is adjusting, judged against a tolerance that does not, can only terminate by going tiny. **The repair is the box, not the tolerance**: widening the tolerance to a fraction would blind the loop to real overflow on the one kind where the check is load-bearing. `.content.cdbox` restates the existing 90%/92% caps as sizes, so `clientHeight` stops following the type and the search lands on the true fit — which is what the LAYER branch has always had for free, because `.ltext` and `.cd-default`'s lines are percentage boxes. **After, 1920×1080 on the same row**: `.content` 1486 × 782, digits at the full declared **192px** (was 25.97), label 46.08px (was 6.23), `onFit { scale: 1, legible: true }` (was 0.135 / false). With `verseSize 40` — a countdown designed at 1536px — the loop still shrinks, to 642px, and still reports `{ scale: 0.418, legible: false }`, so rule 37's genuinely-unfittable case is unchanged; and a verse on the same row is untouched, no `cdbox`, full declared 5cqw, scale 1. **Why the branch and not the rows.** A fresh install cannot reach this any more, so what is left is an existing install — where retirement keeps a legacy row precisely when a channel, a cue, a content look or the default still names it, which is a row a church deliberately uses. Unreachable on a fresh install is not fixed, and this register already records the product intending to keep the path working (`Lower Third · Timer`, deliberately not converted). Converting the survivors would need either a Rust re-implementation of `regionsToLayers` — and two converters is exactly how RG-140's two renderings came to disagree — or a startup pass through the JS one, which `TemplateGallery`'s own comment calls "a separate change with its own evidence, not a line in this one". Neither belongs inside a rendering fix. `regioncountdown.test.js` reads the region literals out of the Rust seed rather than inventing a shape, and is watched to fail both ways: without the class, and with the class but no width and height |
| ✅ RG-142 | **`ensure_retired_presets_are_gone` matches on exact BYTES, and on a used install the bytes have already moved — so twenty of the twenty-one frozen rows can never be matched and are never deleted.** Two independent rewrites, either of which alone is fatal to a string comparison. **One**, `TemplateGallery.upgradeLegacyToLayers` runs on mount and SAVES, turning every region-model row's `region_config_json` from a `regions` list into a `layers` stack whose ids come from `newId()`, which folds in `performance.now()` — so a byte match is not unlikely, it is impossible. **Two**, `style_json` returns through `serde_json`'s BTreeMap on save and comes back alphabetically ordered, where the frozen record keeps the seed's hand-written key order. Identical JSON values, different strings. **Exactly one of the twenty-one escapes both**: `Lower Third · Scripture`, the only layer-model entry in the frozen record, which `upgradeLegacyToLayers` skips because its filter is `!isLayered(t) && Array.isArray(t.layout?.regions)` and that row has layers and no regions. So the migration works on the one row a name collision made load-bearing, and is a no-op on the other twenty | **Safe for data, and that is not the same as harmless.** Nothing an operator has is destroyed — the function's own comment says failing in this direction was the choice, and it is the direction it fails in. What does not happen is the cleanup it was written for: the church keeps the doubled gallery the family reshape was supposed to tidy, thirty-one old rows beside twenty-six new ones, and no instrument says so. **The reachable set is an install carrying the old seed that has NEVER had its Templates workspace opened** — on a fresh install there is nothing to retire, and on a used one there is nothing matchable. **The consequence the wave documented does NOT follow from this, and that was checked rather than assumed**: the family-of-four outcome needs the OLD `Lower Third · Scripture` to survive, and that row is the one entry the conversion leaves alone, so it is retired and the family's keyed member of that name is installed. It survives only where a person has EDITED it, which the migration is deliberately right to respect | Measured 2026-09-16 on the final whole-branch fix pass, against a COPY of a real database — `~/Library/Application Support/com.relay.app/relay.db.bak-20260823-190327`, a genuine pre-wave install at `user_version 2` with 31 templates and 4 `app_settings` keys — driven through the real path (`db::open()` under `RELAY_DB_PATH`, i.e. `Connection::open` then `PRAGMA foreign_keys = ON` then `migrate`). After boot: `user_version 5`, **57 templates, 0 of the 21 frozen rows retired, all 31 originals surviving**. A second boot was byte-identical across every template row and every settings row. The live `relay.db` shows the same 57. First mismatch, quoted: stored layout `{"align":"center","layers":[{"dim":0,…,"id":"b15_99594",…` against frozen `{"regions":["verse_text","reference"],…}`; stored style `{"accent":…,"background":…,"font":…}` against frozen `{"font":…,"background":…,"accent":…}`. The 20-versus-1 split is read off `data/retired_presets.json` itself. The live database is the direct evidence for the exception: the shelf's `Lower Third · Scripture` is gone and the family's own member of that name is installed, on a database whose 19 family rows have all since been converted to layers | **Not proposed as a patch, because the shape of the right answer is a decision.** The follow-up the reviewer identified, recorded so nobody re-derives it: ask the question the diagnostic sentence actually asks — *is the SEEDED row of this name installed?* — by matching `name` against `all_presets()`'s layout and style rather than `retired_presets()`'s. Silent on a healthy fresh install, speaks on one where the name is held by something that is not the seeded row. **One measured wrinkle to carry with it**: the seeded `Lower Third · Scripture` is itself region-model (`BAND_SCRIPTURE`), so `upgradeLegacyToLayers` converts it too — in the live database all 19 family rows are now layer-model with generated ids — and that predicate would then report a false positive on a converted install for the same reason this row exists. Any check that must survive the conversion cannot be a byte comparison at all; it needs an identity the conversion preserves. That is the decision, and it is the same one RG-140 and RG-141 need: whether a seeded row's region form or its converted form is the product | RG-141 and RG-140 are the same conversion seen on the wall · DECISIONS §88 | P2 | M | **And the trade this pass made, stated plainly rather than left to be found.** The diagnostic beside this migration was narrowed on 2026-09-16 from *is a row of this NAME present* to *is the RETIRED row still present, by bytes*, because the name-only version was true on every healthy install from the second boot onwards and printed that a template was not installed over an install where it was — rule 35 in a log. The narrowed one is silent when the retired row's bytes have moved, which is the class this row is about: a church that EDITED its old `Lower Third · Scripture` keeps it, never receives the family member, has a Lower Third family of four — and is now told nothing. Both versions are wrong in one direction each, a false positive on the universal case against a false negative on the rare one, and the follow-up above is the version that would be right in both. Held today by `db::templates::retired_preset_tests::{the_contested_name_reports_nothing_once_the_retired_row_is_actually_gone, the_contested_name_still_reports_when_the_retired_row_really_is_kept}`. **What no test reaches is this row itself**: every test in that module inserts the frozen fixture verbatim, so not one of them has ever asked what happens after `upgradeLegacyToLayers` has been over the same rows. A fixture that converts before it retires is the missing instrument **CLOSED by wave 5 Track A, and this row was never flipped.** `ensure_retired_presets_are_gone` gained an IDENTITY pass that runs before the byte pass and asks the question the conversion cannot move: does the row carry a frozen `legacy.` seed key, and is `edited_at` still null. The byte pass is kept additively rather than deleted, because it is the only one that works on an install whose Templates workspace has never been opened, and the only one that can run at all when `legacy_seed_keys.json` cannot be read. Held by `a_row_the_gallery_already_converted_is_still_retired`, which inserts a row in the shape `upgradeLegacyToLayers` leaves behind rather than the frozen fixture every other test in the module inserts verbatim — which is precisely why none of them could see this. |
| ✅ RG-143 | **`guardedRead` takes three arguments and two call sites pass a fourth, so the reset each of them asked for was dropped on the floor.** `capture.js`'s read guard is `async function guardedRead(key, run, fallback)`; `loadDefaultTemplate` and `loadServiceTarget` each pass a closure as a fourth argument, and each carries a comment at the call site saying in as many words that a fallback VALUE cannot carry a side effect, so the reset is explicit. It was not explicit. JavaScript discards the extra argument in silence and both comments described a behaviour the function had never had | **A store that says the same thing whether the read worked or not is rule 35 one layer down.** A failed `get_setting` left `defaultTemplateId` holding an id the backend could no longer confirm, which is the id every render surface then resolves its final fallback template against; and it left `serviceTargetMinutes` holding a length nobody had answered for, which is what the on-air stopwatch counts against. Neither surface had any way to tell a stale figure from a confirmed one — `readErrors` recorded the failure, and nothing beside the figure read it | Found on 2026-09-16 while reviewing wave 3's Track D, by reading `capture.js:2433` against its own call sites at `:261` and `:1923`. Reproduced as a test before the fix: with the read rejecting, `defaultTemplateId` stayed at 7 and `serviceTargetMinutes` at 75, both of which are the previous good read rather than the absence the call site asked for | `guardedRead` takes an optional fourth `onFail` and calls it in the catch. Optional deliberately: most reads degrade to a value and want nothing there, and a required argument would put an empty closure on thirty-odd call sites for the benefit of two | RG-95 is the same guard's other half · rule 35 | **P2** | S | `readstates.test.js`, *"a failed read resets the store its call site asked to reset"* — two cases, each watched to go red by removing the `onFail()` call from the catch, which is the defect exactly as it shipped |
| ✅ RG-144 | **`package-lock.json` is a fourth copy of the version and `npm run version:check` cannot see it.** Rule 19 names three files — `tauri.conf.json`, `package.json`, `Cargo.toml` — and `scripts/version.mjs` is the one place any of them is read or written. The lockfile carries the version twice more, at its top level and under `packages.""`, and nothing in the repository compares them. Found at `0.2.0-2` against a `package.json` on `0.2.0-3`, with `version:check` passing | **Not the failure rule 19 exists to prevent, and worth saying which failure it is instead.** The updater manifest is stamped from `tauri.conf.json`, so a stale lockfile version cannot make an install compare equal and stop updating — that mechanism is untouched. What it can do is make a clean `npm ci` disagree with the working tree about what is being built, and put a spurious one-line diff in front of the next person who runs `npm install`, which is how the drift was found at all | Observed 2026-09-16 on `feat/wave3-timers`: `package-lock.json` at `0.2.0-2`, `package.json` at `0.2.0-3`, `npm run version:check` green. `npm install` corrected the lockfile as a side effect | **Not fixed, and the choice is a real one.** Either `version:set` learns to rewrite the lockfile's two copies, or `version:check` learns to read them and say so. The second is smaller and keeps `npm` the only writer of a file `npm` owns | Rule 19 · RG-83 | **P3** | S | Nothing pins it. `version:check` asserts three files agree and is right about all three **CLOSED 2026-09-17.** `scripts/version.mjs` reads and writes the lockfile's two copies alongside the other three files, and `--check` reports a drift by naming both figures and the command that repairs it. It is deliberately a WARNING and not a failure, and deliberately not folded into rule 19's trio: those three are what the updater compares, and a drift there means no church ever updates again, silently. A lockfile drift means `npm ci` fails loudly in CI. Flattening the two into one message would make the more serious of them easier to ignore. Proved by drifting the lockfile on purpose and watching the line appear, then restoring it and watching it go. |
| ✅ RG-145 | **The console says a word is on the preacher's monitor after a panic control has taken it off.** `capture.js`'s `stageAlert` store is written only by `sendStageAlert`, and nothing else in the frontend clears it. `clearScreens` and `blackScreen` do not, because they never had a reason to: until DECISIONS §89 the alert genuinely did survive both controls, so the store and the tablet agreed. §89 reverses the tablet and leaves the store where it was. `Dock.svelte`'s Quick tools card then paints an `on stage` badge and enables **Take down** for a word that is already down, and the badge's own comment claims exactly what is no longer true — *"The badge says WHERE it is, and only while it is there… what the preacher's monitor is painting at that moment"* | **Rule 35, on the surface that reports what one screen in the room is showing.** The store's own doc comment narrows its claim to *"a mirror of what Relay SENT"*, and that narrowing is honest about the code and beside the point at the badge: an operator reading `on stage` in amber-adjacent red at 11:05 reads it as the present tense, which is the only tense the badge is written in. The harm is bounded — nothing a congregation sees, and **Take down** still works, so one press resolves it — but it is the one surface that could have told an operator the alert had gone, and after §89 it says the opposite | Found on 2026-09-16 while landing wave 3 Track C, by reading `capture.js:2140` and `:2155` against `Dock.svelte:1101-1123` after the `alert = ''` reset landed in `Stage.svelte`. Not a regression in the stage page: the page is now correct on both the live and the reconnect path, which is what §89 decided. The staleness is entirely on the console side | **Fixed where the row said it should be.** `panicRun`'s success branch sets `stageAlert` to `null`, so both panic controls take the console's mirror with them, and only on success: a panic that FAILED has taken nothing off any screen, and a console that said otherwise would be rule 15 one surface along. The same "after, never before" discipline `sendStageAlert` already documents for itself. One line, at the choke point both controls already run through, rather than one in each wrapper — rule 36, on a path where the repository has four bugs whose shape is a check on one surface and not its twin | DECISIONS §89 · rule 35 · rule 15 · RG-143 is the same store discipline one layer down | **P3** | S | `panic.test.js`, *"a panic control takes the console mirror of the word with it (RG-145)"* — three cases: a clear lets the word go, a blackout answers the same way, and a FAILED clear leaves the mirror alone. The first two were watched to fail with the line absent; the third passes either way and is the control, because a fix that cleared the mirror unconditionally would be the opposite defect and nothing else would catch it |
| ✅ RG-146 | **The wave's own Start control was not clickable at its centre in the window Relay opens in.** `elementFromPoint` over the middle of Live's **Start timer** returned `.r-select.xpick.xdur` — the transport column's transition-duration picker — at **1320 × 860**, which is `tauri.conf.json`'s own default window size, and at 1280 × 800. The button was `x 567.4–641.6, y 327–349`; the select was `x 562–648, y 335.2–357.2`; they overlapped over a 74 × 13.8 px region containing the button's centre and its bottom-right corner, and only its top-left corner still hit the button. Both are `position: static, z-index: auto`, so nothing chose this stacking order. The cause was one level up and PRE-EXISTING: `.rack` is 320.2 px tall inside a 268 px `.con-top` whose `overflow` is `visible`, so the transport painted 52.2 px past the monitors row at 1280 and 36.4 px at 1320. Clear at 1600 × 900 and 1920 × 1080 | An operator clicking the middle of a button got a transition duration instead of a programme timer, on the window size a church actually gets, with no error and no sign that anything went wrong. **It was not a panic-control finding** — `Clear screens` hit-tested as itself at every viewport in the pass, including 960 × 640, and still does — but it is rule 44's shape one control along, and `qa-inventory` cannot see it: it reported 0 handlerless buttons and 0 unnamed controls throughout, and was right both times. Recorded in the same row: the band's comment says wrapping *"costs it one row of height and nothing else"*, and with six timers it is two rows at 1920 and **three** at 1280 and 1320, while the slide grid below fell from 381.6 px to **158 px**. **That second half is NOT closed by this fix** — the band still wraps to three rows at 1320, and the grid is now smaller again by the height the rack had been painting over | `audits/DESIGN-2026-09-16-WAVE3.md` §10, measured on the console against a MOCK bridge — a layout claim, never a behaviour claim. Four viewports, `elementFromPoint` at three points per control. **Reproduced and then re-measured the same way on 2026-09-17**, on `feat/wave3-fix-live`, over ten controls in the region (the band's four, the rack's five, and `Clear screens`) at six viewports. BEFORE, at 1320 × 860: `.con-top` 283.8 px, `.rack` 320.2 px, overflow 36.4 px, Start timer `y 342.8–364.8` → hits `select.r-select.xpick.xdur`. AFTER: `.con-top` 320.2 px, overflow **0**, Start timer `y 379.2–401.2` → hits **itself**, and all ten controls hit themselves. Same result at 1280 × 800 (overflow was 52.2 px), 1366 × 768, 1600 × 900 (overflow was 23.2 px, too small to cover the centre, which is why the audit read it as clear), and unchanged at 1920 × 1080, where the clamp was always taller than the rack | **FIXED** on `feat/wave3-fix-live`: `.con-top`'s `height:clamp(268px,33vh,364px)` is now `min-height:clamp(268px,33vh,364px)`, so the row keeps every promise the clamp made about the small case and lets a taller `.rack` push it down instead of painting outside it. It is the register's own preferred repair — the row giving the rack the height it needs — and it is what `@media (max-width:1180px)` has always done (`height:auto`), so the default now agrees with the narrow steps rather than inventing anything. **Clipping was rejected deliberately**: the thing in the overflow is the transition pair, so `overflow:hidden` there makes a control unreachable rather than merely misplaced, which is strictly worse. **The band was NOT moved** — moving it would have left the next control placed there in the same position. **What it costs, stated**: below about 970 px of window height the row takes the height it was already painting over off the slide grid — 36.4 px at 1320 × 860 (grid 197.8 px), 52.2 px at 1280 × 800 (190 → 137.8 px) and at 1366 × 768 (158 → 105.8 px). Nothing moved off-screen: the console region did not gain a scrollbar at any of those sizes, and the grid sits in a `.pane` whose `.pane-body` is `overflow-y:auto`. 1920 × 1080 is byte-for-byte unchanged | Pre-existing `.rack` overflow; wave 3 Track E put a control in it | **P1** | S | `src/lib/liverackfit.test.js` — four tests, watched to FAIL on the pre-fix tree on the exact declaration that caused it (`.con-top must not declare a definite height`). It holds the stylesheet contract (a floor, never a height its content can exceed; and never bought by clipping, which would hide the pickers; `.rack` stays `align-self:start` with no height of its own) and, from the MOUNTED component, that the two elements in the collision really are the ones it names — the pickers inside `.con-top`, the band its immediate next sibling, Start timer inside the band. **What it cannot prove, said plainly in its own header**: it cannot run `elementFromPoint`. jsdom computes no layout and this repository has no browser driver and no Playwright (`QA_HARNESS.md` Part 1). The hit tests above are a hand-driven browser pass and they are the evidence; the test is the tripwire on the mechanism behind them |
| ✅ RG-147 | **A programme timer past one hour is clipped on the preacher's screen, and what is left reads as a valid time.** `.tval`'s size is `min(clamp(16px, calc(92cqw / var(--tmrs) / 6 / 0.62), 64px), 9cqh)`. The `/ 6` is a SIX-character budget and `formatCountdown` emits **seven** (`h:mm:ss`) once a timer passes an hour. Measured against the real backend with six timers, one of 95 minutes: at 1280 × 720 the text is **215.3 px** in a **199 px** `overflow: hidden` box (16.3 px gone, `scrollWidth − clientWidth = 16`); at 1024 × 768, 171.0 px in 156.3 px. Clean at 1920 × 1080, where the 64 px clamp cap binds. The measured mono advance is 0.600, so a seven-character value is `7 × 0.6 / (6 × 0.62) = 1.129` of its box and overflows by ~12.9 % **whenever the formula rather than the cap is in force** — any width below roughly `259 × timers` px, so six timers clip on anything under about 1554 px | The screenshot reads **`1:30:1`** with the last digit sliced through and running into the neighbouring `0:00` with no gap, so two figures read as one number. A preacher glancing down mid-sermon reads a clock that is wrong by up to ten seconds and looks entirely normal. A stage screen is usually a 1280 or 1024 tablet, and a ninety-five-minute clock is an ordinary thing for a church to set — a livestream, a whole service | `audits/DESIGN-2026-09-16-WAVE3.md` §8. Text width by `Range` over the text node, because `.tval` is a block filling its flex item and `scrollWidth === clientWidth` is TRUE while the text inside it is clipped — the trap that would make the obvious test pass | **Closed 2026-09-17.** The budget now comes from the string the row is painting. `Stage.svelte`'s `progCh` reduces the programme to the length of its longest RENDERED value and hands it to the stylesheet as `--tch`, and `.tval` divides by that instead of by six — the same instrument `figCh`/`--ch` already is for the figure row across the bottom, not a second one. Modelled on the shipped declaration at the audit's own viewports, six timers with a 95-minute clock: 43.9 px of type and 184.6 px of text in the same 199 px box at 1280 × 720, against the measured 215.3 px before. **And a figure that still does not fit ellipsises rather than slicing** — `0.62` is an assumed advance against a measured 0.600 and the 16 px floor can still bind on a narrow enough row, so `1:30:…` says it did not fit where `1:30:1` said something false. That is rule 37's discipline on a page with no operator to report to: shrink and show, and be visibly truncated rather than quietly wrong. **What it deliberately does not do**: build a second fit loop, or measure anything at runtime — the row is still sized by one CSS declaration | `.progrow` had never been in a layout engine; the agent that wrote it said so. RG-154 is the height half of the same declaration, closed with it | **P1** | S | `stageprogrow.test.js` pins the declaration (the divisor is a var fed from the text; the digits ellipsise) and models its arithmetic, reproducing §8's browser figures to a tenth of a pixel — 51.276 px, 215.3 px, 199 px — which is why the model is trusted. `timers.test.js` pins the behaviour: `--tch` is the longest rendered value, gives the budget back when the long timer goes, and follows the clock across the hour boundary. **Neither measures a painted pixel — jsdom computes no layout**, so the `Range` measurement this row asked for is not reproduced here; both were watched to fail against the pre-fix tree |
| ✅ RG-148 | **The stage programme row wears no warning at any threshold, and `warn_ms` crosses the wire with no reader.** `timer_frame_json` carries it and it was read off the hub verbatim — `{"id":9,"label":"Wrap up",…,"warn_ms":30000}` — while `Stage.svelte`'s `programme` mapping takes `id`, `label` and `countdownRemainingMs` and stops. `.progrow` was measured with `querySelectorAll('.warn').length === 0` at every viewport tried, including with a timer inside its own explicit 30-second window and with an expired one at `0:00`. The comment at `Stage.svelte:257` still reads *"`warn_ms` rides in the frame and is deliberately not read here yet: the warning threshold is wave 3 Track D's, on all three surfaces at once"* | Track D has landed, so the comment describes a decision the tree has since made, which is the failure `docs/` has been corrected for repeatedly. The practical half: the surface whose entire purpose is telling a preacher how long is left is the one surface with no signal that the time is nearly gone, while the congregation's screen — which is not the preacher's instrument — has one | `audits/DESIGN-2026-09-16-WAVE3.md` §5c, against the real backend, with the frame quoted from the wire | **Closed 2026-09-17.** `programme` carries `warn: countdownWarning(r.ms, countdownTotalMs(t), t?.warn_ms)` — the one rule with the per-timer override it already takes, and no fourth reading of when to worry. `.tmr.warn .tval` wears the countdown's own red, which is the colour this page already uses for this rule on the figure row and the rail, and is none of amber, cyan or amethyst. The colour is stated unconditionally, the `cdwarn` pulse sits inside `prefers-reduced-motion: no-preference` and a glow inside `reduce`, matching the page's existing treatment rather than the dock's colour-only one. The stale comment is gone in the same commit. **What it deliberately does not do**: reach the threshold an operator CHOSE in Settings. A timer with no `warn_ms` of its own still falls to the shipped 60 s on this page, because `setCountdownWarnDefault` never reaches the stage bundle — that is RG-149, it is somebody else's, and nothing here was reached across to get it | RG-149 is the other half of the same chain and was closed the day after this row, which is where the operator's own figure finally reaches this page | **P2** | S | `timers.test.js` — a row inside its own explicit 30 s window carries the class and one outside it does not, asserted against `countdownWarning` itself rather than a literal so a fourth copy of the rule that agrees today would still fail; and a row that has already run out does not warn. `stageprogrow.test.js` holds the colour outside every motion query, the pulse inside `no-preference`, the glow inside `reduce`, and the red off the three law colours. Watched to fail against the pre-fix tree |
| ✅ RG-149 | **A warning threshold anybody chooses reaches neither a congregation screen nor a stage, and the Settings default reaches only the console.** Three breaks in one chain. **(a)** `countdownWarning(remainingMs, totalMs, warnMs)` gained its third argument in Track D and all three readers call it with two — `TemplateRender.svelte:1145`, `Stage.svelte:220`, `Dock.svelte:589`. **(b)** A `Both` timer has no field to carry one: `start_countdown` hard-codes `warn_ms: None` (`main.rs:2867`) and neither `timers::project_both` nor `main::countdown_content` has a warn field, so the congregation wire form cannot express a chosen threshold even if a surface read one. **(c)** `layers.js`'s `warnDefaultMs` is module state whose only writer is `stores/capture.js`, and `output.html` and `stage.html` import the shared `layers-*` chunk but not `capture.js` — after `npm run build` the settings key `countdown.warn_ms` appears **once** in `assets/main-*.js` and **zero times** in `assets/output-*.js` and `assets/stage-*.js`. `ServicePlanner.addCountdownCue` gained no per-cue field at all | `Settings → General → Countdown warning` is a control that saves a preference the wall and the preacher's page do not read — the defect shape closed on 2026-09-10 for seven other controls, reopened for this one. An operator who sets thirty seconds because their pre-service countdown is short gets thirty seconds on the console and the shipped sixty everywhere a person is actually looking. Bounded: the DEFAULT rule still works on the wall (measured), so nobody loses a warning they had; they lose the one they asked for | `audits/DESIGN-2026-09-16-WAVE3.md` §5d. The bundle counts are from a real `npm run build` on this tree, not from reading the import graph | **Closed 2026-09-17, all three breaks.** **(a)** The three readers pass the third argument — `content?.countdown_warn_ms` on the wall, `cdContent.countdown_warn_ms` on the preacher's page, `$live?.countdown_warn_ms` in the dock — and the rule is untouched: `layers.js::countdownWarning` still ranks chosen, then configured, then the last tenth of a short span, and `countdownRemainingMs` is still the one reader of how long is left. **(b)** `timers::project_both` gained `countdown_warn_ms` and is still the ONE place the congregation wire form is filled in (rule 36); it rides as `OutputContent::countdown_warn_ms` and through `kiosk_content_json` beside the other four. `start_countdown` took `warn_ms: None` as a constant and now takes it from the caller. **(c)** The configured default is DELIVERED rather than read, on frames the two pages already receive: `countdown_warn_default_ms` on the content frame, which is the frame rule 43 retains and replays on `hello`, and `warn_default_ms` beside `timers` on the programme frame, which is what reaches a stage tablet first when a clock is running before anything has been fired. It is stamped at the one content door (`broadcast_with_clock`) rather than at `countdown_content`'s three callers, and held as machine state (`channels::CountdownWarnDefault`) warmed at launch and kept in step by `set_setting`, the one writer of the row. **No new hub frame kind**, so `FRAME_VERDICTS` and both completeness tests are untouched. Measured after `npm run build`: `countdown_warn_ms` 2× in `assets/output-*.js` and 3× in `assets/stage-*.js`, `countdown_warn_default_ms` 2× and 1×, and one standalone `warn_default_ms` in the stage bundle — against zero of all three anywhere in the tree before this branch. `countdown.warn_ms`, the settings KEY, is still main-only and correctly so: the pages are handed the figure, not the key. **What it deliberately does not do**: give anybody a control that CHOOSES a per-timer threshold. `ServicePlanner.addCountdownCue` still has no field for one and `startCountdown`'s new argument is passed as null by its only caller, so the chain now carries a figure nothing yet sets — which is the reverse of the defect but is still a gap, recorded here rather than implied. It also does not re-push a changed setting to a countdown already on a wall: the figure travels with the content, so an operator who moves the setting mid-countdown sees it at the next fire or re-aim. And it does not claim a colour: jsdom computes no layout and there is no browser driver, so every assertion below is about arguments, resolved figures and rendered classes | Track D Tasks 11 and 12 · RG-148 is the other half of the same chain and closed the day before | **P2** | M | `countdownwarnchain.test.js` — mounts `TemplateRender`, `Stage.svelte` and `Output.svelte` and asserts the figure the rule RESOLVES, with a control case in each direction (a chosen figure longer than the shared rule turns the digits red at 90 s; one shorter holds the colour back at 40 s), the delivery on the kiosk door AND the native door, and an unset default falling back to the shipped minute rather than to a window of zero. `countdownwiring.test.js` pins the dock's argument list. Rust: `timers::a_chosen_threshold_survives_the_one_projection`, `channels::the_warning_window_reaches_a_kiosk_screen_as_well_as_a_native_one` (which also asserts the carrier is a retained screen frame), `channels::the_programme_frame_carries_the_configured_warning_window`, and `pipeline::a_warning_threshold_with_no_deadline_is_still_an_empty_screen` — the sweep's other half, holding `is_countdown` to NOT recognising the new fields. Every one watched to fail against the tree before the fix; the Rust three failed as compile errors, because the fields did not exist |
| ✅ RG-150 | **A timer started during a rehearsal survives it, and the stage tablet is never told it exists.** The suppression itself is correct and was watched at the hub rather than at a Tauri event: `start_timer` inside a rehearsal printed `rehearsal: publish_timers SUPPRESSED — nothing left the machine` and no `timer` frame reached either client. What happened afterwards was the gap. The timer stayed in the registry — `list_timers` after `set_rehearsal(false)` returned `Rehearsal only` alongside the real ones — and ending the rehearsal published `clear` and `stage_next` and **no `timer` frame**, so the tablet's set stayed `[7, 9]` while the registry held `[7, 9, 11]` | The preacher's programme and the registry disagree from the moment a rehearsal ends, and the disagreement is invisible until something unrelated republishes — at which point a timer nobody started in this service appears on the monitor, unannounced, mid-sermon. It is the rehearsal sandbox leaking into the live room, which is the one thing rehearsal exists to prevent, on a path where the leak is delayed rather than immediate | `audits/DESIGN-2026-09-16-WAVE3.md` §6, against the real backend, both halves read off the wire | **Closed 2026-09-17 by the operator decision, which the audit correctly refused to take for itself: ending a rehearsal stops every timer started inside it.** The rejected alternative was to republish the set on the way out on the grounds the timers were real all along — the smaller change and the wrong product, because an operator who practises a twenty-minute sermon clock at ten o'clock would then find it on the preacher's tablet when the service starts, counting toward a moment that has passed. **The stop is a property of the TIMER, never a question asked of a screen**: `timers::Timer` carries `started_in_rehearsal`, stamped once by each of the two creators from `channels::rehearsing` at that instant, and `TimerRegistry::stop_started_in_rehearsal` reads the stamp and consults nothing else — the same discipline `Scope` already carries for the panic controls. `main.rs::end_the_rehearsals_timers` stops and THEN publishes, in that order, so the tablet is never shown a set that is about to change. **What it deliberately leaves**: a timer started BEFORE the rehearsal began survives it. It was never a rehearsal's timer, and the alternative reading — that a rehearsal exit clears everything — would take a real service's sermon clock off the preacher's tablet because somebody opened the rehearsal switch for ten seconds. That half is written down at `TimerRegistry::stop_started_in_rehearsal` and pinned, not left to a reading | DECISIONS §89 · Track B's rehearsal gate | **P2** | S | `e2e::a_timer_started_inside_a_rehearsal_does_not_outlive_it`, at the HUB (`qa::Kiosk`) because `publish_timers` emits no Tauri event and `e2e::Wall` cannot see this at all. It asserts both halves the audit measured and each was watched to fail on its own: the registry half reproduced `["Sermon", "Rehearsal only"]`, and with the stop in place but the publish removed the wire half reproduced an exit that told the tablet nothing. Beside it, `e2e::a_timer_that_predates_a_rehearsal_survives_the_end_of_it` holds the deliberate leave (on `Scope::Stage`, because the exit also clears the screens and a clear takes every congregation timer with it under DECISIONS §27 — an older guarantee, not this one), and `timers::tests::ending_a_rehearsal_takes_the_timers_it_started_and_no_others` states both directions where a window is not needed. `REHEARSAL_VERDICTS` and `FRAME_VERDICTS` both pass unedited: no publisher and no frame kind was added |
| ✅ RG-151 | **`B` leaves the preacher's "up next" standing, and `Esc` takes it down.** Driven with no clear before it, because the console's watcher keys off the truthy→falsy edge of `live`: a verse was fired, `set_stage_next('Up next', 'Psalm 23')` published, and `blackout` then published `{"kind":"black"}` **and nothing else**; the following `clear_screens` produced `{"kind":"stage_next","label":null,"text":null}`. The root is one line apart from its twin in `stores/capture.js:507-508` — `output://clear` does `live.set(null)` and `output://black` does not, so `Live.svelte`'s *"the preacher's up next must not outlive the content it was about"* subscription never fires on a blackout. The comment directly above those two lines documents this exact defect being fixed FOR THE CLEAR, at length, with the black listener unfixed underneath it | DECISIONS §89 quotes the stage page's own rule that *"the harsher control must never do less than the milder one… an operator who has just hit the emergency key cannot be asked to remember that it reaches three screens out of four"*, and this is that sentence failing one control along. A second consequence follows from the same line and was not separately measured: `$live` stays populated after a blackout, so any console surface reading it as *what is on the screens* is wrong after `B`. **Pre-existing** — the listener predates wave 3 — but §89 is what turns it from an oversight into a stated contradiction | `audits/DESIGN-2026-09-16-WAVE3.md` §7, read off the hub wire against the real backend | Not fixed by this pass. One line, and obviously right, and filed rather than fixed because this is an audit — `live.set(null)` in the black listener, beside the `screenBlack.set(true)` already there. Whether `screenBlack` should then be derived rather than parallel is the larger question the fix should ask | RG-145 is the same class of stale console mirror, one store along | **P1** | S | Watch the hub: fire content, set an up-next, blackout with no clear before it, assert the `stage_next` clear. A test that clears first passes today and proves nothing **CLOSED 2026-09-17, in one line, on the consolidation branch.** `output://black`'s listener now sets `live` to null as `output://clear`'s twin already did, one line above it, under a comment documenting the same fix. Nothing is on the screens after a blackout, so null is the truth here rather than a convenience, and `screenBlack` is what keeps a blackout distinguishable from a clear. Held by `blackupnext.test.js`, written to fail first and watched doing it: the clear case passed and the blackout case did not, which is the defect exactly. `liveacceptance.test.js`'s writer count moved from three to four and names the fourth, so it still notices a fifth. |
| ✅ RG-152 | **`show_timer` and `adjust_timer` have wrappers and no rendered control, so the wave's own way back onto a congregation screen is operator-unreachable.** Both are registered, both have wrappers in `capture.js`'s documented THROWS group held there by `timerwrappers.test.js`, and `grep` over `src/**/*.svelte` returns one hit for either name — a comment. `qa-inventory` reports **0 commands registered in Rust that no frontend caller addresses** and is right: it traces to the wrapper, one hop short of CLAUDE.md's own stricter test. What an operator CAN do: start a programme timer (Live's band, `scope` hard-coded to `'stage'`), see what is running, stop one, and re-aim the newest congregation countdown through the dock's transport. What they CANNOT: put a timer back on the congregation screens after a verse — the action the plan specifies as the explicit way back — or create a `Both` timer with a chosen `warn_ms` or `plan_item_id` | The wave's stated goal is that a re-aimed countdown can be brought back; the registry does that correctly and nothing rendered can ask it to. It is also the security reduction CLAUDE.md names: every registered command is invokable from the webview, and `show_timer` is one that puts content in front of a congregation | `audits/DESIGN-2026-09-16-WAVE3.md` §12 | **Closed 2026-09-17, on the operator's decision** (`docs/superpowers/plans/2026-09-16-wave3-timers.md`, "Operator decisions taken on 2026-09-17"). The control is in the **dock's Countdown block**, beside Start, Pause, Reset, ±1 and Clear: when a `Both` timer exists and a countdown is not what the screens are showing, the block says so and offers **Put back on screens**, which calls `showTimer` with that timer's id. Live's programme band — the home this row proposed — lost for one reason: it exists only on the Live workspace, and a way back onto a congregation screen must be reachable from anywhere the dock is, which is everywhere. Quick tools stays at THREE blocks, so it sits inside one of the three rather than beside them; `quicktools.test.js`'s pinned count is untouched and did not need overturning. **Its state comes from the fact `main::adjust_countdown` already reads before it decides whether to repaint** — `channels::live_content`, mirrored in the console as `$live` — through `countdown.js::isCountdownContent`, the same three-armed question the engine asks, and never from the transport's `cdRunning`: that reader calls a countdown that has RUN OUT null, and a countdown at `0:00` is still on the wall, so asking it would have offered to put back what a congregation was looking at (rule 35). **It creates nothing**: `wayBack` offers only a `Both` timer that already exists, which is the scope `show_timer` accepts, so the engine's refusal in words is never what an operator meets. A failed `list_timers` prints its reason rather than falling silent, because silence over a broken bridge reads exactly like a quiet Sunday. **`adjust_timer` is still unreachable and that is deliberate**: the dock's transport re-aims the newest congregation countdown through `adjust_countdown`, which is the same action without an id, and nothing rendered yet names a timer to re-aim — a console that shows several `Both` timers is what would need it, and there is no such surface. Also still true: no control chooses a `warn_ms` or a `plan_item_id` (recorded in RG-149's row) | Track E Task 15 | **P1** | M | The test is whether a RENDERED control can reach it, not whether a wrapper exists — the distinction RG-21 was filed on. Held by `wayback.test.js` (17 cases): the pure decision in `countdown.js`, and the rendered control driven through a mounted `Dock`, asserting `show_timer` carries the timer's id and that `start_timer`/`start_countdown` are never reached. Watched to FAIL first — 12 of the 17 red on the tree before the fix, and the drift guard re-checked by swapping `isCountdownContent` for the transport's reading, which reproduces the offer over a countdown still on the wall. **Not measured in a layout engine**: jsdom computes none, so the block's 40px added row is declared CSS arithmetic and is spent inside `.tools`, which is `overflow-y:auto`; the `overflow:hidden` Controls card that carries `Clear screens` is untouched |
| ✅ RG-153 | **An expired programme timer sits at `0:00` for the rest of the service, and its `done_msg` has no reader.** `done_msg` is carried on every timer frame (`"countdown_done": ""` in all of them) and nothing on the stage page reads it: `programme` maps `id`, `label` and the remaining milliseconds, a timer at zero survives the `!= null` filter, and the row paints `0:00`. Observed against the real backend in both hold runs — a 45-second timer read `0:00` for the following twelve minutes, and two of six rows are in that state in the §8 screenshot | The congregation path has an answer for exactly this and it was seen working in the same screenshot: the `Both` wire form's `countdown_done` replaces the digits, and the stage mirror read `Welcome` where the congregation countdown had run out. The programme row has none, so the preacher's own instrument accumulates dead rows over a morning while the congregation's does not — and a row that reads `0:00` cannot be told from one that has just been started at zero | `audits/DESIGN-2026-09-16-WAVE3.md` §11 | **Closed 2026-09-17, on the operator's decision rather than on a reading of the code.** The question this row refused to answer for itself was put and answered: a programme timer that reaches zero **keeps counting, upward, wearing the warning colour**. `+4:37` means the preacher is four and a half minutes over, which is what a stage monitor is actually asked; `0:00` answered neither that nor the question it appeared to answer, because it cannot be told from a timer just started at zero. **No second arithmetic**: `countdown.js::countdownRemainingMs` is still the one reader of how long is left on every surface and took an opt-in `past` that lifts its floor, so the upward figure is that answer negated and handed to the same formatter. Every other caller is byte-identical, which matters because the wall reads zero as "it finished" and paints the done message there; a second exported helper would have been the second subtraction `docs/REBRAND.md` phase 7 records as fixed once already. **The mark is the SIGN, not the threshold rule.** `layers.js::countdownWarning` still answers false at and below zero and did not move — being over needs no threshold to know it, and a row marked only by that rule would lose its colour at the instant the time ran out. The class is the one RG-148 gave this row the day before, resolved to the countdown's own red: not a fourth reading of when to worry, and none of amber, cyan or amethyst. **The width budget needed no change and that is the finding**: a `+` figure is one character wider than the one it replaces, and RG-147 closed three days earlier by making `--tch` the length of the longest RENDERED value — so the sign is measured because it is part of the string. **What it deliberately does not do**: read `done_msg` on this rail. The words an operator typed are what a CONGREGATION countdown says when it lands, and a programme timer is a different instrument — the preacher's own bookkeeping, asked how far over rather than what to announce. That is now a decision written at the call site and pinned by a test, not an oversight; if it is ever revisited it is a new row. It also claims no colour and no pixel: jsdom computes no layout and there is no browser driver | RG-148 is the other end of the same row, and RG-147 is why the `+` fits | **P3** | S | `timers.test.js` — eight cases on the rendered rail (counts up rather than sitting at `0:00`; the figure agrees with the one reader asked for the other side of zero; it keeps counting on the page's existing tick; it wears the row's existing warning class; it is marked at the instant it reaches zero with no unmarked tick in between; a HELD timer whose instant has passed still reads its held figure; the over figure is painted and `done_msg` is not; and `--tch` counts the `+`, at five characters and at eight) plus four on the reader's opt-in, including that an unasked caller still floors at zero. The rewritten expiry case keeps asserting that `countdownWarning` answers false past zero, because the wrong fix for this row is to widen the rule three surfaces share. All watched to fail against the pre-fix tree, nine of them |
| ✅ RG-154 | **The height cap on the stage timer's digits measures the frame, which is the thing its own comment says it replaced.** `.tval` is `min(…, 9cqh)` and `.progrow` declares `container-type: inline-size`, which establishes an INLINE-axis container only — so `cqh` has no eligible container and falls through to the viewport. Decisive measurement at 1920 × 500 with three timers: the row is 74.4 px tall (9 % of it would be 6.70 px), the viewport is 500 px (9 % is 45.00 px), and the computed `font-size` is **45.00 px**. The comment above it reads *"Its own container, so the digits are a share of THIS row and not of the frame — … the bug that rule replaces is a figure that looked right at one width and overflowed at every other."* The `cqw` half is true and does resolve against the row; the `cqh` half, which is the one meant to stop the digits outgrowing the row's HEIGHT, does not | **Stated so the row is not read as worse than it is: no clipping was produced by this at any viewport tried**, including 844 × 390 and 1600 × 300, because a cap that tracks the viewport shrinks with it. The defect is that the cap is not the cap anybody wrote, so nothing bounds the digits against the row if the row's own height ever changes — a second line of label, more padding, a different `max-height`. It is a latent version of the exact bug the comment says it fixed | `audits/DESIGN-2026-09-16-WAVE3.md` §9 | **Closed 2026-09-17.** The `cqh` term is gone, and the honest fix the row predicted is the one taken. `.progrow` declares `--progmax: 20dvh` and uses it for its own `max-height`; `.tval`'s cap is `calc(var(--progmax) * .45)`, which is 9dvh — the same number that was being computed by accident, now computed on purpose and tied to the ceiling it is a share of, so the two cannot drift. `container-type: size` was NOT taken: it would take the content out of the height calculation and collapse a row whose height IS its content's, which is the same reason `.noterow` beside it is `inline-size` and says so. The comment now states which container each unit resolves against and why. **One deliberate change of meaning, recorded rather than hidden**: `max-height: 20%` of `.sr`'s padded content box became 20dvh of the frame, so on a device with safe-area insets the row's ceiling is a few pixels more generous; and the cap now tracks `.sr`'s own `100dvh` frame instead of the small viewport that `cqh` was silently falling back to, which is the better of the two on a mobile browser with a collapsing toolbar | RG-147 is the width half of the same declaration, closed with it | **P3** | S | `stageprogrow.test.js` — no container-HEIGHT unit inside an inline-size-only container, and the row's ceiling and the digits' cap are one figure stated once. Watched to fail against `9cqh`. **A CSS-rule claim, not a measurement**: the 45.00 px this row read at 1920 × 500 cannot be re-read from jsdom, so the test pins the declaration and not the painted result |
| ✅ RG-155 | **Two counts whose own reproducing command now contradicts them, found by running both commands.** `src/lib/layers.js` calls `db/templates.rs::theme_templates()` *"the thirteen on the preset shelf"*, twice, in the site-7 comment; it returns **twenty-five** — five families × five kinds, listed by name in the source. And CLAUDE.md's Tauri-events paragraph says its own command *"yields twenty-one"*; on this tree it yields **twenty-two**, the new one being `output://default_template`, which `git log -S` places in **wave 2 Track A** (`31d058c`) and not in wave 3 | Neither count changes a behaviour and both are the failure mode CLAUDE.md's own paragraph is written about: *"three numbers for one set, none of them right, which is why the instruction below matters more than the figure above it."* The layers.js one is the more misleading of the two, because the claim it supports is CORRECT — every one of the twenty-five does carry an explicit five-kind list — so a reader who checks the number and finds it wrong has no way to tell whether the exposure claim is wrong too. The CLAUDE.md one is the third time that paragraph's figure has drifted | `audits/DESIGN-2026-09-16-WAVE3.md` §13 for the first; `grep -rhoE '"[a-z_]+://[a-z_]+"' src-tauri/src/*.rs \| sort -u \| wc -l` for the second | **Both halves corrected, and the CLAUDE.md one was worse than the row said.** The `layers.js` comment now reads twenty-five, says it used to read thirteen and why (wave 2 reshaped the shelf into five families across five kinds), and carries the `awk` that counts the entries, so the next reader checks rather than trusts. CLAUDE.md's paragraph said **twenty** events over a command reproducing **twenty-two** strings, one of which is not an event: two wrong numbers, not one. Both are now 21 and twenty-two, and the sentence says this is the fourth drift of the same count. The cause of all four is the same and is now named in the paragraph: `output://default_template` landed in wave 2 and reached neither number. **`docs/ARCHITECTURE.md` §6 had not heard of it either** — the table listed 20 of the 21 events, which the row did not know, so it is added there with the same note | RG-20 · RG-48 are the same class | **P3** | S | Nothing pins a prose count and nothing should — the paragraph's own instruction is to reproduce the set rather than trust the figure, and that instruction is what found this. `ipc.test.js` holds the contract that matters in both directions and was green throughout, which is the point: the numbers drifted for four waves while the guarantee never did |
| ⏳ RG-156 | **Two limits on one feature, filed together because they are the same question: which screen is a stage, and is it the same screen twice. ONE OF THE TWO CLOSED ON 2026-09-20 AND THE OTHER IS UNTOUCHED, WHICH IS WHAT THIS ROW IS NOW ABOUT.** **One, and this row has been wrong about it twice in different directions.** As filed, the first limit was that the two stage surfaces disagree about what a panic control does to a Stage Message: `Stage.svelte` left `alert` standing and `output.html` blanked it. DECISIONS §91 reversed §89's third decision and closed that half by RULING rather than by code nobody wrote. What the row was left filing was the real limit underneath it: `output.html` kept the text in renderer state (`Output.svelte`'s `stageMessage`) and no `clear`/`black` branch reset it. `TemplateRender`'s layer stack sits inside `{#if content}` and the page passes `content={visible ? content : null}`, so a cleared wall only HID the layer, and the moment the next verse was fired the message painted again, unbidden, after the one control whose whole meaning is that every sentence on every screen has been taken back. **The two controls did not even hide it the same way**, measured while the merge test was written: `clear` set `visible = false` so the layer stack left the DOM, while `black` left `visible` alone and painted `.blackout` over the top, so after `B` the message was still rendered underneath an opaque cover and whatever lifted the cover showed it again. **That half is CLOSED by `takeDownStageMessage`, called from both branches through one writer (DECISIONS §104).** **Two, and this is the open half**: `channels::stage_alert` publishes to the kiosk hub and emits no Tauri event, so a screen wired as a `native_window` and given the `stage` role receives nothing | **Small, and specific to a rig nobody has yet run.** The seeded `Stage display` is a `network_client`, so what is left needs a church to have configured something else: a confidence monitor on HDMI, as a native output window. It can put nothing on a CONGREGATION screen, because `Output.svelte` refuses a `stage_alert` unless its own role is `stage`, and §104 gates the programme rail on the same predicate. **The failure direction is silence, which is the worse of the two this row used to carry**: the console reports a Stage Message sent, and one class of stage screen never receives it, so a preacher is never told something the operator believes they have been told. That is rule 35 seen from the engine end rather than the badge end | Read from the tree on 2026-09-16 while Track C was written, re-read on 2026-09-17 after the ruling, and re-read again on 2026-09-20: `channels::stage_alert` still calls `publish_kiosk` and nothing else, and `send_stage_alert` is its only caller. The Tauri half is asserted as a GUARANTEE by `e2e::r5_a_word_to_the_preacher_reaches_no_congregation_channel` ("the Tauri door stays shut"), so this limit is a deliberate choice whose cost had never been written down and still has not been paid. The half that closed was read in the same files it was filed from: `src/lib/TemplateRender.svelte`'s `{#if content}`, `src/Output.svelte`'s `content={visible ? content : null}` and its separate blackout overlay, with `stageMessage` reset by nothing but a role change | **Still not proposed as a patch, because what is left is a decision rather than a bug.** It needs `stage_alert` to emit, which would open the door that test exists to hold shut, so it needs a per-channel emit rather than a broadcast, and that is a different shape of publisher from every other one in the module. **The other half was a decision too, and it has now been taken**: DECISIONS §104 resets the message on both panic branches through ONE writer, because the harsher control must never do less than the milder one, and deliberately does NOT take the Stage Timers down with it, because §91's line is between a thing that SAYS something and a thing that COUNTS | DECISIONS §104 · §91 · §89 | P3 | M | **Half closed and pinned, half neither.** `src/lib/stagealertpanic.test.js` now holds BOTH surfaces rather than only the phone: the two `Stage.svelte` cases it was written for, plus one asserting `clear` takes the message off a stage display served by `output.html` and one asserting `black` covers it there by a different mechanism, with the difference stated rather than smoothed over. `stagealerttemplate.test.js` carries a case named for this row: that the message does not come back with the next verse. **Nothing pins the native-window half and nothing can**, because the guarantee currently under test is the opposite one |
| ⏳ RG-157 | **A background chosen in the template editor before wave 5 is stored as a hashed `/assets/…` URL, and that path no longer exists in the bundle.** DECISIONS §90 makes the pictures in `src/backgrounds/` real `media_assets` rows on a fresh install, and a Rust seed cannot know a Vite content hash — so `vite.config.js` now emits those files at a stable, unhashed `backgrounds/<file>` instead. `TemplateEditor`'s background picker writes `b.url` straight into a layer's `image`, so any template saved before this change holds the old path literally | **One picture on one layer of one template, and only where a person edited it.** No seeded template can be affected: a seeded row is written in Rust and could never have carried a build hash. The layer falls back to its fill colour rather than blanking the screen, so a congregation sees a plain background rather than nothing, and re-picking the image in the editor is the whole repair. It is filed because the failure is silent — nothing says why the picture stopped painting | Read from the tree on 2026-09-16 while Track I was written: `TemplateEditor.svelte`'s `on:click={() => set('image', b.url)}` in the Background → Image library block, against `src/lib/backgrounds.js`'s `import.meta.glob(..., { query: '?url' })`, whose build-time value is whatever `assetFileNames` produces. The change itself is asserted by `src/lib/bundledbackgrounds.test.js` and by `db::starter::every_bundled_picture_is_a_row_that_resolves_to_a_real_file`, which checks each seeded row against the real bundle | **Not proposed as a patch, because the repair worth making is larger than the fault.** A one-off migration rewriting stored `/assets/<stem>-<hash>.<ext>` to `backgrounds/<stem>.<ext>` would work and would also be the only code in the repository that knows Vite's naming scheme, which is the shape of thing this row exists because of. The better answer is the one Track I opened the door to: a layer should reference a `media_assets` row by id rather than a URL at all, so the picture's identity survives any change to where its bytes live. That is a decision about the template model, not a fix | DECISIONS §90 | P3 | S | Held today only by being written down. Nothing asserts what an OLD stored template's `image` resolves to, because no fixture in this repository carries one — the same absence RG-142 records from the other side |
| ✅ RG-158 | **Two different things are both called Up Next, and only one of them reaches the preacher.** The Library's staging queue is headed **Up Next** and its own empty state says *"Cue in Live on a selected item stages it here — cueing reaches no screen"*. The preacher's monitor has an **Up Next** too — `stage_next` / `next_reference`, which the seeded `Stage · Rail` renders in a layer literally named `Up Next` — and only `Live.svelte` calls `setStageNext`, only from the plan | **One name over two behaviours, on the two surfaces an operator moves between fastest.** An operator who queues three items in the Library's Up Next has told nobody anything: the preacher's monitor is unchanged, and nothing on either screen says the two are different. Track D exists to remove exactly this shape and removed it for Stage Message and Stage Note; this one survived because the second Up Next is not a label on a control, it is a panel heading in another workspace | Driven on 2026-09-16 in the console against a mock bridge seeded from a real fresh-install database: the Library panel heading renders as `Up Next` (`rw-panettl`) and the empty state is quoted above; `Stage · Rail` (`seed_key` `stage.rail`) carries a text layer named `Up Next` bound to `next_reference`, read from `src-tauri/data/shelf_templates.json`; `grep -rn setStageNext src/` reaches `Live.svelte` and nothing in the Library | **Rename the Library's queue, not the stage's.** Up Next is a statement about what comes next on a screen, and the stage monitor is the surface that makes it true. The Library's panel is a staging list that reaches no screen, and its own text already says so — **Staged** or **Queued** says it in the heading instead of in a sentence under it. `names.test.js` then holds Up Next to the stage concept the way it already holds Stage Message | — | P2 | S | `names.test.js` gains Up Next as a third concept, with the single user-visible string and the files allowed to contain it; it must fail while the Library's heading stands **CLOSED 2026-09-17.** The Library's panel is **Staging** — its heading, its `aria-label`, its button and its message. Deliberately not "Queue": said aloud in a room where a plan already has cues, queue and cue are the same word. The preacher's `stage_next` keeps the name, because it is the concept the register entry is about and because the seeded `Stage · Rail` layer carries it in the database. **The register had NAMED both Library files in `stage_next`'s allow-list**, so this was permitted in writing rather than merely missed: `names.test.js` checked that a concept is said one way and never that a name means one thing. It now checks the inverse — no two entries may claim a name, and no entry's name may be another's forbidden variant — with a guard so the scanner cannot quietly stop matching. Verified by giving the new entry the old name and watching the check fail. |
| ⏳ RG-159 | **Under `prefers-reduced-motion` a notice longer than about sixty characters is silently cut off, on all five shipped crawls, and no instrument in the product can see it.** `.lscroll .lrun` sets `animation: none; padding-left: 0` inside a `white-space: nowrap; overflow: hidden` box, so the run stops moving and everything past the right edge is never shown. There is no paging fallback; the design named one and it was not built. Compounding it, `fitLayers` returns early for `.lscroll` (`TemplateRender.svelte:1332`), so a crawl never gets a fit, never sets `data-fitted` and never reports through `onFit` — rule 37's instrument is blind here by construction | **A congregation reads two thirds of a notice and nobody is told.** Measured with a 229-character announcement at 1920×1080: `Scroll · Banner` runs **6365 px** of words through a **1651 px** box, `Scroll · Ribbon` 5516 through 1651, `Scroll · Glass` 6153 through 1613, `Scroll · Bold` **9759** through 1690, `Scroll · Clear` 6153 through 1651 — between 70% and 83% of the notice never painted. With motion allowed all five crawl correctly, so the failure is confined to the setting a church turns on for a photosensitive member of the congregation, which is the one case where nobody will be watching for it | Driven on 2026-09-16 against the real backend: the five crawls opened on `http://127.0.0.1:8032/output.html`, the same announcement pushed to each, measured in two browser contexts differing only in `reducedMotion`. With motion: `animationName: relay-ticker`, `padding-left: 1651.19px`, visible width equal to the box at every sample. Reduced: `animationName: none`, `padding-left: 0px`, run widths as above, `data-fitted` absent on all ten runs. A 31-character notice fits and is unaffected | **Page it, and let the fitter see it.** Under reduced motion a crawl becomes a sequence of static pages — the same words, a boxful at a time on a timer with no movement, which is what the setting asks for and what the design named. Until then the honest minimum is that `.lscroll` stops being invisible to the fit instrument: report `clipped` through `onFit` when the run is wider than its box and nothing is animating, so Live can say so. **Do not "fix" this by shrinking the text to fit** — a crawl is designed at a size for a distance | — | P1 | M | A test that mounts a crawl under `prefers-reduced-motion: reduce` with a notice wider than its box and asserts the words are reachable rather than truncated; verified by reintroducing `animation: none` alone and watching it fail |
| ⏳ RG-160 | **The shelf ships a five-template Announce role and not one of the forty derives the `announcement` kind, so the Templates gallery has no Announcements bucket at all.** `templateKind` answers `announcement` only for a visible text layer carrying `scroll`, and the five `Scroll · *` crawls that do carry it are matched first by the earlier `band` rule and come back `lower-third`. The five `Announce · *` templates carry no scroll, so four derive `scripture` and `Announce · Bold` derives `song` | **An operator cannot filter to the notices they were given, and the counts misdescribe the shelf.** Driven in the gallery, the KINDS rail reads `All 40 · Scripture 9 · Songs 6 · Lower Thirds 5 · SuperSource 5 · Stage 5 · Media 5 · Timer / Countdown 5` — no Announcements row, because `kindsPresent` lists only kinds something derives. Before wave 5 the seeded `Classic · Announcement` did derive `announcement` (RG-140 records it), so the wave removed the only rows that filled the bucket while adding five templates named for it. `CONTENT_KINDS` still offers **Announcements** as a content look on four surfaces, which is the register the Announce templates are actually reached through | Driven on 2026-09-16 in the console against a mock bridge seeded from a real fresh-install database: `templateKind` imported from the running page and applied to all forty rows gives `scripture` for `Announce · Board`, `Card`, `List` and `Contrast`, `song` for `Announce · Bold`, and `lower-third` for all five `Scroll · *`. The rail was read off the rendered gallery. **The split is deliberate and argued** — `src/lib/shelf.test.js` records it in prose and in a per-name table, and `templateKind.test.js` asserts only that no seeded row derives `custom`, so no existing instrument reports the empty bucket | **Decide whether `announcement` is a role the shelf has.** Either give the Announce family a signal `templateKind` can read — it is the non-scrolling full-screen notice the module's own comment already describes and then declines to act on — or retire `announcement` from `KIND_META` and `KIND_ORDER` so a rail row cannot exist that nothing can fill. What should not stand is the third state: a named bucket, five templates named for it, and no path between them | — | P3 | S | A test that every kind in `KIND_ORDER` is derived by at least one seeded row, or is recorded by name as deliberately unfillable |
| ✅ RG-161 | **Per-cue screen targeting does not exist, and the design that asks for it calls it a column plus a select.** A cue can choose a LOOK and cannot choose a SCREEN. `docs/superpowers/specs/2026-09-15-timers-templates-stage-design.md` §4.2 says *"the missing half is which screens. Add it, defaulting to all"*, which reads as one migration and one UI control. There is no channel dimension on the wire at any point, so this is not an addition to a facility — it is the facility, in four pieces | A plan that names a screen is how a church stops a notice landing on the stage tablet, a lobby TV showing a countdown after the service has started, and a band channel taking the words it is not meant to carry. Today the only per-screen decision is the RECEIVER filtering on content KIND against its own template's `shows` set, which is a coarse standing preference, not a choice the plan makes cue by cue. The Planner is where the plan is built and it cannot express this | Verified by reading on 2026-09-16 on `feat/wave4-stage-planner`. (1) `channels::OutputContent` (`channels.rs:39-135`) carries nineteen fields and none names a screen. (2) `channels::broadcast_content` (`channels.rs:1075`) ends in two unrouted lines: `channels.rs:1089` emits `output://content` app-wide to every webview, `channels.rs:1090` hands one string to the hub's single broadcast sender (`channels.rs:1513`) and therefore to every kiosk client. (3) `src/Output.svelte:293` filters on `kind` alone and never compares its own `channelId` (parsed at `:26`); the kiosk half does the same at `:190-195`. The precedent that it CAN is `channel://retemplate`, emitted app-wide with a `channel` key (`main.rs:6067`, `:6083`) and filtered client-side at `Output.svelte:330` and `:237` — addressing is already done for the template and has never been done for content. (4) Retention is one global slot per kind in `channels::KioskHub` (`channels.rs:1393` screen, `:1424` timers), replayed on hello | Four pieces, in this order, and the second of them is the expensive one. A channel set on the content; a routed publish AT the choke point, never at the call sites (rule 36); a receiver-side filter on the two doors the template swap already uses; and per-channel retention, because rule 43 says a screen joining mid-service is shown what is on the screens and with targeting *the screens* stops being one answer. **The panic controls stay global and unrouted** — a `clear` that has to ask which screens it is talking to is a `clear` that can fail, and rule 15 with DECISIONS §20 forbid one of those. **One product question is open before any code**: what `template_pinned` (`main.rs:3325`, DECISIONS §29) means on a screen the cue does NOT target — untouched, cleared, or shown in the screen's own look are three different Sunday mornings. **A stored-but-unread column is explicitly refused**: that is RG-132 and DECISIONS §69 again, an operator filling something in on a Tuesday that no screen reads on the Sunday | Wave 3 tracks C and E, which are still editing the choke point; the `template_pinned` answer above | P2 | L | A cue targeted at one screen reaches that screen and no other, asserted on BOTH doors — a Tauri event test and a kiosk hub test — because the rehearsal guarantee was green and false for the stage tablet for exactly this reason. Then a client that connects mid-service is sent what is on ITS screen, per channel, which is the `channels::tests` rule-43 trio extended rather than a second matcher. And a `clear` still reaches every screen with no channel argument anywhere on its path |
| ✅ RG-162 | **Wave 4 gave the preacher's programme rail a warning state and a finished message, and no control the product ships can put a timer into either.** `.tmr.warn` and the done-message treatment are real, tested and correct — and they need a `warn_ms` and a `done_msg` on the timer, which nothing anywhere supplies. The one thing in the product that starts a programme timer is a bound plan cue going on air, and it passes neither | The rail is the one surface in the building whose whole job is telling a preacher how long is left, and on a shipped copy it looks the same at four minutes and at ten seconds, then reads `0:00` when it is over. That is the state PR #80 was written to remove, still standing, on every install. Rule 35 in its general form: a figure that cannot change when the thing behind it changes is not telling anybody anything | Driven on 2026-09-17 against the real backend (`audits/2026-09-17-WAVE4-STAGE-PLANNER.md` §1.1, §1.3, §2.1). The rail DOES warn and DOES show the words when a timer carries them — 9:59 and 1:01 plain, 0:58 red with `cdwarn` against a 60000 ms threshold; `Hand over` at 30 px in the body face beside `0:00` at 64 px in the mono. Supplied through a temporary audit bridge, because nothing else can. `startTimer` is exported once (`src/lib/stores/capture.js:1487`) and called from exactly one place in the tree (`src/lib/views/Live.svelte:680`, `startCueTimer`), whose call is `{minutes, label, scope:'stage', planItemId}` — no `warnMs`, no `doneMsg`. Captured on the wire: `start_timer {"minutes":12,"label":"Reading","doneMsg":"","scope":"stage","warnMs":null,"planItemId":2}`, read back from the registry as `warn_ms: null, done_msg: ""`. `grep -rn warnMs src/` outside tests reaches only `layers.js`'s reader and `capture.js`'s parameter; no `#[tauri::command]` in `src-tauri/src/` sets `warn_ms` to anything but `None` | A cue that asks for a clock should be able to ask for a warning and for words at the end, the way a countdown cue already carries `minutes`, `label` and `done`. That is one more nullable column beside `timer_minutes` (or two), the Planner inspector already has the row to put them on, and `startCueTimer` is the single call site that would pass them. **`Settings → General → Countdown warning` is NOT the answer and must not be made into one** — the stage page has no Tauri bridge and cannot read that setting, so falling back to it would warn at a figure nobody chose and the wall does not agree with, which is why Track A refused it (`Stage.svelte:298-317`, docs/REBRAND.md §7). **CLOSED IN HALF on 2026-09-17 (wave 4 Track E), and the half that is not closed is named rather than quietly counted.** The warning half is done: `Live::startCueTimer` passes `warnMs: $countdownWarnMs`, the console's mirror of `countdown.warn_ms` that `App.svelte` loads at launch and the Settings control rewrites — so the figure on the preacher's rail is the same one the dock and the programme pane already turn red at, and no second default was invented. This cell's warning above still holds and is honoured: the stage page does not read the setting, the figure travels on the frame. **The done message is deliberately still absent.** A cue carries none, there is no column for one and no control writes one, and composing a sentence out of what Relay knows about a cue would be Relay's own words presented as somebody's choice. It would also cost something measured: a finished row shows its message at 30 px against the digits' 64 px (audit §1.3), so every bound cue on every install would end LESS legible than `0:00`, with nobody having asked for it. That half belongs to the surface that owns timer message text, **wave 3 Track E**, and is left open here rather than answered in passing | Nothing. The rail's half is built and tested; only the producer is missing | P1 | S | A cue bound with a warning and a done message, fired, reaches the rail with both: the row crosses into `.tmr.warn` at the figure the operator chose and shows the operator's words at zero, asserted on the stage page rather than on the command. And a cue with neither is still a row that never warns, so the absence stays an absence rather than becoming the shipped minute. Held by `progtimerjoin.test.js` — *“the figure Live sends is the one Settings is holding”* and *“and the row on the real stage page crosses into the warning state”*, which drive Live's real fire path and mount the REAL stage page on the frame that comes out of it. Both watched to go red with `warnMs: null` put back, which is the defect exactly as it shipped. A third case pins the absent message as an ABSENCE (`0:00`, never an invented sentence) so the open half cannot drift shut by accident **CLOSED BY THE CONSOLIDATION MERGE, and only half of it by a fix.** The warning half is reachable: wave 3's warn chain carries `warnMs` from `Settings → General → Countdown warning` onto every timer Live starts (`Live.svelte`, `warnMs: $countdownWarnMs`), and ships the same figure to the stage page on frames it already receives, so a programme row now crosses into `.tmr.warn` at a threshold somebody actually chose. **The finished-message half is not fixed — it is withdrawn.** The rail no longer has a done-message treatment to reach: a programme timer past zero counts up, because `--tch` budgets every column from the widest rendered string and prose in that slot re-creates the six-sixty-pixel-columns failure `MIN_TIMER_PX` exists to prevent. The operator's words still land, on the congregation countdown and on the mirror above the rail, which is where they were always going. Recorded as a withdrawal rather than a closure because nothing was built: the feature this row asked for was removed on evidence. |
| ✅ RG-163 | **Nothing removes a programme timer, no control can, and the rail's floor keeps the oldest — so by the middle of a service the clock a preacher is looking for is the one inside `+N more`.** `TimerRegistry` never reaps an expired timer; the panic controls take `Both` and deliberately leave `Stage`; and `stopTimer`, `adjustTimer`, `showTimer` and `listTimers` all exist in `capture.js` with **no Svelte component calling any of them**. The only producer is a bound cue, and there is no consumer | A plan with four bound cues leaves four clocks on the preacher's screen, three of them finished, for the rest of the service, and the operator has no way to take any of them off. On a stage phone in portrait the rail's capacity is two, so the one clock shown is the OLDEST — a dead one from the start of the service — and the live sermon clock is in the count. The rail has told the preacher he cannot have the thing he is looking at, which is the failure Track A's floor was written to prevent, arriving through the door Track B opened | Driven on 2026-09-17 against the real backend (`audits/2026-09-17-WAVE4-STAGE-PLANNER.md` §2.2). One walk of a three-cue plan bound at 5, 12 and 2 minutes, run to zero: `Opening song = 0:00 \| Reading = 0:00 \| Notices = 0:00` at 1024×768. A fourth cue on air: `… \| Sermon = 24:55` at 1024×768, and at 390×844 `Opening song = 0:00 \| +3 more`. `/api/clear` and `/api/black` both left all three standing, which is the documented design (`main.rs` `stop_scope(Both)`; `Stage.svelte:742-744`). `TimerRegistry::stop` is the only remover (`timers.rs:172`) and `snapshot` sorts by id, which is start order (`timers.rs:230-235`); `Stage.svelte:343-348` keeps `programme.slice(0, keep)`. `grep -rln 'stopTimer\|adjustTimer\|showTimer\|listTimers' src --include='*.svelte'` returns nothing. **`qa-inventory` reports 0 unaddressed commands for all four**, and it is not wrong to: its "Commands registered in Rust that no frontend caller addresses" list counts a `capture.js` wrapper as a caller, and the second hop CLAUDE.md describes — to a control something actually renders — is the `reachedFromUi` / `WRAPPER ONLY` verdict on the **create-paths** table, which is keyed on database tables and so never looks at a timer. So four registered commands reach no rendered control and the instrument that exists to say so cannot see them | Two halves, and the first is the one that matters. **A cue's clock should end when the cue does** — `startCueTimer` already knows when a cue leaves the air, and `start_timer` already stops a cue's previous clock by `plan_item_id`, so the reader exists. Failing that, an expired `Stage` timer could be reaped after a grace period, which is a backend change with no UI. **Second, the floor should prefer the clocks that are still running** rather than the oldest — but that is a change to behaviour Track A tested deliberately, so it is a decision and not a tidy-up, and it must keep the guarantee that at least one clock survives. A third possibility, an operator list with a stop beside each row, is the largest and is what `showTimer`/`stopTimer` were built for. **CLOSED on 2026-09-17 (wave 4 Track E), in two places, and deliberately not in a third.** `Live::retireCueTimer` stops the clock of the cue a take retires: `fireSlide` is the single door a plan cue goes on air through, so the transport, the slide grid, the preview take and TAKE are one call site between them. It asks the backend WHICH timer (`list_timers`, matched on `plan_item_id` and `scope` together) instead of remembering the id `startTimer` handed back, because Live is unmounted and remounted every time an operator visits another workspace mid-service and a remembered id does not survive that. `main::end_service` sweeps the rest and publishes the empty set — at the choke point, not at the two controls that call it. **The doors left alone, each for a reason**: a manual fire or an accepted suggestion (the preacher going off-script is the case the sermon clock exists for); the panic controls (wave 3's decision, and rule 15 — a panic control may not gain a question it can fail to answer); and closing or switching a plan (the preacher is still preaching). **The floor still keeps the oldest**, which is Track A's tested behaviour and stays a decision rather than a tidy-up; with no finished clocks left standing, the measured route to it is gone. An operator list with a stop beside each row is still wave 3 Track E's | RG-146 is the same call site and the same wave; the two are best answered together | P1 | M | A plan walked end to end leaves the rail carrying only clocks that are still running, asserted on the stage page. And whatever the floor keeps, a running clock is never hidden behind a count while a finished one is shown — the case measured here, at a phone's capacity of two. Held by `progtimerjoin.test.js` (five cases) and `e2e::ending_a_service_takes_the_programme_clocks_off_the_preachers_rail`. Watched to go red: removing the retire call fails three of the five; making it fire only for a BOUND cue fails exactly one — *“an UNBOUND cue going on air still retires the clock before it”*, which is the door a fix written from the finding alone would have missed. The two negative cases (a panic control, an off-script fire) stay green through both reverts, so they are not the same assertion twice. The Rust test reports `[1, 2, 3]` still running with the sweep removed |
| ✅ RG-164 | **The new programme rail, plus the two panels the stage page has always had, cuts the verse on the preacher's screen.** `Zones` and `Control` are two taps and both operator-initiated. With the rail on — its default — opening both collapses `.reading` to 74.2 px, and the verse, already on its 26 px floor with nothing left for `TemplateRender` to shrink, is cut by 38.9 px | It is the preacher's own screen rather than a congregation wall, which is why this is P2 and not P1. But the page is `overflow: hidden` by design, so there is nothing to scroll and nothing that says the verse is incomplete: a preacher who opened the two panels mid-sermon reads two thirds of a verse and has no way to know it. The rail is the contributing cause — wave 4 added a row to a screen whose rows were already sharing the room | Driven on 2026-09-17 at 1024×768 against the real backend (`audits/2026-09-17-WAVE4-STAGE-PLANNER.md` §2.3). Rail on, panels closed: reading 372.1 px, verse 43.1 px, text block 163.3 px, no cut. Rail **off**, both panels open: reading 170.3 px, verse 26 px, text 98.5 px, no cut. Rail **on**, both panels open: reading **74.2 px**, verse 26 px, text 98.5 px, **38.9 px cut**, with `.reading` reporting `scrollHeight 138` against `clientHeight 74`. Track A's own read expected the opposite failure — the panels summing to 106 dvh and pushing content off screen; measured they sum to **38.8 %** and `documentElement.scrollHeight === innerHeight`, so nothing leaves the screen and the reading is simply what pays | The panels are transient and operator-initiated, so the cheapest honest answer is that opening one yields the rail rather than the reading — the rail is bookkeeping and the verse is the thing the screen exists for. Alternatives: make the panels overlay rather than share the column (they are already `position: static` siblings), or cap the two so only one is open at a time. **Whatever is chosen must keep `stagezones.test.js`'s existing invariants** — nothing leaves the screen (`:220`), the reading takes what is left (`:231`), rows below are `flex-basis` and not `height` (`:236`). **CLOSED on 2026-09-17 (wave 4 Track E)**, by the cheapest honest answer this cell proposed: with either panel open the rail stands down (`panelOpen`, `Stage.svelte`), so the reading keeps the room the panels ask for and the verse stays whole. Closing the panel brings the rail straight back, and nothing is drawn in its place — a line saying where the clocks went would spend the very room the fix reclaims. It is NOT the `programme` zone and writes nothing to `relay.stage.zones`: a moment, not a choice a device keeps. The three invariants this cell names are untouched | Nothing | P2 | S | With every zone on and both panels open at 1024×768, the verse's ink is inside `.reading`'s box — measured as ink against the clip box, not as a line box, and not as `overflowing()`, which rule 42 already records as the check that does not catch this class. Held by `stagezones.test.js` — *“the Zones panel takes the rail’s room and leaves the verse alone”*, *“the Control panel does the same, because it is the same column”* and *“yielding is not switching the zone off”*. jsdom has no layout engine so the 38.9 px is not reproducible there, and the file already records that split; what is asserted is the CAUSE the measurement was a consequence of — whether the rail is in the column at all while a panel is open. Both panel cases watched to go red with `!panelOpen` removed |
| ✅ RG-165 | **`.alert.sm` cannot render.** The stage page sizes an operator's alert in four steps and falls through to the smallest only above 150 characters (`Stage.svelte:502-507`); `send_stage_alert` caps the text at 140 (`main.rs:6178`). The fourth step is dead | Small, and filed because it is a rule this repository keeps finding in other costumes: a branch nothing can reach looks exactly like a branch that works, and the next person to touch the alert sizing will reason about four steps when there are three. It also corrects a claim in PR #80's deferred list, which cites `.alert.sm` at 3 cqw / 11.7 px as the worst case a preacher faces on a phone | Driven on 2026-09-17 (`audits/2026-09-17-WAVE4-STAGE-PLANNER.md` §2.4). A 166-character alert sent through `send_stage_alert` arrived at the page as 140 characters and rendered `.alert.md`. `alert` is set in exactly one place (`Stage.svelte:557`, the `stage_alert` frame) and that frame has one producer. Measured smallest reachable step: **16.4 px** at 390×844 and 43.0 px at 1024×768 | Either raise the `md` boundary to 140 so the steps describe the range that exists and delete the `sm` rule, or lower the backend cap below 150 so the step becomes reachable. The first is almost certainly right: 140 characters is the cap somebody chose on purpose, and a fourth step exists to make a longer message fit. **CLOSED on 2026-09-17 (wave 4 Track E)**, the first way — which this cell called almost certainly right, and was. `ALERT_MAX = 140` is now the last step's boundary and is read from `send_stage_alert`'s own cap by a test; `.alert.sm` is deleted from the stylesheet and the fallback is `md`, the smallest step that exists. **`stagezones.test.js` had a passing test asserting the dead step** — a 240-character message sized `sm`, a length the cap makes unreachable — so the instrument carried the same mistake as the code it covered, which is this register's most-repeated finding about itself | Nothing | P3 | S | The step boundaries and the backend cap agree, asserted from both files at once so neither can move without the other — the shape `crossrefs.test.js` already uses for claims that live in two places. Held by `stagezones.test.js` — *“the last sizing step is exactly the length the backend will deliver”*, which reads `ALERT_MAX` out of `Stage.svelte` and `MAX` out of `main.rs` in one assertion so neither can move without the other, and *“there is no sizing step the stylesheet cannot draw”*, which is the same claim in the opposite direction. Both watched to go red with the 150 boundary and the `sm` rule restored |
| ✅ RG-166 | **A COUNTDOWN PAINTS OVER THE LIVE CAMERA.** `TemplateRender`'s layer path never asked whether the screen was keyed. `showDefaultCountdown` was `layered && countdown_to != null && !hasTimerLayer`, and `.cd-default` is `position:absolute; inset:0` | **Congregation- and stream-facing.** Ten of the forty shelf looks are keyed — they exist to be composited over a shot of the preacher in OBS or on an ATEM — and every one of them painted `Service begins in 4:43` across the whole frame. Nobody in the building can see this happen: the failure is only visible on the broadcast | Reproduced 2026-09-18 against the real renderer and the shipped `src-tauri/data/shelf_templates.json`, before anything was changed. Ten keyed looks, all layer-model, none carrying `lowerThird`, none carrying a timer layer, all declaring `countdown` in `shows`: `Scroll · Banner/Ribbon/Glass/Bold/Clear` and `Source · Word right/Word left/Tall/Inset/Even`. The region path has refused this since the band was built (`{#if countdownTo && !countdownAllowed}`); only one of the two doors was checked | **The predicate, not a new rule** — and it is one this repository had already corrected once for exactly this question. `countdownAllowed` read `bandMode`, i.e. `layout.lowerThird`; `layers.js::isKeyedTemplate` exists because that flag is FALSE for a layer-model lower third, and its own doc comment records the cost: *"blackout blacked out the camera and clear left the band sitting on it"*. Blackout and the transparency law were migrated then; the countdown was not. **CLOSED 2026-09-18.** One predicate, both models: `countdownAllowed = !!countdownTo && !keyedOut`, and `showDefaultCountdown` now ends `&& countdownAllowed`. At the RECEIVER, because which screen is keyed is a property of the template THAT screen resolves (§29/§70); not in `shows`, because a `shows` list is an operator preference and a church could tick it back on and cover its own camera | RG-129 (the same renderer) | P1 | S | `keyedcountdown.test.js` mounts EVERY keyed look the shelf ships with a live countdown on it — eleven of its sixteen cases fail against the pre-fix renderer, watched. Guarded four ways: the shelf parses and ships both kinds, the ten are NAMED rather than counted, each is shown to be a template a countdown actually reaches, and an opaque look must still paint the digits. `hardrules.test.js` — *"RG-166 — no countdown is gated on `layout.lowerThird`"* — checks the absence AND the presence, because an absence cannot tell a correct predicate from no predicate at all. `lowerthird.test.js` gains the second door beside the first, both halves watched to fail separately. **Two things it deliberately does not claim**: five of the ten (`Source · …`) still show a clock inside their own `region` box — a real rendered slide from an opaque built-in at the position its designer chose, asserted by name — and any template with NO background at all is now keyed and refuses the countdown, which is the same answer §82 and blackout already give it |
| ✅ RG-167 | **BOTH CLOCKS WERE SILENT, AND SIX LABELS COULD NOT SAY WHICH WAS WHICH.** Relay runs a countdown that goes to the ROOM and a timer that goes to ONE PERSON. Neither said where it would land, and the names did not distinguish them: `Countdown`, `Countdown timer`, `Timer / Countdown`, `Countdown Timer`, `Programme timer`, `Programme`, `programme clock` | Rule 35, twice. `Start timer` succeeds when nothing holds the `stage` role — correctly (§91) — and the band then read identically to one with a tablet painting beside the preacher, so a church that never opened `Outputs → Screens → Role` started a Stage Timer every Sunday and sent it to nobody. The dock said `on the screens` over a wall where a screen's `shows` list omits the kind (dropped silently) or its template is keyed (refused, RG-166) | Read at source 2026-09-18: `Live.svelte`'s band renders `Reading…` / `No programme timer.` / the error and nothing about reach; `Dock.svelte`'s `cdstatev` reads `on the screens` from `$live.countdown_to` alone. `Channels.svelte`'s `Copy URL` hands out `output.html`, which has no `timer` branch — only `stage.html` renders the rail, and only `channelroles.js::stageRemoteUrl` produces that address. And `names.test.js`'s own `codeOnly` was blanking SEVEN THOUSAND characters of `Stage.svelte`, the `ZONES` table included, because a `/*` inside a `//` line opened a block comment for it | **CLOSED 2026-09-18.** Audience is carried by the first word: **Screen Countdown** reaches the room, **Stage Timer** reaches one person — the family `Stage Message` and `Stage Note` already teach. Both are two words because the register's matcher is a case-insensitive substring. Two pure helpers in `channelroles.js` (`describeStageReach`, `describeCountdownReach`) answer where each clock would land, so the dock and the run surface cannot disagree; the countdown one asks no screen anything, because both exclusions are properties of the template each screen resolves. The wire kind `"timer"` is UNTOUCHED — it is protocol, and a church on an older `stage.html` would silently lose its rail. `'Pre-service countdown'` is deliberately not forbidden: fourteen occurrences of accurate prose, nowhere a label, same shape as `'Congregation timer'` | RG-166 (the keyed half of the reach line) | P2 | M | `stagetimerreach.test.js` — six situations, six different sentences, asserted to be distinct, plus the `Copy URL` trap and the four words `programmetimer.test.js:324` forbids re-asserted from this side. `countdownreach.test.js` — three screens (one omitting the kind, one keyed, one plain); the line names both exceptions BY SCREEN NAME and says so when the read failed. Both files also mount the real surface, and the rendered halves were watched to fail with the lines removed. `names.test.js` carries both register entries; planting `Programme timer` turns it red, and a new test plants `startScreenCountdown`/`stageTimerRows` and requires them NOT to be hits so a future one-word rename cannot detonate. Its `codeOnly` is now one left-to-right pass, guarded against the `/*`-in-`//` bug in miniature and against the real `Stage.svelte`. **Not moved:** the placement. The operator asked whether the countdown should move to the Live desk; declined, because the dock is in the SHELL and renders on every workspace, the countdown is set before a service when the operator is as likely to be in Planner or Templates, and the Stage Timer band is one non-growing row on Live only |
| ⏳ RG-168 | **A disabled SWITCH says nothing, because the kit has no switch — and the button half of the same gap has just been closed, which is what makes this one visible.** `ui/Button`'s `disabledReason` renders `title` AND `aria-describedby`, because neither channel reaches everybody: `title` is invisible to a keyboard or screen-reader operator and `aria-describedby` to a mouse. Settings' nineteen hand-rolled `.r-btn`s went through the component on 2026-09-18 and every one of them now carries its reason. The switches did not, because there is nothing for them to go through: `app.css` publishes `.r-switch` and `src/lib/ui/` publishes no `Switch`. The measured instance is **Send crash reports** (`views/Settings.svelte`, Privacy), `disabled={!$capture.available \|\| !!crashReadFailed}` on a raw `<button class="r-switch">` with no `title` and no `aria-describedby`. The `crashReadFailed` half is explained by a rose `[role="alert"]` paragraph beside it, which is good; the `!$capture.available` half is explained by nothing at all — the switch that decides whether anything leaves this machine simply sits at 45% opacity | **Small, and narrower than it first reads.** One control, on a section an operator reads rather than drives, and the state is reachable only with the Tauri bridge down — at which point a great deal else is also not answering. It is filed rather than folded in for two reasons. First, the fix is a component (`ui/Switch`), and adding a second switch shape by hand is exactly what docs/REBRAND.md §12's one instrument forbids — `settingssections.test.js` has an assertion that Settings defines no switch of its own, and it should stay true. Second, this is `whydisabled.js`'s own measurement one costume along: its header records 27 controls gated on `!$capture.available`, `$safeMode` or `$serviceLock` with 16 saying nothing, and closing the button half does not close the count | Read from the tree on 2026-09-18 while the Settings kit pass was written. `grep -n 'r-switch' src/lib/views/Settings.svelte` gives three: safe mode and Measuring are never disabled, and the crash switch is. The button half is now pinned — `settingssections.test.js`, *“every disabled button says why, through the component that reaches both channels”* — and that test is deliberately scoped to `<Button>` with this row cited beside the exclusion, so the gap is named at the instrument rather than left as an absence in it | A `ui/Switch` that renders `.r-switch` and takes `disabledReason`, then adopted at the three Settings switches and the dock's Detection switch (which is gated on `$safeMode` per DECISIONS §86 and carries a hand-written `title` — a fourth call site keeping one sentence in step with `whydisabled.js` by hand). It is a component rather than a repair: doing it by hand at one call site is the drift §12 exists to stop | DECISIONS §86 · docs/REBRAND.md §12 · DESIGN_SYSTEM §8 | P3 | S | Nothing yet, and the absence is deliberate rather than forgotten: a test asserting that every disabled `.r-switch` carries a reason would fail today, and writing it before the component exists means either a skipped test or a hand-rolled `title` at each call site. `settingssections.test.js` records the exclusion in words at the assertion that covers the buttons |
| ⏳ RG-169 | **Twenty-one test scanners still strip comments with a three-regex chain, which is the shape that blanked 7 KB of `Stage.svelte`.** `names.test.js` stripped comments with `.replace(/<!--…-->/g,'').replace(/\/\*…\*\//g,'').replace(/^\s*\/\/.*$/gm,'')`, and a comment containing `:8032/api/*` opened a block comment that ran to the next real `*/` seven thousand characters later, taking the entire `ZONES` table with it. The register read the preacher's own screen with its labels missing and reported clean. | **Two failure directions, and this pattern has both.** Over-removal blinds a scanner, which is how the stage labels went unchecked. Under-removal leaves a dangling `<!--` behind a nested comment, so text that IS a comment survives and is read as code — which produces a false alarm rather than a blind spot, and is the direction CodeQL flags as `js/incomplete-multi-character-sanitization`. | CodeQL raised three high-severity alerts on PR #88, one per changed file; `grep -rln 'replace(/<!--\['` finds **24 files** carrying it. Three are fixed and `names.test.js` was fixed with RG-167 | `src/lib/codeonly.js` now exists: one left-to-right scanner that knows a quoted run cannot open a comment, and BLANKS rather than deletes so every offset still matches the file. The remaining twenty-one move onto it, one file at a time, each with its own suite watched green — a blanket sed over 21 scanners is how a scanner quietly stops seeing what it was written for | RG-167 | P2 | M | Convert one file, run its suite, and confirm the conversion changes no verdict. A scanner that reports the same before and after is either correct or not scanning; `names.test.js`'s guard, which reproduces the 7 KB bug in miniature, is the shape to copy |
| ✅ RG-170 | **`capture.js` resolves the Tauri bridge with a fresh `await import()` on every call, so concurrent callers race a cold module registry and the losers' reads never start.** In a browser and in the packaged app this is free, because the module registry caches it, which is why it has stood for the life of the project. It is not free when a mounting view fires two or three reads at once against a cold registry. | **It cost two CI failures on two different runtimes, and neither looked like its cause.** A reach line stayed on its rule 35 `Cannot tell which screens would show it` fallback, which was the line answering CORRECTLY about a read that had not landed. And `surface.test.js`'s `TemplateGallery says Loading` timed out -- a test built so it CANNOT fail for the obvious reason, because its read never resolves until `release()`, so `Loading` is the only state it can settle on. `Loading` never appeared because the read never began. `readstates.test.js` already recorded the symptom in prose: *the FIRST of several concurrent dynamic imports of a mocked module resolves and the rest hand back `undefined`*. | Both failures were reproduced and fixed by warming the bridge once per test file. The general fix was WRITTEN AND REVERTED on 2026-09-18: memoising the import on its promise made 45 tests fail across six files, and one of those failures was a genuine change of verdict -- `dockaudio.test.js` expects `no signal` and gets a real reading once the read succeeds. **Those tests encode the race.** | Memoise the import on the PROMISE, not the result, so N concurrent callers await one import; do not cache a rejection, because a plain browser has no bridge and must stay askable. Then absorb the six files' worth of verdict changes ONE FILE AT A TIME, because at least one of them is a test that has been passing partly by not doing the read | RG-169 | P2 | M | Land the memo, run each of the six files, and for every test whose verdict moves decide whether the OLD verdict was the true one. A test that changes answer when a read starts working was not testing what its name says **CLOSED 2026-09-18, and it took three wrong diagnoses to name.** The import is memoised on its PROMISE, so N concurrent callers await one import and there is never a second concurrent importer to be handed `undefined`. A rejection is not cached: a plain browser has no bridge and must stay askable. **The six files did not need absorbing one at a time — they had two causes between them, and both were real.** `loadTemplates` passed a non-array answer STRAIGHT THROUGH into a store declared `writable([])`, so a backend answering `null` set `$templates = null` and every `$templates.find(...)` threw, `Dock.svelte`'s `cdFallbackTpl` among them, taking the Live audio card down with it; a non-array is now a failed read, which records `readErrors.loadTemplates` and leaves the store holding what it had, so the surfaces say the list could not be READ rather than that it is empty. And `cuetimer.test.js` mocked `rehearsal` where the command is `get_rehearsal`, so the case never matched, `loadRehearsal` overwrote the armed value, and **a rehearsal guarantee was passing because a broken read never ran the `set`**. The guard itself was always correct. |
| ✅ RG-171 | **A screen could have ONE template for ALL content, or a per-kind look GLOBAL to every screen, and never both.** `output_channels.template_id` is per-screen and uniform (DECISIONS §29); `app_settings['tpl_<kind>']` is per-kind and global (§70). So the operator's own words — *"each screen should be able to have their own template set to what they want"* — could not be expressed: *"on the main screen scripture looks like this and songs look like that, while the lobby TV uses something else for both"* had no representation anywhere in the product | **It is the shape of the request rather than a defect in what exists.** Nothing was broken; there was simply no column. The cost is a church that styles its wall for scripture and then watches lyrics arrive in a scripture template, with the only lever being a global look that changes every screen at once | The operator's report, and the investigation behind `docs/superpowers/specs/2026-09-18-channel-looks-design.md`. Reproduced by reading the schema: `output_channels` has one `template_id` and `app_settings` has five `tpl_<kind>` rows, and no table joins a screen to a kind | `channel_looks`, a row per (screen, kind), with `template_id NOT NULL` so **no row** is the only spelling of "this kind inherits". It resolves at rung 3 of `resolveOutputTemplate` — the screen's own template FOR THIS KIND, above its blanket template and below a cue's pin — and the transparency law is evaluated ONCE, against the pinned claimant only. The client resolves, because the client is the screen | RG-172 | P2 | M | **CLOSED 2026-09-18.** `channellooks.test.js` (14) enumerates the rungs and was watched red on eight, including the case the naive two-call implementation gets wrong. `channellookspage.test.js` (7) drives the real `Output.svelte` on BOTH doors and asserts the background fill that reached the renderer — four red first. `e2e::r4_a_screen_wears_a_different_look_for_each_kind_on_both_doors` was verified by reverting the Tauri emit. `db::channels::look_tests` (5) holds the table, including that an upgrade back-fills NOTHING |
| ✅ RG-172 | **"Per-screen template selection does nothing" — and the selection was never the problem.** `run_kiosk_server`'s hello reply put its whole template branch inside `if let Some(id) = template_id`, and `Output.svelte` sends `template_id: null` whenever the URL is CHANNEL-keyed. Channel-keyed is the URL **Copy URL** produces and the one CLAUDE.md tells operators to use, because only a channel-keyed source follows a template swap — **so the recommended URL was the one shape that received no screen template at all.** `channelTpl` stayed null, `resolveOutputTemplate(null, …)` answered with the content look or the configured default, and four screens carrying four different templates painted the same one | **It is rule 43's hole one fact over, in the same handshake**: that hole was closed for the retained SCREEN frame (*what is on the screens*) and left open for the screen's own look. It looked correct in testing because a screen that happens to be connected when the operator reassigns a template DOES get the broadcast; it was wrong on every reconnect and every cold start, which is to say every Sunday. Building per-kind looks on top of it would have been building on sand, which is why it was closed FIRST | Measured against the running backend, same screen and same verse: `?channel=3` painted `James 2:1` at x=102 w=1075 fs=32px Fraunces centred; `?channel=3&template_id=68` painted it at x=709 w=476 fs=15.97px Inter left. The live install's DB carried `id 1 NULL · id 3 → 68 · id 4 → 44 · id 5 → 39` | `KioskHub` retains a `channel_template` frame per channel; the hello reply sends the one this client asked to be. **One channel's frame, never a map**: `channel_roles` and `channel_looks` ship whole because they are ids, and a template is BYTES — one in the field was 13 MB. `null` is retained and sent for a follower, because that is an answer. The retention and the publish are ONE call in the hub, because two calls IS the defect | — | P1 | S | **CLOSED 2026-09-18.** `channels::tests::a_channel_keyed_client_is_sent_its_own_screen_template_on_hello` sends exactly what `Copy URL` produces — a channel and a null template id — and asserts both that its own frame arrives and that the other two screens' templates do not. A test exercising the `template_id`-keyed URL passes today over the shipped URL being broken, which is precisely what had happened |
| ✅ RG-173 | **A template's `layout.shows` is the only answer to "which screens show this kind", and it is not editable from any surface in Relay.** It exists in seed data and in `TemplateRender`, and nowhere an operator can reach. Measured in the live install: of 44 templates, 40 list `countdown` in `shows` and the other four declare no `shows` key at all, which `templateShows` reads as showing every kind — so every template in that install paints the congregation countdown, and *"timers still show on all screens, even the live screen"* had no fix an operator could apply | **The operator asked twice.** It is also the report most likely to be answered the wrong way: the obvious fix is to let a per-kind LOOK of NONE mean "skip this kind", which is RG-161's per-cue targeting arriving through the back door with none of its pieces and none of its tests. Reach and look are different axes and had to stay different facts | Read from the live install on 2026-09-18: 44 templates, 40 with `countdown` in `shows`, 4 with no `shows` key. `grep -rn 'shows' src/lib/views/` finds no control | `output_channels.shows_json`, nullable, **ANDed** with `templateShows` at one helper and never replacing it: a template's `shows` is the DESIGNER'S statement about what it can render, the channel's set is the OPERATOR'S about what the screen is for, and both must agree. So it can only NARROW. NULL is NO OPINION — the existing behaviour, so no install changes look and there is no back-fill. Five checkboxes in the screen inspector | RG-171 | P2 | M | **CLOSED 2026-09-18.** `channelshows.test.js` (10) holds the pure rule and both doors, and was verified to go red on two cases when the AND is reduced to `templateShows` alone. Three guard rails are asserted rather than argued: it narrows only; NO OPINION is not an empty set, for six shapes of absence; and **a panic control reaches a screen configured OUT of the kind that is on it** — rule 15's exact failure is the one this feature could most easily introduce |
| ✅ RG-174 | **`regionsToLayers` converts a `scroll:true` REGION row into a full-frame layer at `y20 h54`, so one template renders as two different things.** A scrolling region row is a BAND crawl by construction: `TemplateRender` renders it as a ProPresenter footer ticker pinned to the bottom of the frame, with a fixed label on the left, and its own comment says it *"never occupies the centre of the wall"*. The converted path produced a centred full-frame runner instead | **Not a preview problem.** `TemplateGallery.upgradeLegacyToLayers` runs the conversion on mount and SAVES the result, so a church's announcement template would have been permanently re-shaped by opening the Templates tab — once, in silence, and not undone by going back. It is the converter half of RG-140 | Measured on the region path before the rendering half was fixed — `Scroll · Banner`, a 185-character notice, under `prefers-reduced-motion`: `clientWidth 1651` against `scrollWidth 4299`, so 62% of the notice never on screen, reported as `{scale: 1, legible: true}` | The converter builds the band: a ticker BAR with the same precedence as `TemplateRender::tickerBg` (never transparent, because on a keyed channel that bar is the only thing composited over the camera), the reference as a FIXED label to its left in the same band, the body crawling in the rest of it, and band-default sizes rather than full-frame ones. A keyed template that scrolls now gets neither a band shape nor a background | RG-140 | P2 | S | **CLOSED 2026-09-18.** `noticecrawlconvert.test.js` (6) was watched red on four, and its sixth case is the one that matters to a church with 44 templates: a template that does NOT scroll converts exactly as before, byte for byte in the two numbers the defect was stated in. **One thing the next rebase must do**: `fix/countdown-rendering` carries `noticecrawl.test.js`, a CHARACTERISATION test asserting what the two paths do TODAY. It WILL go red on this change, and the correct response is to replace its converted-path assertions with these — not to delete it and not to relax it |
| ⏳ RG-175 | **A Stage Timer cannot be held, and the preacher's own page renders a `held` row that nothing in the product can produce.** `adjust_timer` takes a `paused` argument and `Stage.svelte`'s programme rail models `held` — frozen, never warned, with its own CSS — but `adjust_countdown` only ever reaches `newest_congregation_timer`, so the only door that can hold a timer is closed to the `stage` scope, and the `+5` grant added with DECISIONS §99 deliberately passes `paused: null`. | A rendering nobody can reach is a rendering nobody has seen. It is the same class as the fourteen passing tests written against `PreviewProgram.svelte` before `qa-inventory` reported that nothing imported it: green, reasoned, and about a surface that does not exist for an operator. The operator cost is smaller than that — a sermon interrupted by a song cannot be paused, only extended — but the instrument cost is the same. | Read at source 2026-09-18: `grep -rn 'adjustTimer' src/` finds the wrapper, two test files and **no component**; `adjust_countdown` is the only other caller of `TimerRegistry::adjust` and it refuses anything but the newest `Both` timer. `Stage.svelte`'s `held` branch and `.tmr.held` CSS have no producer. | **Not improvised, and the reason is structural rather than cost.** `countdownRemainingMs` answers a held timer with its stored figure and that figure is positive by contract, so a clock past zero cannot be held at all: `timers::adjust` computes `next` from the clamped `remaining_ms`, gets 0, and refuses as `TooShort`. A `Hold` button would therefore work on a running sermon clock and refuse on an over-running one — a control whose meaning depends on a state the band is not showing. Either give the hold a figure that may be negative, or decide that a Stage Timer is not holdable and delete the `held` render from the rail. Both are decisions, not fixes. | RG-153, DECISIONS §92, §99 | P3 | S | Whichever way it goes, the rail's `held` branch and the console's transport must agree, and the over-zero case must be answered in words rather than by a refusal an operator reads mid-service |
| ✅ RG-176 | **The operator's mirror of the preacher's clock floored at zero while the preacher's own rail counted up, and the Stage Timer had no transport at all.** Live's band read `0:00` a second after a sermon clock ran out and `0:00` for the rest of the service; `Stage.svelte`'s rail read `+12:40` in red for the same timer (RG-153). And `adjust_timer` — a registered command with a wrapper and tests — was reachable from no rendered control, so extending a sermon meant Stop and Start, which throws away the elapsed figure the preacher is reading. | **Rule 35, and the twin-door shape this register has recorded four times.** RG-153 was fixed on the rail and its mirror was not, so one reading covered two situations needing different actions, on the surface a service is run from, about the one fact only the operator can act on. The unreachable command is the class CLAUDE.md counts as attack surface nobody is watching. A third defect was found on the way in: `timer_refusal`'s `TooShort` sentence told a Stage Timer's operator to *Clear the screens*, a panic control that cannot touch a `stage` timer (§27). | Reported by the operator 2026-09-18 (*"Live Timer thats for the preacher still going on the Live screen"*). Reproduced at source: `timerRemainingMs` forwarded to `countdownRemainingMs` with no `past`, while `Stage.svelte` passes `{ past: true }`. `grep -rn 'adjustTimer' src/` found no component. **The other two thirds of the same report were already closed by RG-167 hours earlier** — the labels the operator quotes (`Programme timer`, `Timer`, `Countdown`) are pre-rename, and the reach lines answering *"not showing anywhere"* landed with it. | **CLOSED 2026-09-18, DECISIONS §99.** `timerRemainingMs` gains the same `{ past }` opt-in `countdownRemainingMs` already carries and forwards it, so this stays ONE subtraction with a floor one caller lifts; Live opts in and the dock does not, because a congregation countdown reads zero as *it finished*. The band prints `+12:40 over` — the sign and the word, in no law colour, because amber is ON AIR, cyan is a guess, amethyst is rehearsal and red is a failure. One `+5` button per chip is the first rendered door to `adjust_timer`; past zero it grants five minutes FROM NOW rather than five onto a debt, because the plain sum is refused as `TooShort` at exactly the moment an operator wants it. `timer_refusal` takes the scope. **The move the operator suggested is declined for the second time**, on RG-167's reasoning: the dock is in the shell and the countdown is set from whichever workspace the operator is in. | RG-153, RG-167 | P2 | M | `stagetimerover.test.js` — eleven cases, watched to fail in five separate reverts (the shared `past` forward, Live's opt-in, the `over` render, the `+5` button, the `Math.max(0, …)` clamp). `e2e::a_refused_stage_timer_is_not_told_to_clear_the_screens` holds both scopes' sentences. **Geometry measured in a real browser** at 1320×860, 1280×800 and 1366×768: every control in the band hit-tests to itself, the reach note is unclipped, and `+5` costs zero band height |
| ✅ RG-177 | **The Planner's `Screens` row renders for all five cue kinds, and only three of them carried the set to the backend.** `ServicePlanner.svelte` does not gate that row by cue type. Scripture, song and announce passed the chosen screens through; **media and countdown did not**: `fire_media` and `start_countdown` never took a `channels` argument, so `OutputContent.channels` was left `None` by `..Default::default()`, and `None` means every screen | **A control reporting a success it did not achieve, three commits old.** An operator ticks one screen of three, reads the hint underneath saying *"Other screens keep what they are showing"*, fires the cue, and all three take it. On a media cue that is the church's picture on the main wall when only the lobby was chosen; on a countdown it is the pre-service clock in front of a congregation that is already seated. **It is the fourth instance in this register of a guarantee kept on some doors and skipped on its twin**: rehearsal gating held on three of four kiosk publishers, the throw-vs-swallow contract on eight of nine group-1 wrappers, and `NavResult` was thrown away by `remote_api` | Read at source while RG-161's `Screens` row was being exercised: the row renders unconditionally in `ServicePlanner.svelte`, while `fire_media` and `start_countdown` took no `channels` parameter and built their `OutputContent` through `..Default::default()`. Both directions were then driven through the real commands rather than inferred, because the defect is in what reaches the wire: `null` is every screen and `[]` is no screen | **CLOSED 2026-09-20, commit `790540b`.** Both commands take the set, and the tests ask about the cue KIND rather than about the command, which is CLAUDE.md's own instruction after the third instance. **A countdown carries its set on the TIMER, not on the call**, and that is the half worth reading twice: a countdown is broadcast by three commands, not one. `start_countdown` puts it up, `adjust_countdown` rebroadcasts on every Pause, Resume, Reset and minute button, and `show_timer` puts it back. A set carried only on the first would have been worse than carrying none, because the countdown would start on the streaming screen alone and then leak onto every screen in the building the moment somebody held it. So it is stamped once by its creator, exactly as `scope`, `warn_ms` and `started_in_rehearsal` already are | RG-161 | P1 | S | `src/lib/cuechannels.test.js` asks about the KIND, not the command: it exercises both wrappers against a mocked bridge and reads the Live call sites as source, for the reason `r2livepath.test.js` already records about mounting that view. A source-shape check does not prove runtime behaviour, so `e2e::a_media_cue_that_names_a_screen_carries_it_to_the_wire` and `e2e::a_countdown_keeps_its_screens_when_it_is_held_and_released` drive the real commands, the second holding the set across a hold and a release. `timerwords.test.js` names the new field rather than loosening its equality, which is what its own comment says to do |
| ✅ RG-178 | **One sentence, decoded twice, into two different books, and both auto-fired.** The rolling window read the same utterance 3.2 seconds apart and disagreed with itself about the book: `Numbers 10:29` at 0.88, then `Genesis 10:29` at 0.88. The preacher said Numbers. **Nothing in the router could see it.** The repeat cooldown is keyed per REFERENCE and those are two different references, which is the blind spot RG-29 records for two verses sharing one window, in a new shape: one sentence across two windows. `decide_live`'s corroboration cannot see it either, because that rule waits for a second pass to AGREE, and this second pass did not disagree, it asserted a different book just as confidently. | **A wrong verse reached a congregation, unattended.** Genesis 10:29 was put on the screens and nobody said it. The operator then walked Numbers 10:30, 10:31 and 10:32 by hand, which is how the ground truth is known. **No threshold could have prevented it**, and that is the part that generalises: both readings scored 0.88, while six CORRECT auto-fires in the same service scored 0.55. Raising the bar discards the six and keeps this one. | Service 24, 2026-09-20, 93.7 minutes on `ggml-large-v3-turbo` with the gate at the sensitivity-50 baseline. `detections` joined to `transcripts` for that service, with `heard_text` carrying both decodes verbatim: *"Numbers chapter 10. I'll read verse 29."* at 375.4s and *"Genesis 10, I'll read verse 29."* at 378.6s. 20 auto-fires in the service, all `direct`, no `semantic` and none dismissed. | **CLOSED 2026-09-20.** `Router::decide` treats the chapter and verse agreeing while the book does not, inside the cooldown, as one utterance heard two ways. The second reading is OFFERED, never fired. Deliberately NOT dropped: the operator may want it, and a silent discard is a different lie. Narrow by construction, asserted in four directions: a different verse in another book is untouched, the same pair outside the cooldown is two real citations, the same reference still Drops to the existing debounce, and a numbered book splits on the right space. | RG-29 | P0 | S | `router::tests::a_second_book_at_the_same_chapter_and_verse_is_offered_never_fired` replays the two field readings at their real timestamps and was watched to reproduce `AutoFire` before the rule existed. `router::tests::the_second_book_rule_is_narrow` holds the four non-cases. `eval.rs`'s scorecard, scored through the real router, is unmoved at recall 100%, 47/47 verses and a 0.0% wrong-verse rate across en/ha/sw/yo, so the rule costs no correct fire on the benchmark corpus. |
| ✅ RG-179 | **A bare "verse N" answered from the passage in memory was labelled `Direct` and auto-fired, for a book nobody said.** Rule 40 recorded the label as a lie on purpose while one service was the evidence. Service 24, 2026-09-20, 23.5 min: Psalms 55 had been on the wall by hand since 19.4 min; the preacher said *"Out of a prophet called Osir. In verse 1 he says, Come and let us return unto the Lord."* (Hosea 6:1, the book misheard into a word no alias knows). Nothing parsed, no chapter was stated, memory answered **Psalms 55:1 at 0.88, `Direct`**, and the router fired it. | **A wrong verse reached a congregation, unattended** — the second in one service, ten minutes after RG-178's. The resolution was right by rule 40's own three rules; the LABEL was the defect, because `Direct` means heard (rule 10) and the book here was assumed. | `detections` joined to `transcripts` for service 24 in the dev database: detection 257, transcript 701, `heard_text` verbatim, status `auto`, confidence 0.88. Not on any recording in `docs/qa/audits/` yet. | **CLOSED 2026-09-21.** `detection::resolve_bare_verse_with_source` returns which rule answered; `DetectionMethod::for_bare_verse` labels a memory answer `UncertainBook` ("chapter and verse heard, the book not", which is exactly this), so the router offers it and fires nothing. An anchor in the same breath is still `Direct`. The console says *From memory* on the card, in cyan, with no percentage. DECISIONS §106. | RG-32 | P0 | S | `detection::field_2026_09_20::*` (three, the pure rule and the label); `e2e::a_verse_answered_from_memory_is_offered_never_fired` replays the field text through the real path and was watched to reproduce `Psalms 55:1` on the wall with the old label; `detect.test.js` *a verse whose book came from memory* pins the three registers. |
| ✅ RG-180 | **An unattended auto-fire never took the plan off air, so the transport bar kept saying SLIDE over a detected verse.** `capture.js`'s `output://content` listener set `live` and `screenBlack` and nothing else; `leavePlan()` ran only in the wrappers and on `clear`/`black`. With a song or notice cue on air, the AI's verse painted the wall, the bar still read SLIDE, and the next `→` fired the next plan slide over the reading. | **A key doing what the bar said rather than what the wall showed** — the shape CLAUDE.md's transport rule exists to stop. Amber was honest (`cellLive` compares the words); the mode badge was not. | Read at source (Phase 1 audit, trace-planner P-1) and reproduced in `autofireplan.test.js` before the fix: the listener left `liveCue.onAir` true for a payload it had not fired. | **CLOSED 2026-09-21.** A plan fire announces its reference (`expectPlanFire`) BEFORE the command, and the listener leaves the plan for any scripture it was not told to expect (`transportmode::contentIsExpectedPlanFire`). Only scripture is judged, because only scripture can arrive with no wrapper running. | RG-11 | P1 | S | `src/lib/autofireplan.test.js` (four cases, including the event landing before and after the promise) and `transportmode.test.js` for the matcher. |
| ✅ RG-181 | **`A`, `D`, the arrows and Space acted on the view UNDER an open dialog.** The overlay probe in `shortcuts.js` lived inside the Escape branch alone. Opening *Why this match?* on the third claim and pressing `A` accepted the FIRST claim — the keyboard half of the bug `Live.svelte` had fixed for the buttons — and `→` stepped the plan while the operator read a panel. | **The wrong claim accepted from the keyboard, or a plan stepped under a reasoning panel.** Rule 16's guarantee kept on one key of five. | Read at source (Phase 1 audit, ws-live D1) and reproduced in `shortcuts.test.js` before the fix. | **CLOSED 2026-09-21.** `overlayOpen()` is one probe over one role list (`OVERLAY_ROLES`), asked by the Escape branch AND before the context switch. `B` stays above it: a panic key, not a context key. | RG-01 | P1 | S | `shortcuts.test.js` *context keys yield to an open dialog* (three cases, one of which asserts `B` still blacks the room); `panicoverlay.test.js` reads the one role list. |
---


## 23a. Renumbering — the ids two waves filed twice

Twice now, two waves have branched from the same commit and both filed upward from the
next free number. Both times the findings were entirely different and both times the
collision was invisible until the branches met, because **a duplicated id still
resolves**. `crossrefs.test.js` asks whether a citation points at a row that exists; it
cannot ask whether it points at the row the author meant. The only instrument that
fires is `relaygap.test.js`, on duplicate ids in this table, and only once both sets are
in the same file.

The rule applied both times: **the wave that filed first keeps its ids; the wave that
met it moves up in order.** Nothing in either audit has to be re-read to find its row.

| filed as | now | what it is | filed by |
|---|---|---|---|
| RG-143 … RG-147 | RG-156 … RG-160 | the stage message that comes back, the missing background asset, the two Up Nexts, the truncated reduced-motion crawl, the Announce role nothing derives | wave 5, 2026-09-16 |
| RG-145 | **RG-161** | per-cue screen targeting does not exist | wave 4, 2026-09-17 |
| RG-146 | **RG-162** | the rail's warning state and finished message, with no control that can reach either | wave 4, 2026-09-17 |
| RG-147 | **RG-163** | nothing removes a programme timer, so the clock a preacher wants ends up inside `+N more` | wave 4, 2026-09-17 |
| RG-148 | **RG-164** | the rail plus both stage panels cuts the verse | wave 4, 2026-09-17 |
| RG-149 | **RG-165** | `.alert.sm` cannot render | wave 4, 2026-09-17 |

**The frozen audits are not rewritten.** `audits/2026-09-17-WAVE4-STAGE-PLANNER.md` and
`audits/DESIGN-2026-09-16-WAVE5.md` keep the ids they were written with and carry a note
at the top pointing here. A frozen document that follows a renumbering is no longer
evidence of what was found on the day.


## 24. GO / NO-GO — the decision, made

**Decided 2026-08-31.** Earlier revisions of this section said **NO-GO** and left it there.
That was right when nothing had ever run in a room and wrong to leave standing now, because a
product that is never allowed out never gets the only evidence that would let it out. **An
indefinite NO-GO is not caution; it is a way of never being wrong.**

### The decision

> ## ⛔ NO-GO for general release · ✅ GO for a supervised pilot
>
> **Two churches. Named operators. Every service supervised by someone who can take the wall
> back by hand. For the length of one season.**

Both halves are load-bearing, and neither is a compromise between them.

### Why NOT general release

Relay is not shippable to a church that finds it and installs it alone, and one line decides
that: **on 2026-08-30 it put a verse nobody said in front of a congregation.** A preacher
cited Luke 10:32–37; the wall showed Proverbs 3:32. The cause is fixed and pinned, but the
*class* is not closed — closing it needs services, not commits.

Three more, each sufficient on its own:

- **Word error rate has never been measured, in any language.** The product's entire premise
  is that it hears well enough. That is an assertion, and the moat scores 3/10 because of it.
- **No code-signing certificate on EITHER platform** (RG-73). Most churches are on the one Relay cannot sign for, and the one it has run a service on is unsigned too — so rule 17's first-signed-build trap has never been sprung.
- **Nobody but the author has ever run a service on it.** Every claim about whether a
  volunteer can operate this under pressure is a claim about a person who wrote it.

### Why GO for a pilot, and why that is the *right* call rather than the brave one

The evidence that would move any of the four blockers **cannot be manufactured here**. Word
error rate needs real preaching on tape. Whether a volunteer can run it needs a volunteer. The
second and third services that RG-32 waits on need second and third services. Twenty-one merged
pull requests this week did not move field validation or language by a single point, and they
never could have.

Against that, the one service that did happen produced **seven findings** — a wrong verse, an
end-to-end metric measuring how long the preacher had been talking, a service record pointing
at the wrong sentence — that months of reading source had not. **That ratio is the argument.**
Fifty minutes in a room is worth more than another week of audit, and the next fifty will be too.

And the risk is bounded in a way that general release is not: with an operator watching and one
key that clears the wall, the worst outcome is a wrong verse for a few seconds and a note in the
register. Without a pilot the worst outcome is the same wrong verse, in a church nobody is
watching, discovered by nobody, fixed never.

### The conditions, and they are the deliverable

A pilot is **GO only with all of these.** Any one missing and it is NO-GO again:

| | |
|---|---|
| **An operator at the desk, every service** | Not a fallback. The panic keys are the product's honesty, and they need a hand |
| **`RELAY_RECORD_WAV` set for at least one full service** | This is how word error rate stops being an assertion. It is one environment variable and it is the single highest-value thing on this page |
| **Rehearsal before the first live use** | The path check exists for this; run it |
| **Diagnostics read after every service** | The per-minute line persists now. A rising one is the thing F11 could only answer once |
| **Every wrong verse written into `RELAY_GAP.md`** | Verbatim from `heard_text`. The Luke 10 case became a regression test and a CI corpus case; the next one should too |
| **macOS only** | Windows is unsigned. Do not hand out an unsigned build |
| **Check the Bible survived the upgrade, on each machine** | Added 2026-09-05 (RG-102 … RG-105). Before and after: `sqlite3 "$HOME/Library/Application Support/com.relay.app/relay.db" "PRAGMA user_version; SELECT COUNT(*) FROM verses;"` — it should end at `3` and `31102`. The repair is held by tests that drive the real `migrate`; it has never run on a database holding a church's own services |
| **If the church runs its own kiosk page, set `RELAY_KIOSK_ANY_ORIGIN=1`** | Added 2026-09-05 (RG-108). The hub now refuses a WebSocket handshake from an origin Relay did not serve. An OBS browser source on `http://<ip>:8032/output.html` is fine by construction, and **nothing here could confirm what `Origin` a real OBS install sends** — so find out on the Saturday |

### What converts a pilot into GO for general release

Not a date and not a feeling. **All five:**

1. **Word error rate measured** on real sermon audio, in at least English and one Tier-1 language.
2. **A native speaker** has reviewed the Yorùbá, Kiswahili and Hausa alias tables.
3. **Six consecutive services with no wrong verse on a wall**, across at least two churches.
4. **A Windows code-signing certificate**, and one update watched installing on a machine that is not the author's.
5. **Three services run end to end by an operator who did not write Relay**, without the author in the room.

Until then this section says exactly what it says now, and **anyone quoting "GO" from it without
the word "pilot" is quoting it wrong.**

---

## 25. Checklist

**Rewritten 2026-08-31.** The old checklist tracked a report being written and P0–P3 buckets
being cleared. Both are finished. What follows is what is actually left, in the order it
matters, and **not one line of it is a commit.**

### Do next — the pilot (§24), in order

- [ ] **Pick two churches and name the operator at each.** The conditions in §24 are the
      deliverable, not the software.
- [ ] **Set `RELAY_RECORD_WAV` for one full service.** One environment variable. It converts
      *"word error rate has never been measured"* — the sentence behind the moat's 3/10 — into
      a number, and nothing else on this page does that. **End the service with Stop and check
      the file exists before closing the laptop**: it buffers in RAM (~570 MB for 50 minutes)
      and writes once, at Stop, so a force-quit loses the whole recording (RG-77).
- [ ] **Run the path check in rehearsal before the first live use.** It exists for this.
- [ ] **Read Settings → Diagnostics after every service.** The per-minute line persists now;
      a rising one is what Stage F11 could only answer once.
- [ ] **Write every wrong verse into §23, verbatim from `heard_text`.** It is on the service's
      own History screen, under the reference, since RG-81 — before that it could only be read
      by opening the database by hand. The Luke 10 case became
      a regression test *and* a CI corpus case. The next one should too.

### Then — what converts the pilot into general release

- [ ] Word error rate measured on real audio, English **and** one Tier-1 language.
- [ ] A native speaker has reviewed the Yorùbá, Kiswahili and Hausa alias tables — and the
      Yorùbá numerals, which parse as of RG-126 and stay capped at *suggest* until one does
      (`r4_05b`; deleting `"unreviewed": true` from the `yo` block is what lifts it).
- [ ] Six consecutive services, two churches, no wrong verse on a wall.
- [ ] A Windows code-signing certificate, and one update watched installing elsewhere.
- [ ] Three services run end to end by an operator who did not write Relay.

### Needs a person, not a Sunday

- [ ] **§19b — should Relay ship a second Bible translation, an import path, or neither?**
      Public-domain corpora are addable today with no licence (DECISIONS §32.4). This report
      got that reason wrong once (RG-50) and will not decide it.
- [ ] **A projector**, for RG-18's contrast and distance thresholds.
- [ ] **The product's name.** Still undecided (`docs/SPEC.md`), and §24 now permits real
      churches to install it. The old README line four documents kept quoting is gone.

### Done, and kept here so the shape of the week is legible

- [x] The report, `AGENTS.md`, and the doc corrections — 2026-08-29
- [x] **RG-01 … RG-22** — the register as originally filed, P0 through P3
- [x] **RG-23 … RG-31** — the field service's own findings, plus the three R4 detection defects
- [x] **RG-33 … RG-48** — the calibrator that never calibrated, a comparator that said `a < b`
      and `b < a`, five untrapped modals, six headingless views, and three documents that
      described defects already fixed
- [x] **RG-49 … RG-56** — two dead menu entries, five unreachable commands deleted, launch
      checks that could not fail, and the migration path proved from both sides
- [x] **Stage F11** — run 2026-08-30. No drift across 49.5 minutes and 2,423 decodes
- [x] **Six real sermon lines in the CI detection corpus** — the first real-world data it has
      ever contained

## 26. Document actions — delete · merge · update

> **Re-run 2026-08-30.** The original round's actions are all done and are kept below the
> line. This is what a second pass over every tracked document found.

### Delete: **`docs/GPT.md`** — done

770 lines restating `CLAUDE.md`, `AGENTS.md` and this file for a model with **no repository
access**. That audience was real when it was written and is not any more: `AGENTS.md` now
carries the tool-agnostic contract, and it deliberately states **no counts at all** — which is
the whole reason it does not drift. `GPT.md` carried its own copy of every count and every one
of them was stale (§18).

Three copies of one set of constraints is three things to keep true, and this was reliably the
least true of the three. The question it existed to ask has been asked; the answer is this file.

**Nothing else is deleted.** Every other tracked document was checked for inbound references:

| Looks orphaned | Why it stays |
|---|---|
| `.claude/agents/relay-qa-*.md` (6) | Loaded by NAME by the `/qa-audit` command, not by link |
| `.claude/commands/qa-audit.md` | A slash command; invoked, never referenced |
| `.github/PULL_REQUEST_TEMPLATE.md` | GitHub loads it by path |
| `PROMPT.md` | Cited by five Rust module docs and `capabilities/default.json` for its phase numbers. **Still must not be deleted** |
| `docs/qa/audits/*` (4) | Frozen evidence. An audit that edits its own history stops being evidence — closures go in its fix log, never in its findings |

### Merge: two duplicates, both mine, both fixed

| Duplicate | Resolution |
|---|---|
| **RG-44 appeared twice** in the register, and **RG-47's row was concatenated onto RG-44's line** with no newline — so the table rendered one malformed row and RG-47 was invisible | Both from a conflict I resolved badly while landing PR #35. De-duplicated by ID; the row split |
| §16 listed twenty features as missing that were **built the same week** | Rewritten to the three genuinely missing, each with its reason; the original list kept below the line |

### The first round's actions — all complete

Verified with `git check-ignore -v`: `ruvector.db`, `.DS_Store`, `docs/.DS_Store` and
`.claude/RESUME.md` are all untracked **and** gitignored. No stray file is tracked.

**`PROMPT.md` must NOT be deleted**, despite reading like a superseded build prompt. The Rust
module docs (`audio.rs`, `stt.rs`, `detection.rs`, `router.rs`, `channels.rs`) and
`src-tauri/capabilities/default.json` cite its phase numbers as their rationale anchor. Recorded
here so a future tidy-up does not remove the target of live citations.

### Merge: **nothing**

`SPEC.md` (the frozen 2026-07-02 brief), `PRODUCT_AUDIT.md` (a human scorecard) and `audits/*`
(frozen machine audits) are deliberately different altitudes, and `docs/README.md` records why an
audit that edits its own history stops being evidence. This file joins them; it replaces none.

### Update: the drift table — executed, and deliberately not maintained

The 2026-08-30 pass listed sixteen file-and-line corrections. **All sixteen were applied, and
every one of the values in them is already stale** — including two that named `docs/GPT.md`, a
file that no longer exists. The table is not reproduced here, because reproducing it would be the
seventeenth wrong number in a document about wrong numbers.

**What was done instead of maintaining it:**

| Change | Effect |
|---|---|
| `CLAUDE.md` stopped stating `main.rs`'s size and command count | It now says the file *"has outgrown every number ever written here"* and names `wc -l` and `grep -c`. A line that cannot drift |
| `QA_HARNESS.md` §0 became **the** register of counts | Every row carries the command that produces it, and every other document cites the table rather than restating the values |
| `AGENTS.md` states **no counts at all** | Which is why it is the only document that has never drifted |
| `relaygap.test.js` grew a sixth assertion | The register's own summary sentence — entries, closed, withdrawn, not closed — is now checked against the table it summarises. It had said 54 and 51 in consecutive sentences |
| Four documents stopped quoting a deleted README line | `README.md` has not said *"Working name — rename freely"* for some time; `RELAY_GAP`, `ROADMAP` and `PRODUCT_AUDIT` (×4) all still quoted it as current. Corrected to cite `docs/SPEC.md`, where the position actually lives |

**The rule this earns, and it is the one worth keeping:** *a count that appears in more than one
file will drift, and the copy that drifts is the one an agent reads first.* The correction is not
a better number — it is one register, one command per row, and a citation everywhere else.

**And the same rule applies to a status, not only to a count.** §2 of this document carried
thirty-one rows that were true on 2026-08-29 and false a week later, while the fix logs directly
above them said so. A stale matrix is more dangerous than a stale number, because it reads as a
decision rather than as a measurement — which is why §2 now cites a **file and a symbol** instead
of a line number, and why every row that resolved by *deciding not to build* says **DECLINED**
rather than going quiet.
